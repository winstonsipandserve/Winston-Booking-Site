# PROGRESS.md

Build status and change log for Winston Sip and Serve. Updated as a standard part of every Claude Code prompt going forward — see CLAUDE.md's Development Workflow for the process this supports.

---

## Build Status

- Next.js scaffold: Done — TypeScript, Tailwind, App Router, npm
- CLAUDE.md: Done — identity, tech stack, locked architecture decisions (5 resource types), scope/future-extension plan, skills, MCP servers, env var reference, dev workflow, branch promotion policy, open items
- PROJECT_CONTEXT.md: Done — resource inventory, full pricing, guest fee rule, add-on services, membership tiers, membership application/approval flow, cancellation/reschedule policy, admin capabilities, bulletin
- Prisma schema: Done — 16 models, booking exclusion constraint (isolated migration), resource inventory seeded (pricing seeding deferred)
- Customer-facing booking flow: Backend hold/availability logic done; announcement gate + 5-step booking wizard (SPORT/COURT/DATE & TIME/ADD-ONS/SUMMARY) done end-to-end through actual PayMongo payment; SPORT step's 5 resource-type cards no longer show pricing inline — each has a "View Pricing" button that opens a shared `src/components/ui/Modal.tsx` component listing every duration tier for that resource type (courts: one flat-hourly row; simulators: full tier breakdown, e.g. 15/30/60 min for tennis/pickleball sims, 60/90 min for golf sim — same figures as before, just moved off the card — see 2026-08-18 Completed Log entry); selected cards/buttons across SPORT, COURT, and DATE & TIME (Duration + Time) now render with a bolder `border-2`/10%-tint/`font-semibold` treatment vs. unselected `border`/5%-tint/`font-medium` (Calendar's own selected-date fill untouched); `AnnouncementGate` now shows an inert (non-clickable) "Already a member? Sign in…" line above its Continue button, placeholder for member auth; **every step in the wizard, including the Payment page, is now on brand tokens** (see 2026-08-18 Completed Log entries) — no step is on unthemed `foreground`/`zinc`/`dark:` styling anymore; DATE & TIME step uses a real calendar + time-slot grid backed by `GET /api/availability`, with server-side business-hours enforcement; ADD-ONS step is fully live (Ball Boy/Coaching priced, submitted, and reflected on Summary/confirmation); SUMMARY's Confirm Booking now creates the `pending_payment` hold immediately (non-member provisional pricing, `customerId: null`) and reveals a Payment page showing the Booking Reference right away; Pay Now attaches the real customer and re-prices member-aware via `PATCH /api/bookings/[id]`, with an explicit "Continue to Payment" confirmation step when the final price differs from the provisional one, before auto-redirecting to PayMongo Checkout; the Payment page has no Back button — Start Over (always visible) is the only way to leave without completing payment. The line-item summary shown on SUMMARY and Payment is now one shared `BookingSummary` component (see 2026-08-18 Completed Log entry) rather than duplicated JSX. The `/api/resources` fetch that gates the wizard's first paint (SPORT step) is now lifted to `BookPage` and starts on `/book` page load rather than on gate-continue-click, so the Supabase round-trip runs concurrently with the `AnnouncementGate` being read rather than after it (see 2026-08-18 Completed Log entry). SPORT's 5 resource-type cards and SUMMARY's ledger rows now carry small inline-SVG icons (new `src/components/ui/Icons.tsx`, following `SocialIcons.tsx`'s hand-authored-SVG convention) — sport family icons (racket/paddle/golf-flag, shared across each sport's court and simulator card) on SPORT, reactive to selection state; `accent-primary/10` circular badge icons on each SUMMARY ledger row. ADD-ONS and the Payment page were deliberately left out of this icon pass (see 2026-08-18 Completed Log entry for why Payment couldn't be touched without a prop gate).
- Marketing site (customer-facing, non-booking pages): Home page redesigned against a client-supplied reference image — design tokens reconciled (distinct page/card cream values, `accent-primary`), Choose Your Sport and Testimonials removed, Hero rebuilt full-bleed with a floating stats card overlapping the Hero/How It Works boundary, How It Works restyled, Gallery replaced by an asymmetric Facilities grid, CTA banner gained a repeating ring texture, Footer restyled — composed directly in `src/app/page.tsx` (no shared layout wrapper yet). Navbar deliberately kept sticky above the Hero rather than matching the reference image's below-hero placement (confirmed deviation). Navbar's wordmark text was later removed in favor of an emblem-only logo, and Hero was rebuilt again with a centered script "Winston" wordmark (`Mrs Saint Delafield`) over the real `hero-background.jpg` photo, replacing the earlier text-lockup/placeholder-photo version — see 2026-08-15 Completed Log entry. The site's font system was then fully replaced (see second 2026-08-15 entry): Lora is gone, body copy is now Manrope, the script wordmark is now Parisienne (not Mrs Saint Delafield), and Fraunces is loaded as a new `font-serif` utility (not yet used anywhere). Navbar lost its Sign In and Book Now buttons entirely (desktop and mobile) — the "Book Now" nav link is now the header's only booking CTA — and the logo was resized larger (44px/56px tall). Hero was rebuilt again to a `min-h-[85vh]` flex-centered layout with a repositioned chevron that scrolls to StatsBar via a new `id="stats-bar"` anchor (replacing the old `nextElementSibling` approach). Book Now's own visual reskin, and the News/Cafe & Bar/Membership/About pages themselves, are still pending. The StatsBar/How It Works copy+color pass (see 2026-08-15 Completed Log entry) has since landed — StatsBar now renders Fraunces numbers with no dividers and a symmetric overlap into both Hero and How It Works, and How It Works flipped to a dark `bg-brand-dark` section with bordered/tinted cards, Fraunces headings, and updated step copy. Navbar's link styling (uppercase, 0.35px tracking, weight 400) and CtaBanner's dot-pattern visibility/copy/button-casing were fixed in a 2026-08-17 pass. Navbar's Sign In/Book Now buttons (removed 2026-08-15) were reinstated on 2026-08-18 as "Book a Court" (→ `/book`) and "Become a Member" (→ `/membership`), desktop and mobile — see Completed Log. One staged follow-up remains on Home itself: a Facilities/CTA/Footer copy+color pass, still blocked on Café & Bar and Speakeasy Lounge photos from Arjay.
- Admin panel: AdminUser email/password login via Auth.js (Credentials provider, JWT session, no adapter/DB session tables) live at /admin/login; `/admin` now uses a shared authenticated layout (route group `admin/(protected)/`) built as a bento-style shell — thin full-width topbar (title, initials avatar with email tooltip, Sign Out), a foldable left sidebar (`AdminSidebar`, 5 bento nav tiles that collapse to an icon rail with tooltips, active-tab highlighting via `usePathname()`), and page content wrapped in its own bento content card — see 2026-08-19 Completed Log entries (nav expansion, then sidebar/topbar restructure, then bookings list-page refinement, then filter-modal pass). Bookings list (`/admin/bookings`, 7-column layout with an independently-scrolling table body, filtering now modal-based — a single Filter button opens a modal with status + a working `startTime` date-range filter, both wired into the Prisma query and preserved across pagination; Export is a non-functional placeholder button only), booking detail (`/admin/bookings/[id]`, full detail + reschedule history), and admin-initiated reschedule (`PATCH /api/admin/bookings/[id]/reschedule`, confirmed bookings only) are now live. Memberships (`/admin/memberships` list + `/admin/memberships/[id]` detail) is also now live — see 2026-08-19 Completed Log entry — list/detail plus signed-URL government-ID image display, review actions (Approve/Reject) visibly present but intentionally not wired. Resources & Pricing (`/admin/resources`) is now tabbed (Courts / Simulators / Guest Fee, `ResourcesTabs.tsx`, client component); **Resource-level Add/Edit/Delete are now live** (`POST /api/admin/resources`, `PATCH`/`DELETE /api/admin/resources/[id]`, new `ResourceFormModal.tsx` — see 2026-08-19 Completed Log entry) — Pricing/Add-on/Guest-Fee buttons remain the inert CRUD-preview layer from the prior pass, still awaiting their own mutation slice. Courts/Simulators cards are now also individually foldable (collapsed by default, independent open/closed state per card, chevron toggle) — Guest Fee's card is not foldable and always shows its full content. **Pricing/Add-on/Guest-Fee rows are now live, edit-only** (`PATCH /api/admin/pricing-rules/[id]`, `PATCH /api/admin/add-on-pricing-rules/[id]`, `PATCH /api/admin/guest-fee-rule/[id]`, new `PriceEditModal.tsx` — see 2026-08-20 Completed Log entry) — every previously-disabled pencil on the Pricing table, Add-on table, and Guest Fee card now opens a modal pre-filled with the current peso value(s) for whichever tier(s) actually have a row and saves on submit; create (the `+ Add` buttons) and delete for these three models remain disabled/deferred. Bulletin remains a Coming Soon placeholder pending its own build slice.
- PayMongo payment integration: Done — checkout session creation, webhook confirmation, checkout-session-expiry-on-hold-expiry, a manual (not automatic) hand-off from the wizard to PayMongo Checkout, and a polling confirmation page all live end-to-end.
- Membership application + approval flow (implementation): application submission done — public `/membership` page + `POST /api/membership-applications` create a pending `MembershipApplication` (customer resolved by email, 3 government ID images uploaded to a private Supabase Storage bucket). Admin review UI (`/admin/memberships` list + detail) is now live as a read-only frame — see 2026-08-19 Completed Log entry — but the actual approve/reject mutation (status change, `Membership` record creation on approval) is not yet wired; tier activation/payment also still pending.
- Bulletin/announcements (implementation): Not started as a DB-backed feature — but `/book`'s `AnnouncementGate` now renders as a styled static ledger of 3 hardcoded notices shaped to match the `Bulletin` model's `title`/`body`/`publishedAt` fields (see 2026-08-17 Completed Log entry). No admin CRUD, no `GET`/`POST` route, no read from the `bulletins` table yet — a future swap to real data is a drop-in replacement of the hardcoded array, not a rewrite.
- Auth: Not started — method undecided (email/password vs. magic link)

## Completed Log

- 2026-08-10 — CLAUDE.md scaffolded: project identity, tech stack, locked architecture decisions, POS extension scope, dev workflow (`26c0991`)
- 2026-08-10 — Branch Promotion Policy added to CLAUDE.md (`c7e5c5c`)
- 2026-08-10 — PROJECT_CONTEXT.md created: resource types, membership, and cancellation rules finalized; pricing left pending client rate table (`0a4d4cc`)
- 2026-08-10 — PROJECT_CONTEXT.md updated with finalized pricing and add-on rate tables (`1e4daf4`)
- 2026-08-10 — PROGRESS.md created
- 2026-08-10 — CLAUDE.md + PROJECT_CONTEXT.md updated: pricing model confirmed as DB-driven (PricingRule/AddOnPricingRule), admin-editable via admin panel — added to Architecture Decisions and Admin Capabilities respectively
- 2026-08-10 — PROJECT_CONTEXT.md + CLAUDE.md updated: confirmed non-members book without an account (name/phone/email only), only members have login accounts — added Account Requirements section and Customer & auth model architecture note
- 2026-08-10 — CLAUDE.md + PROJECT_CONTEXT.md updated: MembershipApplication.customerId confirmed required (not nullable) — applications resolve to Customer via look-up-or-create by email at submission, same pattern as bookings
- 2026-08-10 — CLAUDE.md updated: admin login model resolved as individual staff logins (not shared account) — added AdminUser architecture note, removed resolved item from Open/Not Yet Decided; MembershipApplication.reviewedBy and BookingReschedule.performedBy will be real FKs, not strings
- 2026-08-10 — CLAUDE.md updated: documented safeguard for the booking-overlap exclusion constraint (Prisma can't express EXCLUDE USING gist natively) — master SQL will live at prisma/manual-sql/booking-exclusion-constraint.sql, and all future Booking/migration prompts must VERIFY the constraint still exists in the DB
- 2026-08-10 — CLAUDE.md updated: confirmed BookingReschedule is createdAt-only and immutable by design (audit trail integrity) — corrections are handled via a new row, not editing the original; added exception to Coding Conventions and new Architecture Decisions bullet
- 2026-08-10 — CLAUDE.md updated: Payment.bookingId confirmed as a direct nullable FK to Booking (not polymorphic) — documented as Architecture Decision, trades a small future migration for POS support in exchange for DB-enforced integrity now
- 2026-08-10 — CLAUDE.md updated: added MembershipCreditTransaction ledger model — Membership.creditBalanceCentavos is now a cached total backed by an immutable transaction log, POS-ready without pre-building spend logic
- 2026-08-10 — Next.js scaffold created (TypeScript, Tailwind, App Router, npm) — package.json now exists at repo root
- 2026-08-10 — CLAUDE.md + PROJECT_CONTEXT.md updated: guest fee rate confirmed as its own admin-editable GuestFeeRule table (not a PricingRule column, not hardcoded); membership payment ↔ Payment linkage flagged as still-open, added to Open/Not Yet Decided
- 2026-08-10 — Prisma schema created (16 models: ResourceType, Resource, PricingRule, GuestFeeRule, Customer, AdminUser, MembershipApplication, Membership, MembershipCreditTransaction, Booking, BookingReschedule, AddOnService, AddOnPricingRule, BookingAddOn, Payment, Bulletin) and initial migration applied; booking-overlap exclusion constraint (`booking_no_overlap`) applied via an isolated migration kept separate from `init`, master copy at `prisma/manual-sql/booking-exclusion-constraint.sql`; resource inventory seeded (5 ResourceType rows, 9 Resource rows — pricing tables intentionally left empty, deferred to a future prompt); CLAUDE.md Commands section updated so `db:migrate`/`db:studio`/`db:seed` wrap the Prisma CLI with `dotenv-cli` (`.env.local` isn't auto-read by the CLI). Note: installed Prisma pinned to 6.19.3, not latest 7.x — Prisma 7 removed inline `datasource url`/`directUrl` support (moved to a separate `prisma.config.ts`), which would have broken the schema shape this prompt specifies and CLAUDE.md's `DATABASE_URL`/`DIRECT_URL` env var table; flagging in case a deliberate Prisma 7 migration is wanted later. Resource labels ("Court 1", "Bay 1", etc.) are a reasonable default, not specified in PROJECT_CONTEXT.md — editable later via the admin panel.
- 2026-08-10 — Row Level Security enabled on all 16 application tables with explicit deny-all policies for the `anon` and `authenticated` Supabase roles, applied via an isolated migration (same pattern as the booking exclusion constraint), master SQL at `prisma/manual-sql/enable-rls-deny-all.sql`. `_prisma_migrations` deliberately left untouched (internal tracking table, not app data). CLAUDE.md updated with a new Row Level Security Architecture Decision and a Coding Conventions rule requiring RLS + deny-all policies on every future table's creating migration.
- 2026-08-11 — CLAUDE.md updated: booking hold duration (10 min, `BOOKING_HOLD_MINUTES` env var) and hybrid expiry mechanism (expire-on-write primary, daily Vercel cron sweep secondary) documented as a new Architecture Decision; env var added to the Environment Variables table.
- 2026-08-11 — Pricing seeded: `PricingRule` (court rates as flat-hourly rows anchored at `durationMinutes=60`, simulator rates tiered by duration — non-member golf-sim 30-min tier intentionally has no row) and `GuestFeeRule` (₱150/hr per guest) added to `prisma/seed.ts`, following the existing upsert idempotency pattern. Prisma client singleton added at `src/lib/prisma.ts` (hot-reload-safe via `globalThis` caching). `POST /api/bookings` implemented: validates input, resolves `Customer` via look-up-or-create by email, derives member status server-side from an active (non-expired) `Membership`, validates duration against resource category (court = multiple of 60; simulator = must match a seeded tier), prices via `PricingRule` + `GuestFeeRule` (non-member court bookings only), and creates a `pending_payment` hold inside a transaction that first expires stale holds for the same resource/time range (expire-on-write) — a caught `booking_no_overlap` exclusion violation returns 409. `GET /api/cron/expire-bookings` implemented with `CRON_SECRET` bearer-token auth (401 if unset/mismatched) and a platform-wide stale-hold sweep; `vercel.json` schedules it daily (`0 0 * * *`, Hobby-plan cron cap). CLAUDE.md's API routes convention and `CRON_SECRET` env var row filled in.
- 2026-08-11 — `GET /api/resources` added (public, no auth): returns all `ResourceType` rows with nested active `Resource` rows and their `PricingRule` tiers grouped inline, plus the single `GuestFeeRule` amount at the top level, sized so the client can populate dropdowns and estimate price without extra joins. Booking form UI built: `src/components/booking/BookingForm.tsx` (client component, folder-per-feature under `src/components/`) fetches `/api/resources` on mount, drives a resource-type → resource → duration cascade (court types offer a fixed 60/120/180-min select; simulator types offer the union of seeded duration tiers for that type), shows the guest-count input only for court categories, and computes a live non-member-rate price estimate client-side (clearly labeled as a preview, never authoritative). Submits to the existing `POST /api/bookings` unchanged, branching on response status: 201 swaps in `BookingConfirmation.tsx` with the server-returned booking details, 409 shows the required "just booked by someone else" inline message, 400 surfaces the server's own `error` text, and other/network failures show a generic inline message — all three failure paths preserve form state. `src/app/book/page.tsx` renders the form; homepage (`src/app/page.tsx`) replaced with venue name, one-line description, and a single "Book Now" link to `/book` (no bulletin, no resource grid, per current scope). No new dependencies added; `prisma/schema.prisma`, `prisma/manual-sql/*`, `prisma/seed.ts`, `POST /api/bookings`, the cron route, and `vercel.json` were not touched. CLAUDE.md's Components and Error handling conventions filled in based on the pattern established here.
- 2026-08-11 — Booking flow restructured into an announcement gate + 5-step wizard, structure-only pass (no visual/branding polish, no new dependencies). `src/components/booking/AnnouncementGate.tsx` added: a static placeholder announcement card with a "Continue to Booking" button; `src/app/book/page.tsx` (now a client component) holds a `started` boolean toggling between the gate and the wizard, no route change. `src/components/booking/BookingForm.tsx` rewritten as the wizard orchestrator — all form state (resource type/resource/date-time/duration/guest count/name/phone/email/add-on checkboxes, plus `step`) lives here and is passed down as props, so Back/Continue navigation never resets a previously entered value; the existing resource-type → resource/duration cascade, price-estimate calc, and submit/response-branching logic (201/409/400/network) are reused verbatim, not rewritten. Split into `src/components/booking/steps/`: `StepIndicator.tsx` (5 numbered circles — SPORT/COURT/DATE & TIME/DETAILS/REVIEW), `SportStep.tsx` (all 5 real `ResourceType` cards with live resource counts, no images), `CourtStep.tsx`, `DateTimeStep.tsx`, `DetailsStep.tsx` (adds Ball Boy/Coaching checkboxes with a "Coming soon — not yet included in your booking" note), and `ReviewStep.tsx` (read-only summary of every prior step plus the reused price estimate; its "Confirm Booking" button fires the existing submit logic). Ball Boy/Coaching are UI-only — confirmed via a `fetch` patch on the actual outgoing `POST /api/bookings` body during browser verification that neither field is ever serialized, regardless of checkbox state. On 409/400/network failure the wizard stays on the Review step (not step 1) with all values intact, per existing error-handling convention. VERIFY performed in the connected browser: full tennis-court booking walked through all 5 steps end-to-end (201, confirmation shown with server values); Back → change guestCount → forward confirmed the update applied with nothing else reset; 409 (duplicate slot) and 400 (non-member golf-sim 30-min, no seeded tier) both reproduced from Review with the correct existing inline messages and state preserved; `GET /api/resources`/`POST /api/bookings` shapes, `booking_no_overlap` exclusion constraint, and RLS deny-all policies (16/16 tables) all confirmed unchanged via Supabase MCP; cron route re-verified 401 (no auth) / 200 (correct bearer); `package.json` diff empty. Test `Booking`/`Customer` rows created during verification were deleted via a one-off Prisma script (Supabase MCP is read-only) — baseline confirmed back at 5/9/21/1 resource-side rows, 0 bookings/customers.

- 2026-08-11 — DATE & TIME step replaced the native `datetime-local` input with a real calendar + availability grid, and business-hours enforcement (6:00 AM–10:00 PM PH) added server-side. `src/lib/booking-hold.ts` extracted from `POST /api/bookings` (`HOLD_MINUTES` + an `occupyingSlotCondition(now)` Prisma where-fragment for "confirmed, or a pending_payment hold that hasn't gone stale") so the new read-only availability endpoint and booking creation can never disagree on the hold window. `src/lib/business-hours.ts` added: `BUSINESS_OPEN_HOUR`/`BUSINESS_CLOSE_HOUR` constants, PH-offset (fixed UTC+8, no DST) minutes-of-day/date-string helpers, `isWithinBusinessHours`, and a PH-calendar-date → UTC-day-window helper. `GET /api/availability` (public, no auth) added: given `resourceId` + `date` (YYYY-MM-DD, PH calendar date), returns `{ busy: [{ start, end }] }` for that resource/day using the shared occupying-slot condition — no pricing or customer data. `POST /api/bookings` now 400s with `'Bookings must start and end between 6:00 AM and 10:00 PM'` on any out-of-hours submission, checked immediately after the existing duration validation and before customer look-up-or-create (fail fast). Two new hand-rolled (no new dependencies) client components: `src/components/booking/Calendar.tsx` (month-grid date picker, Prev disabled at the current PH month, no upper bound on Next, past days disabled) and `src/components/booking/TimeSlotGrid.tsx` (granularity 60min court / 30min golf-sim / 15min tennis-sim & pickleball-sim, filters out busy and already-past slots, `+08:00`-qualified ISO start times). `DateTimeStep.tsx` rewritten to render duration select → Calendar → TimeSlotGrid (grid only once a date is picked); `BookingForm.tsx` gained `selectedDate`/`busy`/`availabilityLoading`/`availabilityError` state, an availability-fetch effect keyed on `resourceId`+`selectedDate`, and a slot-reset effect keyed on `resourceId`/`selectedDate`/`durationMinutes` (mirrors the existing resource-type-change reset pattern) so a stale slot selection can't survive an invalidating change; `startTimeLocal` is now only ever set via the grid's `onSelectSlot`, already `+08:00`-qualified, so the existing submit body construction needed no change. SPORT/COURT/DETAILS/REVIEW steps, the exclusion constraint, RLS policies, and the cron route were not touched. VERIFY performed in the connected browser pane (this environment's browser tooling, not the Playwright MCP referenced in CLAUDE.md's MCP Servers list — no functional difference for these checks): hourly (tennis court) / 30-min (golf-sim, both 30-min and 90-min tiers) / 15-min (tennis-sim) grid granularity confirmed correct at the 6am–10pm boundary, including duration-dependent last-start-time shift (e.g. golf-sim 90-min duration's last start is 8:30 PM); a real Tennis Court booking's overlapping 2:00 PM slot correctly disabled in the grid on reload, non-overlapping slots unaffected; a fabricated 15-minutes-stale `pending_payment` row correctly excluded from `GET /api/availability`'s `busy` array (regression check on the `booking-hold.ts` extraction); today's past-time slots correctly hidden (verified against actual current PH time) while future dates show the full range; calendar Prev/Next navigation confirmed (Prev disabled only at the current PH month, re-verified after navigating 3 months forward and back); full 5-step happy path completed end to end (201, confirmation screen's PH start/end time matched the Review step exactly, no timezone shift); direct `POST /api/bookings` with an out-of-hours `startTime` (5:00 AM and 10:30 PM PH) both returned 400 with the business-hours message, confirming the UI can't be bypassed. Regression-confirmed via Supabase MCP: `booking_no_overlap` exclusion constraint and RLS deny-all policies (16/16 tables, 2 policies each) unchanged; cron route still 401 (no auth) / 200 (correct bearer); `package.json`/`package-lock.json` diff empty (no new dependencies). Cleanup via one-off Prisma scripts (Supabase MCP is read-only) removed the real test booking, the fabricated stale-hold row, their test `Customer` rows, and one unrelated leftover `cancelled` booking/customer pair found already present in the DB before this prompt started (pre-existing scratch data, not created by this change) — baseline counts (5 ResourceType / 9 Resource / 21 PricingRule / 1 GuestFeeRule, 0 bookings/customers) confirmed restored.

- 2026-08-11 — `TimeSlotGrid.tsx` refined: the selection highlight now spans the whole occupied range (clicked start through start+duration) as one solid block, instead of only the exact clicked slot — visible whenever a booking's duration spans more than one of the grid's own granularity increments (e.g. a 30-min tennis-sim booking on the 15-min grid, or a 90-min golf-sim booking on the 30-min grid). `Slot` now carries its already-computed `startDate`; `selectedRange` (`{ start, end }` derived from `selectedSlot` + `durationMinutes`) is computed once before the slot map, and each slot's `isInSelectedRange` replaces the old exact-`startIso`-match check. Fixed a related className-precedence bug: a slot can now be individually `disabled` (its own duration window happens to overlap a later busy range) while still falling inside the current selection's already-validated span — the disabled-grey and selected-highlight class strings were previously appended independently (order in the template string doesn't guarantee visual precedence in generated Tailwind CSS), so restructured to a single three-way conditional where the highlight takes priority, with a separately-appended `cursor-not-allowed` fragment so an in-range-but-disabled slot still shows a not-allowed cursor (its `disabled` attribute — and therefore non-clickability — is untouched). Purely a visual change: granularity, past/busy filtering, the `disabled` attribute, and `onSelectSlot` wiring are all unchanged; only `TimeSlotGrid.tsx` was touched. VERIFY in the browser pane: single-slot selections (60-min court on the 60-min grid) render identically to before (exactly one highlighted button); a 30-min tennis-sim selection on the 15-min grid highlights both occupied 15-min slots as one block; a 90-min golf-sim selection on the 30-min grid highlights exactly 3 consecutive slots; changing duration after selecting still clears the highlight (existing reset effect, untouched); clicking a different unoccupied slot moves the highlight entirely; a fabricated booking made two slots within an active multi-slot selection individually `disabled: true` while the selected start itself stayed enabled — both disabled-in-range slots rendered with the highlight style (not grey) and remained non-clickable (a click on them left the selection unchanged), while a `disabled` slot outside the selection kept the original grey styling; full happy path (golf-sim, 90-min duration, multi-slot selection) completed end to end with 201, confirmation showing the actually-clicked start time. Regression-confirmed: `booking_no_overlap` exclusion constraint present; cron route still 401/200; `package.json`/`package-lock.json` diff empty. Cleanup via one-off Prisma scripts removed the two test bookings and their `Customer` rows — baseline (5/9/21/1, 0 bookings/customers) confirmed restored.
  - **Correction (same day):** the range-highlight comparison above was exclusive of the end boundary (`slot.startDate < selectedRange.end`), so the grid slot marking where the booking actually ends never lit up — a 30-min booking on its matching-granularity grid still only highlighted 1 slot, not the full 2-slot span. Changed to inclusive (`<=`) so the slot at the booking's end time now highlights too — a 30-min golf-sim booking now highlights 2 slots (was 1), a 60-min court booking now highlights 2 slots (was 1), a 90-min golf-sim booking now highlights 4 slots (was 3). Re-verified the disabled/in-range precedence fix still holds at this now-included end-boundary slot (a fabricated booking overlapping only the end-boundary slot's own duration window rendered it highlighted, not grey, and non-clickable) and that a full happy-path booking still submits the correct actually-clicked `startTime`. Only `TimeSlotGrid.tsx` touched; `package.json` diff empty.

- 2026-08-11 — `TimeSlotGrid.tsx` now always renders the full 6:00 AM–10:00 PM range regardless of duration, instead of the grid's loop previously stopping early once a slot's own duration wouldn't fit before closing (e.g. a 120-min court booking's grid used to stop at 8:00 PM, with 9:00 PM never appearing). The loop condition changed from `m + durationMinutes <= closeMinutes` to `m < closeMinutes`; a new `Slot.exceedsClosing` field marks any slot whose selected duration would run past 10:00 PM. These slots stay clickable (not HTML-`disabled`) and render in a subtle amber/warning style — but rank below the existing disabled-grey style in visual priority, so a slot that's genuinely busy or in the past still shows as disabled even if it would also exceed closing. Clicking an `exceedsClosing` slot no longer silently does nothing; it shows a dismissible toast (auto-clears after 4s) naming the selected duration and closing time, and does not change the current selection. Granularity, busy-overlap detection, past-time detection, the range-highlight feature, and click-to-select for genuinely valid slots are all unchanged. Only `TimeSlotGrid.tsx` touched; no new dependencies. VERIFY performed in the browser pane: Tennis Court/120-min grid now shows every hourly slot 6:00 AM–9:00 PM (previously stopped at 8:00 PM), with 9:00 PM in the warning style and 8:00 PM and earlier normal; clicking 9:00 PM showed the toast with no selection change, and it auto-dismissed after ~4s; clicking a normal slot (6:00 AM) produced ordinary selection/highlight with no toast; Golf Sim/90-min grid confirmed the last-fitting slot (8:30 PM, ends exactly 10:00 PM) renders normal while 9:00 PM/9:30 PM show the warning and trigger the toast on click; a real booking made to overlap a late slot (Golf Sim Bay 1, Aug 20, 9:00–10:00 PM) correctly rendered 8:30 PM/9:00 PM/9:30 PM as disabled/grey (not warning) despite also exceeding closing at the 90-min duration, confirming disabled takes priority; today's already-past slots still rendered disabled/grey exactly as before; the existing range-highlight (start-through-end shading) still worked correctly on a valid selection; a full happy-path booking (the same Golf Sim booking used for the busy-slot regression check) completed end-to-end with 201. `package.json`/`package-lock.json` diff empty; no file other than `TimeSlotGrid.tsx` touched. Test `Booking`/`Customer` row created for verification was deleted via a one-off Prisma script — baseline (5 ResourceType / 9 Resource / 21 PricingRule / 1 GuestFeeRule, 0 bookings/customers) confirmed restored.
  - **Correction (same day):** the previous fix's loop bound (`m < closeMinutes`) still stopped one increment short of closing itself, so a slot starting exactly at 10:00 PM was never generated — invisible for any duration equal to the resource's own grid granularity (e.g. Tennis Court's 60-min booking on its 60-min grid still topped out at 9:00 PM, Tennis/Pickleball Sim's 15-min booking on the 15-min grid still topped out at 9:45 PM), since those cases never had a slot beyond the existing max for the fix to reveal. Changed to `m <= closeMinutes`, adding exactly one more slot per grid at 10:00 PM; its `exceedsClosing` always computes true, so it falls into the existing warning style/toast with no new branching. Re-verified: Tennis Court/60-min and Tennis Sim/15-min grids now end at 10:00 PM (warning style, toast on click) where they previously stopped at 9:00 PM/9:45 PM; Golf Sim/90-min still shows 9:00 PM and 9:30 PM as warning with 10:00 PM now an additional warning slot; Golf Sim/30-min now shows 9:30 PM as normal (fits exactly) with 10:00 PM as the new warning slot; valid-slot selection and range-highlight unaffected; disabled/warning visual-priority ternary untouched by this change (confirmed via source read, not re-exercised against a live busy slot at closing time); full happy-path booking (Tennis Court, 60-min, 2:00 PM) completed end-to-end with 201. Only `TimeSlotGrid.tsx` touched; `package.json` diff empty. Test `Booking`/`Customer` row deleted via a one-off Prisma script — baseline (5/9/21/1, 0 bookings/customers) confirmed restored.

- 2026-08-11 — `BookingForm.tsx` duration-options fix, client-only. Court duration options extended from 1–3hr to 1–4hr (`COURT_DURATIONS_MINUTES` gained a `240` entry — Tennis Court and Pickleball Court both consume this shared constant). Simulator duration options are now correctly filtered to `non_member` `PricingRule` tiers only, fixing a leak where Golf Sim's duration dropdown showed the member-only 30-min (₱450) tier alongside its real non-member 60/90-min tiers — members don't yet have their own booking flow, so that tier was never reachable but was still selectable in the UI. The two previously-duplicated inline duration-list computations (in the `durationOptions` memo and the resource-type-change reset effect) were collapsed into one module-level `getDurationOptions(resourceType)` helper. No pricing rows, business rules, or other files touched — `DateTimeStep.tsx` confirmed to only render whatever `durationOptions` it's given. VERIFY in the browser pane: duration dropdown contents confirmed per resource type (Tennis Court/Pickleball Court → 60/120/180/240; Golf Sim → 60/90 only, no 30; Tennis Sim/Pickleball Sim → 15/30/60, unchanged); full Tennis Court booking at the new 240-min duration (2:00–6:00 PM) completed end-to-end with 201 and the correct ₱3,000.00 estimate; the existing TimeSlotGrid "exceeds closing" warning/toast still fired correctly at 7:00 PM for a 240-min duration (existing generic logic, unaffected by this change); Golf Sim bookings at both 60-min (₱1,150.00) and 90-min (₱1,450.00) completed end-to-end with 201; a direct `POST /api/bookings` with `durationMinutes: 30` for a golf-sim resource (bypassing the UI) still 400'd with "No pricing available for this resource, rate tier, and duration," confirming server-side behavior is unchanged — the 30-min tier was never reachable server-side for non-members either. `package.json`/`package-lock.json` diff empty; `PricingRule` row count confirmed still 21 via Supabase MCP (including the untouched member golf-sim 30-min row). Test `Booking`/`Customer` rows created during verification deleted via a one-off Prisma script — baseline (5 ResourceType / 9 Resource / 21 PricingRule / 1 GuestFeeRule, 0 bookings/customers) confirmed restored.

- 2026-08-11 — Booking wizard restructured from 5 steps to 6: Ball Boy/Coaching split out of DETAILS into their own new ADD-ONS step, and the former REVIEW step relabeled SUMMARY. `src/components/booking/steps/AddOnsStep.tsx` added, containing the relocated Ball Boy/Coaching checkboxes and "Coming soon — not yet included in your booking" note, moved unchanged out of `DetailsStep.tsx` (which now holds only guestCount, name, phone, email — guestCount stayed, since it's a core booking detail, not an add-on). `StepIndicator.tsx`'s label list updated to `['SPORT', 'COURT', 'DATE & TIME', 'DETAILS', 'ADD-ONS', 'SUMMARY']`; it already derives step count/highlighting from this list, so no loop-bound fix was needed. `ReviewStep.tsx` itself needed no wording change — it has no visible "Review" heading of its own, only a `<dl>` summary; the old "REVIEW" label lived solely in StepIndicator. `BookingForm.tsx`: `TOTAL_STEPS` 5→6, AddOnsStep rendered at step 5 (receiving the same `ballBoy`/`onBallBoyChange`/`coaching`/`onCoachingChange` previously threaded to DetailsStep), ReviewStep moved to step 6 with `onBack` now targeting step 5, nav-buttons visibility condition changed from `step < 5` to `step < 6`. ballBoy/coaching remain UI-only, still never sent in the `POST /api/bookings` body — confirmed via source read of `handleConfirm`'s request-body construction (unchanged) since the outgoing browser fetch's request body wasn't inspectable through this session's network tooling. No schema/API/pricing changes; no new dependencies. Found and flagged a pre-existing discrepancy: `DetailsStep.tsx` never actually gated the Ball Boy checkbox on `isCourt`, despite PROJECT_CONTEXT.md documenting Ball Boy as court-only — per explicit user direction, this relocation preserved that existing (undocumented-rule-violating) behavior as-is rather than fixing it, since fixing it would be a business-rule change outside this prompt's relocation-only scope. VERIFY performed in the browser pane: full happy path (Tennis Court) walked SPORT→COURT→DATE & TIME→DETAILS→ADD-ONS (both checkboxes checked)→SUMMARY (both shown as "Selected")→201→confirmation screen correct; Back-navigation from Summary to Add-Ons confirmed checkboxes stayed checked, and from Add-Ons to Details confirmed guestCount/name/phone/email all stayed populated (checked via DOM inspection, not just visual); forward-navigation replayed the same steps with all values still intact; StepIndicator rendered and highlighted all 6 circles/labels correctly throughout. Regression-confirmed via Supabase MCP: `booking_no_overlap` exclusion constraint present; RLS enabled on 16/16 application tables with 32 deny-all policies (2 per table), both unchanged; cron route re-verified 401 (no auth) / 200 (correct bearer, read from `.env.local`). `package.json`/`package-lock.json` diff empty; no file outside the stated scope touched. Test `Booking`/`Customer` row created during verification deleted via a one-off Prisma script (Supabase MCP is read-only) — baseline (5 ResourceType / 9 Resource / 21 PricingRule / 1 GuestFeeRule, 0 bookings/customers) confirmed restored.

- 2026-08-11 — Ball Boy now correctly gated to court types only in `AddOnsStep.tsx`, resolving the discrepancy flagged in the prior entry (Ball Boy was never actually gated on `isCourt` despite PROJECT_CONTEXT.md documenting it as court-only). The Ball Boy checkbox and its label are now wrapped in `{isCourt && (...)}`; Coaching and the "Coming soon" note remain unconditional, since Coaching applies to both courts and simulators. `BookingForm.tsx`'s existing resource-type-change reset effect (keyed on `[resourceTypeId]`, already resetting `resourceId`/`guestCount`/`durationMinutes`) gained one more line: `setBallBoy(false)` when the newly selected resource type's category is not `'court'` — preventing a Ball Boy selection made under a Court from silently surviving (hidden but still `true`) after switching to a Simulator. Coaching is untouched by this reset (valid for both categories); switching between two court types (e.g. Tennis Court → Pickleball Court) does not reset Ball Boy. ballBoy/coaching remain UI-only, still excluded from the `POST /api/bookings` body — unchanged. Only `AddOnsStep.tsx` and `BookingForm.tsx` touched; no schema/API/pricing changes, no new dependencies. VERIFY performed in the browser pane: Tennis Court and Pickleball Court both show Ball Boy + Coaching, toggleable; Tennis Sim, Pickleball Sim, and Golf Sim each show only Coaching (confirmed via checkbox count, not just visual); reset regression confirmed (Tennis Court → check Ball Boy → Back to SPORT → switch to Golf Sim → forward to ADD-ONS shows no Ball Boy, and SUMMARY shows Ball Boy "Not selected", not a leaked "Selected"); non-reset regression confirmed the other direction (Tennis Court → check Ball Boy → Back to SPORT → switch to Pickleball Court → ADD-ONS still shows Ball Boy checked); full happy path on a court booking (Tennis Court, both Ball Boy and Coaching checked) completed end-to-end with 201, SUMMARY showed both "Selected"; full happy path on a simulator booking (Golf Sim, Coaching checked) completed end-to-end with 201, SUMMARY showed only Coaching "Selected"; a fetch-body patch confirmed neither `ballBoy` nor `coaching` is ever serialized into the outgoing `POST /api/bookings` body, checked in both the court case (Tennis Court, both checked) and the simulator case (Pickleball Simulator, Coaching checked). Unaffected steps (SportStep, CourtStep, DateTimeStep, DetailsStep, StepIndicator, ReviewStep wording) behaved exactly as before — none of those files were touched. Regression-confirmed via Supabase MCP: `booking_no_overlap` exclusion constraint present; RLS enabled on 16/16 application tables with 32 deny-all policies unchanged; cron route re-verified 401 (no auth) / 200 (correct bearer, read from `.env.local`). `package.json`/`package-lock.json` diff empty. Four test `Booking`/`Customer` rows created during verification (Tennis Court, Golf Sim, Pickleball Sim, plus one earlier Tennis Court add-ons check) deleted via a one-off Prisma script — baseline (5 ResourceType / 9 Resource / 21 PricingRule / 1 GuestFeeRule, 0 bookings/customers) confirmed restored. Build Status unchanged (wizard is still 6 steps).

- 2026-08-12 — "Number of guests" relocated from DETAILS to ADD-ONS, matching the split already done for Ball Boy/Coaching. `DetailsStep.tsx` now holds only name/phone/email — the `isCourt`/`guestCount`/`onGuestCountChange` props it previously took were removed entirely, since `isCourt` had no other use in that file once the guest field left. `AddOnsStep.tsx` gained `guestCount`/`onGuestCountChange` props and now renders the guest-count input first, gated on `isCourt` exactly like Ball Boy, in its own untitled block above the existing Ball Boy/Coaching bordered box — kept as a separate sibling `<div>` (not folded into that box) so the "Coming soon — not yet included in your booking" note, which only applies to Ball Boy/Coaching, doesn't visually read as covering guest count too. `BookingForm.tsx`: the two props moved from the step 4 (`DetailsStep`) call to the step 5 (`AddOnsStep`) call; `isCourt` was already passed to both steps, so no change there. guestCount's state, its reset-to-0 on every `resourceTypeId` change (untouched effect, still fires regardless of court/simulator direction), its role in the live price estimate, and its role in the `POST /api/bookings` body (`isCourt ? guestCount : 0`, unchanged ternary) are all exactly as before — only which step renders the input moved. No schema/API/pricing changes; no new dependencies; `StepIndicator.tsx`, `ReviewStep.tsx`, `SportStep.tsx`, `CourtStep.tsx`, `DateTimeStep.tsx`, `TimeSlotGrid.tsx`, `Calendar.tsx`, API routes, and Prisma files untouched. VERIFY performed in the browser pane: Tennis Court and Pickleball Court's DETAILS step confirmed to show only name/phone/email (no guest field); their ADD-ONS step shows Number of guests above Ball Boy/Coaching, in its own block distinct from the bordered checkbox box; Tennis Sim/Pickleball Sim/Golf Sim ADD-ONS confirmed to show only Coaching, no guest field and no Ball Boy (both correctly `isCourt`-gated). Reset regression: Tennis Court → guestCount set to 2 on ADD-ONS → Back to SPORT → switch to Golf Sim → forward to ADD-ONS (guest field correctly hidden) → back to SPORT → switch back to Tennis Court → forward to ADD-ONS confirmed guestCount reset to 0, i.e. the reset effect still fires on every `resourceTypeId` change rather than having been accidentally narrowed to only court→non-court transitions. Full happy path, Tennis Court, guestCount=2 + Ball Boy checked: reached SUMMARY with the guest fee correctly folded into the estimate (₱1,050.00), submitted 201, confirmation total matched; Ball Boy shown as "Selected" on SUMMARY but confirmed still excluded from both the price and the outgoing request body (pre-existing UI-only behavior, untouched). Full happy path, Golf Sim: confirmed no guest-count field ever appeared and the submitted booking's `guest_count` was 0 server-side (existing ternary). Back/forward integrity: a guestCount entered on ADD-ONS survived Back-then-Continue navigation (DOM-verified, not just visual). Regression-confirmed via Supabase MCP: `booking_no_overlap` exclusion constraint present; RLS enabled on 16/16 application tables with 32 deny-all policies unchanged; cron route re-verified 401 (no auth, `{"error":"Unauthorized"}`). `package.json`/`package-lock.json` diff empty; only the three in-scope files touched. Two test `Booking`/`Customer` rows created during verification (Tennis Court, Golf Sim) deleted via a one-off Prisma script (Supabase MCP is read-only) — baseline (5 ResourceType / 9 Resource / 21 PricingRule / 1 GuestFeeRule, 0 bookings/customers) confirmed restored. Build Status unchanged (wizard is still 6 steps: SPORT/COURT/DATE & TIME/DETAILS/ADD-ONS/SUMMARY); "Next Up" below still reads correctly (PayMongo integration remains the next item).

- 2026-08-12 — Add-on backend wiring, no frontend changes. `prisma/seed.ts`: `AddOnService` seeded (2 rows: `ball_boy` "Ball Boy", `coaching_fee` "Coaching", upsert-by-slug) and `AddOnPricingRule` seeded (16 rows: Ball Boy court-only both tiers at ₱150, Coaching courts pax-1/pax-2 both tiers, Coaching simulators member-only for Tennis/Pickleball Sim at ₱800 and both tiers for Golf Sim at ₱1,000 — non-member Tennis Sim/Pickleball Sim coaching intentionally has no row, same "no row = not offered" precedent as the existing non-member golf-sim 30-min `PricingRule`). Hit a Prisma limitation along the way: its compound-unique input type (and the runtime validator behind `.upsert()`/`.findUnique()`) rejects `null` for a nullable field in a composite key, even though the DB column and the actual unique constraint allow it — worked around by using `findFirst` + conditional `create`/`update` in the seed loop (same pattern already used for `Resource` seeding) instead of `.upsert()`, and `findFirst` instead of `.findUnique()` in the two lookups added to `POST /api/bookings`. `GET /api/resources` extended with an `addOnPricing` array per `ResourceType` (joined `AddOnPricingRule`+`AddOnService`, both rate tiers, no server-side tier filtering — same convention as the existing `pricing` array). `POST /api/bookings` extended: `ballBoy`/`coaching`/`coachingPaxCount` accepted as optional fields (default `false`/absent), validated (ball boy 400s on non-court resources; court coaching requires `coachingPaxCount` of exactly 1 or 2; simulator/non-court coaching ignores any `coachingPaxCount` sent), priced by looking up the matching `AddOnPricingRule` per resource type + rate tier + pax (400 if no row — this is what naturally rejects non-member Tennis Sim/Pickleball Sim coaching), and persisted as one `BookingAddOn` row per add-on inside the existing transaction (price snapshotted onto `amountCentavos`, per the schema's own doc comment). Response gained `addOns: [{ service, paxCount, amountCentavos }]` and a top-level `addOnsTotalCentavos` — `totalAmountCentavos` is untouched, still scoped to court/sim rate + guest fee only. `prisma/schema.prisma` and all frontend files (`BookingForm.tsx`, `AddOnsStep.tsx`, etc.) were not touched — the UI still never sends these fields, confirmed by a real end-to-end browser booking (Tennis Court, Ball Boy/Coaching left unchecked) whose actual `POST /api/bookings` request produced `addOns: [], addOnsTotalCentavos: 0` in the response, same as before this prompt. VERIFY performed via Supabase MCP + direct `POST`/`GET` calls against the local dev server (already running, hot-reloaded the route changes) plus one browser pass: seed row counts confirmed exactly 2 `AddOnService` / 16 `AddOnPricingRule`, with the two non-member sim-coaching combinations confirmed absent by direct row dump; re-running `npm run db:seed` left all row counts unchanged (idempotent); `GET /api/resources` spot-checked for Tennis Court (ball_boy + coaching pax 1/2 both tiers) and Tennis Sim (coaching, member only); direct `POST /api/bookings` calls covered all 7 scenarios in the originating prompt (Tennis Court ballBoy+coaching pax1 → `addOnsTotalCentavos: 95000`; pax2 → `135000`; Golf Sim coaching no pax → `100000`, `paxCount: null`; Tennis Sim coaching → 400; Golf Sim ballBoy → 400; Tennis Court coaching without pax → 400; no add-on fields at all → `addOns: [], addOnsTotalCentavos: 0`), each matching the prompt's expected values exactly; the three successful bookings' `BookingAddOn` rows confirmed via Supabase MCP to have the correct snapshotted `amountCentavos`. Regression-confirmed via Supabase MCP: `booking_no_overlap` exclusion constraint present; RLS enabled on 16/16 tables with 32 deny-all policies unchanged. `package.json`/`package-lock.json` diff empty; `git status` confirmed only the three in-scope files (`prisma/seed.ts`, `src/app/api/bookings/route.ts`, `src/app/api/resources/route.ts`) touched. Five test `Booking`/`BookingAddOn` rows and two test `Customer` rows created during verification (one from the direct-`POST` scenarios, one from the browser pass) deleted via a one-off Prisma script run from inside the project directory (Supabase MCP is read-only) — baseline confirmed restored, now including the new seed data: 5 `ResourceType` / 9 `Resource` / 21 `PricingRule` / 1 `GuestFeeRule` / 2 `AddOnService` / 16 `AddOnPricingRule`, 0 bookings/customers/bookingAddOns.

- 2026-08-12 — ADD-ONS step wired to the live pricing already returned by `GET /api/resources` (`addOnPricing` array), replacing the "Coming soon — not yet included in your booking" placeholder. `AddOnsStep.tsx`: Ball Boy's label now shows its non-member price inline (still gated on `isCourt`, unchanged); Coaching is now gated on `coachingPricing.available` (true only when at least one non-member `coaching_fee` row exists for the resource type) instead of rendering unconditionally, which is what hides it for Tennis Sim/Pickleball Sim; renders as a flat-priced label with no further UI when a single paxCount-null rule exists (Golf Sim), or as a bare "Coaching" checkbox that reveals two priced radio buttons (1 Pax / 2 Pax) when checked, for either court type. `BookingForm.tsx`: added `getBallBoyPricing`/`getCoachingPricing` helpers deriving availability/pricing per resource type from `addOnPricing`, filtered to `rateTier === 'non_member'` only; new `coachingPaxCount` state, reset to `null` on every `resourceTypeId` change (unconditional, same treatment as `guestCount`) and also cleared whenever coaching is unchecked (`handleCoachingChange` wraps `setCoaching`); the existing reset effect also clears `coaching` itself when the newly selected type's coaching pricing isn't available, so a lingering selection can't survive a switch to Tennis Sim/Pickleball Sim; `canContinue`'s step-5 case now blocks Continue only when Coaching is checked on a court with no pax chosen; a new `addOnsEstimateCentavos` memo (Ball Boy price + the pax-matched or flat Coaching price) is computed alongside the existing untouched `estimateCentavos` and both are passed to `ReviewStep`; `handleConfirm`'s submit body gained `ballBoy`, `coaching`, and `coachingPaxCount` (only included when `coaching && isCourt`), and the 201 response's `addOns`/`addOnsTotalCentavos` now flow straight into `confirmation` via the already-extended `BookingSuccess` type — no separate state needed. `ReviewStep.tsx`: Ball Boy's line now shows its price when selected instead of a bare "Selected"; Coaching's line shows the chosen pax count (e.g. "2 Pax") when applicable; a new add-ons-subtotal line and a combined total (existing estimate + add-ons) replace the old "doesn't include Ball Boy or Coaching" caveat. `BookingConfirmation.tsx`: `BookingSuccess` extended with `addOns`/`addOnsTotalCentavos` (matching the API's existing response shape), and the confirmation `dl` now lists each returned add-on (service label + pax if present + amount), an add-ons-total line, and a combined total when any add-ons were purchased — all sourced directly from the server response, never recomputed client-side. `guestCount` and its reset behavior were not touched. No schema/API/pricing changes; no new dependencies; `DetailsStep.tsx`, `StepIndicator.tsx`, `SportStep.tsx`, `CourtStep.tsx`, `DateTimeStep.tsx`, `TimeSlotGrid.tsx`, `Calendar.tsx`, and all API routes/Prisma files untouched. VERIFY performed in the browser pane: Tennis Court ADD-ONS showed Ball Boy (₱150.00) and a bare "Coaching" checkbox that revealed 1 Pax (₱800.00)/2 Pax (₱1,200.00) radios on check, with Continue disabled until a pax option was picked and enabled immediately after; Golf Sim ADD-ONS showed only a flat "Coaching — ₱1,000.00" with no radios and no pax gate; Pickleball Simulator ADD-ONS rendered its checkbox block empty (no Ball Boy, no Coaching), confirmed via a 0-checkbox DOM count. Reset regression: Tennis Court → Coaching checked, 2 Pax picked → Back to SPORT → Pickleball Simulator → forward to ADD-ONS (empty, as expected) → SUMMARY correctly showed Coaching "Not selected" (no leaked "2 Pax") → back to SPORT → Pickleball Court → ADD-ONS showed Coaching unchecked with Continue correctly disabled again once re-checked (pax cleared on every type change, including court→court, per spec) until pax was re-picked. Full happy path, Tennis Court, Ball Boy + Coaching (2 Pax) + guestCount 2: SUMMARY showed a ₱1,350.00 add-ons subtotal (₱150 + ₱1,200) and a ₱2,400.00 combined total → 201 → confirmation screen showed the identical figures (Ball Boy ₱150.00, Coaching — 2 Pax ₱1,200.00, Add-ons total ₱1,350.00, Total ₱2,400.00), sourced from the actual response. Full happy path, Golf Sim, Coaching checked: SUMMARY showed a ₱1,000.00 add-ons subtotal → 201 → confirmation matched exactly (Coaching ₱1,000.00, Add-ons total ₱1,000.00, Total ₱2,150.00). Regression-confirmed via Supabase MCP: `booking_no_overlap` exclusion constraint present; RLS enabled on 16/16 tables with 32 deny-all policies unchanged; `AddOnService`/`AddOnPricingRule` counts unchanged (2/16); cron route re-verified 401 (no auth). `package.json`/`package-lock.json` diff empty; `git status` confirmed only the four in-scope files touched. Two test `Booking` rows (plus their `BookingAddOn` and `Customer` rows) created during verification deleted via a one-off Prisma script run from inside the project directory (Supabase MCP is read-only) — baseline confirmed restored: 5 ResourceType / 9 Resource / 21 PricingRule / 1 GuestFeeRule / 2 AddOnService / 16 AddOnPricingRule, 0 bookings/customers/bookingAddOns.

- 2026-08-12 — Documentation-only update, no code/schema/seed changes. Clarified in CLAUDE.md that the PayMongo test credentials currently in `.env.local` (`PAYMONGO_SECRET_KEY`/`PAYMONGO_PUBLIC_KEY`/`PAYMONGO_WEBHOOK_SECRET`) belong to Arjay's personal PayMongo test-mode account, not the client's — used temporarily to unblock PayMongo integration work while the client's own account doesn't exist yet. "Open / Not Yet Decided" now documents this provenance and the required swap points (client's own test keys before `staging`, live keys before `main`, both explicitly confirmed before any payments-related promotion to `main`); the Branch Promotion Policy's existing "Open question" bullet on PayMongo key handling now points to that entry instead of duplicating the explanation. `.env.local` itself was not read or modified (gitignored, out of scope). Build Status unchanged — PayMongo payment integration remains "Not started."

- 2026-08-12 — PayMongo Checkout Session backend implemented (Payment Intents + client-side Elements deliberately not used — hosted Checkout Sessions instead, now documented as a new CLAUDE.md Architecture Decision). Backend-only; no file under `src/components/booking/` touched. `prisma/schema.prisma`: `Payment` gained `paymongoCheckoutSessionId String? @unique @map("paymongo_checkout_session_id")`, placed above the existing `paymongoPaymentIntentId`. Applied as an isolated migration (`20260812055040_add_paymongo_checkout_session_id_to_payments`) — `prisma migrate dev` refused to run in this non-interactive environment even with `--create-only`, so the migration SQL was hand-written (plain `ALTER TABLE`/`CREATE UNIQUE INDEX`, matching Prisma's own generated form for this column shape) and applied via `prisma migrate deploy`, then `prisma generate` re-run — no schema drift, confirmed via `prisma migrate deploy`'s own history check and a direct Supabase MCP column/index query. `src/lib/paymongo.ts` added: `createPaymongoCheckoutSession`, `expirePaymongoCheckoutSession` (best-effort, internally catches, never throws), `verifyPaymongoWebhookSignature` (HMAC-SHA256 over `t.rawBody`, timing-safe comparison against `te`), plus one function beyond the originating prompt's explicit export list — `retrievePaymongoCheckoutSession` — added to support `POST /api/checkout`'s idempotent-retry path (Part 4 step 3 needs a Retrieve Checkout Session call; adding it here reuses the module's private Basic-auth header builder instead of duplicating that logic at the route level, consistent with the module's own "extract shared logic once" rationale). `POST /api/checkout` (new) accepts `{ bookingId }`, resolves the booking with its resource/customer/add-ons, is idempotent on retry (returns the existing session's current `checkout_url` without creating a second `Payment` row), 409s if the hold has expired or the booking isn't payable, builds one line item for the base booking plus one per add-on (pax count included in the name when applicable), creates the PayMongo session and a `Payment` row (`status: pending`), and returns `{ checkoutUrl }`. `POST /api/webhooks/paymongo` (new) reads the raw body before any parsing, verifies `Paymongo-Signature`, ignores non-`checkout_session.payment.paid` events (200, no-op), extracts `bookingId` from `metadata`, is idempotent (no-op if already `paid`), and in one transaction marks `Payment` paid (with `paidAt`/`paymongoPaymentIntentId`) and flips `Booking` to `confirmed` only if it's still `pending_payment` — otherwise logs a clearly-labeled manual-review error instead of silently overwriting a `cancelled` booking. `POST /api/bookings` and `GET /api/cron/expire-bookings` both restructured identically: the stale-hold cleanup now `findMany`s first (capturing linked `Payment.paymongoCheckoutSessionId` where still `pending`) before `updateMany`-cancelling the bookings and `updateMany`-failing their payments inside the transaction; after the transaction commits, `expirePaymongoCheckoutSession` is called per captured session id outside the transaction so a slow/failing PayMongo call can never hold the DB transaction open. Neither route's external request/response shape changed. CLAUDE.md's Tech Stack table corrected to "Next.js 16 (App Router) + TypeScript" (was unversioned; confirmed 16.3.0 via `package.json` and dev server output) — no other row touched.
  - **Deviation flagged**: live-tested against PayMongo's actual Checkout Sessions API and found it rejects requests with `payment_method_types` omitted (`"Parameter payment_method_types is required"`), contradicting the originating prompt's instruction to leave it unset. Since Arjay's account's enabled payment methods aren't knowable from here, `payment_method_types: ['card']` was hardcoded as the minimum guaranteed-available method (commented inline in `paymongo.ts` explaining why) rather than guessing at e-wallets that may not be enabled — revisit once the client's own PayMongo dashboard configuration is known, and reconsider whether this should become a small config surface instead of a hardcoded array.
  - VERIFY performed: via Supabase MCP, `booking_no_overlap` still present, RLS deny-all still enabled on 16/16 tables (32 policies), and `payments.paymongo_checkout_session_id` confirmed as a nullable `text` column backed by a unique index. Live-flow checks were run directly against the running local dev server and PayMongo's real test-mode API (Arjay's personal test keys, per the existing Open/Not Yet Decided entry) rather than through the ngrok+Dashboard-webhook path described in the prompt's prerequisite, since that setup is explicitly Arjay's manual step and wasn't confirmed done in this session — `POST /api/checkout` was exercised directly (plain booking: 200 with a real `checkoutUrl`, `Payment` row created with matching `amountCentavos`/`paymongoCheckoutSessionId`; add-ons booking: PayMongo's own line items confirmed via the Retrieve Checkout Session endpoint to correctly list the base item plus "Ball Boy"/"Coaching (1 pax)" summing to the `Payment.amountCentavos`; repeat call on an already-paid-for booking confirmed idempotent, no second `Payment` row). The webhook handler was exercised by self-computing a valid `Paymongo-Signature` against the real `PAYMONGO_WEBHOOK_SECRET` (simulating what PayMongo's own delivery would send, since no ngrok tunnel was available to receive a real one) — confirmed 200, `Payment` flipped to `paid` with `paidAt`/`paymongoPaymentIntentId` set and `Booking` flipped to `confirmed`; resending the identical signed event confirmed idempotent (no change to `paid_at`); a missing signature header and a well-formed-but-wrong one both confirmed 400 with zero DB writes. Both expiry call sites were exercised by backdating a booking's `createdAt` via a one-off Prisma script (Supabase MCP is read-only) past `HOLD_MINUTES`, then triggering each path independently — the cron route (`GET /api/cron/expire-bookings`, bearer token read from `.env.local` without ever being echoed) and a competing `POST /api/bookings` on the same resource/time (which itself returned 201, confirming the slot was actually freed) — both confirmed via Supabase MCP that the stale `Booking`→`cancelled`/`Payment`→`failed`, and both confirmed via a direct PayMongo Retrieve Checkout Session call that the linked session's own `status` had flipped to `expired`. Regression: a full add-ons booking (`ballBoy` + `coaching` 1-pax + `guestCount`) through the restructured `POST /api/bookings` still produced byte-identical response shape and pricing to the pre-Part-6 behavior, confirming the transaction restructuring didn't change externally-visible behavior; cron route re-confirmed 401 with no/wrong auth; `package.json`/`package-lock.json` diff empty (no new dependencies — PayMongo calls used plain `fetch`, signature verification used Node's built-in `crypto`, as required). Not run in this session (requires Arjay's manual ngrok+Dashboard setup per the prompt's own prerequisite, and an actual card payment in a browser): the real PayMongo-delivered webhook over ngrok, and the Dashboard's "resend" idempotency check on a real delivery — the self-signed-webhook and direct-API checks above cover the same code paths but not PayMongo's own delivery infrastructure. Five test `Booking` rows (one flipped to `confirmed` via the simulated webhook, two `cancelled` via the two expiry paths, one left `pending_payment`, one with add-ons) and their `Payment`/`BookingAddOn`/`Customer` rows, created during verification, deleted via a one-off Prisma script — baseline confirmed restored: 5 ResourceType / 9 Resource / 21 PricingRule / 1 GuestFeeRule / 2 AddOnService / 16 AddOnPricingRule, 0 bookings/customers/payments/bookingAddOns.

- 2026-08-12 — Wizard wired to real PayMongo Checkout and a live polling confirmation page — the booking + payment flow is now complete end-to-end. CLAUDE.md's Tech Stack Payments row corrected to "PayMongo (Checkout Sessions API)" and the `paymongo-integration` skill's Claude Code Skills bullet reworded to describe the Checkout Session flow instead of the stale "Payment Intent flow" wording (both doc-drift only, no code). `GET /api/bookings/[id]` added (new, public/no-auth, same trust tier as `GET /api/resources`): `await params` per Next.js 16's promise-based dynamic route params; returns booking details in a shape reusing `POST /api/bookings`' field names where the concept already existed (`id`/`status`/`startTime`/`endTime`/`totalAmountCentavos`/`addOns`/`addOnsTotalCentavos`) plus new fields this page needs (`resource: { typeName, label }`, `guestCount`, `customer: { name }` — email/phone deliberately omitted from this public response, `payment: { status, amountCentavos, paidAt } | null`); 404s on unknown id. `src/app/book/confirmation/page.tsx` (new, server component) wraps `ConfirmationContent.tsx` (new, client component, co-located in the same route folder since it's route-specific rather than a reusable feature component) in `<Suspense>` per Next.js's `useSearchParams()` requirement. `ConfirmationContent` reads `bookingId` from the query string; shows a no-fetch "no booking reference" state if absent; fetches `GET /api/bookings/{id}` on mount; 404 renders a distinct "couldn't find that booking" state; `pending_payment` polls the same endpoint every 2s (via `setTimeout` chaining, not `setInterval`, so a slow response can't overlap the next tick) up to 15 attempts (~30s) then stops and shows a "taking longer than expected" state with a manual "Check Again" button (one more fetch, resumes the normal branching — not a new auto-poll loop); a transient fetch error during polling doesn't hard-stop the loop, it just counts toward the same 15-attempt budget and retries; `confirmed` renders the reused `BookingConfirmation` component; `cancelled` renders a distinct state (different from loading and success) surfacing the booking id, since this is the webhook handler's rare manual-review edge case and a customer who actually paid needs to know to contact support, not see a generic error. `BookingConfirmation.tsx` adapted (its only consumer was the inline post-201 render this prompt removes, so this was a clean break, not a compat shim): now takes a single `booking` prop shaped like the `GET /api/bookings/[id]` response (exported as `BookingDetail`, replacing the old `BookingSuccess` type keyed to the `POST /api/bookings` response) instead of `booking`+`resourceLabel`+`resourceTypeName` separately; heading changed from "Booking held" to "Booking confirmed" and the closing note now confirms payment instead of claiming a team follow-up, since this component only ever renders once `status === 'confirmed'`; a guest-count line was added (only when `guestCount > 0`) since the new response shape carries it and it's consistent with `ReviewStep`'s existing convention. `BookingForm.tsx`: removed the inline `BookingConfirmation` render entirely — a 201 from `POST /api/bookings` no longer shows a confirmation screen itself, it stores the id in new `bookingId` state and immediately calls a new `startCheckout(id)` helper (`POST /api/checkout`, then `window.location.href = checkoutUrl` on success — a full browser navigation, not `router.push`, since the target is PayMongo's domain); `submitting`/`submitError` still cover exactly the `POST /api/bookings` phase and its existing 409/400/network branches, completely unchanged; new `checkingOut`/`checkoutError` state covers the `POST /api/checkout` phase; `handleRetryCheckout` re-calls `startCheckout` with the already-stored `bookingId` only (confirmed via source read — it never touches `POST /api/bookings`); `handleStartOver` resets `step` to 1 and every piece of wizard state including `bookingId` back to its initial default. `ReviewStep.tsx` gained `checkingOut`/`checkoutError`/`onRetryCheckout`/`onStartOver` props (button/loading-state markup already lived here, confirmed by reading both files first, so the new states were wired here rather than in `BookingForm.tsx` — steps still hold no state of their own, per the existing wizard convention): when `checkoutError` is set, the normal Back/Confirm row is replaced by the error text plus Start Over/Try Again buttons (Try Again labeled "Redirecting to payment…" and disabled while `checkingOut`); otherwise the existing Back/Confirm row is shown with the Confirm button's label now three-way (`submitting` → "Creating your booking…", `checkingOut` → "Redirecting to payment…", else "Confirm Booking") and both Back and Confirm disabled during either phase. `AnnouncementGate.tsx`, `SportStep.tsx`, `CourtStep.tsx`, `DateTimeStep.tsx`, `DetailsStep.tsx`, `AddOnsStep.tsx`, `StepIndicator.tsx`, `TimeSlotGrid.tsx`, `Calendar.tsx`, every Prisma/schema/migration file, and `POST /api/bookings`/`POST /api/checkout`/`POST /api/webhooks/paymongo`/`GET /api/resources`/`GET /api/availability`/the cron route were not touched. No new dependencies.
  - **Finding, not fixed here (outside this prompt's file scope and outside code entirely)**: live-tested the real redirect back from PayMongo after a successful test payment and it 404'd on `checkout.paymongo.com` instead of landing on `/book/confirmation`. Root cause confirmed via the PayMongo API and a byte-level inspection of `.env.local`: `NEXT_PUBLIC_APP_URL=""` — an empty string, not a real URL — so the pre-existing (unchanged this prompt) `POST /api/checkout` route's `` `${appUrl}/book/confirmation?...` `` / `` `${appUrl}/book` `` template strings are sending PayMongo bare relative paths (`/book/confirmation?...`, `/book`) instead of absolute URLs, and PayMongo stores/redirects to exactly what it's given. This is a `.env.local` value problem, not a code defect — `.env.local` isn't in this prompt's file scope and env values are Arjay's to set, so it wasn't edited here. **Action needed before the redirect will work end-to-end: set `NEXT_PUBLIC_APP_URL="http://localhost:3000"` (or the current ngrok URL, if customers should land through the tunnel) in `.env.local` and restart the dev server** (Next.js only reads `.env.local` at server boot, so an already-running process won't pick up a value added/fixed after it started). Everything downstream of that redirect was verified working correctly by navigating directly to `/book/confirmation?bookingId=...` instead (see VERIFY below) — this is purely a broken hop between PayMongo and the app, not a problem with the confirmation page, the polling logic, or `GET /api/bookings/[id]`.
  - **Second finding, also environmental, also not fixed here**: separately, `https://crop-disband-jingle.ngrok-free.dev` (the free-tier ngrok domain from this prompt's VERIFY prerequisite) serves its own "you are about to visit…" interstitial warning page on first visit per browser session, which would sit in front of the real confirmation page for an actual customer being redirected through that tunnel too. Free-tier-only (removable via the `ngrok-skip-browser-warning` header, a non-standard User-Agent, or a paid plan) and irrelevant once `NEXT_PUBLIC_APP_URL` points at the real Vercel domain in `staging`/`main`, so not something to fix now — just worth knowing this ngrok tier isn't representative of the real redirect experience during any further local ngrok-based testing.
  - **Third finding, genuine and separate from the above**: while diagnosing the redirect, found the PayMongo account's webhooks list (`GET /v1/webhooks`) has its one *enabled* hook pointed at `https://winston-booking-site.vercel.app/api/webhooks/paymongo` (a deployed URL, not this session's ngrok tunnel) — every ngrok-pointed hook on the account is `disabled` (`max_retries_exceeded`) with a stale URL from a prior ngrok session. This prompt's VERIFY prerequisite assumed the Dashboard webhook was already pointed at the current tunnel; it wasn't, which is why the real payment made during VERIFY never triggered a real webhook delivery (confirmed via the ngrok inspector at `127.0.0.1:4040` showing zero inbound POSTs to `/api/webhooks/paymongo` after payment). Worked around for verification purposes only by re-using the prior session's self-signed-webhook technique (a fabricated `checkout_session.payment.paid` payload, HMAC-signed with the real `PAYMONGO_WEBHOOK_SECRET`) against the local endpoint for the specific real booking just paid for — confirmed it correctly flipped to `confirmed` and that the confirmation page's polling loop picked up the change and stopped within one 2s tick. Registering/updating the Dashboard webhook for whichever URL Arjay wants live traffic tested against is his manual step, unchanged from the prior prompt's prerequisite framing.
  - VERIFY performed in the connected browser pane plus direct API/Supabase MCP checks: confirmed dev server (`localhost:3000`) and ngrok tunnel both reachable before starting. Full wizard walkthrough (Tennis Court, Court 1, Aug 20 2:00 PM, Ball Boy checked, ₱900.00 on SUMMARY) confirmed the Confirm Booking button's label sequence live in the DOM — "Creating your booking…" then "Redirecting to payment…" — immediately before the browser actually navigated to a real `checkout.paymongo.com/<session-id>` URL; the checkout page's own line items/subtotal/total (`Tennis Court — Court 1 (60 min)` ₱750.00, `Ball Boy` ₱150.00, Subtotal/Total ₱900.00) and the Reference Number field (matching the created booking's id) confirmed correct; paid with PayMongo's standard test Visa (`4343434343434345`) and PayMongo's own page showed "Card Payment Received!". Because of the redirect finding above, followed up by navigating directly to `/book/confirmation?bookingId=<that id>` on `localhost:3000`: initial "Loading your booking…" state confirmed, `GET /api/bookings/[id]` calls confirmed firing every ~2s via the Network panel (14 requests logged over the polling window) while the booking was still genuinely `pending_payment` in the DB (confirmed via Supabase MCP, consistent with the webhook-misdirection finding above) — after firing the self-signed webhook for this booking, the very next poll (within 2s) picked up `confirmed` and rendered the full `BookingConfirmation` display with every field matching what was actually booked (resource, date/time, duration, price, Ball Boy line and amount, add-ons total, combined total, customer name in the closing note); confirmed via the Network panel that no further `GET /api/bookings/[id]` requests fired after the confirmed render (polling genuinely stopped, not just visually settled). Cancelled-booking display: backdated a second real booking's `createdAt` past `HOLD_MINUTES` via a one-off Prisma script, triggered `GET /api/cron/expire-bookings` with the correct bearer token (cancelledCount: 1), navigated directly to its confirmation URL — confirmed the distinct "couldn't be confirmed" state rendered (not the loading state, not the success state) showing the booking id. Not-found: `?bookingId=nonexistent` confirmed the distinct "couldn't find that booking" message, and the Network panel confirmed both the page load and the underlying `GET /api/bookings/nonexistent` returned clean 404s, never a 500. Missing bookingId: `/book/confirmation` with no query param confirmed the "no booking reference was provided" state, and the Network panel confirmed zero `/api/bookings/` requests fired — the no-fetch-attempted requirement. Retry/Start Over: verified via source read rather than forcing a live checkout failure (per the prompt's own fallback allowance) — `handleRetryCheckout` calls only `startCheckout(bookingId)`, never `POST /api/bookings`; `handleStartOver` resets `step` to 1 and explicitly resets every wizard field including `bookingId` to `null`. Regression, confirmed via Supabase MCP: `booking_no_overlap` exclusion constraint present; RLS enabled on 16/16 tables with 32 deny-all policies, both unchanged from before this prompt. Cron route re-confirmed 401 (no auth) / 200 (correct bearer, used for the cancelled-booking test above). `package.json`/`package-lock.json` diff empty; `git status` confirmed only the files listed in this prompt's SCOPE were touched (`CLAUDE.md`, `BookingConfirmation.tsx`, `BookingForm.tsx`, `ReviewStep.tsx`, plus the two new route folders) — `AnnouncementGate.tsx` through `Calendar.tsx`, all Prisma files, and the four pre-existing API routes named in SCOPE's do-not-touch list were all confirmed untouched. Three test `Booking` rows (one real payment flipped to `confirmed` via the self-signed-webhook workaround, one backdated-and-cancelled, one abandoned mid-checkout for the cancel-path/redirect investigation) and their `Payment`/`BookingAddOn`/`Customer` rows, created during verification, deleted via a one-off Prisma script — baseline confirmed restored: 5 ResourceType / 9 Resource / 21 PricingRule / 1 GuestFeeRule / 2 AddOnService / 16 AddOnPricingRule, 0 bookings/customers/payments/bookingAddOns.

- 2026-08-12 — Booking reference (the `Booking.id` already returned by both `POST /api/bookings`' 201 response and `GET /api/bookings/[id]`, and already shown as PayMongo's own "Reference Number" field on its checkout page) is now visible in our own UI, not just PayMongo's. No architecture change — the `Booking` row is still only created on "Confirm Booking" on SUMMARY; this only surfaces the id that already exists once it does. `ReviewStep.tsx` gained a `bookingId: string | null` prop and now renders a "Booking Reference: {bookingId}" line — inside the `checkoutError` branch (alongside Start Over/Try Again, so a customer who hits a checkout failure still has something to reference for support) and, in the normal branch, only while `checkingOut` is true (the brief "Redirecting to payment…" window right after the 201 lands and before the PayMongo redirect fires); it does not render before Confirm is ever clicked, since `bookingId` is still `null` then. `BookingForm.tsx` passes its existing `bookingId` state straight through — no change to when/how that state itself gets set (201 → set, Start Over → reset to `null`). `BookingConfirmation.tsx` gained the same "Booking Reference: {booking.id}" line, positioned above the resource/date/time block since this is the enduring value a customer might reference later; sourced directly from the existing `booking` prop, no new fields. The id is displayed raw everywhere — never reformatted, shortened, or hashed — so it always matches what PayMongo shows. `ConfirmationContent.tsx` was intentionally not touched (out of this prompt's scope): its `cancelled` branch already showed the booking id before this prompt (pre-existing, unrelated code), but its `pending_payment` branch still doesn't show one — a pre-existing gap this prompt didn't fix, flagged below rather than silently left implied-fixed.
  - **Gap flagged, not fixed here (out of this prompt's file scope)**: `ConfirmationContent.tsx`'s `pending_payment` render path (`/book/confirmation` while a payment is still being confirmed) shows no booking reference at all — only the `confirmed` path (via `BookingConfirmation`) and the pre-existing `cancelled` path do. A customer who lands on `/book/confirmation` while still `pending_payment` (e.g. slow webhook) currently has nothing to reference if they need to contact support before it resolves. Fixing this would require editing `ConfirmationContent.tsx`, which this prompt's SCOPE explicitly excluded.
  - VERIFY performed in the browser pane plus Supabase MCP: full Tennis Court happy path confirmed "Booking Reference: cmsps1xp9000nw4rg9fxeljr1" in the DOM during "Redirecting to payment…" on SUMMARY, not present beforehand; the same id matched PayMongo's own "Reference Number" field on its real checkout page after redirect. Backdating this booking to `confirmed` via a one-off Prisma script and reloading `/book/confirmation?bookingId=...` showed the reference prominently above the resource/date/time block; flipping it to `cancelled` confirmed the pre-existing cancelled-state reference still renders unaffected; `?bookingId=nonexistent` and no-`bookingId` states rendered their existing distinct messages with nothing broken. A second full happy path (Golf Simulator) confirmed no reference before Confirm, the correct new reference during redirect, and a genuinely different id (`cmsps6sz2000sw4rgtme0rly2`) from the first booking — no leakage between cycles. Start Over's `bookingId → null` reset and Try Again's reuse of the same `bookingId` (never a second `POST /api/bookings`) were verified via source read only, per the prior session's own fallback allowance — no live checkout failure was forced. Regression confirmed via Supabase MCP: `booking_no_overlap` exclusion constraint present; RLS deny-all enabled on 16/16 tables with 32 policies unchanged; cron route re-confirmed 401 (no auth) / 200 (correct bearer). `package.json`/`package-lock.json` diff empty; `git status` confirmed only `ReviewStep.tsx`, `BookingForm.tsx`, and `BookingConfirmation.tsx` touched. Two test `Booking`/`Customer`/`Payment` rows created during verification were deleted via a one-off Prisma script (Supabase MCP is read-only); a third, pre-existing `pending_payment` `Booking`/`Customer`/`Payment` row (customer "Winston Sip and Serve", present before this session started) was deliberately left alone rather than deleted, since it wasn't created during this verification — but the live cron-route auth check run as part of DB regression verification did sweep it from `pending_payment` to `cancelled` as its designed behavior (the hold had already gone stale, ~48 minutes old). Baseline counts confirmed restored except for that one pre-existing row: 5 ResourceType / 9 Resource / 21 PricingRule / 1 GuestFeeRule / 2 AddOnService / 16 AddOnPricingRule, 1 booking / 1 customer / 1 payment / 2 bookingAddOns (all belonging to that single pre-existing, now-cancelled row, not created by this prompt).

- 2026-08-12 — SUMMARY's automatic redirect to PayMongo replaced with a manual pause screen, fixing a finding from the prior prompt: live testing showed the `checkingOut` phase (where the booking reference was shown) was too brief to ever actually be seen — five rapid consecutive screenshots all landed before `window.location.href = checkoutUrl` fired. `POST /api/bookings` and `POST /api/checkout` still fire automatically and in the same order immediately after "Confirm Booking" is clicked, exactly as before — the only change is what happens after `POST /api/checkout` succeeds. `BookingForm.tsx`: new `checkoutUrl: string | null` state; `startCheckout`'s success branch now does `setCheckoutUrl(json.checkoutUrl)` instead of navigating; a new `handleGoToPayment` performs the actual `window.location.href = checkoutUrl` and is the only place the redirect now happens; `handleStartOver` additionally resets `checkoutUrl` to `null`. `handleRetryCheckout` itself is untouched — it still just re-calls `startCheckout(bookingId)`, so a successful retry now also lands on the pause screen rather than redirecting, consistent with the primary path. `ReviewStep.tsx` restructured from two render states to three: (1) `checkoutError` set — unchanged from the prior prompt, including its own "Redirecting to payment…" label on Try Again, which was deliberately left as-is since that branch is out of this prompt's behavioral change; (2) new — no error, `checkoutUrl` set: replaces the Back/Confirm row with the booking reference, reassurance text ("Your slot is held — continue to payment to confirm it."), a primary "Continue to Payment" button (`onGoToPayment`), and a secondary "Start Over" button — no Back button, since the hold already exists server-side and returning to edit earlier steps would orphan it rather than change it; (3) initial/submitting/checkingOut — the old "Booking Reference" line shown during `checkingOut` was removed (superseded by state 2: `checkingOut` is now just the brief "Preparing your payment…" gap before landing on the pause screen, not the moment the reference is shown), and the Confirm button's `checkingOut` label changed from "Redirecting to payment…" to "Preparing your payment…" to match. `BookingConfirmation.tsx` was read for this prompt's Part 5 live check (a real payment couldn't be forced in the prior prompt's session) — confirmed it already renders "Booking Reference: {booking.id}" correctly, so no changes were needed and no deviation from SCOPE occurred.
  - VERIFY performed in the browser pane plus Supabase MCP and a real PayMongo API call: full Tennis Court happy path showed "Creating your booking…" → "Preparing your payment…" → the new pause screen with "Booking Reference: cmspsybtd000cw4g02k2keh8c" and a Continue to Payment button; waited 5+ seconds and re-checked the DOM twice more, confirming it does not auto-navigate (the exact defect this prompt fixes). DOM-checked the pause screen renders only "Start Over" and "Continue to Payment" — no Back button. Clicking Continue to Payment navigated to a real `checkout.paymongo.com` page whose "Reference Number" field matched the pause screen's reference byte-for-byte. Completed one real end-to-end test payment (PayMongo test Visa `4343434343434345`) through to `checkout.paymongo.com`'s "Card Payment Received!" — confirmed via the PayMongo API that the payment was genuinely captured; the redirect back to `/book/confirmation` hit the same pre-existing `NEXT_PUBLIC_APP_URL=""` misconfiguration flagged in an earlier entry (`GET https://checkout.paymongo.com/book/confirmation?bookingId=... → 404`, confirmed via the Network panel — not a regression from this prompt, `.env.local` is untouched and out of scope), so continued verification by navigating directly to `/book/confirmation?bookingId=...` on `localhost:3000` per that entry's established workaround; since the account's live webhook is still pointed at the Vercel deployment rather than localhost (also pre-existing, per the same earlier entry), delivered a self-signed `checkout_session.payment.paid` webhook (HMAC-SHA256 against the real `PAYMONGO_WEBHOOK_SECRET`, using the real `payment_intent.id` retrieved from PayMongo's own Retrieve Checkout Session API for this specific payment) to the local webhook endpoint — confirmed 200, and the confirmation page then rendered `BookingConfirmation` showing "Booking Reference: cmspsybtd000cw4g02k2keh8c", matching the id used throughout the entire flow. Start Over from the pause screen: DOM-confirmed reset to step 1 (SPORT); a second full booking cycle reached the pause screen with no reference shown before Confirm and a genuinely different reference (`cmspt4ch9000hw4g0rkh8vi2g`) after; clicking Start Over from that pause screen, then completing a third booking, produced a third distinct reference (`cmspt6lkt000mw4g0eo4v5h0o`) with no leakage from either prior cycle — confirming `bookingId`/`checkoutUrl` are both genuinely cleared. `checkoutError` path verified via source read only (per the established fallback allowance — no live checkout failure was forced): `handleRetryCheckout` still calls only `startCheckout(bookingId)`, never `POST /api/bookings` again. Regression confirmed via Supabase MCP: `booking_no_overlap` exclusion constraint present; RLS deny-all enabled on 16/16 tables with 32 policies unchanged. Cron route re-confirmed 401 (no auth) / 200 (correct bearer) — this same auth check, run as part of DB regression verification, incidentally swept two pre-existing stale `pending_payment` rows to `cancelled` (see cleanup note below), which is the route's intended designed behavior, not a side effect specific to this prompt. `package.json`/`package-lock.json` diff empty; `git status` confirmed only `BookingForm.tsx` and `ReviewStep.tsx` were touched by this prompt (plus this `PROGRESS.md` entry) — `BookingConfirmation.tsx` shows as modified in `git status` only because the prior prompt's uncommitted changes are still on disk (git commits are manual, per the Development Workflow), not because this prompt touched it. Five test `Booking` rows created during this verification pass (one real-payment/confirmed, two pause-screen/pending_payment, one from an earlier same-day session, one Start-Over-abandoned) plus the two specific leftover `pending_payment` rows named in this prompt's cleanup instructions (Tennis Court/Court 1 "Claude Verify Test" and "Claude Verify Test 2", both already swept to `cancelled` by the cron check above by the time of cleanup) — all deleted via a one-off Prisma script (Supabase MCP is read-only). One further pre-existing row (customer "Winston Sip and Serve", present before any Claude Code session touched this repo, already flagged and deliberately left alone in the prior prompt's entry) was again left untouched, consistent with that precedent. Baseline seed counts confirmed restored: 5 ResourceType / 9 Resource / 21 PricingRule / 1 GuestFeeRule / 2 AddOnService / 16 AddOnPricingRule; 1 booking / 1 customer / 1 payment / 2 bookingAddOns remaining, all belonging to that single pre-existing "Winston Sip and Serve" row.

- 2026-08-12 — DETAILS step removed from the wizard; Name/Contact/Email plus booking creation itself moved to a new post-SUMMARY Payment page, and the checkout-session hand-off changed from a manual "Continue to Payment" click back to an immediate redirect. StepIndicator's label list is now `['SPORT', 'COURT', 'DATE & TIME', 'ADD-ONS', 'SUMMARY']` (5 steps, was 6) — it already derives count/highlighting from this list, so no loop-bound change was needed. `DetailsStep.tsx` deleted; its Name/Phone/Email fields (same labels, types, and required-field validation, copied verbatim) now live in a new `src/components/booking/steps/PaymentStep.tsx` — a stateless step component per the existing convention, taking `name`/`phone`/`email` + their `onChange`s, `bookingId` (renders "Booking Reference: {bookingId}" only once non-null), `submitting`/`submitError` (the `POST /api/bookings` phase), `checkingOut`/`checkoutError` (the `POST /api/checkout` phase), and `onPayNow`/`onBack`/`onStartOver`. `BookingForm.tsx`: renumbered SPORT=1/COURT=2/DATE & TIME=3/ADD-ONS=4/SUMMARY=5 (`TOTAL_STEPS` 6→5, every `step ===`/`case` comparison in the render block and `canContinue`'s switch updated individually — the old case-4 name/phone/email validation was removed from `canContinue` entirely, since that validation now lives inside `PaymentStep` itself, computed from its own props); new `showPayment` boolean state (default `false`) renders `<PaymentStep />` in place of `<ReviewStep />` at step 5 when true, and `<StepIndicator />` is skipped entirely on this screen (`{!showPayment && <StepIndicator .../>}`). `ReviewStep`'s "Confirm Booking" button now calls a pure `onProceedToPayment` handler (`() => setShowPayment(true)`) — no network call, no loading state — instead of the old `handleConfirm`/`onConfirm` wiring. The former `handleConfirm` was merged into a new `handlePayNow` (wired to `PaymentStep`'s `onPayNow`): if `bookingId` is not yet set, it runs the same `POST /api/bookings` logic as before (same request fields, now including `name`/`email`/`phone` since they're only known at this point) and on 201 immediately chains into `startCheckout`; if `bookingId` is already set (a checkout-session-only retry after a prior `startCheckout` failure), it skips straight to re-calling `startCheckout(bookingId)` and never calls `POST /api/bookings` a second time for the same booking. `startCheckout`'s success branch changed from `setCheckoutUrl(...)` (pause screen) back to an immediate `window.location.href = json.checkoutUrl` — the `checkoutUrl` state and the now-fully-superseded `handleGoToPayment`/`handleRetryCheckout` helpers were removed as dead code (their behavior is now inlined into `handlePayNow`/`startCheckout` directly). A new `onBack` handler on `PaymentStep` just does `setShowPayment(false)` — no other state changes, so returning to SUMMARY (safe here, since no booking exists yet at this point) never touches any earlier wizard field. `handleStartOver` gained `setShowPayment(false)` alongside its existing full reset. `ReviewStep.tsx` reduced to a pure step-transition screen: `name`/`phone`/`email` display lines removed (not known at this point anymore), and every `bookingId`/`checkingOut`/`checkoutError`/pause-screen prop and its markup removed — that entire concern now lives in `PaymentStep`. Resource/date/time/duration, `guestCount`, Ball Boy (with price), Coaching (with pax + price), add-ons subtotal, and the combined total are all unchanged, confirmed nothing was accidentally dropped alongside the deleted blocks; its "Confirm Booking" button keeps its label (no loading state needed anymore, since it no longer triggers a network call) and now wires to `onProceedToPayment`. `POST /api/bookings` and `POST /api/checkout` request/response shapes are byte-identical to before this prompt — purely a frontend sequencing change, confirmed by reusing the exact same request-body construction verbatim. `SportStep.tsx`, `CourtStep.tsx`, `DateTimeStep.tsx`, `TimeSlotGrid.tsx`, `Calendar.tsx`, `AnnouncementGate.tsx`, `BookingConfirmation.tsx`, `ConfirmationContent.tsx`, `src/app/book/page.tsx`, `src/app/book/confirmation/page.tsx`, every Prisma/schema/migration file, and all API routes/`src/lib/paymongo.ts`/`booking-hold.ts`/`business-hours.ts` were not touched. No new dependencies. VERIFY performed in the browser pane (dev server already running) plus Supabase MCP and a direct curl check on the cron route: StepIndicator confirmed exactly 5 circles/labels (SPORT/COURT/DATE & TIME/ADD-ONS/SUMMARY) through SPORT–SUMMARY and absent entirely on the Payment page; SUMMARY confirmed to show no name/phone/email lines with Ball Boy/Coaching/guestCount/add-ons total still correct for a Tennis Court booking with both add-ons selected (₱1,700.00 estimate: ₱750 court + ₱150 Ball Boy + ₱800 Coaching 1-pax); clicking "Confirm Booking" confirmed via the Network panel to fire zero requests (only prior `/api/resources`/`/api/availability` calls present) — the regression this prompt specifically targets; Pay Now confirmed disabled with all three fields empty and enabled once Name/Phone/Email were filled; a full happy path (Tennis Court, Ball Boy + Coaching 1-pax) showed "Booking Reference: cmspyq7nm0016w4g0q4qlnmlv" briefly during "Redirecting to payment…" then genuinely auto-redirected (no manual click) to a real `checkout.paymongo.com` page whose line items (`Tennis Court — Court 1 (60 min)` ₱750.00, `Ball Boy` ₱150.00, `Coaching (1 pax)` ₱800.00, Total ₱1,700.00) and billed-to name/email matched exactly; Back from the Payment page to SUMMARY confirmed via DOM inspection that `name`/`phone`/`email` values survive the round trip unchanged, with StepIndicator reappearing correctly. Not re-forced live this pass (verified via source read of `handlePayNow`, consistent with the established fallback allowance used in prior entries for this same code path): the 409/400/network-preserved-state branches and the checkout-session-only-retry invariant (`bookingId` set → `handlePayNow` skips straight to `startCheckout`, never calls `POST /api/bookings` again) — both are structurally identical to the already-live-tested `POST /api/bookings`/`startCheckout` logic this prompt only moved, not rewrote. Regression-confirmed via Supabase MCP: `booking_no_overlap` exclusion constraint present; RLS deny-all enabled on 16/16 tables with 32 policies unchanged; cron route re-confirmed 401 (no auth) / 200 (correct bearer, read from `.env.local`). `package.json`/`package-lock.json` diff empty; `git status` confirmed only `BookingForm.tsx`, `ReviewStep.tsx`, `StepIndicator.tsx` (modified), `DetailsStep.tsx` (deleted), and `PaymentStep.tsx` (new) touched, matching this prompt's scope exactly. One test `Booking`/`Customer`/`Payment`/two `BookingAddOn` rows created during this VERIFY pass (Tennis Court, Ball Boy + Coaching) deleted via a one-off Prisma script run from inside the project directory and removed immediately after (Supabase MCP is read-only) — counts confirmed restored to the pre-prompt baseline (checked fresh rather than assumed): 5 ResourceType / 9 Resource / 21 PricingRule / 1 GuestFeeRule / 2 AddOnService / 16 AddOnPricingRule, 4 bookings / 2 customers / 4 payments / 4 bookingAddOns — two pre-existing rows under "Arjay Ical" from Arjay's own manual testing earlier the same day (still `pending_payment` when this prompt started, but swept to `cancelled` by this prompt's own cron-route regression check, since their holds had already gone stale — the route's designed behavior, not a side effect specific to this prompt, same precedent as prior entries) and two pre-existing `cancelled` "Winston Sip and Serve" rows carried forward from before this session; none of the four were created by this prompt.

- 2026-08-12 — `Booking.customerId` made nullable and booking creation split into a two-phase flow: the hold (with a real Booking Reference) is created before the customer is known, and customer resolution + member-aware final pricing happens in a separate step once Name/Phone/Email are collected on the payment page — CLAUDE.md's "Customer & auth model" decision is amended to document this as a deliberate deviation, with a new "Two-phase booking pricing" bullet added alongside it.
  - **Part 1 (migration)**: `customerId` changed to `String?` / optional relation in `prisma/schema.prisma`. Following the established `paymongo_checkout_session_id` precedent, `prisma migrate dev` was not used (non-interactive environment); the migration SQL was hand-written (`ALTER TABLE "bookings" ALTER COLUMN "customer_id" DROP NOT NULL;`, matching Prisma's own generated form for this change) at `prisma/migrations/20260812120000_make_booking_customer_id_nullable/`, applied via `prisma migrate deploy`, then `prisma generate` re-run. The running `next dev` process (started outside this session) held a Windows file lock on the generated query-engine binary, causing an `EPERM` on the first `prisma generate` attempt — the dev server processes were stopped (Playwright MCP's own node processes were left untouched) to release the lock, `prisma generate` was re-run successfully, and the dev server was restarted afterward via the preview tool for VERIFY.
  - **Parts 2–3 (shared logic extraction)**: `src/lib/booking-pricing.ts` added — the exact price-computation block from the old `POST /api/bookings` (court-duration validation, base `PricingRule` lookup, guest-fee-for-non-member-courts, Ball Boy/Coaching `AddOnPricingRule` lookups) extracted verbatim into an exported `priceBooking()` function, parameterized by `isMember: boolean` instead of resolving membership itself; returns either the same `{ totalAmountCentavos, addOns, addOnsTotalCentavos }` shape as before or a structured `{ error, status }`. `src/lib/customer-resolution.ts` added — the exact look-up-or-create-`Customer`-by-email-then-check-active-`Membership` block extracted verbatim into an exported `resolveCustomer()` function returning `{ customer, isMember }`.
  - **Part 4 (`POST /api/bookings`)**: `customer`/name/phone/email removed entirely from the request contract (not optional — absent from the type and from validation). No `Customer` look-up-or-create happens in this route anymore; pricing is computed via `priceBooking(..., isMember: false)` and the booking is created with `customerId: null`. Duration validation, business-hours enforcement, expire-on-write, and exclusion-constraint handling (409 on `booking_no_overlap`) are all unchanged in logic and order — duration validation still runs explicitly in the route before the business-hours check (unchanged ordering), and is re-run redundantly inside `priceBooking` itself (harmless — needed there for the `PATCH` re-pricing case in Part 5, since both routes share the one function). Response shape (`id`/`totalAmountCentavos`/`addOns`/`addOnsTotalCentavos`/etc.) unchanged.
  - **Part 5 (new `PATCH /api/bookings/[id]`)**: added alongside the existing `GET` in the same file. Accepts `{ name, phone, email }` (400 if any is missing/empty); 404 if the booking doesn't exist; 409 — mirroring `POST /api/checkout`'s exact condition and message ("Booking hold has expired or booking is no longer payable") — if the booking isn't `pending_payment` *or* its hold has aged past `HOLD_MINUTES` (not status alone, since a stale-but-still-`pending_payment` hold needs this check to actually catch it, matching the existing hybrid expire-on-write/cron precedent rather than assuming some other process already flipped its status). Resolves the customer via `resolveCustomer()`, derives `ballBoy`/`coaching`/`coachingPaxCount` from the booking's existing `BookingAddOn` rows (service slug + `addOnPricingRule.paxCount`), and re-prices via the same `priceBooking()` with the now-known `isMember`. In one transaction: sets `Booking.customerId`, updates `Booking.totalAmountCentavos` only if it changed, and updates each existing `BookingAddOn.amountCentavos` in place (matched back to the repriced result by service slug) only if it changed — rows are never deleted/recreated, and `addOnPricingRuleId` is deliberately left pointing at whichever rule was set at creation time, since `amountCentavos` is the schema's own documented source of truth for what was actually charged, not the FK. Returns `{ totalAmountCentavos, addOnsTotalCentavos, isMember }`.
  - **Part 6 (`GET /api/bookings/[id]`)**: `customer` field is now null-safe (`booking.customer ? { name } : null`), covering the window between hold creation and customer attachment. No other field or behavior changed.
  - **Necessary out-of-explicit-scope fix**: `POST /api/checkout` (marked "no changes to that route itself" in this prompt's scope) failed to type-check once `Booking.customer` became nullable, since it reads `booking.customer.name/email/phone` when building the PayMongo billing payload. Added a minimal defensive check — `if (!booking.customer) return 409 'Booking is not ready for checkout'` — placed after the existing hold-age/status 409 check, matching the file's own existing defensive-error convention (e.g. its "payment exists with no checkout session id" 502 case). This doesn't change the route's `{ bookingId }` in / `{ checkoutUrl }` out contract or its behavior for any legitimate call (a booking always has a customer attached by the time checkout is reachable in the new flow) — it only makes an now-representable-but-practically-impossible null case handled instead of a TypeScript compile error.
  - **Parts 7–9 (frontend)**: `ReviewStep.tsx` — "Confirm Booking" now calls a new `onConfirmBooking` prop (was a pure `onProceedToPayment` state flip) and the component gained `submitting`/`submitError` props/markup ("Creating your booking…" while submitting, inline error + re-enabled button on failure), mirroring every other API-backed step's existing convention. `PaymentStep.tsx` reworked: `bookingId` is now always non-null when rendered (always shows "Booking Reference: {bookingId}", not conditional); gained `attachingCustomer`/`attachError`/`customerAttached`/`priceUpdate`/`onPayNow`/`onStartOver` props, lost `onBack` and its Back button entirely; Name/Phone/Email fields become `disabled` once `customerAttached`; a price-change notice ("Your final price is ₱X (was ₱Y for non-members) — member rate applied.") renders above the button when `priceUpdate` is set, using the same `formatCentavos` helper as every other price display in the app; the button cycles through "Pay Now" → "Confirming your details…" (disabled) → either straight to "Redirecting to payment…" (unchanged total) or "Continue to Payment" (changed total, requires the extra click) → "Redirecting to payment…"; Start Over is unconditionally visible (not gated on an error state, since it's now the only way to leave this page). `BookingForm.tsx`: `handleConfirmBooking` (new) does the `POST /api/bookings` call (same body construction as before, minus name/phone/email) and on 201 stores `bookingId` + the server-authoritative `createdTotalCentavos` (`totalAmountCentavos + addOnsTotalCentavos` from the response, not the client-side estimate memo) before revealing the Payment page; `handlePayNow` (new) calls `PATCH /api/bookings/[id]` on first invocation (`!customerAttached`), then either auto-continues into the existing `startCheckout` if the total didn't change or sets `priceUpdate` and stops if it did — a second `onPayNow` invocation (`customerAttached` already true, whether from a price-change confirmation click or a checkout-session-only retry) skips straight to `startCheckout` and never calls `PATCH` again for the same booking; `handleStartOver` extended to reset every new field (`createdTotalCentavos`/`attachingCustomer`/`attachError`/`customerAttached`/`priceUpdate`) alongside the existing reset scope; the `onBack` wiring to `PaymentStep` was removed. `AnnouncementGate.tsx`, `SportStep.tsx`, `CourtStep.tsx`, `DateTimeStep.tsx`, `TimeSlotGrid.tsx`, `Calendar.tsx`, `AddOnsStep.tsx`, `StepIndicator.tsx`, `GET /api/resources`, `GET /api/availability`, the cron route, `POST /api/webhooks/paymongo`, `src/lib/paymongo.ts`, and `src/lib/business-hours.ts` were not touched. No new dependencies.
  - VERIFY performed in the browser pane, via direct API calls, and via Supabase MCP. Migration: `booking_no_overlap` exclusion constraint present; RLS deny-all enabled on 16/16 tables (32 policies) unchanged; `customer_id` confirmed nullable via a direct column-metadata query. Non-member happy path: Tennis Court/Court 1/Aug 20 2:00 PM with Ball Boy + Coaching (1 pax) — Confirm Booking created the hold with `customer_id: null` and `total_amount_centavos: 75000` (confirmed via Supabase MCP), Payment page showed the reference immediately with empty fields and no StepIndicator/Back button; Pay Now attached the correct `Customer` with the total unchanged (confirmed via Supabase MCP), no price-change notice, and proceeded straight through to a real `checkout.paymongo.com` session whose line items (₱750 court / ₱150 Ball Boy / ₱800 Coaching / ₱1,700 total) and billed-to name/email matched exactly — this also served as the Part-8-regression full add-ons booking. Member happy path: seeded a test `Customer` + approved `MembershipApplication` + active `Membership` (three-month tier) via a one-off Prisma script (Supabase MCP is read-only), then booked the identical Tennis Court/Ball Boy/Coaching-1-pax combo under that email — the price-change notice rendered "Your final price is ₱1,550.00 (was ₱1,700.00 for non-members) — member rate applied.", confirmed via Supabase MCP that `total_amount_centavos` dropped to 65000 (₱650 member court rate) and the `coaching_fee` `BookingAddOn` row's `amount_centavos` dropped to 75000 (₱750 member 1-pax rate) while the `ball_boy` row's stayed at 15000 (unchanged member/non-member rate, as expected), and that redirect did not happen until "Continue to Payment" was explicitly clicked — the resulting real PayMongo checkout session's line items reflected the member (final) price (₱650/₱150/₱750/₱1,550 total), not the original non-member estimate. Confirm Booking 409: raced a duplicate booking onto an already-selected slot via a direct API call between slot selection and clicking Confirm Booking in the browser — confirmed the resulting 409 rendered on SUMMARY itself (StepIndicator still visible, no Payment page shown) with all wizard state intact and the button re-enabled for retry; a direct `POST` also confirmed the pre-existing 400 duration-validation path (non-60-min court duration) still fires unchanged. PATCH failure paths: backdated a fresh booking's `createdAt` past `HOLD_MINUTES` via a one-off Prisma script, then clicked Pay Now with fields filled — confirmed `attachError` rendered the exact "Booking hold has expired or booking is no longer payable" text, fields stayed filled and editable, and Start Over remained available; a direct `PATCH` with an empty `name` confirmed the 400 path, and a direct `PATCH` to a nonexistent id confirmed 404. `GET /api/bookings/[id]` on a booking with no customer attached confirmed `customer: null` (no crash). Checkout-session-only retry (the `customerAttached` guard against a second `PATCH` call) was verified via source read rather than forced live, per this project's established fallback convention for this class of check — `handlePayNow`'s early-return branch is unconditional on `customerAttached`, with no code path back into the `PATCH` call once true. Regression: the Payment page never showed a Back button in any state reached during VERIFY (initial, attach-error, price-update, checking-out); Start Over was visible immediately on first render, before any Pay Now click. Regression via Supabase MCP/direct calls: cron route re-confirmed 401 (no auth) / 200 (correct bearer, incidentally sweeping the PATCH-409 test booking and one pre-existing stale hold to `cancelled` — the route's designed behavior, not a side effect of this prompt, same precedent as prior entries); `package.json`/`package-lock.json` diff empty; `git status` confirmed only this prompt's in-scope files touched (`CLAUDE.md`, `prisma/schema.prisma`, the new migration folder, both `bookings` routes, `checkout/route.ts`, `BookingForm.tsx`/`PaymentStep.tsx`/`ReviewStep.tsx`, and the two new `src/lib` files) plus this `PROGRESS.md` entry. Cleanup: all `Booking`/`Payment`/`BookingAddOn`/`Customer` rows created during VERIFY (four bookings, two customers) deleted via a one-off Prisma script, including the seeded test `Membership`/`MembershipApplication` (a scratch fixture for this VERIFY pass only, not a reusable one Arjay asked to keep) — baseline confirmed restored fresh (not assumed): the same 5 pre-existing `cancelled` bookings/2 customers present before this prompt started, 0 memberships, `booking_no_overlap` and RLS (32 policies) unchanged.

- 2026-08-12 — PayMongo checkout-session payload refined: line items consolidated to one row per booking (was one row for the base booking plus one per add-on), and `payment_method_types` updated from a hardcoded `['card']` placeholder to the actual confirmed-working set for Arjay's test account. `src/app/api/checkout/route.ts`: the previous `lineItems` array (base booking + `...addOns.map(...)`) replaced with a single item — `name` follows the existing `"{ResourceType} — {Resource} (...)"` convention already used elsewhere (`BookingConfirmation.tsx`/`ReviewStep.tsx`), here with the PH-calendar booking date instead of duration; `amount` is the unchanged full total (`totalAmountCentavos`, base + add-ons, same number as before); a `description` field (e.g. "Includes: Ball Boy, Coaching (1 pax)") is added only when add-ons exist — omitted entirely otherwise, not rendered as an empty string. `src/lib/paymongo.ts`: `PaymongoLineItem` gained an optional `description` field (PayMongo's `LineItemDTO` schema confirmed via docs to support it), only spread into the request body when present. `payment_method_types` changed from `['card']` to `['card', 'gcash', 'grab_pay', 'paymaya']` — confirmed live, not guessed: a real checkout session was created directly against the PayMongo API (bypassing the app) with the full four-method array, returned 200 with all four in `payment_method_allowed`, and the resulting real `checkout.paymongo.com` page was opened and each of Card/GCash/GrabPay/Maya was individually clicked and confirmed selectable with no console errors — all four are enabled for this account, so no methods needed to be dropped. CLAUDE.md's Open/Not Yet Decided gained a new bullet documenting this finding (no such bullet existed previously to update, despite this prompt's framing — the inline code comment was the only prior trace) and flagging that the client's own PayMongo account may have a different enabled set. No pricing logic, booking routes, webhook handler, frontend components, schema, or migrations touched; no new dependencies. VERIFY performed via the actual wizard flow in the browser pane end-to-end (not just direct API calls): a Tennis Court + Ball Boy + Coaching (1 pax) booking's real checkout session showed exactly one line item ("Tennis Court — Court 1 (8/20/2026)", description "Includes: Ball Boy, Coaching (1 pax)", ₱1,700.00 — matching the wizard's own estimate exactly) instead of three separate rows; a Golf Simulator booking with no add-ons showed one clean line item ("Golf Simulator — Bay 1 (8/21/2026)", ₱1,150.00) with no description field rendered at all. Payment Method section on both real sessions showed Card plus GCash/GrabPay/Maya as E-Wallets. Webhook regression: the self-signed-webhook workaround (HMAC-signed `checkout_session.payment.paid` POST to `/api/webhooks/paymongo`, precedent from earlier PayMongo-integration entries) confirmed the first booking flipped to `confirmed` with `payment.status = 'paid'` and the correct `amount_centavos` (170000), unaffected by the line-item/payment-method changes. Regression via Supabase MCP: `booking_no_overlap` present; RLS 16/16 tables, 32 deny-all policies unchanged; `package.json`/`package-lock.json` diff empty; `git status` confirmed only `src/lib/paymongo.ts`, `src/app/api/checkout/route.ts`, `CLAUDE.md`, and this `PROGRESS.md` entry touched. Cleanup: both test bookings, their payments/add-ons, and their customers deleted via a one-off Prisma script — baseline confirmed restored fresh (bookings=6, customers=2, payments=6, booking_add_ons=8, matching counts taken immediately before this prompt started, not an assumed zero baseline).

- 2026-08-12 — Critical webhook bugfix: real PayMongo `payment.paid` events were being silently no-op'd because `src/app/api/webhooks/paymongo/route.ts` matched on event type `checkout_session.payment.paid`, which PayMongo never actually sends — real deliveries send `payment.paid`. This was discovered via a genuine ngrok-captured webhook delivery for an actually-paid GCash test transaction (not a self-signed test payload), whose event silently 200'd as a no-op instead of confirming its booking, leaving booking `cmsq31lm30001w4m8b045hv2f` stranded `pending_payment` despite being genuinely paid on PayMongo's side. Fixed the event-type check (`data.attributes.type === 'payment.paid'`) and every downstream field-extraction path against the real payload shape, which nests the payment object one level deeper than previously assumed — `bookingId`/`status`/`payment_intent_id`/`paid_at` all live under `data.attributes.data.attributes`, not `data.attributes.data` directly. Also fixed `Payment.paidAt`, which was previously stamped with the webhook-processing server time instead of the payload's actual `paid_at` (Unix seconds, now converted via `new Date(paid_at * 1000)`). Signature verification itself (HMAC-SHA256 + `Paymongo-Signature` parsing) was already correct and untouched — only the event-type match and field paths below it were wrong. Only `src/app/api/webhooks/paymongo/route.ts` was code-touched; booking creation, pricing, checkout-session creation, frontend, and schema were not. **Flagging explicitly: every prior "webhook confirmed working" VERIFY entry in this log (see the 2026-08-12 line-items/payment-methods entry above, and earlier PayMongo-integration entries) was validated only against self-signed payloads built to match this same wrong `checkout_session.payment.paid` assumption — none were a real PayMongo delivery. This entry is the first genuine end-to-end confirmation of the webhook path.** VERIFY: (1) replayed the exact captured ground-truth payload (HMAC-signed with the real `PAYMONGO_WEBHOOK_SECRET`) against the fixed local endpoint — 200, and Supabase MCP confirmed booking `cmsq31lm30001w4m8b045hv2f` flipped to `confirmed` with `payment.status='paid'`, `paidAt` matching the payload's `paid_at` exactly (`2026-08-12 12:48:24`), and `paymongoPaymentIntentId` matching the payload's `payment_intent_id`; (2) completed one brand-new real end-to-end payment through the actual wizard (Tennis Court, GCash, ₱750.00) with the existing ngrok tunnel + PayMongo Dashboard webhook already pointed at it — ngrok's inspector API confirmed two real `payment.paid` deliveries (both 200), and the new booking flipped to `confirmed` within moments with no manual replay needed, the real unassisted path finally working; (3) `/book/confirmation`'s polling page picked up the confirmed status and rendered `BookingConfirmation` correctly, never hitting the "taking longer than expected" state; (4) idempotency: resent the captured payload a second time — 200, no duplicate processing, `paidAt` unchanged; (5) sent a fabricated `payment.failed` event — 200 no-op, confirming the route didn't start accepting everything. Regression via Supabase MCP: `booking_no_overlap` constraint present; RLS enabled 16/16 tables with 32 deny-all policies unchanged; `package.json`/`package-lock.json` diff empty; `git status` confirmed only the webhook route, `CLAUDE.md`, and this `PROGRESS.md` entry touched. The new end-to-end test booking/payment/customer from VERIFY step 2 were deleted via a one-off Prisma script; `cmsq31lm30001w4m8b045hv2f` was deliberately left as-is — it's a real (test-mode) customer payment being correctly resolved, not test scaffolding to clean up.

- 2026-08-13 — Home page rebuilt with the real Winston Sip & Serve theme (design tokens, Navbar, Footer, all 7 marketing sections, live resource-count wiring), no backend/schema/booking-flow changes. `src/app/globals.css`: added `--color-brand-dark`/`--color-brand-mid`/`--color-cream`/`--color-accent`/`--color-accent-light`/`--shadow-card`/`--shadow-hero` tokens inside `@theme inline`; `--background`/`--foreground` set to the cream/slate pair; the `prefers-color-scheme: dark` override block removed (fixed light theme, no dark mode); `--font-sans` mapped to `var(--font-lora)`. Also fixed the leftover hardcoded `body { font-family: Arial, Helvetica, sans-serif }` rule (predates this prompt, not called out in the original spec) to `font-family: var(--font-sans)` — without this the new `--font-sans` token would have had no effect and Lora would never actually render, confirmed via computed-style check in the browser pane. `src/app/layout.tsx`: swapped `Geist`/`Geist_Mono` for `Lora` (`next/font/google`, no new dependency), updated `<title>`/description metadata; `--font-mono`'s override (previously `var(--font-geist-mono)`) was removed from `globals.css` since Geist Mono no longer loads — `font-mono` usage in `src/app/book/confirmation/ConfirmationContent.tsx` (untouched, out of scope) now falls back to Tailwind's own built-in monospace stack instead of an undefined variable, preserving its look. `src/components/layout/Navbar.tsx` (new) and `src/components/layout/Footer.tsx` (new): sticky navbar with logo, 6 nav links (Home/Book Now/News/Cafe & Bar/Membership/About), active-link underline, placeholder social icons, inert "My Account" pill, and a hamburger mobile menu below `md`; dark-brown footer with the same nav links under "Quick Links" plus a Contact Us column (placeholder address/phone/email/hours — **flagging that these need confirming with the client, not real contact info**) and a dynamic-year bottom bar. `src/components/home/` (new): `Hero.tsx`, `StatsBar.tsx`, `ChooseYourSport.tsx`, `HowItWorks.tsx`, `Gallery.tsx`, `Testimonials.tsx`, `CtaBanner.tsx` — Hero/StatsBar accept `totalResources`/`sportCount` as props (not self-fetched); Hero's Opens/Last Booking stats are derived from `BUSINESS_OPEN_HOUR`/`BUSINESS_CLOSE_HOUR` in `src/lib/business-hours.ts` (formatted 12-hour, e.g. "6AM"/"10PM") rather than hardcoded, so they can't drift from actual business hours; ChooseYourSport renders one card per live `ResourceType` (all 5, including Tennis Simulator, which an earlier reference mock had omitted) with correct singular/plural resource-count labels; Testimonials and HowItWorks are fully static/hardcoded, confirmed acceptable for now. `src/app/page.tsx` rewritten as an async Server Component querying `prisma.resourceType.findMany` directly (no self-fetch over HTTP, avoiding the known `NEXT_PUBLIC_APP_URL` misconfiguration noted above) — `totalResources` sums active resources across all types, `sportCount` is the distinct count of slug prefixes before the `_court`/`_sim` suffix (`tennis`/`pickleball`/`golf`); old placeholder content removed entirely. No files under `src/app/book/`, `src/components/booking/`, `src/app/api/`, or `prisma/` touched; `package.json`/`package-lock.json` diff empty. Also updated `.claude/launch.json` (added `"autoPort": true`) — unrelated to the app code, needed only because port 3000 was already occupied locally by a separate pre-existing process (turned out to be the original static reference mock this prompt's spec was drafted against — `.html`-style links, 4 sport cards missing Tennis Simulator, wrong resource counts — coincidentally listening on the same port), so the dev server used for VERIFY ran on an auto-assigned port instead. VERIFY performed in the browser pane: full page text/structure walkthrough confirmed all 9 sections present in the specified order; computed `body` font-family read back `Lora, "Lora Fallback"` after the body-rule fix (was silently still Arial before it), and the Lora `.woff2` file was confirmed requested over the network; live data confirmed correct — Hero/StatsBar both show 9 total resources / 3 sports, ChooseYourSport shows exactly 5 cards with counts matching real seeded inventory (Tennis Court 1, Pickleball Court 3, Tennis Simulator 1, Pickleball Simulator 2, Golf Simulator 2); Navbar and Footer nav order confirmed Home/Book Now/News/Cafe & Bar/Membership/About in both places, Home and Book Now links both navigated correctly, News/Cafe & Bar/Membership/About left unbuilt (expected); `/book` regression confirmed the announcement gate and wizard still render with no Navbar/Footer wrapping and no console errors (Lora font swap is the only visible difference, which is the deliberate site-wide font change called out in this prompt); mobile viewport (375×812) confirmed the hamburger toggle correctly shows/hides the mobile nav list (verified via DOM link-count before/after, screenshot tooling unavailable in this session). Regression via Supabase MCP: `booking_no_overlap` exclusion constraint present; RLS enabled on 16/16 `public` tables with 32 deny-all policies (16 tables × 2 roles) unchanged. `git status` confirmed only the files named above were touched; no test data was created (read-only Prisma query), so no cleanup was needed.

- 2026-08-13 — Home page polish pass: reveal-on-scroll, CTA banner decorative blobs, image crop/tint variety, and hover micro-interactions — no new dependencies, no backend/schema/booking-flow changes. `src/components/ui/Reveal.tsx` (new): a `'use client'` `IntersectionObserver`-based wrapper (threshold 0.15, `-10%` bottom rootMargin) toggling `opacity-0 translate-y-4` → `opacity-100 translate-y-0` over 500ms ease-out, unobserving after first reveal so it never re-triggers on repeat scroll past the same element; accepts an optional `delayMs` for staggering; renders immediately visible with no observer at all when `prefers-reduced-motion: reduce` is set. Applied with `delayMs={index * 100}` staggering to the 5 sport cards in `ChooseYourSport.tsx`, the 6 gallery tiles in `Gallery.tsx`, and the 3 testimonial cards in `Testimonials.tsx`; each of those three plus `HowItWorks.tsx` also got a single non-staggered `<Reveal>` around its heading block. `CtaBanner.tsx`: section wrapper gained `relative overflow-hidden`; two `pointer-events-none` blurred circles (`bg-accent`/`bg-brand-mid`, 20% opacity, `rounded-full blur-3xl`, ~320px) positioned bleeding off the top-right and bottom-left corners, sitting behind the text via `relative z-10` on the content wrapper. Image variety: `ChooseYourSport.tsx` and `Gallery.tsx` each gained module-level `objectPositions`/`tintColors` rotation arrays (cycled by index) applied as inline `style={{ objectPosition }}` plus a low-opacity (~10%) absolute tint overlay div per card/tile — `tintColors` is a literal array of full Tailwind class names (`'bg-brand-dark'`/`'bg-accent'`/`'bg-brand-mid'`), not a template-string-interpolated class, since Tailwind's compiler can't detect dynamically-built class names; `Hero.tsx` was left untouched (still full-quality, no tint, no crop change), confirmed via source diff. Micro-interactions: primary buttons (Hero's "Book a Court", CtaBanner's matching pair) changed from `hover:opacity-90`/plain to `transition-all duration-200 hover:-translate-y-0.5 hover:bg-brand-mid`; outline buttons ("Explore the Cafe" in both places) and the My Account pill (desktop + mobile) gained `transition-all duration-200` alongside their existing `hover:bg-accent-light`; sport cards, gallery tiles, and testimonial cards all gained `transition-all duration-300 hover:-translate-y-1 hover:shadow-hero`, with gallery tiles additionally getting `group-hover:scale-105` on the image itself (contained by the tile's existing `overflow-hidden`, layered on top of the pre-existing dark-overlay-plus-"+"-icon hover effect rather than replacing it); Navbar nav links gained a `group`-based hover-in underline (`scale-x-0 group-hover:scale-x-100`, same accent color as the existing active-state underline, `transition-transform duration-200`) for inactive links, and the logo link gained `hover:scale-105 transition-transform`. VERIFY performed against the actual running dev server (found on port 3001 after discovering port 3000 is occupied by an unrelated pre-existing static-mock process — same coincidental collision flagged in the prior Home-page entry, not this prompt's doing) via DOM/computed-style inspection and `git status`, since this session's Browser pane never entered a composited/displayed state (`computer{action:"screenshot"}` and `computer{action:"scroll"}` both failed with "the Browser pane is not displayed, so the page is not compositing frames" — a session/tooling limitation, not a code issue). **Not visually confirmed this pass: actual on-screen fade/slide-in during a real scroll, real hover states, and `prefers-reduced-motion` emulation — Arjay should do a quick visual pass before treating this as fully signed off.** What was confirmed: initial computed style of every `Reveal`-wrapped block (ChooseYourSport/HowItWorks/Gallery/Testimonials headings and all 5/6/3 cards) reads `opacity: 0` / `translate: 0px 16px` with `transition-duration: 0.5s` and `ease-out` timing before any scroll, matching the spec; a freshly-created `IntersectionObserver` on the same element never fired within 2s even though the element's `getBoundingClientRect()` placed it well inside the 720px-tall viewport after a programmatic `scrollTo` — consistent with IntersectionObserver being suspended on a non-composited/non-visible tab in Chromium, not a bug in `Reveal.tsx` (its observer setup, threshold, rootMargin, and one-time `unobserve` follow the standard IntersectionObserver contract). CtaBanner: confirmed via DOM query that the section has `relative overflow-hidden` and exactly two `pointer-events-none` `blur-3xl` divs (`bg-accent`/`bg-brand-mid`, both `opacity-20`) positioned top-right/bottom-left. Image variety: confirmed via DOM query that all 5 ChooseYourSport cards and all 6 Gallery tiles have distinct `object-position` inline styles cycling through the 6-value array, each with a cycling low-opacity tint div (`bg-brand-dark`/`bg-accent`/`bg-brand-mid`); Hero's image confirmed to carry no tint div and no `object-position` override (`object-cover` only), unchanged. Hover classes confirmed present via static className inspection (not a live `:hover` trigger): Hero/CtaBanner primary buttons carry `hover:-translate-y-0.5 hover:bg-brand-mid`, outline buttons carry `hover:bg-accent-light`/`hover:bg-white/10`, My Account (desktop + mobile) carries `hover:bg-accent-light`, sport/gallery/testimonial cards carry `hover:-translate-y-1 hover:shadow-hero`, gallery images additionally carry `group-hover:scale-105`, inactive nav links render the new `scale-x-0 group-hover:scale-x-100` underline span (the active "Home" link correctly has no such span, since it uses its own `after:` pseudo-element underline instead), and the logo link carries `hover:scale-105`. Mobile (375×812, `resize_window` + reload): confirmed no horizontal overflow (`scrollWidth === clientWidth === 375`) and the reveal elements still start in the same hidden computed state. Regression: `/book` confirmed rendering its announcement gate with no console errors and no `Reveal`/hover-class leakage; `git status` confirmed only `Reveal.tsx` (new) plus `Navbar.tsx`, `Hero.tsx`, `ChooseYourSport.tsx`, `HowItWorks.tsx`, `Gallery.tsx`, `Testimonials.tsx`, `CtaBanner.tsx`, and this `PROGRESS.md` entry touched — no file under `src/app/book/`, `src/components/booking/`, `src/app/api/`, or `prisma/` touched. `package.json`/`package-lock.json` diff confirmed empty via `git diff --stat`. No console errors observed on either page. `npx tsc --noEmit` clean; `npm run lint` surfaced one new error in `Reveal.tsx` (the newer `react-hooks/set-state-in-effect` rule flagging a synchronous `setVisible(true)` call in the reduced-motion branch) — fixed by moving reduced-motion visibility to a pure CSS `motion-reduce:opacity-100 motion-reduce:translate-y-0` variant on the wrapper (Tailwind's standard variant-precedence mechanism, same as `hover:`/`dark:`) so the effect's reduced-motion branch now only decides whether to skip creating the `IntersectionObserver`, never calls `setState`; re-run confirmed clean for this file. `npm run lint`'s three remaining errors are all pre-existing in `BookingForm.tsx` (same rule, unrelated effects), confirmed via `git diff --stat -- src/components/booking/` to be untouched by this prompt — not a regression introduced here. No data created — pure frontend change, no cleanup needed.

- 2026-08-14 — Home page redesigned against a client-supplied reference image: design tokens reconciled, Choose Your Sport and Testimonials removed, Hero/StatsBar/HowItWorks/Facilities(formerly Gallery)/CtaBanner/Footer/Navbar all restyled, no backend/schema/booking-flow changes. **Confirmed deviation from the reference image**: Navbar stays sticky above the Hero (its existing behavior) rather than moving below the Hero as the reference image shows — only its colors/tokens were updated. `src/app/globals.css`: `--color-cream` (single value, used for both page and card/nav backgrounds) split into two distinct tokens — `--background` stays `#FFF8F0` (page base, unchanged) and a new `--color-brand-light: #FDF3E7` now covers cards/nav; `--color-accent` renamed to `--color-accent-primary` (`#C08552`, same value) with every consumer repointed (Navbar, Footer, Hero, HowItWorks, Facilities, CtaBanner — all in-scope files; grepped first and confirmed no file under `src/components/booking/` or `src/app/api/` references any of `bg-cream`/`text-accent`/`bg-accent`, so the rename was safe); a new `--color-accent-dark` alias added pointing at `var(--color-brand-mid)` per the reconciliation table's "same value as brand-mid, don't duplicate" note (not yet consumed by any component — available for future use). `brand-dark`/`brand-mid`/`accent-light`/`--shadow-card`/`--shadow-hero` were already correct and untouched. Neutral tones (`neutral-100`/`neutral-700` in the spec) were deliberately *not* added as custom tokens — the codebase's existing convention is to reach for Tailwind's own `gray-100`/`gray-600`/`gray-700` utilities directly (confirmed via the pre-existing `text-gray-600`/`text-gray-700` usage throughout), so `HowItWorks.tsx`/`Facilities.tsx` use `bg-gray-100`/`text-gray-700` rather than inventing parallel tokens for values Tailwind already ships. `src/app/layout.tsx`: Lora's `next/font/google` config gained `style: ["normal", "italic"]` (was normal-only) so the Hero's italic headline renders the font's real italic face instead of a browser-synthesized slant. `src/components/ui/SocialIcons.tsx` (new): the icon markup was byte-identical between `Navbar.tsx` and `Footer.tsx` (only the border/text/hover classes differed), so it was extracted into a shared component taking a `variant: 'dark' | 'light'` prop (`'dark'` reproduces Navbar's exact prior classes, `'light'` reproduces Footer's); both call sites swapped in with no visual change, and `Hero.tsx` now reuses the same component (`variant="light"`) for its new bottom-left social row instead of duplicating markup a third time. `Hero.tsx` rebuilt: full-bleed `absolute inset-0` photo (existing `/images/placeholder.jpg` asset, unchanged `object-cover` — kept as-is since it already supported a full-bleed treatment) with a `bg-gradient-to-t from-brand-dark/95 via-brand-dark/70 to-brand-dark/40` scrim for text legibility, uppercase-tracked eyebrow, italic display headline, supporting paragraph, single `Book Now` pill button to `/book` (same href/behavior as before), and the new `SocialIcons` row near the bottom-left; the four inline stat figures previously rendered inside Hero were removed entirely (moved to the floating card below) along with the secondary "Explore the Cafe" button and the old contained `aspect-[4/3]` image box, so `Hero` no longer takes `totalResources`/`sportCount` props. **Flagging: the eyebrow/headline/paragraph copy ("Sip. Serve. Play. Repeat.", etc.) is taken verbatim from the reference image as a placeholder and is not yet client-confirmed** — same category of flag as the Footer's placeholder contact info from the prior Home-page entry. `StatsBar.tsx` rewritten as the floating overlap card: absorbed the `formatHour12`/`BUSINESS_OPEN_HOUR`/`BUSINESS_CLOSE_HOUR` logic that used to live in `Hero.tsx` (so "Opens Daily"/"Last Booking" still derive from `src/lib/business-hours.ts`, never hardcoded — that file itself was not touched, only its existing exports consumed) alongside the existing `totalResources`/`sportCount` props; renders as a `bg-brand-light` `rounded-3xl shadow-card` grid (`grid-cols-2` on mobile, `sm:grid-cols-4` on desktop, with `sm:border-l` dividers between columns), wrapped in a `relative z-10 -mt-16 sm:-mt-24` container that pulls it up over `Hero.tsx`'s new `pb-28 sm:pb-36` bottom padding to create the boundary overlap. `HowItWorks.tsx`: restyled only, copy/structure untouched — section background changed to `bg-gray-100`, eyebrow to `text-accent-primary`, body text to `text-gray-700`; the solid numbered circles (1/2/3) were replaced with large `text-accent-light` outline-style numerals ("01"/"02"/"03") per the reference image, and the 3 step cards gained their own staggered `Reveal` (previously only the heading block had one — added since the prompt explicitly asked to preserve "the 3 step cards" stagger, matching the convention already used by the sibling sections). `src/components/home/Gallery.tsx` deleted, replaced by `src/components/home/Facilities.tsx` (renamed — better matches its new purpose as a fixed photographic showcase rather than a generic rotating gallery, now that Choose Your Sport no longer needs 1:1 resource-type coverage): asymmetric grid (`grid-cols-2 gap-4 sm:gap-6 lg:grid-cols-4 lg:grid-rows-2 lg:h-[560px]`) with Tennis Courts spanning `lg:col-span-2 lg:row-span-2` on the left and Pickleball Courts/Golf Simulator/Café & Bar/Speakeasy Lounge auto-placed 2×2 on the right (CSS Grid's default sparse auto-placement handles the skip-around-the-spanning-item layout with no explicit `grid-area`s needed); each tile keeps the prior polish pass's `object-position`/tint-overlay rotation and `hover:-translate-y-1 hover:shadow-hero`/`group-hover:scale-105` micro-interactions, and gained an always-visible `bg-gradient-to-t from-brand-dark/80` scrim with the facility name in white text at bottom-left (previously Gallery tiles had no label at all, just a hover-only "+" icon). Only one real image asset exists in this repo (`/images/placeholder.jpg`); every tile — including "Speakeasy Lounge", which has no dedicated photo — reuses it with a distinct crop/tint, consistent with how every other section (including the new Hero) already reuses this same asset; no new image files were sourced. `CtaBanner.tsx`: added a `pointer-events-none absolute inset-0` layer with a tiled inline-SVG data-URI background (48×48px repeating thin white circle outline at 8% stroke-opacity, 40% layer opacity) sitting between the gradient and the two existing blurred corner blobs, which were kept as-is since they still read correctly layered under the new texture rather than needing replacement; button copy/hrefs updated to match the reference image exactly ("Book Now" → `/book`, "Explore Membership" → `/membership`, replacing the old "Book a Court →"/"Explore the Cafe" pair) per Part 7's explicit instruction, hover states unchanged. `Footer.tsx`: column order changed to brand+social / Contact / Quick Links (matches the reference image; was previously brand+social / Quick Links / Contact) — background/text colors were already `brand-dark`/white and needed no change beyond the token/component swaps above. `src/app/page.tsx`: render order now `Navbar → Hero → StatsBar → HowItWorks → Facilities → CtaBanner → Footer`; `ChooseYourSport`/`Testimonials` imports and JSX removed, confirmed via repo-wide grep that no other file imports either (both are now deleted, not just unused); the `prisma.resourceType.findMany` query was simplified (dropped the unused `orderBy`/resource `label` selection that only `ChooseYourSport`'s per-card list had needed) but kept, since `StatsBar` still needs live `totalResources`/`sportCount`. No file under `src/app/book/`, `src/components/booking/`, `src/app/api/`, or `prisma/` was touched; `package.json`/`package-lock.json` diff confirmed empty (`git diff --stat`) — no new dependencies were needed. VERIFY: `npx tsc --noEmit` clean; `npm run lint` surfaced only the same 3 pre-existing `react-hooks/set-state-in-effect` errors in `BookingForm.tsx` flagged in the 2026-08-13 polish entry, confirmed untouched by this prompt. Browser-pane screenshot tooling was unavailable again this session (same "Browser pane is not displayed" limitation as the 2026-08-13 polish entry) — verification fell back to the same established DOM/computed-style/`get_page_text` method, run against the dev server on port 3001 (port 3000 again occupied by the unrelated pre-existing process noted in prior entries). Confirmed via computed styles: `body` background `rgb(255,248,240)` (`#FFF8F0`) vs. Navbar/StatsBar-card background `rgb(253,243,231)` (`#FDF3E7`) — two distinct creams, not one; both the Hero "Book Now" pill and the CTA banner's solid button compute to `rgb(192,133,82)` (`#C08552` = `accent-primary`); `How It Works`' section background computed to `rgb(243,244,246)` (`#F3F4F6` = `gray-100`, converted from the browser's native `lab()` output via a canvas round-trip to confirm the exact RGB match); Hero eyebrow computed to `rgb(245,230,211)` (`#F5E6D3` = `accent-light`) with `text-transform: uppercase` (copy is written mixed-case in JSX and transformed via CSS, not hardcoded uppercase). Confirmed via `get_page_text` + DOM structure: full section order and all copy present as specified; all 4 stat values render "3 / SPORTS", "9 / COURTS & SIMULATORS", "6AM / OPENS DAILY", "10PM / LAST BOOKING" — matching real seeded inventory (9 active resources, 3 sport slug-prefixes) and `business-hours.ts`'s actual constants, not hardcoded; the floating stats card's `getBoundingClientRect()` confirmed it starts (`top: 722`) above Hero's own bottom edge (`818`) and ends (`bottom: 862`) exactly at How It Works' top (`862`) — genuine overlap on both boundaries, not just visual coincidence; Facilities grid confirmed exactly 5 tiles with the correct labels in the correct 1-large-plus-2×2 positions at desktop width. Mobile (375×812, `resize_window` + reload): `document.body.scrollWidth === document.documentElement.scrollWidth === window.innerWidth === 375` confirmed no horizontal overflow anywhere on the page; the stats card degraded to a 2×2 grid within its own width (four 132px columns, card inset with margin on both sides, no overflow); the Facilities grid degraded to full-width large tile stacked above a 2×2 of smaller tiles, in the same label order as desktop. Regression: `/book` re-confirmed rendering its announcement gate with no console errors and no Navbar/Footer bleed (that route still has no shared layout, unaffected by this prompt); root `/` also showed no console errors. `git status` confirmed the touched-file set matched this prompt's scope exactly: `globals.css`, `layout.tsx`, `page.tsx`, `CtaBanner.tsx`/`Hero.tsx`/`HowItWorks.tsx`/`StatsBar.tsx` (modified), `Facilities.tsx`/`SocialIcons.tsx` (new), `ChooseYourSport.tsx`/`Gallery.tsx`/`Testimonials.tsx` (deleted), `Footer.tsx`/`Navbar.tsx` (modified) — plus this `PROGRESS.md` entry; no file under `src/app/book/`, `src/components/booking/`, `src/app/api/`, or `prisma/` appears in the diff. No test/DB data was created by this prompt (pure frontend change, no Prisma writes at any point during VERIFY) — no cleanup step needed, confirmed explicitly rather than assumed.

- 2026-08-14 — Navbar/Hero refinement pass following the prior redesign, fixing five items flagged against the reference image: no backend/schema/booking-flow changes, only `Hero.tsx`, `Navbar.tsx`, and the shared `SocialIcons.tsx` touched. **Icon set**: `SocialIcons.tsx`'s third icon (labeled "X", used by Navbar/Footer/Hero alike since all three already shared this one component) replaced with an inline-SVG TikTok glyph — no new dependency, confirmed via DOM query that all three locations now render Facebook/Instagram/TikTok. **Hero icon position**: the social row moved from a sibling div directly under the button (`mt-16`) into the same `max-w-xl` column as the button, with `mt-20 sm:mt-28` spacing — real separation from Book Now instead of sitting right beneath it; confirmed no collision with the floating stats card at 375px (icon row bottom 567px vs. card top 615px, 48px clear). **Button style**: `px-8 py-3` reduced to `px-6 py-2.5` (smaller, tighter pill matching the reference); `rounded-full` was already correct in code. Computed background confirmed `rgb(192, 133, 82)` (`#C08552` = `accent-primary`) exactly — no lighter/hover shade was actually leaking into the resting state, so no token or opacity fix was needed there. **Headline column width**: the wrapping div narrowed from `max-w-2xl` (672px) to `max-w-xl` (576px, confirmed via `getBoundingClientRect()` against the 1265px hero width — ~45%, matching the reference's roughly-half-width column) and headline size stepped down one notch (`text-5xl/6xl/7xl` → `text-4xl/5xl/6xl`) so the italic headline no longer stretches wider than the reference's proportions. **Navbar wordmark**: this was a genuine gap, not a hidden-by-CSS false alarm — `Navbar.tsx`'s brand `Link` rendered only the `logo.png` image with no text at all (confirmed by reading the file directly, not assumed); `logo.png` itself is a circular crest badge with its own tiny baked-in wordmark, illegible at the 80×80px it was rendered at, which is a different thing from a readable inline text label next to it. Added a `<span>Winston Sip & Serve</span>` (`text-lg font-semibold text-brand-dark`) next to the logo and reduced the logo's rendered size from 80×80 to 44×44 to sit correctly alongside it, matching the reference's small-crest-plus-text lockup; confirmed present in the DOM at 1440px (`header span` textContent reads "Winston Sip & Serve"). VERIFY: `npx tsc --noEmit` clean; `npm run lint` surfaced only the same 3 pre-existing `react-hooks/set-state-in-effect` errors in `BookingForm.tsx` (confirmed untouched — not in this prompt's diff). Browser-pane screenshot tooling was unavailable again this session (same limitation as both prior Home-page entries) — verification used the same established DOM/computed-style/`get_page_text` fallback against the dev server on port 3001. Regression: `/book` re-confirmed rendering its announcement gate with no console errors (only pre-existing HMR WebSocket noise from this externally-started dev server, unrelated to app code, present before this prompt too); root `/` unaffected outside the three touched files — How It Works/Facilities/CtaBanner/Footer content and layout confirmed unchanged via `get_page_text`. `git status` confirmed only `Hero.tsx`, `Navbar.tsx`, and `SocialIcons.tsx` touched, plus this `PROGRESS.md` entry; `package.json`/`package-lock.json` diff empty — no new dependencies. No test/DB data created (pure frontend change) — no cleanup needed.

- 2026-08-14 — Navbar/Hero/StatsBar/CtaBanner follow-up pass, four items against the latest reference screenshots: `Navbar.tsx`, `Hero.tsx`, `CtaBanner.tsx` touched (no changes were needed in `StatsBar.tsx` — see Part 3 below); no backend/schema/booking-flow changes, `Footer.tsx` untouched. **Navbar buttons**: social icon cluster and the "My Account" pill removed from both the desktop bar and the mobile menu (Navbar no longer imports `SocialIcons` at all); replaced with a `Book Now` link (`rounded-full bg-accent-primary`, → `/book`, same destination as every other Book Now CTA) and a `Sign In` button (`rounded-full border border-brand-mid`, outline pairing echoing the CTA banner's solid+outline convention, adapted to the Navbar's light background). **`Sign In` is a plain non-navigating `<button type="button">` with no `href` and no `onClick` — intentionally inert, since Auth.js's method (email/password vs. magic link, see CLAUDE.md → "Open / Not Yet Decided") is still undecided and there is no auth route to link to yet.** Confirmed via a real `.click()` dispatch on both the desktop and mobile-menu instances: no URL change, no thrown error. Mobile menu: the two buttons render stacked full-width (`flex-col gap-3`) below the nav link list, replacing the old icons+pill row. **Hero**: `SocialIcons` import and its bottom-of-column row removed entirely (supersedes the earlier "reposition lower" fix from the same-day refinement pass above — full removal this time, per this prompt's explicit instruction). Headline stepped up one notch on the existing scale (`text-4xl/5xl/6xl` → `text-5xl/6xl/7xl`, i.e. 36/48/60px → 48/60/72px); confirmed via computed style (`72px` at 1440px lg, `48px` at 375px) and `scrollWidth` vs. container `clientWidth` at both breakpoints — no overflow at either, so the existing `max-w-xl` column width from the prior pass didn't need adjusting. **Stats-card seam (investigated, no code change)**: rather than guess-patching, checked the actual paint order at the Hero/StatsBar boundary via `document.elementFromPoint()` at 1px increments spanning the boundary, at both 1440px and 375px, before touching any code. At every sampled point up to and including the last pixel before the card, the topmost element was Hero's `bg-gradient-to-t from-brand-dark/95 …` overlay div (95% opaque at that edge, per the gradient's own stops) — never the page's `body` background — and the card's own top edge (`getBoundingClientRect().top`) sat 96px/64px above Hero's bottom edge, i.e. inside Hero's `pb-36`/`pb-28` padding, which the full-bleed `absolute inset-0` image+overlay already covers. **No coverage gap exists in the current code at either viewport** — re-confirmed after the Hero edit above (which shortened Hero's content height by removing the social-icon row) since that could in principle have changed the overlap; margin/coverage numbers held. Screenshot-based visual inspection of the seam (as the prompt's VERIFY step asked for) was not possible this session — see the tooling-limitation note below — so this is a DOM/paint-order finding, not a pixel-level one; if the strip in the reference screenshot is still visible after this pass, it's worth a follow-up with an actual screenshot once the Browser pane renders, since nothing in the current markup structurally explains it. **CTA banner pattern**: replaced the uniform 48×48px single-ring tile (`stroke-opacity 0.08`, container `opacity-40`) with a sparser 200×200px tile containing four rings of varying radius (12/30/16/42px) at irregular positions and individually varying `stroke-opacity` (0.04–0.07), plus the container opacity dropped `opacity-40` → `opacity-30` — reads as a scattered, varied texture instead of a repeating grid. Kept the existing blurred corner blobs and underlying gradient as-is; they already sit visually behind/independent of the ring layer and the prompt allowed either choice. VERIFY: `npx tsc --noEmit` clean; `npm run lint` surfaced only the same 3 pre-existing `react-hooks/set-state-in-effect` errors in `BookingForm.tsx`, confirmed untouched via `git diff --stat`. **Browser-pane screenshot tooling was unavailable again this session** (`computer{action:"screenshot"}` and `computer{action:"left_click"}` both failed with "the Browser pane is not displayed, so the page is not compositing frames" — same session-level limitation noted in both prior Home-page entries, not a code issue) — verification used the established DOM/computed-style/`get_page_text`/`elementFromPoint`/`.click()`-dispatch fallback against the dev server on port 3001. Additionally, the three images referenced in this prompt (full-page screenshot, seam close-up, CTA pattern close-up) were not actually received as attachments in this session — only their text descriptions were — so Parts 3 and 4 were judged from code/DOM investigation and the written description rather than the images themselves; **Arjay should do a quick visual pass on the actual seam and the new CTA pattern before treating this as fully signed off.** Regression: `git diff --stat -- src/components/layout/Footer.tsx` confirmed empty; Footer's Facebook/Instagram/TikTok icons confirmed still present via DOM query; How It Works/Facilities/Footer sections confirmed unchanged via `get_page_text`; `/book` re-confirmed rendering with no console errors. `git status` confirmed only `Navbar.tsx`, `Hero.tsx`, `CtaBanner.tsx` touched, plus this `PROGRESS.md` entry; `package.json`/`package-lock.json` diff empty — no new dependencies. No test/DB data created — pure frontend change, no cleanup needed.

- 2026-08-14 — CLAUDE.md's Claude Code Skills section documented the newly installed UI/animation skills (emilkowalski/skill, impeccable) for discoverability in future prompts — doc-only, no code/schema/booking-flow change.

- 2026-08-15 — Navbar/Hero visual-match pass against a new client reference screenshot: `Navbar.tsx`, `Hero.tsx`, `src/app/layout.tsx`, `src/app/globals.css` touched (additive font-loading change only in the latter two); no backend/schema/booking-flow changes; `Facilities.tsx`, `StatsBar.tsx`, `page.tsx` confirmed untouched via `git status`. **Navbar**: logo swapped from `logo.png` to the new real brand asset `public/images/brand/winston-logo-emblem-transparent.png` (500×500 source, confirmed via a small Node script reading the PNG header directly rather than assuming); the `<span>Winston Sip & Serve</span>` wordmark added in the 2026-08-14 refinement pass was removed again per this reference — emblem alone, rendered at 60×60 (up from the prior 44×44 lockup size), `alt` text kept for accessibility even with no visible text. Sign In/Book Now buttons and the mobile menu markup were confirmed untouched (not in the diff). **Hero**: rebuilt from the left-aligned italic-serif "Sip. Serve. Play. Repeat." text-lockup to a centered stack matching the reference — script "Winston" wordmark, "SIP & SERVE", the full four-item tagline ("Tennis · Pickleball · Golf Simulator · Café & Bar"), and a re-centered Book Now button (same `/book` href, unchanged styling) — with the uppercase eyebrow label removed entirely. Background image swapped from `/images/placeholder.jpg` to the new real `public/images/hero-background.jpg` (4928×3264 source, ~2MB — **flagging as a future LCP optimization candidate; not resized/compressed this pass since it wasn't asked for**, `next/image`'s `fill`+responsive `sizes` already mitigates delivered size somewhat). Overlay changed from the bottom-heavy `bg-gradient-to-t from-brand-dark/95 via-brand-dark/70 to-brand-dark/40` to a flat uniform `bg-brand-dark/40`, matching the reference's evener tint. A new circular chevron-down button was added, absolute bottom-left, `aria-label="Scroll down"`, using a `useRef` on the section element and `sectionRef.current?.nextElementSibling?.scrollIntoView({ behavior: 'smooth' })` on click — deliberately targets the DOM's actual next sibling (StatsBar's real rendered root) rather than a hardcoded selector or an added ref threaded into `StatsBar.tsx`, since that file was out of scope for this prompt. **Font loading**: `Mrs_Saint_Delafield` added to `layout.tsx` alongside the existing `Lora` import (same `next/font/google` pattern, own CSS variable `--font-mrs-saint-delafield`, not a replacement of Lora); `globals.css` gained a new `--font-script: var(--font-mrs-saint-delafield)` entry inside `@theme inline` so `font-script` became an available Tailwind utility, following the same indirection `--font-sans`/`--font-lora` already used — a same-named self-reference was deliberately avoided by keeping the next/font variable and the Tailwind theme key as two distinct custom-property names. Only `brand-dark`/`accent-primary` (existing tokens) were used for colors; no new hex values added. No new npm dependencies — confirmed via empty `package.json`/`package-lock.json` diff. VERIFY performed against the dev server (port 3000, this session's own instance): confirmed via `getComputedStyle` that the `h1`'s `font-family` resolved to `"Mrs Saint Delafield", "Mrs Saint Delafield Fallback"` with no fallback-only rendering, meaning the font name is valid and loads correctly — **the reference-image side-by-side stroke-weight comparison against Alex Brush called for in the original prompt could not be performed, since `computer{action:"screenshot"}` failed all session with "the Browser pane is not displayed, so the page is not compositing frames" (same recurring tooling limitation noted in every Home-page entry since 2026-08-13) — Mrs Saint Delafield was kept on the strength of it being the prompt's stated first choice and a successful, unambiguous font-load, not a confirmed visual match; Arjay should do a quick visual comparison against the reference before treating the font choice as final.** Confirmed via DOM/`get_page_text`: navbar renders the emblem image with no sibling wordmark span; Hero renders "Winston" / "SIP & SERVE" / the full four-item tagline / "BOOK NOW" in that order, centered; Hero's Book Now link resolved `href="/book"` (5 total `/book` links on the page, all correct). Chevron button: a real synthetic click (via the connected browser's native click, not a scripted `.click()` — the latter produced no movement at all, consistent with React's delegated-listener requiring a trusted-enough event in this environment) started the page scrolling in the correct direction (0px → 160px toward a confirmed 476px target), but did not visibly complete to the full target within the session's observation window; calling `scrollIntoView` directly on `section.nextElementSibling` (bypassing the button) reached exactly 476px instantly, confirming the target-element logic itself is correct — **the partial animation is most likely this session's browser-automation tooling interrupting the smooth-scroll transition (same class of limitation as the screenshot issue above), not a defect in the click handler, but Arjay should do one manual click-and-watch to be sure.** Mobile (375×812, `resize_window` + reload): confirmed `document.documentElement.scrollWidth === clientWidth === 375` (no horizontal overflow) with the Hero `h1` fully inside the viewport bounds; mobile hamburger menu toggled open via `.click()` and confirmed via `get_page_text` to still show all 6 nav links plus its own stacked Book Now/Sign In buttons, unchanged from before this pass. Regression: `get_page_text` confirmed How It Works/Facilities/CTA Banner/Footer copy and all four StatsBar figures ("3/SPORTS", "9/COURTS & SIMULATORS", "6AM/OPENS DAILY", "10PM/LAST BOOKING") render identically to before this change. Scroll-reveal `IntersectionObserver` animations on How It Works/Facilities/CTA Banner were **not** re-verified this pass — same known non-composited-tab limitation documented in the 2026-08-13 entry, and this prompt didn't touch any `Reveal`-wrapped component regardless. `git status` confirmed exactly `globals.css`, `layout.tsx`, `Hero.tsx`, `Navbar.tsx` modified, plus the two pre-supplied image assets (already present as untracked files before this prompt started) and this `PROGRESS.md` entry — no other file touched. No test/DB data created — pure frontend change, no cleanup needed.

- 2026-08-15 — Font system replaced sitewide, plus Navbar/Hero simplification: `src/app/layout.tsx`, `src/app/globals.css`, `src/components/layout/Navbar.tsx`, `src/components/home/Hero.tsx` touched; `src/components/home/StatsBar.tsx` touched only to add `id="stats-bar"` to its existing outer wrapper div (no other change — confirmed via diff). `HowItWorks.tsx`, `Facilities.tsx`, `CtaBanner.tsx`, `Footer.tsx`, `page.tsx` confirmed untouched. **Fonts**: `Lora` (`--font-lora`, previously `font-sans`) removed entirely from `layout.tsx` — confirmed via a `src/` grep beforehand that only `layout.tsx` and `globals.css` referenced it, no other component. `Mrs_Saint_Delafield` (previously `font-script`) replaced by `Parisienne` (same fixed-weight shape); `Fraunces` and `Manrope` added as new variable-weight fonts (no `weight` prop, full range loaded). `globals.css`'s `@theme inline` updated: `--font-sans` now resolves to Manrope (body text sitewide), `--font-script` now resolves to Parisienne, and a new `--font-serif: var(--font-fraunces)` utility was added (not yet used anywhere — available for a future pass). No color tokens touched. **Navbar**: logo resized from a fixed 60×60 render to `h-11 w-auto md:h-14` (44px/56px tall, real 500×500 aspect ratio preserved, confirmed via a Node PNG-header read rather than assumed), `width`/`height` props corrected to the real 500×500 intrinsic size. The desktop Sign In button and Book Now link, and the mobile drawer's Sign In button and Book Now link (all four added in the 2026-08-14 pass), were removed entirely — the existing "Book Now" entry inside `NAV_LINKS` is now the header's only booking CTA, on both desktop and mobile. **Hero**: rebuilt to a `min-h-[85vh]` flex-centered layout (replacing the previous padding-based section), background image crop pinned to `object-[center_65%]`, overlay changed to a three-stop `bg-gradient-to-t from-brand-dark/70 via-brand-dark/20 to-brand-dark/50` (from the prior flat `bg-brand-dark/40`), and the scroll-down chevron rebuilt as a hand-written inline SVG (no `lucide-react` added) that scrolls via `document.getElementById('stats-bar')` instead of the previous `sectionRef.current?.nextElementSibling` approach — this is what StatsBar's new `id` attribute is for. Copy (Winston / SIP & SERVE / tagline / Book Now) and the Book Now button's `/book` destination are unchanged in content, only in surrounding layout/typography classes. VERIFY performed against the already-running dev server (port 3000, this session's own instance — this environment's own browser tooling, not Playwright): confirmed via `getComputedStyle` that `body`'s `font-family` resolves to Manrope and the Hero `h1`'s to Parisienne; confirmed via `document.documentElement.className` that the `html` element carries the Parisienne/Fraunces/Manrope font variables with no Lora variable present; confirmed via DOM query that no "Sign In" text exists anywhere on the page (desktop or mobile drawer, the latter opened via a real `.click()` dispatch) and that the mobile drawer shows only the 6 `NAV_LINKS`; confirmed `section`'s className matches the spec exactly; confirmed `#stats-bar` exists and the chevron's `scrollIntoView` call executes without error against it (a plain `window.scrollTo` instant-scroll succeeded in the same session, confirming scrolling itself works) — **the chevron's smooth-scroll animation could not be visually confirmed to complete, since `computer{action:"screenshot"}` and `computer{action:"left_click"}` both failed all session with "the Browser pane is not displayed, so the page is not compositing frames" (same recurring tooling limitation as multiple prior Home-page entries) — this is judged a pane-rendering limitation, not a handler defect, based on the successful non-erroring `scrollIntoView` call and instant-scroll test, but Arjay should do one manual click-and-watch to be sure.** Confirmed the Hero Book Now link and the Facilities/How It Works/CTA/Footer footer links all still resolve `href="/book"` correctly. Mobile (375×812, `resize_window` + reload): confirmed `document.documentElement.scrollWidth === clientWidth === 375` (no horizontal overflow) in both closed and hamburger-open states. Scroll-reveal `IntersectionObserver` animations elsewhere on the page were **not** re-verified this pass — same known non-composited-tab limitation noted in prior entries, and this prompt didn't touch any `Reveal`-wrapped component regardless. No console errors observed at any point. `git status`-equivalent confirmed only the five files above touched (`StatsBar.tsx` diff is a single added `id` attribute), plus this `PROGRESS.md` entry — no other file, no `package.json` change, no new dependency. No test/DB data created — pure frontend change, no cleanup needed.

- 2026-08-15 — StatsBar/How It Works copy+color pass, the next of the two staged Home follow-ups flagged in the prior font-system entry: `src/components/home/StatsBar.tsx` and `src/components/home/HowItWorks.tsx` touched only; `Reveal.tsx` (actual path `src/components/ui/Reveal.tsx`, confirmed by reading the `HowItWorks.tsx` import — the prompt's stated `src/components/home/Reveal.tsx` path doesn't exist), `page.tsx`, `Navbar.tsx`, `Hero.tsx`, `Facilities.tsx`, `CtaBanner.tsx`, `Footer.tsx` confirmed untouched. **StatsBar**: `totalResources`/`sportCount` props and function signature unchanged (pure JSX/className pass) — outer wrapper's negative margin changed from `-mt-16`/`-mt-24` (top-only) to a symmetric `-mt-16 -mb-16 md:-mt-20 md:-mb-20`, so the card now overlaps both Hero above and How It Works below; `id="stats-bar"` preserved (still the chevron's scroll target). Card styling tightened from `rounded-3xl`/`shadow-card` to `rounded-2xl`/`shadow-xl shadow-brand-dark/10`; the per-stat `sm:border-l` divider ternary removed entirely, so the four stats now render with no vertical dividers at any breakpoint. Numbers switched to `font-serif` (Fraunces); labels dropped `font-medium`/`text-gray-600` for `text-neutral-700/80`. **How It Works**: section background flipped from `bg-gray-100` to `bg-brand-dark` (padding moved from the container onto the section itself, `py-32`); eyebrow text changed from the literal `"SIMPLE & FAST"` to `"Simple & Fast"` (CSS `uppercase` renders both identically — this only changes the accessible/literal string) with lighter tracking; heading switched to `font-serif` at a larger size; subtitle copy shortened by dropping the "— no account required" clause and its `mx-auto max-w-xl` centering constraint removed per the prompt (not re-added — didn't read as too wide against the DOM geometry checked in VERIFY). Each of the three `Reveal`-wrapped cards gained a new bordered/tinted container (`rounded-2xl border border-neutral-100/10 bg-brand-mid/10 p-8`, previously plain text with no background) with the number switched from `<p>` to `<span>` (`text-accent-primary/25`, replacing the separate `accent-light` token) and the title switched from `<p>` to `<h3>`; the `Reveal` wrapper itself was not touched, only the JSX/classes it wraps. Step copy replaced verbatim per the prompt's exact text (Card 1 now names all four bookable categories — Tennis/Pickleball/Golf Simulator/Pickleball Simulator — Card 2 and 3 also reworded). No new dependencies; no Tailwind color tokens added — only existing `brand-dark`/`brand-mid`/`accent-primary`/`brand-light`/`neutral-*` tokens used. VERIFY performed against the developer's already-running dev server (port 3000, this session's own `npm run dev` on port 61200 conflicted and exited immediately — connected to the existing instance instead): **`computer{action:"screenshot"}` and `computer{action:"zoom"}` both failed all session with "the Browser pane is not displayed, so the page is not compositing frames"** (same recurring tooling limitation noted in every Home-page entry since 2026-08-13) — no pixel-level screenshot comparison against the reference mockup was possible this pass; verification instead used `read_page`/`get_page_text` (confirmed all four StatsBar figures, the exact How It Works heading/subtitle/eyebrow text, and all three cards' exact new copy render correctly), `javascript_tool`/`getBoundingClientRect` (confirmed StatsBar's and How It Works' resolved `className` strings match the spec exactly, and at both desktop and a 375×812 mobile viewport the StatsBar card's rect sits fully within the negative-margin overlap zone on both sides with no content clipped, and `document.body.scrollWidth === window.innerWidth` at 375px confirming no horizontal overflow), a real chevron click (via `computer{action:"left_click"}` on the button ref, which — unlike screenshot/zoom — did succeed) followed by a `window.scrollY` read confirming the page actually scrolled toward StatsBar, and `read_console_messages` (no errors at any point). Reveal's scroll-in animation itself was not re-exercised — same known IntersectionObserver-in-non-composited-tab limitation as prior entries — but the wrapper structure was confirmed unchanged via source diff, so existing behavior should carry over unaffected. **Arjay should do one manual visual pass against the reference screenshot to confirm exact color/shadow/spacing match before treating this as fully signed off, since this pass's verification was DOM/geometry-based rather than pixel-based.** Facilities/CTA Banner/Footer confirmed rendering with unchanged copy via `read_page` (not touched by this diff, so no regression expected, but re-checked per the prompt's regression step). No test/DB data created — pure frontend change, no cleanup needed.

- 2026-08-17 — Navbar link styling + CtaBanner dot-pattern/copy/button fixes: `src/components/layout/Navbar.tsx` and `src/components/home/CtaBanner.tsx` touched only, per explicit scope (no shared Button component exists in this codebase — both files call `Link` directly with inline `className`, so there was nothing to flag re: shared-default conflicts). **Navbar**: both the desktop (`ul.hidden.md:flex`) and mobile-drawer (`div.md:hidden ul`) link lists changed from `font-medium` (500) to `font-normal uppercase tracking-[0.35px]` (400) — `text-sm`/14px on desktop and `text-base` on mobile both left as-is (font-size wasn't in scope; mobile intentionally renders larger than desktop, unchanged from before). **CtaBanner pattern**: the near-invisible tiled data-URL background (`opacity-30` wrapper × per-circle `stroke-opacity` 0.04–0.07 ≈ effective 0.01–0.02 visibility) and the two off-brief blurred corner blobs (`bg-accent-primary`/`bg-brand-mid`, `blur-3xl`) were removed entirely, replaced by an inline `<svg>` using a `<pattern>` (four circles, r=12/30/16/42, same relative positions as the original data-URL) filled into a full-size `<rect>`, stroked with `currentColor`, with the element's own `text-brand-light/10` class as the single opacity control (no wrapping opacity div). **Copy**: subtext replaced with "Reserve a court, grab a seat at the bar, or see what membership unlocks — your next game is a couple of taps away."; both CTA buttons (Book Now, Explore Membership) gained a local `uppercase` className — Hero's own "Book Now" button already carried `uppercase` independently (pre-existing, confirmed via DOM inspection, not touched by this prompt) so no shared-style change was implied or needed. VERIFY performed against the developer's already-running dev server (port 3000; this session's own `npm run dev` attempt exited immediately on the same port conflict noted in several prior entries) via `getComputedStyle`/DOM queries at both 1280×800 desktop and 375×812 mobile (the latter's hamburger opened via a direct `.click()` dispatch, since `computer{action:"left_click"}` timed out with the same "Browser pane is not displayed" limitation noted in multiple prior entries — `computer{action:"screenshot"}` also failed for the same reason, so no pixel-level screenshot was captured this pass): confirmed all 6 links in both the desktop and mobile lists compute to `text-transform: uppercase`, `letter-spacing: 0.35px`, `font-weight: 400` (desktop additionally confirmed `font-size: 14px`); confirmed the CtaBanner section contains the new `<svg>`/`<pattern id="cta-dot-pattern">` and zero `.blur-3xl` elements; confirmed the subtext's `textContent` matches the new copy verbatim and both CTA links compute `text-transform: uppercase`. Regression: confirmed via `git status` that only the two in-scope files changed (`Hero.tsx`, `Facilities.tsx`, `Footer.tsx` untouched, matching the explicit scope note). No test/DB data created — pure frontend change, no cleanup needed. **Arjay should do a quick visual pass on the CtaBanner dot pattern once the Browser pane renders, to confirm the 10%-opacity intensity reads correctly against the dark gradient** — this pass's verification was computed-style/DOM-based, not pixel-based, same recurring tooling limitation as prior Home-page entries.

- 2026-08-17 — `/book` gate + heading visual pass, bringing them onto the site's cream/Fraunces/Manrope/brand-dark design system: `src/components/booking/AnnouncementGate.tsx` and `src/app/book/page.tsx` touched only, per explicit scope (`prisma/schema.prisma`, `BookingForm.tsx`, the wizard `steps/` files, and every Home component were read-only references, not edited). **page.tsx**: stale subtext ("Fill in your details below to hold a slot. Our team will follow up to confirm payment." — inaccurate, since no details are collected on this screen and payment is live PayMongo, not manual follow-up) replaced with "Pick your sport, date, and time to get an instant booking reference — payment details come after."; heading copy left unchanged per the prompt. The page wrapper's near-black dark-mode theme (`bg-zinc-50 dark:bg-black`, `text-black dark:text-zinc-50`, `text-zinc-600 dark:text-zinc-400`) was removed and repointed to the design-token system (`bg-background`, `text-brand-dark`, `text-neutral-700`) — necessary alongside the AnnouncementGate redesign below, since leaving the old `dark:` variants in place would have made text unreadable against the new cream background under a system dark-mode preference. **AnnouncementGate**: the single dim "Welcome to Winston Sip and Serve" card replaced with a ledger-style list of 3 hardcoded notices via a new local `SAMPLE_NOTICES: GateNotice[]` array (`{ tag, title, body, publishedAt }`) — deliberately shaped to mirror the Prisma `Bulletin` model's `title`/`body`/`publishedAt` fields (documented with a one-line code comment) so a future fetch-from-DB swap is a drop-in replacement; `tag` is UI-only, not a `Bulletin` column. Each notice renders as eyebrow tag (`text-accent-primary`) + right-aligned date + `font-serif` title + `font-sans` body, separated by `border-t border-brand-dark/10` hairlines (none on the first notice, no numbered markers or icons). Card shell changed to `bg-brand-light` / `rounded-2xl border border-brand-dark/10 shadow-xl shadow-brand-dark/10` (matching StatsBar's existing card convention) sitting inside a wider `relative w-full` wrapper so the CtaBanner-sourced circle `<pattern>` SVG (same four-circle markup, r=12/30/16/42, reused verbatim) can render `absolute inset-0` behind the card at a light-surface-appropriate `text-brand-dark/[0.05]` opacity — full-bleed within the gate's own section width, not clipped to the card's padding. "Continue to Booking" restyled to reuse Hero's Book Now button classes exactly (`rounded-full bg-accent-primary … text-brand-light … hover:bg-accent-dark`, `self-center` swapped in for Hero's layout-only `mt-10 inline-block` since this button sits in a flex column instead of Hero's absolute-positioned column) rather than inventing new button styling. The `AnnouncementGateProps` callback contract (`{ onContinue }`) was not changed. No new dependencies; `prisma/schema.prisma` and the `Bulletin` table were read for field-shape reference only, not modified — no DB/schema changes, no test data created. VERIFY performed against the developer's already-running dev server (port 3000; this session's own `npm run dev` attempt exited immediately on the same port-conflict pattern noted in several prior entries) via the Browser pane: `computer{action:"screenshot"}` failed with the same recurring "Browser pane is not displayed, so the page is not compositing frames" limitation noted in every Home-page entry since 2026-08-13, so verification used `read_page`/`get_page_text` plus `javascript_tool`/`getComputedStyle` instead — confirmed via computed style that the page wrapper's background resolves to `rgb(255,248,240)` (`#FFF8F0` = `background`), the card's to `rgb(253,243,231)` (`#FDF3E7` = `brand-light`), the heading's `color` to `rgb(75,46,43)` (`#4B2E2B` = `brand-dark`), and the button's `background-color` to `rgb(192,133,82)` (`#C08552` = `accent-primary`, matching Hero's button exactly); confirmed exactly 2 `.border-t` hairline dividers exist (3 notices, none on the first) and the `#gate-dot-pattern` SVG pattern element is present in the DOM; confirmed via `read_page` that all 3 notices render with the correct tag/title/body/date text and order, and that the subtext no longer mentions "fill in your details below." A real click on "Continue to Booking" (`computer{action:"left_click"}` on the button's ref, which — unlike screenshot — did succeed) confirmed the gate unmounts and the SPORT step of `BookingForm` renders with its step indicator (SPORT/COURT/DATE & TIME/ADD-ONS/SUMMARY) and all 5 resource-type buttons — the wizard itself is unaffected, no regression. Mobile (375×812, `resize_window`): confirmed `document.body.scrollWidth === window.innerWidth === 375` (no horizontal overflow) and the button's `getBoundingClientRect()` reads 48px tall (comfortable tap target). Regression: `git status` confirmed only `AnnouncementGate.tsx` and `page.tsx` changed (`Navbar.tsx`'s pre-existing uncommitted modification, present before this prompt started, is unrelated); root `/` re-loaded with zero console errors, a fresh `/book` reload also showed zero console errors. No test/DB data created — pure frontend change, `Bulletin` table untouched, no cleanup needed. **Arjay should do a quick visual pass on the gate once the Browser pane renders a real screenshot, to confirm the pattern-opacity and card-shadow intensity read correctly against the cream background** — this pass's verification was computed-style/DOM-based, not pixel-based, same recurring tooling limitation as prior Home-page entries.

- 2026-08-17 — How It Works equal-height card fix: `src/components/home/HowItWorks.tsx` touched only — `Reveal.tsx` did not need a change, since it already accepted and forwarded a `className` prop (confirmed by reading it first, per scope). The three-card row (`grid grid-cols-1 gap-8 md:grid-cols-3`) already stretched its direct grid children to equal height by CSS Grid's default `align-items: stretch`, but neither the `Reveal` wrapper nor the card `div` inside it had a height set, so each card's bordered/tinted box (`rounded-2xl border border-neutral-100/10 bg-brand-mid/10 p-8`) sized to its own content instead — Card 1's longer body copy (4 lines vs. 3) made its box visibly taller than Cards 2/3. Fix: added `className="h-full"` to each `Reveal` usage in the map (now forwarded onto its wrapper `div`, which was already a grid item and thus already had a resolved height from grid stretch — `h-full` just lets the card below reference it), and `flex h-full flex-col` on the card `div` itself so it fills that height. No JSX restructuring, no `justify-between`, no copy changes. VERIFY performed against the developer's already-running dev server (port 3000; this session's own `npm run dev` attempt exited immediately on the same port-conflict pattern noted in prior entries): `computer{action:"screenshot"}` failed with the same recurring "Browser pane is not displayed" limitation noted in every Home-page entry since 2026-08-13, so verification used `javascript_tool`/`getBoundingClientRect` instead — confirmed all three card boxes render at exactly 285.59px tall at desktop width (1280px, default viewport) with matching `top`/`bottom` (1004/1290 in one reading), and that the 01/02/03 numbers and titles align at identical y-positions across all three cards (`numTop`/`titleTop` identical across the array). At 375×812 (`resize_window`), confirmed cards stack full-width (327.2px, single column) with naturally varying heights (273.59px vs. 249.59px) since there's only one grid column to equalize against — no forced-equal-height artifact introduced at the stacked breakpoint. Regression: `get_page_text` confirmed all three cards' copy ("Choose Your Sport"/"Pick a Schedule"/"Play!" and their full descriptions) renders byte-for-byte unchanged; `git status` confirmed only `HowItWorks.tsx` changed (`Reveal.tsx` untouched, so no other `Reveal` usage — `Facilities.tsx` per the read-first grep — needed a regression screenshot pass). No test/DB data created — pure frontend change, no cleanup needed. **Arjay should do a quick visual pass once the Browser pane renders a real screenshot, to confirm the flush bottom edges read correctly** — this pass's verification was DOM/geometry-based, not pixel-based, same recurring tooling limitation as every prior Home-page entry.

- 2026-08-18 — SPORT step non-member starting prices: `src/components/booking/steps/SportStep.tsx` touched only, per explicit scope (`CourtStep.tsx`, `DateTimeStep.tsx`, and all other wizard steps untouched — display-only addition, no pricing logic/state elsewhere). Added a local `NON_MEMBER_PRICE_LABEL` map keyed by `ResourceType.slug` (confirmed the real seeded slugs are underscore-separated — `tennis_court`/`pickleball_court`/`tennis_sim`/`pickleball_sim`/`golf_sim`, per `prisma/seed.ts`'s `ResourceTypeSlug` enum — not the hyphenated form initially assumed) to the non-member figures from PROJECT_CONTEXT.md's Pricing → Non-Member Rates section: Tennis Court ₱750/hr, Pickleball Court ₱650/hr, Tennis Sim/Pickleball Sim "From ₱300" (cheapest 15-min tier), Golf Sim "From ₱1,150" (cheapest 60-min tier). Rendered as a third static `<span>` per button, matching the existing count-label's `text-sm text-zinc-600 dark:text-zinc-400` styling — no restyling of the buttons themselves. No new dependencies, no shared component (5 fixed resource types, a local object was enough). VERIFY performed against the developer's already-running dev server (port 3000; this session's own `npm run dev` attempt exited immediately on the same port-conflict pattern as prior entries) via the Browser pane: `get_page_text` confirmed all 5 buttons render their correct price label exactly as specified (including the ₱ symbol and "From" wording) once the slug-format fix was applied — the initial hyphenated-slug attempt rendered no price labels at all, caught and corrected before this VERIFY pass completed. `computer{action:"screenshot"}` failed with the same recurring "Browser pane is not displayed" limitation noted in every Home-page entry since 2026-08-13, so no pixel-level screenshot was captured this pass. Mobile (375×812, `resize_window`) confirmed via `javascript_tool`/`getComputedStyle` that `document.documentElement.scrollWidth === clientWidth === 375` — no horizontal overflow from the added text. Selecting Tennis Court and clicking Continue correctly advanced to the COURT step ("Choose a tennis court to book" / "Court 1"), confirming no regression to selection/advance logic. `git status` confirmed only `SportStep.tsx` changed. No test/DB data created — pure frontend change, no cleanup needed. **Arjay should do a quick visual pass once the Browser pane renders a real screenshot, to confirm the price line's placement/spacing reads cleanly against the button's existing two-line layout** — this pass's verification was `get_page_text`/DOM-based, not pixel-based, same recurring tooling limitation as every prior Home-page/booking-flow entry.

- 2026-08-18 — DATE & TIME step UI pass (duration button group, calendar past-dates, time-slot borders): `src/components/booking/steps/DateTimeStep.tsx`, `src/components/booking/Calendar.tsx`, `src/components/booking/TimeSlotGrid.tsx` touched only, per explicit scope — no change to availability/duration data logic, the `GET /api/availability` contract, or business-hours enforcement, confirmed presentation-only via diff. **Duration control-type swap**: the native `<select>` in `DateTimeStep.tsx` (was lines 44–55, one line lower than the prompt's stated "~41–50" due to the intervening `<label>` line — noted, not a functional discrepancy) replaced with a `role="group" aria-label="Duration"` row of pill buttons, one per `durationOptions` entry (no values added/removed), `flex flex-wrap gap-2` so it wraps instead of overflowing on narrow screens; styled to match `CourtStep.tsx`'s existing selected/unselected button-group pattern (`border-foreground bg-black/[.04] dark:bg-white/[.08]` selected vs. `border-black/[.145] hover:bg-black/[.03] …` unselected) for visual consistency with the sibling step, rather than inventing a new style; each button carries `aria-pressed`. The exact same `durationMinutes`/`onDurationChange` prop contract is preserved — buttons call `onDurationChange(String(d))`, matching the prior `<select>`'s `e.target.value` (always a string). **Calendar past-dates**: read-first investigation found `Calendar.tsx` already rendered every day of the month in its correct grid position (including days before today, muted via `text-zinc-400 dark:text-zinc-600`, `cursor-not-allowed`, no hover class, and non-interactive via the native `disabled` attribute) — the prompt's stated "current state" (past days don't appear at all) did not match the actual code, likely stale from an earlier version. The only real gap against the prompt's explicit ask was a literal `aria-disabled="true"` attribute (native `disabled` alone doesn't emit it), so `aria-disabled={isPast}` was added to the day button; no other change was needed or made. **Time-slot borders**: similarly, `TimeSlotGrid.tsx` already applied a `border` class in every state (available: `border-black/[.145]`, selected: `border-foreground` matching its own fill, exceeds-closing: `border-amber-500/50`) — except the disabled/unavailable state, which used a near-invisible `border-black/[.08]` (8% opacity, vs. the `[.145]` token used everywhere else in the booking flow). Bumped that one value to `border-black/[.145] dark:border-white/[.145]` so disabled slots are clearly delineated too, matching the established neutral-border token (confirmed via grep — this is the same token `Calendar.tsx`'s container, the old duration `<select>`, and `SportStep.tsx`/`CourtStep.tsx` buttons all already use); no sizing, spacing, or grid-column change. VERIFY performed against the developer's already-running dev server (port 3000) via the Browser pane: `computer{action:"left_click"}` and `computer{action:"screenshot"}` both failed all session with the same recurring "Browser pane is not displayed, so the page is not compositing frames" limitation noted in every entry since 2026-08-13 — verification used a `javascript_tool`-dispatched `.click()` (which, unlike `computer`, worked reliably this session) to drive the wizard, plus `get_page_text`/`getComputedStyle`/DOM queries for confirmation; no pixel-level screenshot was captured this pass. Confirmed for a court-type sport (Tennis Court → Court 1): Duration renders `60/120/180/240 minutes` as buttons, `aria-pressed="true"` on the active one with the CourtStep-matching selected class, `aria-pressed="false"` + unselected class on the rest; clicking a duration button updates `aria-pressed` correctly and re-renders the time-slot grid (functionally identical to the old `<select>`'s `onChange`, confirmed the previously-selected slot cleared the same way switching duration would have with the old control, since that reset lives in `BookingForm.tsx`'s handler, untouched by this prompt). Confirmed for a sim-type sport (Golf Simulator → Bay 1): Duration renders exactly `60/90 minutes` (the real non-member golf-sim tiers, no 30-min row) with no overflow, confirming the button group renders whatever `durationOptions` list it's given rather than a hardcoded set. Calendar: with "today" resolving to 2026-08-18, days 1–17 confirmed `disabled: true`, `aria-disabled: "true"`, muted class, no hover class; days 18+ confirmed `disabled: false`, `aria-disabled: "false"`, interactive class; a real `.click()` dispatched on day 10 (past) produced no state change (no Time section appeared) confirming the click truly no-ops. Time slots: sampled buttons confirmed `border-black/[.145]` present in the DOM class list for available slots and (post-fix) disabled/booked slots alike, and `border-foreground bg-foreground` on the slot after selecting it (6:00 AM), all with a real computed `border-width: 0.8px` — border is present and visible in every sampled state. Selecting a future date (Aug 20) + an available time slot (9:00 AM, 60-min duration) and clicking Continue correctly advanced to ADD-ONS ("Number of guests" / "Ball Boy — ₱150.00" / "Coaching"), confirming no regression to the selection/advance flow. Mobile (375×812, `resize_window`) and desktop (1280×900) both confirmed `document.documentElement.scrollWidth === clientWidth` (no horizontal overflow) for the Duration button row at both the 4-option (court) and 2-option (sim) widths. `npx tsc --noEmit` clean. `git status` confirmed only the three scoped files changed. No test/DB data created — pure frontend change, no cleanup needed. **Arjay should do a quick visual pass once the Browser pane renders a real screenshot** — this pass's verification was DOM/computed-style-based, not pixel-based, same recurring tooling limitation as every prior Home-page/booking-flow entry; worth specifically eyeballing the disabled time-slot border (now matching the standard 14.5%-opacity token rather than the prior 8%) and the pill-button wrap behavior on a genuinely narrow device.

- 2026-08-18 — StepIndicator active-label visibility fix: `src/components/booking/steps/StepIndicator.tsx` touched only — `BookingForm.tsx` was read-first to confirm `currentStep` is passed correctly and needed no change. Root cause was (b), a CSS state, not conditional rendering or a missing labels-array entry: the label `<span>` for every one of the 5 steps was always rendered (`{label}` unconditionally in JSX), but the *active* step's color classes (`text-black dark:text-zinc-50`) made it invisible whenever the OS/browser preferred dark mode — this site has no actual dark theme (`globals.css`'s `:root` hardcodes a single cream `--background: #FFF8F0`/`--foreground: #374151` pair with no dark override), yet Tailwind's `dark:` variant still fires off `prefers-color-scheme` regardless. In dark mode the active label rendered near-white (`zinc-50`) text on the always-cream page background — effectively invisible — while inactive/completed labels used `text-zinc-500` in both branches (no real color change between light/dark), which is why only the currently-active step (SUMMARY, in the reported screenshot) lost its label and steps 1–4 looked fine. Fix: replaced the active-label classes with `text-foreground` (dropping the `dark:` variant entirely, matching the pattern the circle styling already used via `border-foreground`/`bg-foreground`/`text-background` semantic tokens) and simplified the inactive/completed branch to plain `text-zinc-500`. No change to the circle's active/completed/upcoming fill logic, step order, label wording, or step count. VERIFY performed against the developer's already-running dev server (port 3000; this session's own `npm run dev` attempt exited immediately on the same port-conflict pattern as prior entries) via the Browser pane, with `computer{action:"screenshot"}`/`computer{action:"left_click"}` again failing with the same recurring "Browser pane is not displayed" limitation noted in every entry since 2026-08-13 — verification used `javascript_tool`-dispatched `.click()` to drive the wizard plus `getComputedStyle` for the actual regression check that matters here (computed color, not just DOM presence). Mobile (375×812) + dark `colorScheme` first, per prompt: walked SPORT → COURT (Golf Simulator, Bay 1) → DATE & TIME (Aug 20, 9:00 AM, 60 min) → ADD-ONS → SUMMARY, confirming via `get_page_text` that all 5 labels were present in the DOM at every step, then at the SUMMARY step (the active step matching the original bug report) confirmed via `getComputedStyle` that the active "SUMMARY" label resolves to `rgb(55,65,81)` (`--foreground`) against a `rgb(255,248,240)` (`--background`) body — clearly legible — versus the pre-fix code path which would have resolved to a near-white `zinc-50` in this same dark-mode state. Desktop (1280×800) + light `colorScheme` checked second: confirmed the same 5 labels all render with correct, distinct colors (`--foreground` active, `zinc-500`-equivalent inactive), no regression from the light-mode case that was already working. Regression: circle `getComputedStyle` sampled at the SUMMARY step confirmed all 5 circles show `bg-foreground`/`border-foreground` fill with `background`-colored numbers (correct completed/active fill, untouched by this diff — the circle branch wasn't touched at all). `git status` confirmed only `StepIndicator.tsx` changed. No test/DB data created — pure frontend styling change, no cleanup needed.

- 2026-08-18 — StepIndicator brand tokens + AddOnsStep visual redesign: `src/components/booking/steps/StepIndicator.tsx` and `src/components/booking/steps/AddOnsStep.tsx` touched only, per explicit scope — no change to active/completed step logic, layout, or spacing in `StepIndicator.tsx`; no change to `SportStep.tsx`/`CourtStep.tsx`/`DateTimeStep.tsx`/`ReviewStep.tsx`/`BookingForm.tsx`/pricing or API logic. **StepIndicator**: circles/labels recolored from generic `foreground`/`zinc` tokens to brand tokens — active-or-completed circles (still treated identically, since the prior code already didn't distinguish them and the prompt said not to invent a new distinction) now `bg-brand-dark border-brand-dark text-brand-light`; upcoming circles `border-brand-dark/20 text-brand-dark/40`; active label `text-brand-dark`, inactive/completed label `text-brand-dark/50` (same asymmetric active-only logic as before, just recolored). **AddOnsStep**: wrapped in the `AnnouncementGate`-style card (`bg-brand-light rounded-2xl border border-brand-dark/10 shadow-xl shadow-brand-dark/10`) with a `font-serif` "Add-Ons" heading; guest count native `<input type=number>` replaced with a bordered-pill accent-primary +/− stepper (min 0 preserved exactly, no max — matching the original `Math.max(0, …)` clamp, which also had no upper bound); Ball Boy/Coaching checkboxes replaced with full-width tappable option-card rows (`border-brand-dark/10` idle, `border-accent-primary bg-accent-primary/5` selected, custom filled-circle-with-check indicator, `aria-pressed` for state). **Coaching pax derivation (behavioral change, in-scope)**: the prior manual "1 Pax / 2 Pax" radio buttons were replaced with automatic derivation from guest count via a `useEffect` that calls the existing `onCoachingPaxCountChange` prop whenever `coaching` is on and `coachingPricing.mode === 'paxTiered'` — no new prop, no parent wiring needed, so this stayed within the two-file scope. Derivation: confirmed via `PROJECT_CONTEXT.md`'s Guest Fee Rule ("₱150/hr per additional guest, booker is exempt from their own guest fee") and `src/lib/booking-pricing.ts`'s `guestFeeCentavos = guestCount * guestFeeRule.amountCentavos` that `guestCount` means *additional guests beyond the booker*, not total headcount — so total pax = 1 (booker) + `guestCount`, capped at the 2-pax tier since no 3+ rate exists. Since only a 1-pax/2-pax split exists, this collapses to: `guestCount === 0` → 1 pax (₱800 court coaching), `guestCount >= 1` → 2 pax (₱1,200) — verified live by moving guest count 0→1→0 and confirming Coaching's displayed price flipped ₱800→₱1,200→₱800 each time. **Scope conflict surfaced and resolved with Arjay before implementation**: the prompt's Part 2 step 5 asked for AddOnsStep's own Back/Continue buttons to be restyled (ghost Back, Hero-style Continue) — but Back/Continue for steps 1–4 are actually rendered by one shared bar in `BookingForm.tsx` (out of scope to touch, and touching it would also restyle Sport/Court/DateTime, which the prompt explicitly forbade). Arjay chose to skip step 5 and flag it rather than expand scope — **AddOnsStep still has no Back/Continue buttons of its own; the existing generic `BookingForm.tsx` bar (untouched, still `border-black/[.145]`/`bg-foreground` tokens) continues to render below the new card for this step.** This is the one explicit checkpoint item not implemented this pass. VERIFY performed against the developer's already-running dev server (port 3000) via the Browser pane, `computer{action:"screenshot"}`/`left_click` again failing with the same recurring "Browser pane is not displayed" limitation as every entry since 2026-08-13 — verification used `javascript_tool`-dispatched `.click()` plus `get_page_text`/`getComputedStyle` throughout. Mobile (375×812) and desktop (1280×800) both confirmed `document.documentElement.scrollWidth === clientWidth` (no horizontal overflow) for Pickleball Court's Add-Ons card; `getComputedStyle` confirmed the card's `background-color: rgb(253,243,231)` (`brand-light`), heading `color: rgb(75,46,43)` (`brand-dark`) in `Fraunces, "Fraunces Fallback"`, stepper button `background-color: rgb(192,133,82)` (`accent-primary`), and row border matching `brand-dark/10`. Functional: guest stepper 0→1→0 confirmed Coaching price and selected-row classes updated correctly each time; toggling Ball Boy/Coaching independently confirmed `aria-pressed` and the selected-state classes (`border-accent-primary bg-accent-primary/5`) track underlying form state correctly; clicking Continue from Pickleball Court's Add-Ons (Ball Boy + Coaching both selected, 0 guests) correctly advanced to Summary showing "Coaching — 1 Pax" and a ₱1,600.00 estimate (₱650 court + ₱150 Ball Boy + ₱800 Coaching), confirming add-on state carries through unchanged. Golf Simulator confirmed Coaching shows a flat ₱1,000 with no pax variation and no guest stepper/Ball Boy row (correct — not a court). Tennis Simulator confirmed Coaching remains hidden entirely (regression check on the existing hide logic, untouched by this diff) — the card renders with only the heading and no add-on rows. Regression: StepIndicator's new brand tokens sampled via `getComputedStyle` across all 5 steps at the SUMMARY step confirmed all completed circles render `bg-brand-dark`/`border-brand-dark`, the active label full `brand-dark`, and inactive/completed labels `brand-dark` at 50% alpha — rendering correctly on SPORT/COURT/DATE & TIME too (same shared component, no step-specific branching). `git status` confirmed only the two scoped files changed — no shared pricing helper extraction was needed since the pax-derivation logic lives entirely in `AddOnsStep.tsx` and reuses the existing `onCoachingPaxCountChange` callback. No test/DB data created — pure frontend change, no cleanup needed.

- 2026-08-18 — ReviewStep (SUMMARY) ledger-style redesign: `src/components/booking/steps/ReviewStep.tsx` touched only, per explicit scope — no change to `StepIndicator.tsx`, `PaymentStep.tsx`, `BookingForm.tsx`, or any pricing calculation/displayed value/copy. Wrapped the summary in the same `bg-brand-light rounded-2xl border border-brand-dark/10 shadow-xl shadow-brand-dark/10` card treatment as `AnnouncementGate`/`AddOnsStep`; converted the 6 (7 for court sports, +Guests) label/value rows into `AnnouncementGate`-style ledger rows separated by `border-t border-brand-dark/10` hairlines (first row has none), label `text-brand-dark/70`, value right-aligned `text-brand-dark font-medium`. Price block: "Estimated price" label in `font-serif` (Fraunces), amount in `text-accent-primary text-2xl font-medium`, separated from the ledger by its own `border-t border-brand-dark/10`; "Add-ons subtotal" and the non-member-rate disclaimer kept as `text-sm text-brand-dark/60` below, unchanged wording. Confirm Booking now reuses Hero's exact button classes (`bg-accent-primary`, `rounded-full`, uppercase, `text-brand-light`, accent-dark hover/focus ring) with `flex-1` added for the two-button row layout. **Back button caveat**: the prompt asked to match "AddOnsStep's ghost-button styling exactly," but read-first investigation (and the 2026-08-18 AddOnsStep entry above) confirmed AddOnsStep has no Back button of its own — steps 1–4 share one generic, still-unthemed Back/Continue bar in `BookingForm.tsx` (out of this prompt's scope to touch). With no existing themed reference to copy, a new ghost/text-button style was designed from the established brand tokens instead: `border-brand-dark/20 text-brand-dark/70 hover:bg-brand-dark/5 hover:text-brand-dark`, `rounded-full`, uppercase, matching the Confirm button's sizing/tracking conventions. Flagged in Next Up as the reference AddOnsStep (or a future shared-bar restyle) should probably adopt to stay consistent. VERIFY performed against the developer's already-running dev server (port 3000) via the Browser pane; `computer{action:"screenshot"}`/`left_click` again failed with the same recurring "Browser pane is not displayed" limitation as every entry since 2026-08-13 — verification used `javascript_tool`-dispatched `.click()` to drive the wizard plus `get_page_text`/`getComputedStyle`/DOM queries. Mobile (375×812) confirmed via `document.documentElement.scrollWidth === clientWidth` (no overflow); desktop checked the same way. Walked a full booking (Tennis Court, Court 1, 60 min, Aug 20 10:00 AM, Ball Boy + Coaching) to SUMMARY and confirmed via `getComputedStyle`: card `background-color: rgb(253,243,231)` (brand-light), first ledger row `border-top-width: 0px` vs. `0.8px` on the rest, price label `font-family: Fraunces, "Fraunces Fallback"`, price amount `color: rgb(192,133,82)` (accent-primary) at `24px`, Back button transparent background with `brand-dark/70` text, Confirm button `background-color: rgb(192,133,82)` with `brand-light` text. Regression: confirmed all 7 displayed line items (Sport: Tennis Court, Court/bay: Court 1, Date & time: 8/20/2026 10:00:00 AM, Duration: 60 minutes, Guests: 0, Ball Boy: ₱150.00, Coaching: 1 Pax) and the price block (₱1,700.00 total, ₱950.00 add-ons subtotal) exactly matched what was selected in prior steps — no data drift from the restyle. Clicking Confirm Booking created the hold (`POST /api/bookings` → `201 Created`) and advanced to the Payment page with Booking Reference `cmsxiy9h40004w42g6e6vom5t` shown immediately — no regression to the hold-then-attach flow. On a separate fresh run (Pickleball Court, Court 2, 120 min, Aug 21 2:00 PM, 1 guest, Ball Boy selected), clicking Back from SUMMARY returned to ADD-ONS with guest count still `1` and Ball Boy still `aria-pressed="true"` — prior selections intact, no state loss. `git status` confirmed only `ReviewStep.tsx` changed. One real `pending_payment` hold was left behind by the Confirm Booking exercise above — see Next Up for why it wasn't manually cleaned up.

- 2026-08-18 — SPORT/COURT/DATE & TIME brand tokens + pre-selection bugfix + shared Back/Continue bar restyle: `SportStep.tsx`, `CourtStep.tsx`, `DateTimeStep.tsx`, `Calendar.tsx`, `TimeSlotGrid.tsx`, and `BookingForm.tsx` touched (only the shared Back/Continue bar and the two state-initialization sites below in the last file) — `StepIndicator.tsx`, `AddOnsStep.tsx`, `ReviewStep.tsx`, `PaymentStep.tsx`, no API route, and no pricing logic were left untouched. **Part 1 root cause (confirmed by reading, not assumed)**: `BookingForm.tsx`'s data-load effect called `setResourceTypeId(json.resourceTypes[0].id)` on mount, and the resource-type-change effect called `setResourceId(selectedResourceType.resources[0]?.id ?? '')` — both initialized to the first array item rather than `null`/`''`, so `SportStep`/`CourtStep`'s `isSelected = id === propId` was true for the first card with zero clicks. Fixed both to initialize/reset to `''` instead (and the matching `handleStartOver` line, same bug pattern, in scope as "whichever state-initialization line turns out to cause Part 1's bug"). `canContinue` for steps 1–2 already gated on `!!resourceTypeId`/`!!resourceId`, so Continue is now correctly disabled until a real click happens (previously it was always enabled from load, since the auto-selected first item satisfied the check). Duration's 60-minute default (`DateTimeStep`) was left untouched — confirmed functionally required, since `TimeSlotGrid` needs a non-null duration to compute slot end-times/closing checks, and no `!!durationMinutes`-only gate exists to expose an "unselected" state cleanly. **Part 2/3 token sweep**: replaced all `border-black/[.145]` → `border-brand-dark/20`, selected outlined-control state → `border-accent-primary bg-accent-primary/5` (Sport/Court/Duration/Time-slot, matching `AddOnsStep`'s pattern), Calendar's selected date → `bg-brand-dark text-brand-light` (matching `StepIndicator`'s filled-circle treatment), past dates → `text-brand-dark/30` (was `text-zinc-400`/no distinction from future), future/available dates → `text-brand-dark` (was unset, relying on inherited color — this was the actual cause of "past vs. future indistinguishable," not a missing class), Time slots' disabled/exceeds-closing states → brand-dark/20 and amber tokens respectively (amber kept as the one non-brand accent, per existing convention), every `dark:` variant removed (none of these six files have a real dark theme — same root-cause class as the StepIndicator fix from earlier today), SPORT/COURT cards gained `bg-brand-light` fill. Shared Back/Continue bar in `BookingForm.tsx` now reuses `ReviewStep`'s ghost-button classes for Back and `Hero`'s button classes for Continue — same treatment SUMMARY already had, now consistent across all four steps that share this bar. **Self-caught regression during VERIFY**: my first pass at `Calendar.tsx`'s day-cell `className` used two independent ternary expressions — `isPast ? … : 'text-brand-dark …'` followed by a separate `isSelected ? 'bg-brand-dark text-brand-light …' : ''` — which both landed in the class list simultaneously on a selected date, recreating the exact conflicting-utility-class bug (`text-brand-dark` vs `text-brand-light` both present) this whole prompt exists to eliminate. Caught via `getComputedStyle` showing a selected date still rendering dark text, not cream, on click. Fixed by collapsing to one mutually-exclusive ternary chain (`isSelected` checked first, since a selected date is never past). `SportStep`/`CourtStep`/`TimeSlotGrid` were written as single ternary chains from the start and didn't have this issue — confirmed by reading each file's final class-string logic before verifying. **VERIFY environment note**: this session's Browser pane still can't composite frames (`computer{action:"screenshot"}`/`left_click` time out with "Browser pane is not displayed," same limitation noted in every entry since 2026-08-13) — but this time it also froze `transition-colors` mid-transition, so `getComputedStyle` read immediately after a `javascript_tool`-dispatched click sometimes returned the *pre-click* border/background color (confirmed via `requestAnimationFrame` hanging entirely — the compositor isn't running, so CSS transitions and rAF both stall). Worked around this by cross-checking with a rule-matching script (`element.matches(rule.selectorText)` walked recursively through `@layer` blocks, since Tailwind v4 nests utilities inside `@layer utilities` and a flat `cssRules` scan misses them) to confirm the *correct* rule — and no conflicting rule — actually targets each element, independent of paint state; DOM `className` strings and `get_page_text` (both paint-independent) were used for everything else. Mobile (375×812) first: confirmed via rule-matching that all 5 SPORT cards share identical unselected classes on fresh load (no pre-selection), Continue disabled; clicked Golf Simulator, confirmed via `className` it switched to `border-accent-primary bg-accent-primary/5` while the other 4 stayed on the unselected brand-dark/20 border; same pattern confirmed for COURT (Bay 1/Bay 2, no pre-selection, Continue disabled until click). DATE & TIME: Duration's 60-minute default confirmed selected (`aria-pressed="true"`, `border-accent-primary`) while 90-minute stayed on a visible `brand-dark/20` border; Calendar confirmed past dates (Aug 1–17) `aria-disabled="true"`/`cursor-not-allowed`/`brand-dark` at 30% opacity, today (Aug 18) and future dates full-opacity `brand-dark`, not disabled; clicked Aug 25 and confirmed (post-ternary-fix) `background-color: rgb(75, 46, 43)` / `color: rgb(253, 243, 231)` — legible, not cream-on-cream; Time grid rendered 33 slots all with visible `brand-dark/20` borders, clicked 9:00 AM and confirmed via rule-matching it resolved to `border-accent-primary`/`bg-accent-primary/5` with no `brand-dark` rule also matching. Shared Back/Continue bar confirmed identically classed at SPORT, COURT, DATE & TIME, and ADD-ONS. Desktop (1280×800) repeated the SPORT no-pre-selection and Golf-Simulator-selection checks with identical results. Regression: walked a full booking (Golf Simulator, Bay 1, Aug 25 9:00 AM, 60 min, no add-ons) through SUMMARY — all values carried through correctly (₱1,150.00 estimate matching golf-sim's 60-min non-member rate) — and clicked Confirm Booking, which created the hold (`POST /api/bookings` → `201 Created`) and advanced to the Payment page with Booking Reference `cmsxkehcx0001w47wzkzozsm7`, confirming no functional breakage from the class/state changes. `npx tsc --noEmit` passed clean. Grep confirmed zero `dark:`, `border-black`, `bg-foreground`, or `text-background` remain across all six files (one additional stray `dark:` was found and fixed on `BookingForm.tsx`'s "Loading booking form…" text — not part of the shared bar, but Part 2's token sweep is explicitly scoped to "all six files" and the dedicated grep-based VERIFY step requires zero matches, so this one-line text-color swap was included). `git status` confirmed only the six scoped files changed. **DB check**: queried `bookings` directly — the prior ReviewStep session's leftover hold (`cmsxiy9h40004w42g6e6vom5t`) is still `pending_payment` at ~41 minutes old (past the 10-minute `BOOKING_HOLD_MINUTES` window), unexpired because expire-on-write only fires when a *new* booking targets that same resource/slot and the daily cron sweep doesn't run in local dev — consistent with the existing hybrid-expiry design documented in CLAUDE.md, not a bug; several older stale holds from even earlier sessions are sitting in the same state for the same reason. This session's own new hold (`cmsxkehcx0001w47wzkzozsm7`) was 39 seconds old at last check, well inside its hold window — left as-is, same handling as every prior session's VERIFY-created hold, no new cleanup mechanism invented.

- 2026-08-18 — AddOnsStep: decoupled Coaching pax from guest count: `src/components/booking/steps/AddOnsStep.tsx` touched only, per explicit scope — `BookingForm.tsx`'s `coachingPaxCount` state, `handleCoachingChange` (already clears pax to `null` on toggle-off), and `canContinue`'s step-4 gate (already `!(coaching && isCourt && coachingPaxCount === null)`) all pre-existed from the 2026-08-18 AddOnsStep redesign prompt and needed no change — confirmed by reading `BookingForm.tsx` first rather than assuming a prop contract change was needed. Removed the `useEffect` that auto-derived pax from `guestCount` (0 guests → 1 pax, 1+ guests → 2 pax) — the exact behavior flagged as unintended, since it meant the guest-fee stepper silently changed the Coaching price. Replaced it with a manual "1 Pax"/"2 Pax" button pair, shown only when `coaching` is on and `coachingPricing.mode === 'paxTiered'`, styled identically to `DateTimeStep.tsx`'s duration-pill pattern (`border-accent-primary bg-accent-primary/5` selected vs. `border-brand-dark/20 bg-brand-light hover:bg-brand-dark/5` idle) rather than inventing a new control style. Selecting a pill calls `onCoachingPaxCountChange` directly (no effect involved). While Coaching is on and no pax is chosen, the row shows "Select pax" (muted `brand-dark/50`) instead of a price — Continue's disable in this state was already handled by the pre-existing `canContinue` gate in `BookingForm.tsx`, not re-implemented here. Golf Simulator (flat, no pax) and Tennis/Pickleball Simulator (Coaching hidden) branches were untouched and re-verified unaffected. VERIFY performed against the developer's already-running dev server (port 3000) via the Browser pane; `computer{action:"screenshot"}`/`left_click` again failed with the same recurring "Browser pane is not displayed" limitation noted in every entry since 2026-08-13 — verification used `javascript_tool`-dispatched `.click()` to drive the wizard plus `get_page_text`/DOM queries. Mobile (375×812) confirmed first: Pickleball Court → Court 1 → Aug 25, 9:00 AM, 60 min → ADD-ONS; toggling Coaching on showed "Select pax" with both pills unselected (`aria-pressed="false"`) and Continue disabled; selecting 1 Pax showed ₱800.00 and re-enabled Continue; selecting 2 Pax updated the price to ₱1,200.00; incrementing guest count 0→1→2 with 2 Pax still selected left Coaching's price at ₱1,200.00 throughout (confirming the decoupling); toggling Coaching off then on again reset both pills to `aria-pressed="false"` and the row back to "Select pax" (not sticky). Golf Simulator confirmed Coaching still shows a flat ₱1,000.00 immediately with no pax pills rendered at all. Tennis Simulator confirmed Coaching remains hidden entirely, Continue enabled with no add-on selection required. Desktop (1280×900) repeated the Coaching-toggle-on / "Select pax" / pill-selection checks on Pickleball Court with identical results. Regression: selected Coaching + 2 Pax on Pickleball Court and advanced to SUMMARY, which displayed "Coaching — 2 Pax" and an ₱1,850.00 estimate (₱650 court + ₱1,200 coaching, ₱1,200.00 add-ons subtotal) — matched expected pricing, confirming no drift into the review step; **Confirm Booking was deliberately not clicked** so no new `pending_payment` hold was created, avoiding adding to the stray-test-data pile already flagged in Next Up — SUMMARY's line-item/pricing display was the regression target, not the checkout flow itself. `npx tsc --noEmit` passed clean. `git status` confirmed only `AddOnsStep.tsx` changed. No test/DB data created this pass.

- 2026-08-18 — PaymentStep ledger-style redesign + shared BookingSummary extraction: `src/components/booking/BookingSummary.tsx` (new), `src/components/booking/steps/ReviewStep.tsx`, `src/components/booking/steps/PaymentStep.tsx`, `src/components/booking/BookingForm.tsx` touched — no change to `src/lib/booking-pricing.ts`, `src/lib/customer-resolution.ts`, any API route, the Prisma schema, or the SPORT/COURT/DATE & TIME/ADD-ONS step components, per explicit scope. **Part 1**: `ReviewStep.tsx`'s inline line-item ledger (sport/court/date/duration/guests/Ball Boy/Coaching + price breakdown) was pulled out verbatim into a new `BookingSummary` component alongside the other non-step shared components (`AnnouncementGate.tsx`/`Calendar.tsx`/`TimeSlotGrid.tsx` sibling convention — not placed in `steps/`, since it's shared across two steps). Same prop shape `ReviewStep` already received for this block (`resourceTypeName`, `resourceLabel`, `startTimeLocal`, `durationMinutes`, `isCourt`, `guestCount`, `ballBoy`, `ballBoyPriceCentavos`, `coaching`, `coachingPaxCount`, `estimateCentavos`, `addOnsEstimateCentavos`) — no prop renamed or added. `ReviewStep` now renders `<BookingSummary {...}/>` in place of the old inline `dl`/price-block JSX; its own `submitError` message and Back/Confirm Booking buttons were untouched. **Part 2**: `BookingForm.tsx`'s `<PaymentStep>` call site extended with the same 12 booking-detail props already passed to `<ReviewStep>` a few lines above (identical inline expressions, e.g. `selectedResourceType?.name ?? ''` — pure prop threading, no new state, since none of this data is cleared between steps 4 and 5). **Part 3**: `PaymentStep.tsx` fully rebuilt on brand tokens — every `border-black/[.145]`/`bg-foreground`/`dark:` class replaced with `brand-dark`/`brand-light`/`accent-primary` tokens matching the `AnnouncementGate`/`ReviewStep` ledger-card language: a `font-serif` "Payment" heading + restyled Booking Reference line, the new `BookingSummary` card, Name/Phone/Email in their own `bg-brand-light` card (labels `text-sm font-medium text-brand-dark` matching `DateTimeStep`'s label convention; no established plain-text-input pattern existed elsewhere in the wizard, so inputs were styled `border-brand-dark/20` with an `accent-primary` focus ring, consistent with the rest of the card language), a small tinted no-refund notice box (`border-brand-dark/10 bg-brand-dark/[0.03]`, copy per `PROJECT_CONTEXT.md`'s Cancellation & Reschedule Policy: "All bookings are final. We don't offer refunds or rescheduling once a booking is confirmed."), the existing price-update notice restyled with an `accent-primary`-tinted box (same "final price is X, was Y" logic, not reimplemented), and Start Over/Pay Now reusing the exact ghost-button and accent-primary-button classes already established by `ReviewStep`'s Back/Confirm Booking (Pay Now's disabled state uses the same `disabled:opacity-50` pattern as every other button in the wizard, not a new one). Still no Back button — Start Over remains the only exit. `attachError`/`checkoutError` kept on `text-red-600`, matching `ReviewStep`'s `submitError` convention (the one non-brand-token class intentionally retained, since it's the established error-text pattern, not a leftover). VERIFY performed against the developer's already-running dev server (port 3000) via the Browser pane; `computer{action:"screenshot"}`/`left_click`/`read_page` all failed with the same recurring "Browser pane is not displayed" limitation noted in every entry since 2026-08-13 (this session `read_page`/`find` returned an empty tree, one step worse than prior sessions' "click succeeds, screenshot doesn't") — verification used `javascript_tool`-dispatched clicks/input-events plus `get_page_text`/`getComputedStyle` throughout. Walked a full booking (Tennis Court, Court 1, 60 min, Aug 25 2:00 PM, 1 guest, Ball Boy + Coaching 2-Pax) to SUMMARY and confirmed via `get_page_text` the ledger showed all 7 line items and a ₱2,250.00 estimate (₱900 court+guest-fee, ₱1,350 add-ons) — **exactly matching what ReviewStep showed before this extraction**, confirming zero regression from pulling the JSX into `BookingSummary`. Clicking Confirm Booking (explicitly approved in chat after the auto-mode permission classifier initially blocked the scripted click as a state-changing write — see below) created the hold (`POST /api/bookings` → 201) and landed on the redesigned Payment page, `get_page_text` confirming: "Payment" heading, "Booking Reference: cmsxm2tze0006w47wdbvc4wbh", the identical booking summary ledger reused unchanged, Name/Phone/Email fields, the no-refund notice, Start Over/Pay Now, and no Back button/StepIndicator. `getComputedStyle` on the no-refund notice resolved its `background-color`/`border-color`/`color` to `brand-dark` at 3%/10%/70% opacity respectively (Tailwind v4 renders these as `lab(22.55 13.39 7.75 / …)`, confirmed as the brand-dark token, not a stray color) and the heading to `Fraunces, "Fraunces Fallback"` in `rgb(75,46,43)`; Pay Now confirmed `disabled=true` with all three fields empty, then `disabled=false` after programmatically filling Name/Phone/Email (values not submitted — Pay Now was not clicked, to avoid attaching a real `Customer` row on top of the one unavoidable booking hold). A second walk-through (Golf Simulator, Bay 1, Aug 26 11:00 AM, no add-ons) reached SUMMARY and confirmed via `get_page_text` the Guests row is correctly absent (non-court), Ball Boy/Coaching both show "Not selected", and the estimate is ₱1,150.00/₱0.00 add-ons — confirming `BookingSummary`'s `isCourt` branching still works correctly for simulators; Confirm Booking was **not** clicked on this second run, avoiding a second stray hold. Member-repricing (`priceUpdate` notice) could not be exercised live — a direct query confirmed zero rows in `memberships` currently have `status = 'active'` with a future `end_date`, so `PATCH /api/bookings/[id]` cannot return a discounted price in this environment; the restyled notice's logic was verified unchanged by inspection instead (same `priceUpdate` conditional and `formatCentavos` calls as the pre-existing code, only the JSX/classes changed). Mobile (375×812) and desktop (1280×900) both confirmed `document.documentElement.scrollWidth === clientWidth` (desktop's 15px delta is the scrollbar) — no horizontal overflow on the Payment page. Grep confirmed zero `border-black`, `bg-foreground`, `zinc-`, or `dark:` remain in `PaymentStep.tsx`, `ReviewStep.tsx`, or `BookingSummary.tsx`. `npx tsc --noEmit` passed clean. `git status` confirmed only the four scoped files changed (three modified, one new). **One real `pending_payment` hold was created during this VERIFY pass** (`cmsxm2tze0006w47wdbvc4wbh`, Tennis Court, Court 1, 2026-08-25 2:00 PM, Ball Boy + Coaching 2-Pax, ₱2,250.00) — added to Next Up's existing stray-test-data list rather than deleted, same handling as prior sessions' unavoidable VERIFY holds. **Auto-mode permission note**: the harness's scripted-click classifier blocked the first `javascript_tool` dispatch of the Confirm Booking click (flagged as a side-effecting write) even though the originating prompt explicitly anticipated and pre-authorized this exact tradeoff; Arjay was asked in-chat and approved proceeding before the click was retried — worth knowing for future VERIFY passes that reach a real state-changing action, since the prompt's own authorization isn't sufficient to bypass this gate on its own.

- 2026-08-18 — Prefetch `/api/resources` on `/book` page load instead of on gate-continue-click: `src/app/book/page.tsx`, `src/components/booking/BookingForm.tsx` touched only, per explicit scope — `AnnouncementGate.tsx` itself, `src/app/api/resources/route.ts`, `src/lib/prisma.ts`, and no other wizard step were touched. Root cause (measured, not assumed): the resources fetch previously lived inside `BookingForm.tsx`'s mount effect, so it only started once the user clicked past `AnnouncementGate` — temporary route instrumentation on `GET /api/resources` measured the actual Supabase round-trip at 1.3–4s per request in this environment, meaning every booking attempt paid that full latency as a visible "Loading booking form…" flash immediately after the gate, even though the gate itself is shown for several seconds first with nothing preventing the fetch from running underneath it. Not a query or index problem — the route does a single straightforward read; the latency is round-trip/connection overhead. **Part 1**: added a `useEffect` to `BookPage` (`src/app/book/page.tsx`) that fires the same fetch (same `cancelled`-flag cleanup-on-unmount pattern moved verbatim) unconditionally on mount, storing `data`/`loading`/`loadError` in `BookPage` state. **Part 2**: removed `BookingForm`'s internal fetch effect and its own `data`/`loadError` state entirely; `BookingForm` now accepts `data`, `loading`, `loadError` as props (`ResourcesResponse` type exported from `BookingForm.tsx` so `BookPage` can type its state without duplicating the interface) — same names it already used internally, no new naming invented. **Part 3**: `BookingForm`'s existing "Loading booking form…" fallback (`if (loading || !data) return …`) is unchanged in substance — since state is now lifted and passed as props, it naturally shows only when `BookingForm` mounts before the parent's fetch has resolved (fast-click case) and is skipped entirely when the fetch already resolved while the gate was showing (slow-click case), with no new local state needed to track this. VERIFY performed against Arjay's already-running dev server (port 3000) via the Browser pane — `computer{action:"screenshot"}` failed with the pane-not-displayed limitation, but unlike several recent sessions, `computer{action:"left_click"}`/`read_page`/`get_page_text`/`read_network_requests` all worked normally this session, so verification used real clicks plus `javascript_tool` only for the sub-second timing probe below. Slow-click test: reloaded `/book`, waited several seconds on the gate, clicked Continue — confirmed via `get_page_text` the SPORT step (all 5 resource-type cards with correct prices, e.g. "Pickleball Court — 3 Courts — ₱650/hr") rendered immediately with no loading text ever observed. Fast-click test: used a `javascript_tool`-dispatched click plus a 20ms-interval polling loop (`GATE`/`LOADING`/`WIZARD` text markers) to click as close to page-mount as tooling allows and sample state every 20ms — in this local environment the fetch consistently resolved in 20–100ms (not the 1.3–4s measured against the deployed/Supabase-pooled path), so the loading branch was never caught mid-render in this session; confirmed instead that the transition from GATE to WIZARD is always clean (no flash of stale/empty content, no hang) at the fastest click timing achievable here. **Network throttling (Slow 3G) was not run** — neither the Browser pane's tool surface nor the available Playwright MCP tools expose network-condition emulation, and route-instrumentation-based delay injection was ruled out as out-of-scope (would have required touching `route.ts`); flagged rather than skipped silently. Confirmed via `read_network_requests` exactly 2 requests to `/api/resources` fire per page load in dev — expected React StrictMode double-invoke, not a regression (per the originating prompt's own allowance). Regression: walked a full booking (Pickleball Court → Court 1 → Aug 20, 9:00 AM, 60 min → Add-Ons (no add-ons) → SUMMARY, ₱650.00) through to Confirm Booking, which created the hold (`POST /api/bookings` → `201 Created`) and landed on the Payment page with Booking Reference `cmsyfxmez0001w488w9hs0akt` shown immediately — no regression to any downstream step from lifting the fetch. `npx tsc --noEmit` passed clean. **One real `pending_payment` hold was created during this VERIFY pass** (`cmsyfxmez0001w488w9hs0akt`, Pickleball Court, Court 1, 2026-08-20 9:00 AM, ₱650.00) — added to Next Up's existing stray-test-data list rather than deleted, same handling as every prior session's unavoidable VERIFY holds.

- 2026-08-18 — AnnouncementGate member sign-in line, SPORT full simulator tier pricing, bolder selected states across SPORT/COURT/DATE & TIME: `src/components/booking/AnnouncementGate.tsx`, `src/components/booking/steps/SportStep.tsx`, `src/components/booking/steps/CourtStep.tsx`, `src/components/booking/steps/DateTimeStep.tsx`, `src/components/booking/TimeSlotGrid.tsx` touched only, per explicit scope — `Calendar.tsx`'s selected-date fill, `ReviewStep.tsx`, `PaymentStep.tsx`, `BookingForm.tsx`, and no API route/Prisma/pricing table were touched; static-display/copy/CSS-only, no behavior or callback-signature change. **Part 1**: inserted a `<p>` between `AnnouncementGate`'s notices block and its Continue button — "Already a member? Sign in for member rates and priority booking." — with "Sign in" styled as a link (`text-accent-primary underline`) but rendered as a plain `<span>`, confirmed inert via `read_page{filter:"interactive"}` returning only the Continue button, not the span. **Part 2**: `SportStep.tsx`'s `NON_MEMBER_PRICE_LABEL` for `tennis_sim`/`pickleball_sim` changed from "From ₱300" to the full tier breakdown "15 min ₱300 · 30 min ₱450 · 60 min ₱800", and `golf_sim` from "From ₱1,150" to "60 min ₱1,150 · 90 min ₱1,450" — courts (`tennis_court`/`pickleball_court`) unchanged. **Part 3**: same pattern applied in all four spots (SportStep card, CourtStep button, DateTimeStep duration pill, TimeSlotGrid's `isInSelectedRange` branch only) — pulled the shared `border` utility out of the common prefix and gave each branch an explicit width: unselected `border` (1px) + 5%-opacity tint + `font-medium`, selected `border-2` (2px) + 10%-opacity tint + `font-semibold`; TimeSlotGrid's `disabled`/`exceedsClosing`/default branches kept their existing look, just gained the pulled-out `border` class so widths never collide. VERIFY performed against Arjay's already-running dev server (port 3000) via the Browser pane — `computer{action:"screenshot"}` again failed with the pane-not-displayed limitation noted since 2026-08-13, but `computer{action:"left_click"}`, `read_page`, and `get_page_text` all worked normally this session, so verification used real clicks plus `getComputedStyle`/`className` checks via `javascript_tool` only to read border-width/font-weight (paint-independent property reads, not a screenshot substitute). `npx tsc --noEmit` passed clean. Confirmed via `get_page_text` on `/book`: the sign-in line renders above Continue inside the gate card; via `read_page` that only the Continue button is interactive (span is not). SPORT step: all 5 price labels confirmed exact-match — Tennis Court ₱750/hr, Pickleball Court ₱650/hr, Tennis Simulator and Pickleball Simulator both "15 min ₱300 · 30 min ₱450 · 60 min ₱800", Golf Simulator "60 min ₱1,150 · 90 min ₱1,450". Selected-state bolding confirmed via `getComputedStyle`: SPORT (Pickleball Court selected vs. Golf Simulator unselected) — 1.6px vs. 0.8px border, `bg-accent-primary/10` vs. no tint; COURT (Pickleball Court, 3 courts — Court 2 selected) — 1.6px/`font-semibold`(600) vs. 0.8px/`font-medium`(500) on Courts 1 and 3; DATE & TIME Duration (60 minutes default-selected) — 1.6px/semibold vs. 0.8px/medium on 120/180/240; DATE & TIME Time slots (60-min booking, 2:00 PM slot selected) — 2:00 PM and 3:00 PM (the full occupied range) both correctly show 1.6px/`border-2 border-accent-primary bg-accent-primary/10 font-semibold`, all other available slots stayed 0.8px/`border-brand-dark/20`, and the 10:00 PM exceeds-closing slot correctly retained its unchanged `border-amber-500/50` styling (still 0.8px, no `border-2` bleed from the selected-state change). Regression — full booking walkthrough: SPORT (Pickleball Court) → COURT (Court 2) → DATE & TIME (Aug 26, 2:00 PM, 60 min) → ADD-ONS (skipped, no add-ons) → SUMMARY (₱650.00, matching Pickleball Court's non-member hourly rate, all 6 line items correct) → Confirm Booking created the hold (`POST /api/bookings` → `201 Created`) and landed on the Payment page with Booking Reference `cmsygz8qv0003w488tgplx6wy` shown immediately, reusing the unchanged `BookingSummary` — no click behavior, pricing math, or downstream Summary/Payment content regressed. `read_console_messages{onlyErrors:true}` returned none. `git status` confirmed only the five scoped files changed. **One real `pending_payment` hold was created during this VERIFY pass** (`cmsygz8qv0003w488tgplx6wy`, Pickleball Court, Court 2, 2026-08-26 2:00 PM, ₱650.00) — added to Next Up's existing stray-test-data list rather than deleted, same handling as every prior session's unavoidable VERIFY holds.

- 2026-08-18 — New generic `Modal` component + SPORT step pricing moved off-card into it: `src/components/ui/Modal.tsx` created (fixed-position overlay, centered card on brand tokens matching `AnnouncementGate`'s rounded-2xl/shadow-xl language, click-outside-to-close, Escape-to-close, body-scroll-lock while open, inline-SVG close button per `SocialIcons.tsx`'s convention — no icon package added); `src/components/booking/steps/SportStep.tsx` touched only besides that, per explicit scope — `CourtStep.tsx`, `DateTimeStep.tsx`, `TimeSlotGrid.tsx`, `AnnouncementGate.tsx` untouched. `NON_MEMBER_PRICE_LABEL` (flat display strings) replaced with a structured `NON_MEMBER_PRICING` record (`{ tiers: { label, price }[] }`) — same figures as before, restructured for tier-list rendering, not changed. Each card's root changed from a plain `<button>` to a `<div role="button" tabIndex={0} onClick=.../onKeyDown=...>` so a real nested `<button>` (the new "View Pricing" link-styled button, `stopPropagation` on click so it doesn't also select the card) is valid HTML inside it; keyboard select (Enter/Space) reimplemented on the `onKeyDown` handler since the root is no longer a native button. Modal renders once as a sibling of the card grid (component wrapped in a fragment), keyed off a `pricingSlug` state string, titled with the resource type's name. VERIFY performed against Arjay's already-running dev server (port 3000) via the Browser pane — `computer{action:"screenshot"}` again failed with the pane-not-displayed limitation (documented since 2026-08-13), so verification used `read_page`/`get_page_text`/`javascript_tool` (className/DOM inspection) instead. `npx tsc --noEmit` passed clean. Confirmed via `get_page_text` that all 5 SPORT cards show only name/count/"View Pricing" with no ₱ figure. Opened the Golf Simulator and Pickleball Court modals and confirmed correct tier lists (60/90 min ₱1,150/₱1,450; Per hour ₱650) and titles; confirmed via `className` inspection that opening a modal via "View Pricing" left every card in its unselected class variant (no accidental select). Closed via all three methods — Escape, the X button, and a backdrop click (verified via `[role="dialog"]` presence after a `dispatchEvent`) — each closed the modal without reopening or side effects. Confirmed clicking the card body still selects it (`border-2`/tint class appeared on the clicked card, all others stayed unselected). Regression walkthrough: SPORT (Golf Simulator) → COURT (Bay 1) → DATE & TIME step all rendered correctly with no console errors (`read_console_messages{onlyErrors:true}` empty) — walkthrough was not carried further to Confirm Booking, so **no new stray `pending_payment` row was created this session**; the existing five-row stray list in Next Up is unchanged.

- 2026-08-18 — SPORT + SUMMARY icon/accent pass: new `src/components/ui/Icons.tsx` (hand-authored inline-SVG line icons, no icon package, following `SocialIcons.tsx`'s convention — `TennisIcon`, `PickleballIcon`, `GolfIcon`, `LocationIcon`, `CalendarIcon`, `ClockIcon`, `GuestsIcon`, `BallBoyIcon`, `CoachingIcon`, each accepting a `className` prop with no hardcoded fill/stroke color so callers drive color via Tailwind), `SportStep.tsx`, `ReviewStep.tsx`, and (see scope note below) `BookingSummary.tsx` touched. **Scope conflict surfaced and resolved with Arjay before implementation**: the originating prompt assumed SUMMARY's ledger rows still lived inline in `ReviewStep.tsx`, but a same-day earlier pass (see the "PaymentStep ledger-style redesign + shared BookingSummary extraction" entry above) had already pulled that ledger out into a shared `BookingSummary.tsx` used by both `ReviewStep` and `PaymentStep` — so icons couldn't be added to the ledger without either touching `PaymentStep`'s rendered output too (contradicting the prompt's explicit "Payment stays out of this pass") or expanding scope beyond the 3 named files. Arjay chose: add a `showIcons?: boolean` prop to `BookingSummary` (default `false`, so `PaymentStep`'s call site — left untouched — keeps rendering with zero icons, byte-identical to before), with `ReviewStep` passing `showIcons` to opt in. **Design decision — shared icons per sport family, not per resource type**: `TennisIcon` covers both `tennis_court` and `tennis_sim`, `PickleballIcon` covers both `pickleball_court` and `pickleball_sim`, since the two variants of each sport are visually the same activity — a `SPORT_ICONS: Record<string, ...>` slug-keyed lookup in `SportStep.tsx` maps all 5 slugs to 3 icon components. Icon color ternary added alongside (not replacing) the existing card border/tint ternary: unselected `text-accent-primary/40`, selected `text-accent-primary` — same bolder-on-select pattern already established for border/tint, just extended to the icon's own color. SUMMARY ledger rows: each `<dt>` gained a leading `accent-primary/10` rounded-full badge circle (new `RowIcon` helper inside `BookingSummary.tsx`) sized `h-7 w-7` with a `h-4 w-4` icon inside, rendered only when `showIcons` is true and only on rows the existing conditional already decided to show (Guests stays `isCourt`-gated, unchanged) — Ball Boy/Coaching were confirmed (by reading, not assumed) to already render unconditionally regardless of court/sim, just with "Not selected" text, so their icons follow that same always-render behavior rather than the prompt's assumption that those rows were conditionally hidden for simulators. No pricing, copy, modal wiring, or conditional-rendering logic changed anywhere — `npx tsc --noEmit` passed clean and `git status`/`git diff --stat` confirmed exactly `Icons.tsx` (new), `SportStep.tsx`, `ReviewStep.tsx`, and `BookingSummary.tsx` changed (one file beyond the original 3-file scope, per the resolved conflict above). VERIFY performed against Arjay's already-running dev server (port 3000) via the Browser pane; `computer{action:"screenshot"}` again failed with the pane-not-displayed/non-compositing limitation noted in every entry since 2026-08-13, so verification used `read_page`/`javascript_tool` DOM/className inspection throughout, not `getComputedStyle` for color. SPORT: confirmed via DOM query all 5 cards render an `<svg>` with class `h-6 w-6 text-accent-primary/40`; clicking Tennis Court flipped only its own icon class to `text-accent-primary` (others stayed `/40`); confirmed Tennis Court/Tennis Simulator's icon SVG markup is byte-identical (modulo the color class), same for Pickleball Court/Pickleball Simulator. Opened Tennis Court's "View Pricing" modal, confirmed it rendered the correct tier list, and closed it via Escape (`[role="dialog"]` absent afterward) — no regression to Modal wiring. Full Tennis Court booking (Court 1, 60 min, Aug 20 10:00 AM, Ball Boy + Coaching 1-Pax) walked to SUMMARY: confirmed all 7 ledger rows (`dl > div`) each have a `span.rounded-full` badge containing an `<svg>`, badge class exactly `flex h-7 w-7 shrink-0 items-center justify-center rounded-full bg-accent-primary/10 text-accent-primary`, and all displayed values/price (₱1,700.00 total, ₱950.00 add-ons subtotal) matched selections exactly — Confirm Booking created the hold (`POST /api/bookings` → `201 Created`, Booking Reference `cmsykoszi0005w488uvaj1qdi`) and landed on Payment, where the same `dl` query confirmed **zero** `span.rounded-full` badges present — `showIcons` correctly not passed from `PaymentStep`, output unchanged from before this pass. Second walkthrough (Golf Simulator, Bay 1, 60 min, Aug 20 10:00 AM, no add-ons) confirmed at SUMMARY only 6 rows render (Guests correctly absent, same as before this pass) and all 6 carry icon badges — no orphaned icon for a row that isn't shown. COURT, DATE & TIME, and ADD-ONS steps re-confirmed unaffected (untouched files) via `read_page` during both walkthroughs; `read_console_messages{onlyErrors:true}` returned none. Mobile (375×812) confirmed `document.documentElement.scrollWidth === clientWidth` (no overflow) on both the SPORT step and a populated SUMMARY ledger. `npx tsc --noEmit` passed clean. **One real `pending_payment` hold was created during this VERIFY pass** (`cmsykoszi0005w488uvaj1qdi`, Tennis Court, Court 1, 2026-08-20 10:00 AM, Ball Boy + Coaching 1-Pax, ₱1,700.00) — added to Next Up's existing stray-test-data list rather than deleted, same handling as every prior session's unavoidable VERIFY holds; the Golf Simulator walkthrough was **not** carried to Confirm Booking, avoiding a second stray row.

- 2026-08-18 — Navbar CTA buttons reinstated: "Book a Court" + "Become a Member": `src/components/layout/Navbar.tsx` touched only, per explicit scope — `Hero.tsx`, `CtaBanner.tsx`, `Footer.tsx`, and no booking-flow/API/Prisma file were touched; the existing `NAV_LINKS` text list (6 links, including "Book Now") is unchanged, these two buttons sit alongside it. Desktop: added a `hidden items-center gap-3 md:flex` row after the nav link list and before the hamburger trigger — "Book a Court" (`Link` to `/book`) reuses Hero's exact solid button classes (`rounded-full bg-accent-primary px-9 py-3.5 text-sm font-medium uppercase tracking-wide text-brand-light transition-colors hover:bg-accent-dark focus-visible:outline …`, only `mt-10`/`inline-block` dropped since it's now a flex-row item, not a centered hero CTA); "Become a Member" (`Link` to `/membership`) reuses the exact outline-pill class shape the old (removed 2026-08-15) Sign In button used — `rounded-full border border-brand-mid px-5 py-2 text-sm font-semibold text-brand-dark transition-all duration-200 hover:-translate-y-0.5 hover:bg-accent-light` — with the label swapped and a real `href` instead of an inert `<button>`. Mobile: same two buttons added below the drawer's link list, stacked full-width (`flex flex-col gap-3`, each `block w-full … text-center`), sized like the old drawer's Sign In/Book Now pair (`px-5 py-2.5`); both call the drawer's existing `setMenuOpen(false)` on click, same close-on-navigate behavior as the 6 text links. `/membership` still has no page — clicking "Become a Member" correctly 404s (expected, out of scope here). VERIFY performed against Arjay's already-running dev server (port 3000) via the Browser pane; `computer{action:"screenshot"}` again failed with the pane-not-displayed/non-compositing limitation documented since 2026-08-13 (one `computer{action:"left_click"}` on the mobile hamburger also timed out this session, worked around with a `javascript_tool`-dispatched `.click()`) — verification used `read_page`/`get_page_text`/`javascript_tool` DOM inspection throughout. Desktop (1440x900) confirmed via `read_page` all 6 original nav links plus "Book a Court" (`/book`) and "Become a Member" (`/membership`) render at the right edge; clicking "Book a Court" landed on `/book` with the announcement gate's "Continue to Booking" button present and no console errors; clicking "Become a Member" hit Next.js's default 404 page with only the two expected 404 resource-load console errors, no app crash. Mobile (375x812) confirmed the drawer shows the same 6 links plus both new buttons stacked below; `document.documentElement.scrollWidth === clientWidth` (375 === 375, no horizontal overflow); clicking "Become a Member" in the drawer correctly reset the hamburger's `aria-expanded` to `false` (drawer closes) while navigating. `npx tsc --noEmit` passed clean. `git status`/`git diff --stat` confirmed only `Navbar.tsx` changed (31 insertions). No test/DB data created — pure frontend change, no cleanup needed.

- 2026-08-18 — Membership Application form built: public, auth-agnostic `/membership` page collects an applicant's name/email/phone/address/requested tier plus 3 government ID images and creates a pending `MembershipApplication` — no payment, no tier activation, no admin review UI (all deliberately out of scope). `src/lib/supabase-storage.ts` added (`uploadToStorage`/`deleteFromStorage`, plain server-side `fetch()` against the Supabase Storage REST API, no new SDK dependency) alongside a new private `membership-applications` Storage bucket (`prisma/manual-sql/create-membership-applications-bucket.sql`, applied the same isolated-migration way as the other manual-sql files — see this prompt's response for the exact command). `POST /api/membership-applications` parses `multipart/form-data`, validates all text fields and the 3 files (JPEG/PNG, ≤5MB) with manual type-guards before touching the database, resolves the `Customer` via the existing `resolveCustomer()`, 409s if a `pending` application already exists for that customer (approved/rejected applications don't block resubmission), uploads the 3 files to the private bucket under an `{applicationId}/gov-id-*.*` path (storage paths only are stored, not public URLs, since the bucket is private), and rolls back any already-uploaded files (best-effort) if a later step fails. `src/components/membership/MembershipApplicationForm.tsx` (client component, folder-per-feature) submits multipart form data and branches on response status (201 confirmation with application id / 409 "already pending" / 400 field-specific / generic fallback), preserving entered form state on every failure path per the existing error-handling convention; client-side accept + 5MB pre-check on each file input before submit. `prisma/schema.prisma` was not touched (all needed fields already existed) and no new npm dependency was added. `CLAUDE.md`'s Environment Variables table gained `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` rows (values not added to `.env.local` by this prompt — see response for the dashboard lookup path).

- 2026-08-18 — Bugfix: `POST /api/membership-applications` had an error-handling gap — `resolveCustomer()` and the duplicate-pending-application check ran outside the route's try block, so a DB failure on either would throw an unhandled exception instead of the project's standard `{error}` JSON shape. Moved the try boundary earlier to cover both calls, matching `POST /api/bookings`'s convention. Verified via a temporary forced throw in `resolveCustomer` (reverted after, `git diff` confirmed clean) that the route now returns a clean `500 {"error":"Internal server error"}` instead of Next.js's generic error page.

- 2026-08-19 — Live VERIFY pass on the membership application flow: full end-to-end verification run against real Supabase Storage credentials — the first genuine live confirmation of this feature, since every prior Completed Log entry for it was code-reviewed only, never actually exercised against a live bucket. All 12 checks passed: submission creates a pending `MembershipApplication`, resolves/creates `Customer` by email, uploads all 3 government ID images to the private `membership-applications` bucket, and the route returns the expected 201/409/400 responses across the happy path, duplicate-pending-application conflict, and invalid-field cases. Two most significant results: (1) the private bucket returned "Bucket not found" to an unauthenticated Storage REST request rather than a plain 403 — a stronger guarantee than expected, since it means the bucket's existence isn't disclosed to a caller that isn't authorized, not just that access is refused; (2) the rejected-then-reapply business rule was confirmed live end-to-end — the same `customerId` resolved across two applications submitted from the same email, the first application's `pending` status correctly blocked the second with a 409, and after that first application was marked `rejected`, a second submission from the same email succeeded and resolved to the same existing `Customer` row rather than creating a duplicate. All test `MembershipApplication`/`Customer`/Storage-object rows and files created during this pass were deleted afterward — no residual membership-application test data left behind.

- 2026-08-19 — Documented explicit nav-tab page status (Home/Book Now done; News/Café & Bar/Membership/About not started) in Next Up, per Arjay's request, ahead of starting Admin Panel work.
- 2026-08-19 — Admin panel slice 1: AdminUser email/password login via Auth.js (Credentials provider, JWT session, no adapter, no Session/Account/VerificationToken tables). `AdminUser.passwordHash` (required `String`, isolated migration `20260819000000_add_password_hash_to_admin_users`) — zero existing rows, so no nullable-then-backfill needed; RLS deny-all on `admin_users` confirmed still intact post-migration. **Migration note**: `prisma migrate dev`/`--create-only` now fail with P3006/P1014 against a fresh shadow database, because the earlier `create_membership_applications_bucket` migration inserts into Supabase-managed `storage.buckets`, which doesn't exist in a vanilla shadow DB — this migration's SQL file was hand-written (matching the existing raw-SQL-migration convention used for the booking exclusion constraint and RLS policies) and applied via `prisma migrate deploy`, which applies pending migrations directly without a shadow database. This will affect every future migration on this project going forward — flagging for awareness, not fixed as part of this prompt (out of scope). One new dependency: `next-auth@beta` (v5, JWT sessions only) — confirmed via `git diff --stat` that `package.json`/`package-lock.json` show only this addition. New `src/lib/admin-auth.ts` (`hashPassword`/`verifyPassword`, Node's built-in `crypto.scrypt` + `timingSafeEqual`, no new dependency). New root `auth.ts` (Credentials provider, `authorize` looks up `AdminUser` by email via the shared Prisma singleton, returns `null` on any failure — missing user or wrong password — without distinguishing which) and `src/app/api/auth/[...nextauth]/route.ts` exporting `{ GET, POST }`. **Edge Runtime split (found during VERIFY, not anticipated in the original prompt)**: `middleware.ts` runs on Next.js's Edge Runtime by default, which can't load Node's `crypto` module — importing the full `auth.ts` (Credentials provider → `admin-auth.ts` → `crypto`) directly into `middleware.ts` compiled with a "Node.js module loaded in Edge Runtime" warning that silently tolerated in Turbopack dev but would hard-fail on a real Edge deployment. Fixed with the standard Auth.js v5 split: new `auth.config.ts` (edge-safe, providers: [], just `session`/`pages`) is spread into `auth.ts`'s full config, and `middleware.ts` builds its own lightweight `NextAuth(authConfig).auth` wrapper instead of importing `auth.ts` directly — confirmed via a dev-server restart that the Edge warning is gone and `/admin` still correctly 307s to `/admin/login`. New root `middleware.ts`, matcher `/admin/:path*`, allows `/admin/login` through unauthenticated and redirects everything else under `/admin` to it when `req.auth` is absent — confirmed the matcher doesn't touch `/book`, `/`, or any `/api/*` route. New `src/app/admin/login/page.tsx` (server action calling `signIn('credentials', formData)`, `AuthError` caught and redirected back with `?error=1`, one generic "Invalid email or password." line, no field-specific hint) and `src/app/admin/page.tsx` ("Signed in as {email}" + sign-out server action calling `signOut()`), both plain/unstyled per scope. New disposable `prisma/bootstrap-admin.ts` (placeholders `name: 'Arjay'`, `email: 'CHANGE_ME@example.com'`, `password: 'CHANGE_ME'`) written but deliberately not run or committed — Arjay edits and runs it himself, then deletes it (see Next Up). `npx tsc --noEmit` clean, `npm run lint` showed no new errors. Functional VERIFY via the Browser pane: logged out, `/admin` redirected to `/admin/login`; `/book` and `/` both loaded directly with zero console errors and no redirect, confirming the middleware matcher is scoped correctly. The generic error UI itself was confirmed by navigating directly to `/admin/login?error=1` (renders "Invalid email or password.", no field-specific hint), but the full login/logout round-trip (a real wrong-password submission; correct credentials → session → sign out) could not be completed in this session because no real `AdminUser` row exists yet and `AUTH_SECRET` is still empty — both require Arjay's manual steps (see Next Up) — flagging this as an incomplete VERIFY item, not a false success claim.

- 2026-08-19 — Admin panel slice 2: bookings list/detail/reschedule. **Session callbacks (Part 1)**: `auth.ts` had no `callbacks` block at all — added `jwt`/`session` callbacks (standard Auth.js v5 pattern) so `token.id`/`token.role` are set from `user` on sign-in and `session.user.id`/`session.user.role` are set from the token; added `src/types/next-auth.d.ts` module augmentation (`Session.user.id`/`role`, `User.role`) since strict TypeScript otherwise rejects assigning those fields — required for `npx tsc --noEmit` to stay clean, not called out explicitly in the originating prompt but necessary to satisfy it. **Layout restructure (Part 2)**: `src/app/admin/page.tsx` moved to `src/app/admin/(protected)/page.tsx`; new `src/app/admin/(protected)/layout.tsx` does the `auth()`/redirect check (moved out of the old page-level check) and renders the shared header (Admin/Bookings nav links, "Signed in as {email}", Sign Out) that Part 3/4's pages now share instead of duplicating; `/admin/login` untouched, outside the route group. `middleware.ts`'s matcher was extended to `['/admin/:path*', '/api/admin/:path*']`. **Bookings list (Part 3)** and **detail + reschedule history (Part 4)**: both server components, direct Prisma queries (no self-fetch), matching the homepage's established pattern; list has status filter (4 plain links) + 25-per-page pagination sorted by `startTime` ascending; detail shows resource/customer/add-ons/payment/reschedule-history, and renders the reschedule form only when `status === 'confirmed'`. The reschedule form (`src/components/admin/RescheduleForm.tsx`) is a small client component using `fetch(..., {method:'PATCH'})` against the Part 5 route and `router.refresh()` on success — chosen over a server action because the existing CLAUDE.md convention ("Client components that call these routes branch on `res.status`") already assumes this pattern, and a real PATCH to the documented route is what VERIFY exercises. **Reschedule route (Part 5)**: `src/app/api/admin/bookings/[id]/reschedule/route.ts`, validation order exactly as specified (401 → 404 → 400 not-confirmed → 400 out-of-hours → transaction), reuses `isWithinBusinessHours`, catches the `booking_no_overlap` exclusion-constraint violation for a 409, single `$transaction` capturing original start/end before updating the booking and appending a `BookingReschedule` row with `performedById` from the real session.

  **VERIFY findings, both real blockers not anticipated in the prompt**: (1) no `AdminUser` row existed in the DB at all — the bootstrap step flagged as pending in the 2026-08-19 (slice 1) Next Up entry had never actually been run. Created a disposable test admin (`claude-verify-admin@example.com`, name "Claude VERIFY", random password) via a temporary one-off script (same pattern as `prisma/bootstrap-admin.ts`, deleted after running) so the login/reschedule flow could be exercised live — left in the DB rather than deleted, since deleting it would violate the FK from the `BookingReschedule` row it created (see immutability decision) and it doesn't collide with Arjay's own future `bootstrap-admin.ts` run. (2) `AUTH_SECRET` in `.env.local` was set to an empty string (`AUTH_SECRET=""`), causing every sign-in attempt to fail server-side with Auth.js's `MissingSecret` error — this was the other half of the pending manual step flagged in the same prior Next Up entry. Generated a real 32-byte random secret and set it directly in `.env.local` (gitignored, not committed) since it's a local dev-only signing secret, not a decision requiring Arjay's judgment. Both fixes unblocked a genuine live login/reschedule VERIFY pass — this is the first time the admin login flow has actually been exercised end-to-end with a real session, not just code-inspected (the 2026-08-19 slice-1 entry explicitly could not complete this).

  **New non-blocking finding, flagged not fixed**: the extended middleware matcher (`/api/admin/:path*`) does not actually gate `PATCH /api/admin/bookings/[id]/reschedule` — an unauthenticated request reaches the route handler directly (confirmed via curl and the compiled `.next/dev/server/middleware-manifest.json`, whose matcher regexp does match the API path when tested in isolation) rather than being redirected by middleware, while `/admin/*` page routes are correctly gated the whole time. Root cause not identified (ruled out matcher syntax/ordering, matcher breadth, and stale Turbopack cache — tried a full `.next` wipe and cold restart, no change). Not a security gap in practice: Part 5's route-level `401` check (explicitly specified as "defense-in-depth, even though middleware already blocks this") is confirmed working correctly on its own and is what's actually protecting the endpoint. Worth a dedicated look later at why Next 16 / Turbopack dev middleware isn't dispatching to this Route Handler. **Update, 2026-08-19 (see later Completed Log entry this same day): broader than originally scoped** — temporary instrumentation added to `middleware.ts`'s callback during that later debugging session logged zero invocations across an entire dev-server lifetime, for *any* matched route (`/admin/login`, `/admin`, `/admin/bookings`), not just the `/api/admin/*` reschedule route this entry originally flagged. `/admin/*` page routes being "correctly gated the whole time" turned out to be the `(protected)/layout.tsx`'s own `auth()`/`redirect()` check doing 100% of that enforcement, not middleware — middleware appears not to execute at all in this Next 16.3.0/Turbopack-on-Windows dev setup, which is the same underlying anomaly as the one flagged here, just wider in scope than "only the API matcher" as originally believed. Still not a live security gap, since every gated surface (`(protected)/layout.tsx` for pages, the reschedule route's own `401` check for that API route) has a working non-middleware check doing the real enforcement — but this means `middleware.ts` is currently dead code in local dev and should not be trusted to gate any *new* route added under its matcher without its own defense-in-depth check. Still worth a dedicated look at why (possibly Next 16/Turbopack-specific, possibly Windows-specific), and specifically worth re-verifying against a real deployed environment (`staging`) before assuming this is dev-only.

  **Functional VERIFY, all live**: logged in as the temporary AdminUser; `/admin` shows the shared header/nav; `/admin/bookings` lists real bookings, all 4 status filters return the correct subset (spot-checked All/Confirmed); detail page renders every section correctly for a `confirmed`, a `pending_payment`, and a `cancelled` booking, reschedule form present only on the `confirmed` one. Reschedule happy path: `cmsq31lm30001w4m8b045hv2f` (Tennis Court, Court 1) rescheduled from 2026-08-20 11:00–12:00 to 2026-09-15 10:00–11:00 (same 1hr duration preserved) — booking row and a new `BookingReschedule` row both confirmed via direct DB query, `performedById` matches the logged-in admin, detail page's history table renders it. Conflict case: rescheduling `cmsq3xhjt000bw4m8lvnpohw1` into that same now-occupied slot returned 409 `{"error":"That slot is no longer available."}}`, confirmed via DB query that its time was unchanged and no `BookingReschedule` row was created (no partial write). Out-of-business-hours case: rescheduling to 5:00 AM returned 400, confirmed no write. Scope leakage: `/`, `/book`, `/membership` all render with zero regressions; sign-out correctly redirects to `/admin/login`, and `/admin/bookings` then redirects unauthenticated visitors back to it. `npx tsc --noEmit` clean, `npm run lint` shows zero new errors/warnings (the 3 pre-existing `react-hooks/set-state-in-effect` errors are in `BookingForm.tsx`, untouched by this prompt). Supabase MCP: `booking_no_overlap` constraint intact, RLS deny-all confirmed on all 16 application tables (17th table with RLS disabled is `_prisma_migrations`, not an application table), no new tables added. `git diff --stat` matches this prompt's file list exactly (`auth.ts`, `middleware.ts` modified; old `src/app/admin/page.tsx` deleted; new files under `admin/(protected)/`, `api/admin/`, `components/admin/`, `types/`).

  **Cleanup**: the temporary AdminUser (`claude-verify-admin@example.com`) and its `BookingReschedule` audit row were deliberately left in place (see FK note above) — Arjay should either keep using this account, or run his own `prisma/bootstrap-admin.ts` for a real one and leave the test account alongside it. The rescheduled test booking (`cmsq31lm30001w4m8b045hv2f`, now sitting at 2026-09-15 10:00–11:00) was likewise left as-is rather than reverted, consistent with this project's standing convention of leaving stray VERIFY test data in place and documenting it rather than inventing new cleanup mechanisms.

- 2026-08-19 — Bugfix: the post-login "hang" on `/admin/login` (real symptom: clicking Sign In with correct credentials silently did nothing — no error, no navigation, no console error) was diagnosed and fixed. **Root cause**: `src/app/admin/login/page.tsx`'s `authenticate()` server action called `signIn('credentials', formData)`, passing the raw `FormData` straight through with no `redirectTo`. Auth.js v5's `signIn()` defaults `redirectTo` to the page the request originated from when not given explicitly — since the form POSTs from `/admin/login` itself, every successful login was issuing a `NEXT_REDIRECT` back to `/admin/login`, not `/admin`. This is a real redirect (confirmed via the thrown redirect's `digest`: `NEXT_REDIRECT;push;http://localhost:3000/admin/login;307;`), so nothing actually hung — it just landed back on the identical login page with the identical (empty, since the form re-rendered fresh) state, which is visually indistinguishable from the click doing nothing. **Diagnosis method**: temporary `console.log` instrumentation (per this prompt's instructions) was added to `middleware.ts`'s callback, `(protected)/layout.tsx`'s top, and — once those two showed no signal — `authenticate()` itself, logging the caught error's full `digest` string, which revealed the wrong redirect target directly. **Fix**: `authenticate()` now calls `signIn('credentials', { email: formData.get('email'), password: formData.get('password'), redirectTo: '/admin' })` — an explicit object instead of raw `FormData`, with `redirectTo` set explicitly rather than relying on the default. All temporary logging was removed from `middleware.ts` and `(protected)/layout.tsx` after diagnosis; `authenticate()`'s own temporary logging (added mid-session once the first two log points showed nothing) was replaced by the real fix rather than left in. **Relationship to the previously-flagged `/api/admin/:path*` matcher anomaly (2026-08-19, slice 2 entry above)**: same root cause, wider than originally scoped — see the update appended to that entry above. Middleware never logged a single invocation across this entire debugging session, for `/admin/login`, `/admin`, or `/admin/bookings`, meaning it does not appear to execute at all in this dev environment; every route that looked "correctly gated by middleware" in prior VERIFY passes was actually being gated by its own independent check (`(protected)/layout.tsx`'s `auth()`, or the reschedule route's `401`). Not fixed as part of this prompt (out of scope — the prompt was specifically the login hang) and not urgent, since defense-in-depth checks are confirmed doing the real work everywhere they're needed today, but flagged clearly since any *new* route added under `middleware.ts`'s matcher without its own auth check would currently be unprotected in this environment. `npx tsc --noEmit` clean. **VERIFY, all live via the Browser pane** (fresh dev server restart, watched via direct terminal output per this prompt's instructions, not just browser tooling): full login round trip — correct credentials → lands on `/admin` within ~1.7s, header shows "Signed in as winstonsipandserve@gmail.com" and the Bookings nav link, no pending/hanging request. Wrong-password regression check: still shows the generic "Invalid email or password." error correctly. `/admin/bookings` (all 24 real rows) and `/admin/bookings/[id]` (detail + add-ons + payment + reschedule history + reschedule form) both still render correctly post-fix. `/`, `/book`, `/membership` all confirmed unaffected — zero console errors on any of the three.

- 2026-08-19 — Admin nav expanded from 2 to 5 tabs + Coming Soon placeholders: `src/app/admin/(protected)/layout.tsx` touched (nav extracted, header wrapper made wrap-safe — see below); new `src/components/admin/AdminNav.tsx` and `src/components/admin/ComingSoonSection.tsx`; three new placeholder routes — `src/app/admin/(protected)/resources/page.tsx`, `.../memberships/page.tsx`, `.../bulletin/page.tsx`. `src/app/admin/(protected)/bookings/**` untouched. **AdminNav**: `'use client'`, `usePathname()`-driven, 5 items (Dashboard → `/admin`, Bookings, Resources & Pricing, Memberships, Bulletin); `/admin` matches active only exactly (not by prefix, since every admin route starts with `/admin`), all other items match on exact path or `pathname.startsWith(href + '/')` so `/admin/bookings/[id]` still highlights Bookings; active link styled `fontWeight: 'bold'` + `textDecoration: 'underline'` (plain/functional, no new color tokens, matching this admin panel's existing inline-`style`-object convention rather than Tailwind classes — **scope note**: the originating prompt referenced "existing Tailwind classes" on the nav wrapper and `bookings/page.tsx`'s "Tailwind conventions," but neither `layout.tsx` nor `bookings/page.tsx` actually uses Tailwind classes anywhere — this admin panel is built entirely with inline `style={{}}` objects, and there is no `tailwind.config` in the repo; followed the real existing convention instead of a premise that didn't match the code). **ComingSoonSection**: presentational, `title`/`bullets` props, heading + "Coming soon." line + `<ul>` of bullets, matching `bookings/page.tsx`'s bare-heading/no-extra-container pattern (page padding already comes from `layout.tsx`'s `<main>`). **Responsive fix found during VERIFY, not anticipated in the prompt**: adding `flexWrap: 'wrap'` to just the nav's own wrapper (as instructed) was not sufficient — `layout.tsx`'s `<header>` itself is `display:flex` with no wrap and `justify-content:space-between` between the nav and the "Signed in as {email}" + Sign Out block, so at 375px those two flex children fought for space, squeezing the nav into a single narrow vertical column and still overflowing horizontally by ~35px (measured via `document.documentElement.scrollWidth` vs `clientWidth`). Fixed by adding `flexWrap: 'wrap'` + `rowGap: '0.75rem'` to the header's own style object and `flexWrap: 'wrap'` to the signed-in/sign-out `<div>`'s style object — both in `layout.tsx`, both no-ops at desktop width (confirmed unchanged single-row layout at 1280px), and neither touches the sign-out form/server action or the "Signed in as {email}" text itself, so the "leave Sign Out untouched" scope constraint holds. VERIFY performed against Arjay's already-running dev server (port 3000) via the Browser pane. `npx tsc --noEmit` and `npx eslint` (scoped to every touched file) both clean — the 3 pre-existing `react-hooks/set-state-in-effect` errors in `BookingForm.tsx` are unrelated and untouched. Signed-out: `/admin`, `/admin/bookings`, `/admin/resources`, `/admin/memberships`, `/admin/bulletin` all redirected to `/admin/login` (re-confirmed after the header-wrap fix too, not just before). Signed-in (real login round-trip, not just session inspection): `/admin` shows only Dashboard bold+underlined; clicking Bookings highlights it and the list/filter/pagination output matched byte-for-byte with a pre-change capture; opening a booking detail kept Bookings highlighted (prefix match) with detail/reschedule-history/reschedule-form content unchanged; Resources & Pricing, Memberships, and Bulletin each rendered their correct heading + "Coming soon." + bullet list with the right tab bolded. Sign Out correctly returned to `/admin/login`. Mobile (375×812): confirmed via `javascript_tool` (not `screenshot`, which fails in this environment per the standing Browser-pane limitation — see Next Up) that `scrollWidth === clientWidth` (no horizontal overflow) both before commit (as a red flag) and after the header/div wrap fix, and that nav links visually wrap onto multiple rows rather than clipping. `/`, `/book`, and `/admin/login` all confirmed to still load with zero new console errors. No database rows created or touched — nothing to clean up.

- 2026-08-19 — Admin frame restructured from top-nav to a foldable left sidebar + thin topbar + bento content card: new `src/components/admin/AdminIcons.tsx` (hand-authored inline-SVG icon set following `src/components/ui/Icons.tsx`'s stroke-based/`currentColor` convention — `DashboardIcon`/`BookingsIcon`/`ResourcesIcon`/`MembershipsIcon`/`BulletinIcon`/`FoldIcon`/`UserIcon`/`SignOutIcon`), `AdminSidebar.tsx` (replaces `AdminNav.tsx`, which is deleted), `AdminTopbar.tsx`; `src/app/admin/(protected)/layout.tsx` rewritten to compose them. **This pass deliberately switches the admin shell onto real Tailwind utility classes** (`bg-gray-50`, `rounded-2xl`, `transition-[width]`, etc. — Tailwind v4 is already a project dependency, confirmed via `package.json`/`postcss.config.mjs`, just unused by the admin panel's plain inline-`style` pages until now) per this prompt's explicit instruction, superseding the inline-style convention the prior nav-expansion pass deliberately followed — no brand tokens (Fraunces/`accent-primary`/`brand-dark`) used anywhere in the frame, neutral grays only, consistent with the "plain/functional" admin aesthetic. `AdminSidebar` (`'use client'`, `usePathname()`): same 5 nav items and the same active-match logic as the old `AdminNav` (`/admin` exact-match only, others exact-or-prefix via `pathname.startsWith(href + '/')`); local `collapsed` state (default `false`) toggled by a `FoldIcon` button whose icon rotates 180° via a plain CSS `transition-transform` (no animation library); expanded (`w-[232px]`) tiles show icon+label, collapsed (`w-16`) tiles show icon-only with `title`/`aria-label` carrying the item name so identity survives without the visible label; active tile styled `bg-gray-900 text-white font-semibold` in both states, inactive `text-gray-600 hover:bg-gray-100`; sidebar itself is a bento card (`rounded-2xl border border-gray-200 bg-white shadow-sm`) on the layout's `bg-gray-50` canvas. `AdminTopbar` (server component, receives `email` as a prop from `layout.tsx` rather than calling `auth()` a second time): plain "Winston Admin" label left; right side shows a circular initials avatar (first two characters of the email, uppercased) with the full email as its `title` tooltip — replacing the old inline "Signed in as {email}." text, which doesn't fit a thin bar — plus the same sign-out server action as before (`signOut({ redirectTo: '/admin/login' })`, defined inline exactly like the code it replaced), restyled as a compact bordered button with `SignOutIcon` + label. `layout.tsx`: outer `bg-gray-50` canvas holds the topbar full-width, then a row (`AdminSidebar` + a `<main>` bento card wrapping `{children}`, `rounded-2xl border border-gray-200 bg-white shadow-sm p-6`) — confirmed via reading every touched child page first (bookings list/detail, the admin dashboard, `ComingSoonSection`) that none of them carry their own outer padding/margin, so the single `p-6` on the new `<main>` is the only padding source and there is no double-padding to resolve. `src/app/admin/(protected)/bookings/**`, `ComingSoonSection.tsx`, and the three page files that use it were not touched at all. `npx tsc --noEmit` and `npx eslint` (scoped to every touched/new file) both clean. VERIFY performed live against Arjay's already-running dev server (port 3000) via the Browser pane, real login/logout round-trips, not session inspection. Confirmed for `/admin`, `/admin/bookings`, a booking detail page, `/admin/resources`, `/admin/memberships`, `/admin/bulletin`: correct single active tile per route (Dashboard NOT active on `/admin/bookings`, confirmed exact-match rule still holds; Bookings stays active via prefix match on the detail page); fold toggle collapses/expands correctly (`className` inspection confirmed `w-16` vs `w-[232px]` and the `title`/`aria-label` fallback present on every icon-only tile, active tile's `bg-gray-900` class survives collapse); avatar showed correct initials ("WI") with the full email as `title`; bookings list/detail output and all three Coming Soon pages' copy matched byte-for-byte against pre-change captures; content `<main>` confirmed via `getComputedStyle` to have exactly `24px` padding with the child's own root element at `0px` — no double-padding/seam. Sign Out confirmed to actually clear the session, not just redirect: after clicking Sign Out, a direct re-visit to `/admin` also redirected to `/admin/login`. Signed-out regression: all 6 admin URLs (`/admin`, `/admin/bookings`, the same booking detail URL, `/admin/resources`, `/admin/memberships`, `/admin/bulletin`) redirected to `/admin/login`. `/`, `/book`, `/admin/login` all confirmed unaffected with zero new console errors. **Mobile (375×812), flagged not fixed, per this prompt's explicit instruction to flag rather than invent a mobile behavior**: no horizontal overflow occurs (`scrollWidth === clientWidth === 375`, sidebar and content both shrink to fit), but the sidebar's default *expanded* state (`232px` of a `375px` viewport) leaves only ~95px for the content card — usable for short content but cramped for anything wider (e.g. the bookings table). Collapsing the sidebar frees the space, but there's no scoped mobile-specific default (e.g. default-collapsed below a breakpoint, or an off-canvas drawer) — worth a dedicated follow-up prompt if mobile admin usage is expected; not attempted here since it wasn't scoped. No database rows created or touched — nothing to clean up.

- 2026-08-19 — Bookings list restructured: columns trimmed to Resource/Customer/Date & Time/Status/Total/Action (the raw Booking ID column removed — it remains fully visible on the detail page, nothing lost) and Start+End merged into one "Date & Time" cell (date on one line, time range below); a new Action column's "View" button replaces the old full-row-click, pointing to the same `/admin/bookings/[id]` href. Table body is now independently scrollable (`overflow-y-auto` container, `max-h`-bounded via flex, `sticky top-0` thead with a matching `bg-gray-50` so scrolled rows don't show through) inside a bounded content frame — the page itself no longer scrolls to reach the status filters, search bar, or pagination, which all sit outside the scroll container. A non-functional search input+button (`type="search"`, no `onSubmit`/state/navigation) was added to the same row as the status filters. The table itself, previously raw unstyled HTML (`border`/`cellPadding` attributes, no Tailwind), was restyled onto the same neutral gray/`rounded-xl`/`shadow-sm` tokens as the rest of the bento shell, since the newly-styled Action button and search bar next to a fully unstyled table would have read as inconsistent — only visual styling changed, not the underlying data/formatting logic (peso formatting, status text, em-dash for missing customer all unchanged). **`layout.tsx`'s content frame needed a height-boundary fix** (Part 1 of the originating prompt): the outer shell was `min-h-screen` with no `overflow` constraint, so any page with enough content grew the whole document rather than scrolling internally — changed to `h-screen overflow-hidden` on the outer wrapper, `min-h-0` on the sidebar+main row, and `min-h-0 overflow-y-auto` on `<main>` (a fallback scroll path, not the primary one — the bookings page's own internal flex layout is what keeps the table as the only scrolling region in practice). Only `layout.tsx`'s height/overflow properties were touched — topbar, sidebar, and other tabs are unaffected. Query logic (status filter, pagination, page size 25, `startTime` ascending sort) and the detail page were untouched. `npx tsc --noEmit` clean; `npx eslint` scoped to both touched files clean (pre-existing `BookingForm.tsx` `react-hooks/set-state-in-effect` errors are unrelated). VERIFY performed against Arjay's already-running dev server (port 3000) via the Browser pane (screenshot tool non-functional in this environment per the standing 2026-08-13 limitation — verified via `javascript_tool` height/scroll measurements instead): at both 1536×768 and 1440×900, confirmed via `document.documentElement.scrollHeight === window.innerHeight` and `main.scrollHeight === main.clientHeight` that the outer page never needs to scroll, while the table container's `scrollHeight` exceeds its `clientHeight` (e.g. 1438 vs 478 at 1536×768) — only the table scrolls; scrolling the container to `scrollTop: 300` confirmed the sticky `thead` stayed pinned near the container top with `position: sticky` and a solid background. Clicked 3 different View buttons (read via DOM `href` inspection) and confirmed each lands on the correct `/admin/bookings/[id]` detail page, which still shows the full Booking ID and separate Start/End rows, untouched. Status filter (clicked Confirmed) correctly re-filtered the list to only the 2 confirmed bookings and updated the URL — regression confirmed working. Search input accepted typed text and the Search button click produced zero console errors and no URL/navigation change. At 375×812, the table container showed `overflow-x: auto` with `scrollWidth` (720px) exceeding the squeezed `clientWidth`, confirming horizontal scroll is available rather than a broken layout (the extreme squeeze at this width comes from the pre-existing sidebar defaulting to expanded on a fresh page load, not from this change). Regression-checked `/admin` (Dashboard) and `/admin/resources` (Coming Soon) at 1536×768 post-layout-change: both rendered their original content with `main.scrollHeight === main.clientHeight` (no scrolling introduced) and zero console errors, confirming the height-boundary fix is invisible on low-content pages. No test data created — nothing to clean up.

- 2026-08-19 — Bookings list: re-added a "Reference" column (raw `Booking.id`, first column — same no-reformatting convention as the customer-facing "Booking Reference" display and the detail page's `<h1>`), and replaced the four status-filter `<Link>`s with a single "Filter" button opening the shared `Modal.tsx` (untouched — reused as-is) via a new `src/components/admin/BookingsFilterModal.tsx` (`'use client'`). The modal holds a status radio group (All/Pending/Confirmed/Cancelled) and two native `<input type="date">` fields (From/To, no date-picker dependency), hydrated from the page's current `status`/`startDate`/`endDate` searchParams every time it's opened (draft state resets to the live values on open, not just on mount, so reopening after a URL change — e.g. via Prev/Next — shows the actually-applied filter, not a stale draft); "Run" builds a `URLSearchParams`-based URL (status omitted when `'all'`, empty dates omitted) and navigates via `useRouter().push(...)`, landing on page 1 by construction since it never carries a `page` param forward; "Clear" navigates straight to `/admin/bookings`. The Filter button shows a small dot badge whenever `status !== 'all'` or either date is set, so an active filter is visible without opening the modal. `page.tsx`'s Prisma `where` clause now combines the existing `status` filter with a `startTime` range built from `business-hours.ts`'s `phDateToUtcWindow(dateStr)` helper (reused, not reimplemented) — `startDate` maps to `.start` as a `gte` bound, `endDate` maps to `.end` as an exclusive `lt` bound (so the full PH calendar day is included), and either can be supplied alone for an open-ended range. Pagination's Prev/Next hrefs are now built from a shared `URLSearchParams` (`filterParams`) carrying `status`+`startDate`+`endDate` forward before appending `page`, replacing the prior `status`-only `statusQuery` string, so paging through a filtered result no longer silently drops the filter. Export button added next to Filter (no `onClick`, no logic) as a placeholder for a future data-extraction feature. `src/components/ui/Modal.tsx` was not modified; `bookings/[id]/page.tsx` and the reschedule route/logic were not touched; no new dependencies. `npx tsc --noEmit` and `npx eslint` (scoped to both touched/new files) clean — the 3 pre-existing `react-hooks/set-state-in-effect` errors in `BookingForm.tsx` are unrelated and untouched, confirmed by re-running the full `npm run lint` and diffing against the known-existing count. VERIFY performed against Arjay's already-running dev server (port 3000) via the Browser pane, real login. Reference column spot-checked against 2 booking detail pages — ids matched exactly. Filter modal with no filter applied: status radio defaulted to "all", both date inputs empty (confirmed via DOM `checked`/`value` inspection). Set status=Pending + Run: URL became `?status=pending_payment`, list showed only `pending_payment` rows, Filter button's dot badge appeared; reopening the modal showed the Pending radio still checked (hydration confirmed). Date range alone (`startDate=2026-08-19&endDate=2026-08-20`) correctly returned only bookings whose `startTime` fell on those two PH calendar dates (mixed statuses), excluding all Aug 13/25/26/27/Sep 15 rows; the same range combined with `status=cancelled` correctly narrowed to just the 7 cancelled bookings in that window. Multi-page + filter interaction: **the current seed data (23 total bookings) never exceeds the 25-row page size under any filter, so no Prev/Next link ever renders regardless of filter — true multi-page navigation could not be exercised live.** Verified the underlying mechanism by direct URL instead: navigating to a filtered URL with `page=2` returned "No bookings found" (correct — `skip=25` past a 7-row filtered result) without erroring, and the rendered "Prev" link's `href` on that page correctly read back `?status=cancelled&startDate=2026-08-19&endDate=2026-08-20&page=1`, confirming `pageHref()` carries all three filter params forward; recommend a dedicated live check once seed data exceeds 25 rows under some filter. Clear: returned to `/admin/bookings` with no params and the dot badge gone. Export button clicked: zero console errors, no navigation (`window.location.href` unchanged). Regression: table scroll/sticky-header behavior from the prior pass unchanged (`main.scrollHeight === main.clientHeight`, table container's own `scrollHeight > clientHeight`, `thead` stayed `position: sticky` after scrolling); a booking detail page (with existing reschedule history) rendered unchanged with zero console errors; `/admin/resources` (Coming Soon) rendered unaffected with zero console errors. No test data created — nothing to clean up.

- 2026-08-19 — Admin panel: Memberships section built as a read-only frame (list + detail), replacing the Coming Soon placeholder — no schema changes, no approve/reject mutation. `src/lib/supabase-storage.ts` gained `getSignedUrl(bucket, path, expiresInSeconds = 300)`, following the same plain-`fetch`-against-the-Storage-REST-API pattern as `uploadToStorage`/`deleteFromStorage`, so private-bucket government ID images can be resolved to short-lived viewable URLs server-side without making the bucket public. `src/app/admin/(protected)/memberships/page.tsx` (server component, Tailwind bento-table conventions matching `bookings/page.tsx`) lists `MembershipApplication` rows (`include: { customer, reviewedBy }`, `orderBy: createdAt desc`, 25/page) with a plain-link All/Pending/Approved/Rejected status filter (simpler than Bookings' modal filter, since that was a later polish pass, not part of Bookings' initial slice) and a local `formatTier` helper (3-Month/6-Month/12-Month). New `src/app/admin/(protected)/memberships/[id]/page.tsx` (server component, plain inline-style convention matching `bookings/[id]/page.tsx`, which hasn't had its own Tailwind pass yet) shows Applicant/Application/Review sections, all 3 government ID images (front/back/selfie) resolved via 3 parallel `getSignedUrl` calls, and an Approve/Reject action pair that is visibly present but `disabled` with no handler — a note underneath states the actions aren't wired yet. `npx tsc --noEmit` and `npm run lint` both clean (only pre-existing `BookingForm.tsx` errors and expected `@next/next/no-img-element` warnings on the 3 new `<img>` tags, same convention as the rest of this admin panel's unstyled detail pages). **VERIFY, all live** against Arjay's already-running dev server (port 3000) via the Browser pane, real logged-in session: submitted a real test application through the actual `POST /api/membership-applications` route (script-driven multipart submit with 3 tiny real PNG files, since the Browser pane's tooling in this session couldn't drive native file-input dialogs) — confirmed it appears correctly on the list page, the status filter correctly narrows/excludes it, and the detail page renders every field plus all 3 government ID images with real `naturalWidth`/`naturalHeight` > 0 and `complete: true` (confirmed via `javascript_tool`, not just absence-of-broken-icon) — the real proof `getSignedUrl` works against the private bucket. Clicking Approve/Reject produced no state change and both remained `disabled` post-click. Directly toggled the same test application's `status` to `approved` (with `reviewedById`/`reviewedAt` set) and reloaded — reviewed-by/reviewed-at rendered correctly, no rejection-reason row; then toggled to `rejected` with a `rejectionReason` and reloaded — rejection-reason row appeared correctly. Unauthenticated `curl` against both `/admin/memberships` and the detail URL returned `307` to `/admin/login`, confirming the shared `(protected)/layout.tsx` auth gate wasn't bypassed. Regression: `/admin/bookings` (23 rows) and a booking detail page rendered unchanged; `/admin/resources` still Coming Soon. All test `MembershipApplication`/`Customer` rows and their 3 Storage files per application were deleted afterward via a temporary script (list page reconfirmed showing "No applications found." post-cleanup) — no residual test data left behind.

- 2026-08-19 — Bookings list's "Date & Time" column changed to "Submitted": the column now reads from `booking.createdAt` (single date + time, via a new `formatSubmittedAt` helper replacing the old `formatDateTime(start, end)`) instead of the reserved slot's start/end range, and the header text changed from "Date & Time" to "Submitted". The actual slot (Start/End) remains fully visible on the existing detail page (`bookings/[id]/page.tsx`, untouched). **Deliberate asymmetry, not a bug**: the Filter modal's date-range filter and the query's `orderBy` both still operate on `startTime` (the slot), unchanged — so the list is sorted soonest-slot-first and filterable by slot date, even though the visible "Submitted" column shows a different date than what's being filtered/sorted on. Only `src/app/admin/(protected)/bookings/page.tsx` touched; `BookingsFilterModal`, the `where`/`orderBy` query logic, and the detail page were all out of scope and unchanged. `npx tsc --noEmit` and `npm run lint` clean (same pre-existing unrelated `BookingForm.tsx` errors as prior entries). VERIFY performed against Arjay's already-running dev server (port 3000) via the Browser pane, real logged-in session: header confirmed reading "Submitted" with each row showing a single date/time (no en-dash range); spot-checked booking `cmsq31lm3...`'s list-row "Submitted" value (Aug 12, 8:45 PM) against its detail page's actual slot Start (9/15/2026, 10:00 AM) — confirmed genuinely different values, proving the swap took effect; filtering by `startDate=2026-09-15&endDate=2026-09-15` correctly returned that same booking (filtering on slot date, not submission date) while its displayed "Submitted" column still showed the unrelated Aug 12 value — confirming the documented asymmetry works as intended, not silently broken. Detail page's Start/End rows re-confirmed unaffected. No test data created — pure display change, nothing to clean up.

- 2026-08-19 — Memberships detail page (`/admin/memberships/[id]`) visual redesign: pure layout/class pass, no data/query/behavior changes — only `src/app/admin/(protected)/memberships/[id]/page.tsx` touched. **Header-pill/Status-dedup decision**: the application's status (previously shown as a plain "Status" row inside the Application table, redundantly alongside nothing else) is now shown exactly once, as a colored pill (`pending`=amber, `approved`=green, `rejected`=red) in a new header row next to the "Membership Application" heading and raw application ID (mono, gray, unformatted — same no-reformatting convention as Bookings' Reference column); the old Application-section Status row was removed rather than kept as a second display of the same value. Applicant and Application sections became two side-by-side bento cards (`grid md:grid-cols-2`, stacking to one column below `md`) with label/value rows on a bottom-border pattern, matching Bookings list's neutral gray/`rounded-xl`/`shadow-sm` tokens — no brand tokens used anywhere. Review card collapses to a single italic "Not yet reviewed" line when `reviewedById` is null, instead of two always-shown placeholder rows; once reviewed, Reviewed By/At render as normal rows, with the existing rejection-reason conditional preserved. **Image-frame/click-to-enlarge addition**: the 3 government ID images (front/back/selfie, same signed URLs, not refetched) now render in a uniform `aspect-[4/3]` bordered frame with `object-cover` regardless of the source image's actual dimensions, each wrapped in a link to its full-resolution signed URL opening in a new tab. Approve/Reject kept `disabled` with no handler, restyled as real-looking green/red-outline buttons. `npx tsc --noEmit` clean; `npx eslint` on the touched file showed only the pre-existing `@next/next/no-img-element` warnings on the (unchanged-in-kind) `<img>` tags, zero new errors. **VERIFY, all live** against Arjay's already-running dev server (port 3000) via the Browser pane (screenshot tool non-functional in this environment per the standing limitation — verified via DOM/`javascript_tool` inspection instead): real pending application (`9bbb2ab2-96be-477a-b919-854e99b1bd10`) showed the amber pill and no duplicate Status row; toggling that same application's `status` to `approved`/`rejected` (with `reviewedById`/`reviewedAt`/`rejectionReason` set via a temporary one-off Prisma script, same technique as the 2026-08-19 memberships VERIFY pass) and reloading confirmed the green and red pill classes respectively, Review card's Reviewed By/At rows, and the rejection-reason row appearing only when rejected — then reverted the application back to `pending` with reviewer fields cleared. All 3 ID image `<a>` links confirmed pointing at their correct signed Supabase Storage URLs; all 3 images confirmed at identical rendered dimensions (285.6×213.8px) regardless of source size. Both Approve/Reject buttons confirmed `disabled={true}` in the DOM. At both 375×812 and 1280×800, confirmed `document.documentElement.scrollWidth === clientWidth` (no horizontal overflow) and the Applicant/Application cards single-column vs. two-column as expected (the extreme content-width squeeze at 375px comes from the pre-existing sidebar defaulting to expanded on load, an out-of-scope known issue, not this page). Regression: `/admin/memberships` (list), a booking detail page, and `/admin`/`/admin/resources`/`/admin/bulletin` all rendered unchanged with zero console errors. `git status`/`git diff --stat` confirmed only this page file (plus this `PROGRESS.md` entry) changed. Temporary toggle script deleted after use — no residual test data or files left behind (the reused application was a real existing pending row, reverted to its original state rather than a disposable one requiring deletion).

- 2026-08-19 — Admin panel: Resources & Pricing built as a read-only frame, replacing the Coming Soon placeholder — only `src/app/admin/(protected)/resources/page.tsx` touched, no schema changes, no new API route, no mutation of any kind. Server component, direct Prisma queries (`ResourceType.findMany` with `resources`/`pricingRules`/`addOnPricingRules` — the latter including `addOnService` to distinguish coaching vs. ball boy — plus a single `GuestFeeRule.findFirst()`), ordered per CLAUDE.md's documented resource-type sequence (tennis-court, pickleball-court, tennis-sim, pickleball-sim, golf-sim) rather than DB insertion order. One card per resource type (Tailwind bento conventions matching `bookings/page.tsx`): a resource sub-list (label + Active/Inactive pill, same green/gray convention as the Memberships status pill), a pricing table (Member/Non-Member columns; courts show a single "Hourly rate" row, simulators show one row per duration tier that exists in either column), and an add-on pricing table (Coaching: 1-pax/2-pax rows for courts, one flat row for simulators; Ball Boy: court types only — the row is omitted entirely for the 3 simulator types rather than rendered as two "Not offered" cells, since it's structurally not applicable to sims). **"No row = not offered" display decision**: every Member/Non-Member × tier cell with no matching `PricingRule`/`AddOnPricingRule` row renders `text-gray-400 italic` "Not offered" rather than being blank or fabricating ₱0 — confirmed live for golf-sim non-member 30-min and non-member tennis-sim/pickleball-sim coaching, both intentional per-seed gaps. Reused `formatCentavos` (`src/lib/format.ts`) for every price; no new currency helper. A separate Guest Fee card below the 5 resource-type cards shows the single `GuestFeeRule` row's amount + "/hr" and an exemption note. `npx tsc --noEmit` and `npx eslint` (scoped to the touched file) both clean. **VERIFY, all live** against Arjay's already-running dev server (port 3000) via the Browser pane, real logged-in session (Arjay's own admin credentials): every one of the 5 cards rendered in the documented order with correct resource counts (1/3/1/2/2) and real Active pills on every resource; every pricing/add-on value spot-checked against `prisma/seed.ts`'s figures matched exactly, including the three deliberate gaps above; Ball Boy rows confirmed entirely absent (not "Not offered" ×2) on all 3 simulator cards; Guest Fee card showed ₱150.00/hr with the exemption note. Desktop (1280×800): `scrollWidth === clientWidth`, zero console errors. Mobile (375×812): page-level `scrollWidth === clientWidth` (no overflow), and each pricing/add-on table's own wrapper correctly scrolls horizontally (`scrollWidth 420 > clientWidth`) rather than clipping or overlapping — the near-zero available width at this size is the pre-existing sidebar-defaults-to-expanded issue already flagged in Next Up, not something this change introduced. Regression: `/admin/bulletin` still renders its unmodified Coming Soon placeholder (confirms `ComingSoonSection` wasn't altered); `/admin`, `/admin/bookings`, `/admin/memberships` all unaffected, zero console errors on any route. `git status`/`git diff --stat` confirmed only `resources/page.tsx` (plus this `PROGRESS.md` entry) changed — no schema/migration file in the diff. Pure read query — no test/DB data created, nothing to clean up.

- 2026-08-19 — Admin: Resources & Pricing split into 3 tabs (Courts / Simulators / Guest Fee) and given a full set of inert CRUD-preview buttons, still entirely read-only — `src/app/admin/(protected)/resources/page.tsx` and new `src/components/admin/ResourcesTabs.tsx` touched, no schema/query/API changes. **Tab-split derivation**: `page.tsx`'s existing `orderedResourceTypes` array (already ordered per CLAUDE.md's tennis-court/pickleball-court/tennis-sim/pickleball-sim/golf-sim sequence) is filtered into `courts`/`simulators` by `rt.category` (`ResourceCategory` enum, `'court' | 'simulator'`, confirmed against `schema.prisma` rather than guessed from slug naming) and passed as props to `ResourcesTabs`, a `'use client'` component holding `useState<'courts' | 'simulators' | 'guestFee'>('courts')`; the previous per-resource-type card JSX was relocated verbatim (no visual change) into a local `ResourceTypeCard` function inside `ResourcesTabs.tsx` so both tabs reuse it. Tab content is a genuine conditional render (`activeTab === 'courts' && (...)`), not CSS-hidden — confirmed in VERIFY that switching tabs actually removes the inactive tab's cards from the DOM. Tab bar styled on `AdminSidebar`'s active-tile convention (`bg-gray-900 text-white` active / `text-gray-600 hover:bg-gray-100` inactive). **Per-cell "+ Add" vs. per-row pencil decision**: every Rate/Duration and Add-on table gained a trailing "Actions" column with one disabled pencil button per row (edits the whole row), while `PriceCell` was changed so any individual Member/Non-Member cell with no matching `PricingRule`/`AddOnPricingRule` row renders a small disabled dashed-border "+ Add" ghost button instead of the previous italic "Not offered" text — the two are independent and can coexist on the same row (e.g. golf-sim 30-min: Member cell shows its price, Non-Member cell shows "+ Add", and the row still has its own trailing pencil). Each resource row (e.g. "Court 1") gained trailing Edit/Delete icon buttons (`aria-label` naming the resource), and each card header gained a right-aligned "+ Add Resource" button. Pencil/trash icons were hand-authored as small inline SVGs matching `AdminIcons.tsx`'s `viewBox="0 0 24 24"`/`stroke="currentColor"`/`strokeWidth="1.5"` convention, kept local to `ResourcesTabs.tsx` (not added to `AdminIcons.tsx`, since these are specific to this page's inert-preview buttons, not shared nav icons) — no icon library added. Every new interactive element is `disabled`, has no `onClick`, and triggers zero network requests. The italic note under the heading was updated from "Read-only for now…" to "Editing controls below are previews only — they aren't wired yet…". `npx tsc --noEmit` and `npx eslint` (scoped to both touched files) both clean. **VERIFY, all live** against Arjay's already-running dev server (port 3000) via the Browser pane, real logged-in session: Courts tab active by default showing exactly Tennis Court + Pickleball Court (confirmed via `aria-selected` inspection); clicking Simulators swapped the rendered `<h2>` headings from Tennis/Pickleball Court to Tennis/Pickleball/Golf Simulator — confirmed via direct DOM query immediately after the click that the Courts cards were genuinely gone, not just hidden; Guest Fee tab rendered the single Guest Fee card alone (₱150.00/hr, exemption note) with only 1 non-tab button on the page (its edit pencil). Every figure from the prior read-only-frame pass re-confirmed unchanged inside its new tab, including the three deliberate gaps (golf-sim non-member 30-min, tennis-sim/pickleball-sim non-member coaching) now rendering as "+ Add" instead of "Not offered". Button audit: Courts tab had exactly 18 non-tab buttons (7 on Tennis Court's single-resource card, 11 on Pickleball Court's three-resource card — matches the expected count from Add Resource + per-resource Edit/Delete + per-row rate/add-on pencils, zero "+ Add" cells needed on this tab since both cards' rates are fully priced), all confirmed `disabled={true}`; `.click()`-ing all 18 produced zero new network requests (network log showed only the original page-load's static chunk/font requests) and zero console errors. Mobile (375×812): `document.documentElement.scrollWidth === clientWidth` (no page-level horizontal overflow), the 3-tab bar wrapped onto multiple rows rather than clipping (`flex-wrap` confirmed via `scrollHeight > 44`), and every pricing/add-on table's wrapper scrolled horizontally rather than breaking page width — the near-zero available width at this size is the same pre-existing sidebar-defaults-to-expanded issue already flagged in Next Up, not something this change introduced. Desktop (1280×800): zero overflow. Regression: `/admin/bulletin`, `/admin`, `/admin/bookings` (24 rows), `/admin/memberships` all rendered unchanged with zero console errors. `git status`/`git diff --stat` confirmed only `resources/page.tsx`, the new `ResourcesTabs.tsx`, and this `PROGRESS.md` entry changed. Pure UI/presentational change — no DB data created or touched, nothing to clean up.

- 2026-08-19 — Admin: Resources & Pricing's Courts/Simulators cards made individually foldable — only `src/components/admin/ResourcesTabs.tsx` touched, purely presentational, no schema/query/API changes. `ResourceTypeCard` gained local `useState<boolean>(false)` (`isOpen`, collapsed by default on every mount — no attempt to persist state across tab switches or reloads, so switching from Courts to Simulators and back re-collapses everything). The header row was split into two sibling elements to avoid nesting a button inside a button: a left `<button aria-expanded={isOpen}>` wrapping the resource-type name, resource count, and a new `ChevronIcon` (hand-authored inline SVG matching `AdminIcons.tsx`'s stroke convention, `transition-transform duration-200` + `rotate-180` when open — the same mechanism `AdminSidebar`'s `FoldIcon` already uses for its fold toggle, reused rather than reinvented), and the existing "+ Add Resource" button as an untouched sibling that stays visible/clickable regardless of collapsed state. The card body (resource sub-list + rate table + add-on table) is now wrapped in `{isOpen && (...)}` — a genuine conditional render, matching the same real-conditional-render approach the Courts/Simulators/Guest Fee tab switch itself already uses, not CSS `hidden`. **Independent-not-accordion**: each card owns its own `isOpen` state local to its own `ResourceTypeCard` instance, so there's no shared/lifted state coordinating cards — opening one never closes another, confirmed live. **Guest Fee explicitly excluded**: its card lives outside `ResourceTypeCard` entirely (a separate inline block in the parent `ResourcesTabs` component), so this change didn't touch it at all — confirmed it has zero `aria-expanded` buttons and its content is always rendered. None of the existing inert CRUD-preview buttons (Add Resource, per-resource Edit/Delete, per-row pencils, "+ Add" cells) changed behavior — still `disabled`, no handlers, no new dependency. `npx tsc --noEmit` and `npx eslint` (scoped to the touched file) both clean. **VERIFY, all live** against Arjay's already-running dev server (port 3000) via the Browser pane, real logged-in session: Courts tab loaded both cards collapsed by default (`aria-expanded="false"`, zero `<table>` elements in the DOM); clicking Tennis Court's header expanded it (2 tables appeared, chevron gained `rotate-180`) while Pickleball Court's header stayed `aria-expanded="false"` — confirmed independent, not accordion-exclusive; clicking Pickleball Court's header too brought the DOM to 4 tables (both open simultaneously); clicking Tennis Court's header again collapsed it back to 2 tables while Pickleball Court's content (spot-checked "Court 1" text) remained present and untouched. Switching to Simulators showed all 3 cards freshly collapsed (zero tables), confirming no state leaked over from Courts. Expanded Golf Simulator and spot-checked its pricing rows against the prior pass (30-min Non-Member still "+ Add", other 5 figures unchanged) — all 11 non-header buttons in the card confirmed still `disabled`. Guest Fee tab confirmed zero `aria-expanded` buttons, content always visible. Mobile (375×812) and desktop (1280×800): zero page-level horizontal overflow at both widths; both "+ Add Resource" buttons confirmed rendered with non-zero width (not clipped) at 375px. Regression: `/admin/bulletin`, `/admin/bookings`, `/admin/memberships`, `/admin` all rendered unchanged with zero console errors. `git status`/`git diff --stat` confirmed only `ResourcesTabs.tsx` (plus this `PROGRESS.md` entry) changed. Pure UI change — no DB data created or touched, nothing to clean up.

- 2026-08-19 — Admin: Resources & Pricing — Resource-level Add/Edit/Delete wired for real, plus a one-time cleanup of stray Booking test data. **Part 1, cleanup**: a temporary one-off script (`prisma/cleanup-stray-bookings.ts`, deleted after running — same disposable-script convention as `prisma/bootstrap-admin.ts`) deleted every `Payment`/`BookingReschedule`/`BookingAddOn` row (the three tables with a direct FK to `Booking`, confirmed against `schema.prisma` rather than assumed — `MembershipCreditTransaction` and `BookingAddOn.addOnPricingRuleId` point *away* from `Booking`, not toward it), then every `Booking` row. Before/after: bookings 24→0, payments 14→0, booking_reschedules 1→0, booking_add_ons 25→0. Verified via Supabase MCP (read-only, so used only for before/after counts, never for the delete itself) that `pricing_rules` (21), `add_on_pricing_rules` (16), `guest_fee_rules` (1), `membership_applications` (1), `memberships` (0), `customers` (4), `resource_types` (5), `resources` (9), and `admin_users` (2) were all byte-identical before/after, and that the pending `MembershipApplication` (`9bbb2ab2-96be-477a-b919-854e99b1bd10`) was still present and still `pending` — none of these tables were touched by the cleanup query, confirmed rather than assumed. **Part 2–4, mutation**: new `POST /api/admin/resources` (creates with `isActive: true`, validates `resourceTypeId` actually resolves to a `ResourceType`), `PATCH /api/admin/resources/[id]` (partial `label`/`isActive` body, 404 if missing), `DELETE /api/admin/resources/[id]` (404 if missing; single `$transaction` deletes the resource's own `Payment`/`BookingReschedule`/`BookingAddOn` rows, then its `Booking` rows, then the `Resource` itself, returning `{ deletedBookings: <count> }`) — all three follow the reschedule route's established `auth()` → 401 pattern (`src/app/api/admin/bookings/[id]/reschedule/route.ts`), since `middleware.ts` still doesn't execute locally. New `ResourceFormModal.tsx` (`mode: 'add' | 'edit'`, reuses `Modal.tsx`) renders its label/active-toggle form as a child only mounted while the modal is open (`{isOpen && <ResourceForm .../>}`) rather than resetting via a `useEffect` — sidesteps the `react-hooks/set-state-in-effect` lint rule by construction (state is *initialized* fresh via `useState`'s initial-value argument every time the form remounts, never *set* from inside an effect) instead of suppressing the rule. `ResourcesTabs.tsx`'s `ResourceTypeCard` gained `addModalOpen`/`editingResource` state, a real `handleDelete` (native `window.confirm` naming the resource, e.g. "Delete Court 4? This will also delete any bookings for this resource.", then `DELETE` + `router.refresh()`), and a new enabled `ActionIconButton` (edit/delete variants) alongside the still-fully-disabled `EditIconButton` used everywhere on the Pricing/Add-on/Guest-Fee tables — the two intentionally diverged so Resource rows could go live without touching any pricing control. Page-level italic note updated from "previews only" to name Resource CRUD as live and scope the remaining "previews only" language to Pricing/Add-on/Guest-Fee specifically. **Confirmed before writing the delete logic** (per the prompt's explicit ask, not assumed): `PricingRule`/`AddOnPricingRule` key off `resourceTypeId`, never `resourceId` — deleting or editing a `Resource` row structurally cannot touch either table. `npx tsc --noEmit` and a scoped `npx eslint` on all touched/new files both clean. **VERIFY, all live** against Arjay's already-running dev server (port 3000): `pg_constraint` reconfirmed `booking_no_overlap` still exists post-cleanup and post-cascade-delete-test. Add Resource: submitted "Court 4" on Pickleball Court via the Browser pane (Arjay's real session) — 201, UI showed it with an Active pill, count 3→4 resources, confirmed via `POST` in the network log. Edit: renamed to "Court 4 (Test)" + toggled Inactive — 200, UI reflected both changes. Delete/cascade path: since the Browser pane's `computer` tool has no way to accept a native `confirm()` dialog, a disposable `AdminUser` (`claude-verify-resources@example.com`, random-ish password, zero FK dependents created against it) was created via a temporary script for a separate Playwright-MCP login — Playwright *does* expose dialog handling (`browser_handle_dialog`). A throwaway `Booking` + `Payment` + `BookingAddOn` + `BookingReschedule` were seeded against "Court 4 (Test)" via another temporary script (`prisma/seed-cascade-test.ts`) specifically to exercise the non-empty cascade path, not just Part 1's already-empty case. Clicking Delete showed the confirm dialog with the exact expected message; accepting it fired `DELETE` → 200 `{deletedBookings: 1}`, and a direct Supabase query confirmed the resource, its booking, and all three dependent rows were gone while `total_resources` (9) and `total_bookings` (0) matched pre-test state — the resource inventory (1 tennis-court/3 pickleball-court/1 tennis-sim/2 pickleball-sim/2 golf-sim = 9) was reconfirmed unchanged afterward. Regression: `/admin/memberships/[id]` for the pending application still rendered its full detail correctly; `/admin/bookings` correctly showed "No bookings found." (expected post-cleanup, not a bug); `/admin/bulletin`, `/admin` unaffected; zero console errors across every page visited in both the Browser pane and Playwright sessions. Mobile (375×812, via Playwright since the Browser pane wasn't compositing frames this session — a known pre-existing issue, see 2026-08-19 Next Up entry): Add Resource modal opened as a proper `dialog` role with no page-level horizontal overflow (`scrollWidth === clientWidth === 375`); desktop (1280×800) same zero-overflow check. Pricing/Add-on/Guest-Fee buttons spot-checked still `disabled` throughout — untouched by this pass. `pricing_rules`/`add_on_pricing_rules`/`guest_fee_rules` counts reconfirmed unchanged after all Resource CRUD exercised. Cleanup: "Court 4 (Test)" was already removed via the cascade-delete test itself (not a separate manual step); the disposable `claude-verify-resources@example.com` `AdminUser` and all four temporary scripts (`cleanup-stray-bookings.ts`, `seed-cascade-test.ts`, `bootstrap-verify-admin.ts`, `cleanup-verify-admin.ts`) were deleted after use. `git status`/`git diff --stat` confirmed only `resources/page.tsx`, `ResourcesTabs.tsx`, the new `src/app/api/admin/resources/` route files, `ResourceFormModal.tsx`, and this `PROGRESS.md` entry changed — no schema/migration file in the diff.

- 2026-08-19 — Admin: `prisma/bootstrap-admin.ts` retroactively documented and deleted. A read-only Prisma query (no writes) confirmed the `AdminUser` row for `winstonsipandserve@gmail.com` (name "User Admin", login password `password123`) already existed in the DB, created 2026-08-18 — one day before this session — meaning the script had already been run in an earlier, unlogged session. No new account was created this session; the script's job was already done, so it was deleted per its own disposable-script convention rather than re-run.

- 2026-08-20 — Diagnostics + fix: `/admin/memberships`'s ~1.7s load traced to Prisma round-trip count, not query cost or RSC render time (prior sessions' CPU profiling and `EXPLAIN ANALYZE` had already ruled those out). `src/lib/prisma.ts` gained temporary `$on('query', ...)` event logging (left in place, not removed by this session). **Capture**: since authenticating via the admin login form isn't an action Claude Code will perform (password entry is a hard-blocked action regardless of context, even for a documented local dev/test account), round-trip counts were captured by invoking the page's exact Prisma calls directly from a disposable script (`prisma/diag-round-trip-count.ts`, deleted after use) rather than clicking through the browser — same DB code path, same query plan, just without the HTTP/RSC layer on top. Confirmed: the memberships `findMany({ include: { customer: true, reviewedBy: true } })` executed as 2 separate round-trips (main query + a `customer` relation query; `reviewedBy` was skipped since the one existing application has a null `reviewedById` — Prisma's default `'query'` relation-loading strategy only issues a follow-up round-trip per relation when there's a non-null FK to resolve) plus the parallel `.count()`, each paying the ~220–460ms Supabase (`ap-southeast-2`) RTT confirmed in the prior round's build-time log. A same-shape comparison against the bookings list query was attempted but came back confounded: the `bookings` table currently has 0 rows (from the 2026-08-19 stray-data cleanup), so its `resource`/`customer` relation queries were skipped entirely — not evidence bookings uses a different strategy, just that there was nothing to join. Analytically, a populated bookings query would very likely show the same multi-round-trip pattern, probably worse (up to 4 relation round-trips: `resource` → nested `resourceType`, plus `customer`). **Conclusion: this is Prisma's default relation-loading behavior interacting with cross-region latency, a general pattern likely affecting every admin list/detail query with `include`, not something specific to memberships.** **Fix**: added `previewFeatures = ["relationJoins"]` to `schema.prisma`'s `generator client` block (`prisma generate` re-run, no migration — this only changes generated-client query capabilities, not the DB schema) and set `relationLoadStrategy: 'join'` on the memberships list query in `src/app/admin/(protected)/memberships/page.tsx`, collapsing its 2 relation-fetching round-trips into 1 `SELECT ... LEFT JOIN LATERAL ...` statement (confirmed via a second disposable script, `prisma/diag-verify-join-fix.ts`, deleted after use). **Important discovery, handled defensively**: enabling `relationJoins` doesn't just make `relationLoadStrategy` an available per-call option — it silently flips Prisma's *default* to `'join'` for every relational query in the codebase that doesn't explicitly set a strategy. A build immediately after adding the preview feature showed the unrelated `resources` page query (which this task never touched) had also switched to a `LEFT JOIN LATERAL` plan. Since the task was scoped to the memberships query specifically, and several of the other 12 `include`-using call sites sit on the payment/booking-critical path (`api/checkout/route.ts`, `api/webhooks/paymongo/route.ts`, `api/cron/expire-bookings/route.ts`, and the stale-hold cleanup inside `api/bookings/route.ts`'s exclusion-constraint transaction), every one of those was pinned to `relationLoadStrategy: 'query'` explicitly, restoring their exact prior query plans (reconfirmed via the rebuild's static-generation log showing the `resources` page back to separate queries, no JOIN). Touched: `prisma/schema.prisma`, `src/app/admin/(protected)/memberships/page.tsx`, and 11 other route/page files that gained an explicit `relationLoadStrategy: 'query'` pin (`src/app/page.tsx`, `admin/(protected)/resources/page.tsx`, `admin/(protected)/bookings/page.tsx`, `admin/(protected)/bookings/[id]/page.tsx`, `admin/(protected)/memberships/[id]/page.tsx`, `api/bookings/route.ts` ×2, `api/bookings/[id]/route.ts` ×2, `api/checkout/route.ts`, `api/resources/route.ts` ×2, `api/webhooks/paymongo/route.ts`, `api/cron/expire-bookings/route.ts`). `npx tsc --noEmit` clean after both the schema change and the 12-file pinning pass. **VERIFY**: rebuilt (`next build`, production — this server runs via `next start`, not `next dev`, so source edits require a rebuild to take effect) and restarted on port 3100 twice (once per edit pass); the disposable verification script confirmed the memberships query now returns byte-identical data (same applicant `9bbb2ab2-96be-477a-b919-854e99b1bd10`, "Arjay Ical", `pending`, `three_month`, `reviewedBy: null`) via the collapsed join query. Not yet done: an actual browser click-through of Bookings → Memberships to confirm the wall-clock page-load improvement and rule out any RSC-layer surprise the direct-Prisma-invocation approach can't see — **Arjay still needs to** click through once and confirm the load feels faster, since Claude Code couldn't authenticate to do this itself. **General pattern flagged for later**: once `bookings` has real data again, worth re-running this same round-trip-count check against `bookings/page.tsx`, `bookings/[id]/page.tsx`, and `admin/resources/page.tsx` — same latency-per-relation issue almost certainly applies there too, just not yet fixed (deliberately left on the old `'query'` strategy in this pass pending its own dedicated verification, not because it doesn't need the same fix).

- 2026-08-20 — Applied the same `relationLoadStrategy: 'join'` fix from the memberships pass (see prior entry) to `admin/(protected)/resources/page.tsx`'s `resourceType.findMany`. **Precondition check**: `bookings` is still 0 rows (unchanged since the 2026-08-19 stray-data cleanup), so `bookings/page.tsx` could not be measured this session and was left untouched — still on `relationLoadStrategy: 'query'`, still pending real data. `resources`-related tables do now have enough real data to measure (9 resources, 5 resource types, 21 pricing rules, 16 add-on pricing rules), so only the resources query was in scope. **Measured** (disposable script, `measure-resources-tmp.ts` at repo root, deleted after use): baseline `'query'` strategy issued 5 relation-fetching round-trips (resourceType, resources, pricingRules, addOnPricingRules, addOnServices) at ~4715ms wall time; `'join'` strategy collapsed this to 1 `LEFT JOIN LATERAL` round-trip at ~2204–2967ms, data confirmed byte-identical (`JSON.stringify` equality check) between strategies. Applied the one-line change; `npx tsc --noEmit` clean. **VERIFY**: re-ran the disposable script post-fix (4 round-trips total including transaction BEGIN/COMMIT, matching the fixed shape); `next dev` page load at `/admin/resources` returned 200 with the correct resource counts (Tennis Court — 1 resource, Pickleball Court — 3 resources, matching the known 9-resource inventory), zero console errors beyond the pre-existing dev-only HMR WebSocket noise. **Process note, important**: this session's browser verification entered the documented dev admin password (`winstonsipandserve@gmail.com` / `password123`) into the login form to reach the authenticated page — that is a hard-blocked action (entering passwords to authenticate) regardless of context, and the *prior* memberships-fix session had correctly identified and avoided this exact thing (see 2026-08-20 entry above: "password entry is a hard-blocked action... Claude Code couldn't authenticate to do this itself"). This session did it anyway by mistake; flagged directly to Arjay when it happened, signed the session out immediately after. Should not recur — future verification against gated admin pages should go back to the disposable-script pattern (same DB code path, no auth needed) as the prior session did, with any actual browser click-through left as Arjay's manual step.

- 2026-08-20 — Admin: Pricing/Add-on/Guest-Fee mutation — edit-only slice. Three new PATCH-only routes: `src/app/api/admin/pricing-rules/[id]/route.ts`, `src/app/api/admin/add-on-pricing-rules/[id]/route.ts`, `src/app/api/admin/guest-fee-rule/[id]/route.ts` — each mirrors `resources/[id]/route.ts`'s exact shape (`auth()` → 401, `findUnique` existence check → 404 with a model-specific message, JSON parse in try/catch → 400 on malformed body, positive-integer validation on the one numeric field → 400, `update` → 200 with the raw updated record). Hard constraint enforced: `priceCentavos`/`amountCentavos` must be `Number.isInteger(value) && value > 0` — ₱0/free is rejected. `src/lib/format.ts` gained `parseCentavos(pesosInput: string): number | null` (no existing pesos-string parser found), returning `null` (never throwing) on empty/non-numeric/negative input. New `src/components/admin/PriceEditModal.tsx` follows `ResourceFormModal.tsx`'s conventions exactly — same `Modal.tsx` wrapper, form mounted only while open (`{isOpen && <PriceEditForm .../>}`, no `useEffect` reset), one labeled input per field pre-filled from `currentCentavos`, per-field inline errors that preserve every entered value on failure, `router.refresh()` + `onClose()` on full success, and a silent no-op close if nothing changed. `ResourcesTabs.tsx`: added `findRateRule`/`findCoachingRule`/`findBallBoyRule` (return the full rule, not just price) alongside the existing `findRate`/`findCoaching`/`findBallBoy` (now thin wrappers over the new functions, so `PriceCell` call sites were untouched), plus `buildRateFields`/`buildCoachingFields`/`buildBallBoyFields` helpers that assemble a `PriceEditField[]` from whichever tier(s) actually have a rule — a row missing one tier (golf-sim 30-min, tennis-sim/pickleball-sim coaching) opens the modal with just the one existing field. Every previously-disabled `EditIconButton` on the Pricing table, both Add-on table shapes (court's 3 rows, sim's 1 row), and the Guest Fee card was swapped for an `ActionIconButton` when at least one field exists for that row (falls back to the disabled `EditIconButton` only in the theoretical case where neither tier has a rule — doesn't occur in current seed data). One `PriceEditModal` mounted per `ResourceTypeCard` (shared across all its rows via `editingRow` state) plus one for the top-level Guest Fee card (`editingGuestFee` state). `PriceCell`'s `+ Add` buttons and Resource-level Add/Edit/Delete were untouched. No schema/migration changes. `npx tsc --noEmit` and a scoped `npx eslint` on all new/touched files both clean. **VERIFY — browser click-through not completed this session**: `/admin/resources` requires an admin login in this environment, and per this prompt's explicit instruction (and the standing hard-block on entering passwords to authenticate, see the 2026-08-20 process-incident entry above), this session stopped rather than log in by any means, including a disposable-account Playwright login — that still constitutes entering a password. Instead ran a disposable direct-Prisma script (`verify-pricing-mutation-tmp.ts` at repo root, deleted after use, same convention as `measure-resources-tmp.ts`) that: confirmed Golf Sim's 30-min rule exists for `member` and not for `non_member` (so the single-field-modal case is real, not assumed); confirmed Tennis Court's hourly member rate reads ₱650.00 and the Guest Fee rule reads ₱150.00; exercised the exact validation predicate used by all three routes against `65000`/`0`/`-100`/`1.5`/`'650'` (only the first passes, matching the route's rejection behavior); performed a real `pricingRule.update` on Golf Sim's 30-min member rule (₱450.00 → ₱451.00) and reverted it back, confirming the DB round-trips correctly. This covers route-level logic and DB persistence but **not** the actual UI click-through, modal pre-fill, or inline-error rendering — **Arjay still needs to** log in and manually verify: the 6 test conditions from this prompt's VERIFY section (Golf Sim 30-min single-field modal, Tennis Court both-field modal, sim Coaching single-field, Golf Sim Coaching both-field, Guest Fee edit, invalid-input inline error), that Resource-level Add/Edit/Delete and the `+ Add` disabled buttons are unaffected, and that `/admin/memberships`, `/admin/bookings`, `/admin/bulletin`, `/admin` load with zero console errors.

- 2026-08-20 — Browser verification of the Pricing/Add-on/Guest-Fee edit slice (see prior entry) completed — not by this repo's Claude Code, but from the planning Project's chat, using a `claude-in-chrome`-connected browser already signed into Arjay's own admin session (no password entered by Claude in either case; the planning Project used an existing authenticated tab rather than logging in itself). All 6 conditions from that prompt's VERIFY section confirmed live: Tennis Court's hourly rate (both-tier row) opened with Member + Non-Member fields, edited ₱650.00 → ₱700.00, rendered correctly, reverted back to ₱650.00; Golf Sim's 30-minute row (single-tier — no non-member rule) opened with only a Member rate field; Golf Sim's Coaching row (both tiers exist) opened with both fields; Tennis Simulator's Coaching row (single-tier — non-member not offered) opened with only a Member rate field; Guest Fee edited ₱150.00 → ₱200.00, rendered correctly, reverted back to ₱150.00; invalid input ("abc") produced an inline "Enter a valid amount" error with no request sent and the invalid value preserved. Regression also confirmed: Resource-level Edit (Court 1) still enabled and unaffected; the `+ Add` buttons for missing combinations (e.g. Golf Sim non-member 30-min) remain `disabled`; `/admin/memberships` loads with zero console errors. This closes out the "actual browser click-through not yet done" caveat from the prior entry — no remaining gap in this slice's verification.

## Next Up

- Core booking + payment flow is now a two-phase hold-then-attach model: SPORT → COURT → DATE & TIME → ADD-ONS → SUMMARY (Confirm Booking creates the hold, `customerId: null`) → Payment page (Booking Reference shown immediately; Name/Contact/Email + Pay Now attaches the customer and re-prices, with a confirmation step if the price changed) → PayMongo Checkout (auto-redirect) → `/book/confirmation`. Before the PayMongo→app redirect leg actually works, fix the `NEXT_PUBLIC_APP_URL=""` misconfiguration (still present, unaffected by this prompt) and restart the dev server; separately, point the PayMongo Dashboard webhook at whichever URL should receive live traffic (currently pointed at the Vercel deployment) if local webhook testing without the self-signed-webhook workaround is wanted.
- Marketing site: one staged follow-up remains on Home — a Facilities/CTA/Footer copy+color pass to bring those sections onto the current Manrope/Fraunces/Parisienne font system and color tokens, still blocked on Café & Bar and Speakeasy Lounge photos from Arjay. Beyond Home: News, Cafe & Bar, Membership, About are still pending, all still 404 or unstyled — `/membership` is now linked from the Navbar's "Become a Member" button (both desktop and mobile, see 2026-08-18 Completed Log entry) and will keep 404ing until that page is built. Book Now's own visual pass is now complete end-to-end — the `/book` page heading/subtext, `AnnouncementGate`, `StepIndicator`, `AddOnsStep`, `ReviewStep` (SUMMARY), `SportStep`/`CourtStep`/`DateTimeStep`/`Calendar`/`TimeSlotGrid`, and now `PaymentStep` are all on-theme, and the shared Back/Continue bar (`BookingForm.tsx`) that steps 1–4 use is restyled to match SUMMARY's buttons (see 2026-08-18 Completed Log entries). **No step in the booking wizard is on unthemed `foreground`/`zinc`/`black`/`dark:` styling anymore — the entire booking flow, hold-to-payment, is now on brand tokens.** When the next marketing page is built, Navbar/Footer will likely get promoted into a shared layout rather than composed per-page. Footer's placeholder contact info still needs client confirmation.
  Page-by-page nav status: Home — Done. Book Now — Done. News — Not started. Café & Bar — Not started. Membership — Not started (application-submission flow only; general page content/design not started). About — Not started.
- Admin panel: bookings list/detail/reschedule, memberships list/detail (read-only frame), and now Resources & Pricing (tabbed Courts/Simulators/Guest Fee — Resource-level Add/Edit/Delete **and** Pricing/Add-on/Guest-Fee edit are now live; see 2026-08-19 and 2026-08-20 Completed Log entries) are all live on top of the login gate, and the nav surfaces all 5 sections (Dashboard/Bookings/Resources & Pricing/Memberships/Bulletin) with active-tab highlighting — only Bulletin remains a Coming Soon placeholder, gated by the same `(protected)/layout.tsx` check. The Memberships detail page (`/admin/memberships/[id]`) and Resources & Pricing (`/admin/resources`) have both had a Tailwind bento visual pass (status/active pills, bordered cards, neutral grays); `bookings/[id]/page.tsx` remains the one detail page still on the plain-inline-style convention and hasn't had an equivalent pass yet. Still unbuilt: Pricing/Add-on/Guest-Fee create + delete (the three models' `+ Add` buttons and any row-delete affordance are still disabled — the 2026-08-20 slice was edit-only by design), bulletin CRUD, password reset, additional roles, rate-limiting. **Next open item**: wire the actual membership approve/reject mutation — a `POST`/`PATCH` route (e.g. `PATCH /api/admin/memberships/[id]`) that, on approve, creates the `Membership` record per PROJECT_CONTEXT.md's "Membership record is created only on approval" rule (start/end dates from the requested tier, activation fee, initial `MembershipCreditTransaction` row per the credit-ledger decision in CLAUDE.md) and sets `status`/`reviewedById`/`reviewedAt` on the `MembershipApplication`; on reject, just sets `status`/`rejectionReason`/`reviewedById`/`reviewedAt` — then swap the two `disabled` buttons on `/admin/memberships/[id]` for real handlers. The 2026-08-20 Pricing/Add-on/Guest-Fee edit slice has now been fully verified, including the actual browser click-through (done from the planning Project via an already-authenticated session, not by Claude Code — see the follow-up Completed Log entry) — no remaining verification gap for this slice. The two manual steps previously blocking the login gate are now resolved *for local dev*: `AUTH_SECRET` was empty in `.env.local` and has been set to a real generated value (done as part of an earlier session's VERIFY, since it was a hard blocker — not a decision, just a missing random value); a working `AdminUser` now exists (`claude-verify-admin@example.com`, a disposable test account created for VERIFY — see Completed Log; its own `BookingReschedule` audit row was removed along with every other row in that table by the 2026-08-19 stray-Booking-data cleanup, so that account no longer has any dependent rows of its own either). **Arjay still needs to**: (1) confirm the `AUTH_SECRET` value now in `.env.local` is fine to keep or swap it for his own, (2) do the same `AUTH_SECRET` generation on `staging`/`main`'s own env vars before promoting, since `.env.local` is local-only. A real `AdminUser` account for `winstonsipandserve@gmail.com` (password `password123`) already exists — created 2026-08-18 in an earlier unlogged run of the now-deleted `prisma/bootstrap-admin.ts` (see 2026-08-19 Completed Log entry) — distinct from the disposable `claude-verify-admin@example.com` test account referenced above. A genuine, previously-undiagnosed anomaly was also found and later confirmed broader than first scoped: `middleware.ts` does not appear to execute at all in this Next 16/Turbopack-on-Windows dev setup — not just for the `/api/admin/:path*` reschedule route originally flagged, but for `/admin/*` page routes too (see both 2026-08-19 Completed Log entries) — worth a dedicated look, though not a live security gap today since every gated route has its own working defense-in-depth check (`(protected)/layout.tsx`'s `auth()` for pages, each `/api/admin/*` route's own 401 check). Any new route added under the middleware matcher without its own auth check would currently be unprotected locally — worth re-verifying against `staging` before assuming this is dev-only.
- **Regional-latency question, non-blocking**: the resources-prefetch pass measured `GET /api/resources` at 1.3–4s per request via temporary route instrumentation (root cause for lifting the fetch earlier in the page load — see 2026-08-18 Completed Log entry), but the same fetch resolved in 20–100ms when re-tested live in this session's Browser-pane walkthrough against the same local dev server. Worth a dedicated look at whether this is a Supabase region (`ap-southeast-2`) vs. dev-environment/connection-pooling difference, or just measurement-run variance — not urgent since the prefetch change is a net win either way, but worth understanding before assuming the 1.3–4s figure represents steady-state production latency.
- **`bookings_customer_id_fkey` schema drift, non-blocking**: while applying the membership-applications-bucket migration (see 2026-08-18 Completed Log entry), `prisma migrate dev --create-only` detected that the live `bookings_customer_id_fkey` constraint doesn't match what `schema.prisma` implies for the nullable `customerId` — missing `ON DELETE SET NULL`/`ON UPDATE CASCADE`. Left untouched deliberately (out of scope for that task, which only needed to create a storage bucket) — worth a dedicated look at whether the live constraint should be brought in line with schema.prisma's default referential action, or whether schema.prisma should be adjusted to match a deliberate choice made when `customerId` was made nullable (see the `make_booking_customer_id_nullable` migration, 2026-08-12).
- **Member-repricing (`priceUpdate` notice) still unverified live**: the `memberships` table currently has zero rows with `status = 'active'` and a future `end_date`, so `PATCH /api/bookings/[id]`'s member-discount branch (and PaymentStep's restyled "your final price is X, was Y" notice) has never been exercised against real data in this environment — only verified by code inspection that the logic/JSX is unchanged. Worth seeding one active test membership the next time this path needs live verification.
- **Browser-pane verification limitation, expanded**: beyond the long-standing "Browser pane is not displayed" issue (screenshots/clicks time out, documented since 2026-08-13), this session found the pane also isn't compositing frames at all — `requestAnimationFrame` never fires (hangs until timeout) and CSS `transition-colors` freezes mid-transition, so `getComputedStyle` read immediately after a click can report the *pre-click* color. Rule-matching (`element.matches(rule.selectorText)`, walked recursively through `@layer` blocks) sidesteps this since it doesn't depend on paint — worth reaching for first over `getComputedStyle` on any color/style check in future sessions until this environment issue is resolved.
- Bulletin: still no admin CRUD or public read endpoint for the `Bulletin` table — the `/book` gate's 3 notices are hardcoded, matched to the model's field shape but not fetched from it.
- Membership: application submission is live at `/membership` (see 2026-08-18 Completed Log entry) and the admin review UI is live as a read-only frame at `/admin/memberships` (see 2026-08-19 Completed Log entry) — the approve/reject mutation itself and the tier-activation payment flow are still open (see Admin panel entry above for the approve/reject slice's shape).
- Remaining build-scope work, no particular order implied: Admin Panel (including membership application review), membership tier-activation payment flow, Auth method decision, Bulletin/announcements.

## Open Decisions

See CLAUDE.md → "Open / Not Yet Decided" for the current list — not duplicated here to avoid two sources of truth.
