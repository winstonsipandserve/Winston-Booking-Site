# PROGRESS.md

Build status and change log for Winston Sip and Serve. Updated as a standard part of every Claude Code prompt going forward — see CLAUDE.md's Development Workflow for the process this supports.

**Entry format (going forward):** Completed Log entries should be 1–3 sentences — what changed, why (only if the decision isn't obvious), and current status. Skip VERIFY step-by-step narration, exact test data/scripts, row counts, and routine confirmations (`tsc` clean, `git status` matches scope, cleanup done) — those are true of every prompt by convention, not per-entry information. Full pre-2026-08-28 history, including every original VERIFY pass, is preserved verbatim in `PROGRESS_ARCHIVE.md` — grep it for a date or keyword and read only the matched lines; don't read it in full.

---

## Build Status

- **Stack**: Next.js 16 (App Router) + TypeScript, Prisma 6.19.3, Supabase (Postgres/RLS/Storage), Auth.js v5, PayMongo (Checkout Sessions API), Resend, Tailwind v4 (CSS-first `@theme`, no config file). Deployed to Vercel.
- **Schema**: 20 Prisma models (16 original, including `Bulletin` — later reshaped/unified, not newly added — + `MemberActivationToken`, `PasswordResetToken`, `MembershipPayment`, `CheckInLookupAttempt`). `booking_no_overlap` exclusion constraint and RLS deny-all (all tables, `anon`+`authenticated`) both live as isolated migrations, master SQL under `prisma/manual-sql/`. Resource inventory: 1 tennis court, 3 pickleball courts, 1 tennis sim, 2 pickleball sims, 2 golf sims.
- **Booking + payment flow**: Complete end-to-end, now member-aware. Anonymous: announcement gate → 5-step wizard → hold created on Confirm (`customerId: null`, provisional non-member pricing) → Payment page (reference shown immediately; Pay Now attaches customer, always re-prices at non-member rate regardless of any matched `Customer`'s membership status, confirms if price changed for other reasons e.g. a live pricing edit) → PayMongo Checkout (auto-redirect) → `/book/confirmation`. Member rate and F&B credit are only available via the session-aware member path below — see the 2026-09-01 Completed Log entry. Logged-in member with an active `Membership`: `/book` detects the session server-side, hold creation prices at the member rate and attaches `customerId` directly (no PATCH round-trip), member-tier durations/pricing surface throughout the wizard, guest-count UI is hidden, and Payment skips straight to a "Booking under {name} ({email})" summary. Every step, including Payment, is on brand tokens. The session-aware member direct-booking path now also supports full-or-nothing F&B-credit redemption: if the member's `Membership.creditBalanceCentavos` fully covers the grand total, `POST /api/bookings` confirms the booking instantly (no PayMongo checkout) and debits the credit atomically — see the 2026-08-31 Completed Log entry. Active members whose credit balance won't fully cover a booking's total now see an advisory modal before confirming, explaining they'll be charged in full via PayMongo with their credit balance left untouched — see the 2026-09-01 Completed Log entry.
- **Marketing site**: All 6 pages done — Home, Book Now, News, Café & Bar, Membership (info + `/membership/apply`), About. Home verified against a real mock UI. Navbar `fixed`, transparent-over-Hero (`bg-brand-light/0`, fully transparent — corrected 2026-09-02 from a faintly-visible `/[0.01]`) → solid-on-scroll on `/`, `/about`, `/membership`, `/cafe-bar`; `/book` and `/news` have no hero and instead force the navbar permanently solid (`FORCE_SOLID_PAGES` in `Navbar.tsx`), rendering their content directly below `<Navbar />` with no extra padding wrapper (their own root elements' padding already clears it) — see the second and third 2026-09-02 Completed Log entries. The mobile hamburger icon now follows `headerSolid` (dark on solid, light unscrolled-over-Hero) instead of a static dark color. `/book` and `/news` now have `Navbar`/`Footer` and are on brand tokens end-to-end, same as every other page. `/book`'s pre-wizard announcement gate is now vertically centered with more breathing room (wizard itself unaffected); `/news` gained a 7-pill category filter (All + 6 categories) backed by a new shared `CATEGORY_LABELS` map, and its cards' border/category badges now use that same human-readable labeling — see the fourth and fifth 2026-09-02 Completed Log entries. Placeholder content pending client input: Home Hero copy, About's Our Story, Footer contact info, Café & Bar hours/photos, News social links.
- **Auth**: Email/password, live for admin (`credentials` provider) and member (`member-credentials` provider), JWT sessions, no adapter. Every `/api/admin/*` route + `(protected)/layout.tsx` require `role === 'admin'`. `/account` gates on `role === 'member'`, shows real `Customer`/`Membership`/`Booking` data. Member sign-in on `/login` is client-side `signIn()` (Navbar updates immediately, no tab-switch needed) with a password show/hide toggle; the public Navbar's Sign Out (desktop + mobile) now requires confirming a modal first — see the 2026-08-31 Completed Log entry.
- **Forgot password**: Live end-to-end — `PasswordResetToken` (1hr expiry), enumeration-safe `/api/auth/forgot-password` (always 200), `/api/auth/reset-password`, `/forgot-password` + `/reset-password` pages.
- **Membership application/approval**: Submission (`/membership/apply`) → admin review (`/admin/memberships`) → approve/reject mutation, live. Approval no longer creates a `Membership` directly — it only flips status + sends a "complete your payment" email with a `/membership/pay/[id]` link. `Membership` + credit ledger + `MemberActivationToken` are now created by the PayMongo webhook on confirmed tier-activation payment. Rejection is unchanged — sends a branded email with the admin's reason. Staff now also receive a `[Membership Application]` notification email on every successful submission — see the 2026-09-01 Completed Log entry.
- **Membership tier-activation payment**: Live end-to-end — `POST /api/membership-payments` (create-or-resume PayMongo checkout for an approved application, new `MembershipPayment` model) and public `GET /api/membership-applications/[id]` (status-check endpoint, tier/status/hasMembership only, no PII) back a customer-facing `/membership/pay/[id]` (ledger + Complete Payment) and `/membership/pay/confirmation` (polling, mirrors `/book/confirmation`). Self-service renewal for already-expired members is now also live — session-gated `POST /api/account/membership-renewal`, public `GET /api/membership-payments/[id]`, `/account/renew` + `/account/renew/confirmation`, `MembershipStatusCard`'s "Renew Membership" CTA — see the 2026-08-29 Completed Log entry. Admin-side manual fallback also live — `POST /api/admin/memberships/[id]/send-renewal-link` + public `POST /api/membership-payments/[id]/checkout` back `/membership/renew/[id]`, surfaced via a "Send Renewal Link" button on `/admin/memberships/[id]` (only when that customer's membership is expired) — see the second 2026-08-29 Completed Log entry. `handleMembershipPaymentWebhook` now also sends a staff notification on both the first-time-activation and renewal payment paths (`[Membership Payment]`/`[Membership Renewal]`) — see the second 2026-09-01 Completed Log entry.
- **Transactional email**: `buildBrandedEmail` (shared layout) has 13 consumers — activation, rejection, forgot-password, booking confirmation, staff booking notification, staff membership-application notification, staff membership-activation notification, staff membership-renewal notification, membership-payment-link, membership-expiry-reminder, membership-expired, membership-renewal, renewal-payment-link — all built and live, booking confirmation (and its staff-notification counterpart) firing from the real PayMongo `payment.paid` webhook, activation firing from that same webhook's membership-payment branch (first-time only), and renewal firing from the same branch's new renewal path (with an optional/always-present receipt block: activation fee / F&B credit / total paid). Sending from verified domain `no-reply@winstonsipandserve.club`, reply-to `winstonsipandserve@gmail.com`. Membership expiry/renewal reminders are now live — see the 2026-08-28 Completed Log entry. Staff booking notification (`[Booking]` subject prefix) is from 2026-08-31; staff membership-application notification (`[Membership Application]` subject prefix) is from 2026-09-01; staff membership-payment-activation (`[Membership Payment]`) and staff membership-renewal (`[Membership Renewal]`) notifications are new — see the second 2026-09-01 Completed Log entry. First-time membership activation emails now also carry a branded PDF membership certificate (`@react-pdf/renderer`) in place of the inline perks list, and the staff `[Membership Payment]` email carries a PayMongo Reference row — see the third 2026-09-01 Completed Log entry.
- **Admin panel**: Dashboard (5 stat cards, 3 recharts charts, Recent Bookings + Pending Applications lists — UI-complete, now wired to real Prisma queries via `getDashboardData()`), Bookings (list/detail/reschedule; list's Filter is a neutral (white/gray) modal, not brown), Memberships (list/detail + approve/reject; list's status filter now splits on the derived display status — Pending/Awaiting Payment/Active/Expired/Rejected — not the raw `ApplicationStatus` enum; the 6-option pill row is now a `MembershipsFilterModal` "Filter" button + non-functional "Export" stub button, mirroring Bookings' layout), Resources & Pricing (tabbed Courts/Simulators/Guest Fee — resources are edit (pricing) + disable/enable only, no create/delete; Pricing/Add-on rows now have full create/edit/delete; Guest Fee stays edit-only, permanently, by design), Bulletin (full CRUD + optional image upload; 6-category enum, each with its own required-field rules — `affectedFacility`/`impact`/`action`/`eventStartAt` required for all categories, `imageUrl`/`eventEndAt`/`expiresAt`/CTA required only for categories that need them — surfaced on `/news`/`AnnouncementGate` with shared `expiresAt`/sort logic; its Add/Edit modal is now neutral-styled with Category+Title sharing one row), Check-In (camera QR scan + a manual 6-digit code fallback, `Customer.checkInToken`/`checkInCode` always created/rotated together; code-entry lookups are now rate-limited, 10 failures per rolling 5-minute window per admin; `/admin/check-in` now requires an explicit Scan-or-Enter-Code choice before either mounts — neither the camera nor the code form auto-starts on page load — see the 2026-09-01 Completed Log entry) — all live, gated by `(protected)/layout.tsx`. Not built: admin-side password reset, additional roles. Shared `Modal` component now supports an optional `variant: 'brand' | 'neutral'` prop (default `'brand'`, unchanged) — admin's Bookings filter, Memberships filter, and Bulletin form modals opt into `'neutral'`; every public/member-facing modal (Navbar sign-out, `/account`'s QR modals, `/book`'s insufficient-credit advisory, etc.) still renders on the original brand styling by default.
- **Known dev-environment quirks** (mirrors CLAUDE.md's own Known Issues): `middleware.ts` does not execute locally (Next 16/Turbopack/Windows) — every gated route has its own working `auth()` check doing the real enforcement; re-verify against `staging` before assuming this is dev-only. The Browser pane doesn't composite frames in this environment — prefer DOM/class-based checks over `getComputedStyle`/screenshots for VERIFY.

---

## Completed Log

- 2026-08-10 — CLAUDE.md scaffolded: identity, tech stack, architecture decisions, POS scope, dev workflow (`26c0991`)
- 2026-08-10 — Branch Promotion Policy added (`c7e5c5c`)
- 2026-08-10 — PROJECT_CONTEXT.md created: resource types, membership, cancellation rules; pricing left pending client rate table (`0a4d4cc`)
- 2026-08-10 — PROJECT_CONTEXT.md updated with finalized pricing/add-on rate tables (`1e4daf4`)
- 2026-08-10 — PROGRESS.md created
- 2026-08-10 — Pricing model confirmed DB-driven (`PricingRule`/`AddOnPricingRule`), admin-editable
- 2026-08-10 — Confirmed non-members book without an account (name/phone/email only); only members log in
- 2026-08-10 — `MembershipApplication.customerId` confirmed required — resolved via look-up-or-create by email, same pattern as bookings
- 2026-08-10 — Admin login resolved as individual staff logins, not shared; `reviewedBy`/`performedBy` will be real FKs
- 2026-08-10 — `BookingReschedule` confirmed createdAt-only/immutable (audit integrity) — corrections via a new row, not edits
- 2026-08-10 — `Payment.bookingId` confirmed a direct nullable FK to `Booking`, not polymorphic — trades a small future POS migration for DB-enforced integrity now
- 2026-08-10 — `MembershipCreditTransaction` ledger model added — `creditBalanceCentavos` is a cached total backed by an immutable transaction log
- 2026-08-10 — Next.js scaffold created (TypeScript, Tailwind, App Router)
- 2026-08-10 — Guest fee confirmed as its own admin-editable `GuestFeeRule` table, not hardcoded; membership-payment↔`Payment` linkage flagged open
- 2026-08-10 — Prisma schema created (16 models); `booking_no_overlap` exclusion constraint applied in an isolated migration; resource inventory seeded (5 types, 9 resources; pricing deferred). Prisma pinned to 6.19.3 — 7.x removed inline `datasource url` support
- 2026-08-10 — RLS enabled on all 16 tables, deny-all for `anon`/`authenticated`, isolated migration
- 2026-08-11 — Booking hold duration (10 min) + hybrid expiry (expire-on-write primary, daily cron secondary) documented as an Architecture Decision
- 2026-08-11 — Pricing seeded; Prisma client singleton added; `POST /api/bookings` implemented (validation, pricing, expire-on-write hold, 409 on overlap); cron route (`CRON_SECRET` bearer auth) + daily Vercel schedule added
- 2026-08-11 — `GET /api/resources` added; booking form UI built (resource/duration cascade, live non-member price estimate, 201/409/400 branching); homepage simplified to name/description/Book Now link
- 2026-08-11 — Booking flow restructured into announcement gate + 5-step wizard (structure only)
- 2026-08-11 — DATE & TIME step: real calendar + availability grid, business-hours enforcement (6am–10pm PH) added server-side; `booking-hold.ts`/`business-hours.ts` extracted; `GET /api/availability` added
- 2026-08-11 — `TimeSlotGrid`: selection highlight now spans the full occupied duration, not just the clicked slot
- 2026-08-11 — `TimeSlotGrid`: grid always renders the full 6am–10pm range regardless of duration (previously stopped early); slots that would run past close are marked `exceedsClosing`
- 2026-08-11 — Court duration options extended 1–3hr → 1–4hr; simulator duration dropdown fixed to only show non-member tiers (was leaking a member-only golf-sim tier)
- 2026-08-11 — Wizard split 5→6 steps: Ball Boy/Coaching moved out of DETAILS into a new ADD-ONS step; REVIEW relabeled SUMMARY
- 2026-08-11 — Ball Boy correctly gated to court types only in `AddOnsStep` (was ungated despite docs saying court-only)
- 2026-08-12 — Guest count relocated from DETAILS to ADD-ONS, alongside Ball Boy/Coaching
- 2026-08-12 — Add-on backend wiring: `AddOnService` (2 rows) + `AddOnPricingRule` (16 rows) seeded — non-member Tennis/Pickleball Sim coaching intentionally has no row ("no row = not offered")
- 2026-08-12 — ADD-ONS step wired to live add-on pricing from `GET /api/resources`, replacing the "Coming soon" placeholder
- 2026-08-12 — Doc-only: clarified PayMongo test credentials in `.env.local` are Arjay's personal test account, not the client's
- 2026-08-12 — PayMongo Checkout Session backend implemented (hosted Checkout Sessions, not Payment Intents/Elements) — documented as an Architecture Decision. `payment_method_types` turned out to be a required field on the API, not optional as first assumed
- 2026-08-12 — Wizard wired to real PayMongo Checkout + polling confirmation page — booking+payment flow complete end-to-end. `GET /api/bookings/[id]` + `/book/confirmation` (2s poll, 15 attempts) added
- 2026-08-12 — Booking reference (`Booking.id`) surfaced in our own UI (`ReviewStep`, `BookingConfirmation`), not just PayMongo's checkout page. Gap flagged: confirmation page's `pending_payment` state still shows no reference (out of scope, not fixed)
- 2026-08-12 — SUMMARY's auto-redirect to PayMongo replaced with a manual "Continue to Payment" pause screen — the auto-redirect fired too fast to ever be seen/tested. Environmental findings, not fixed here: `NEXT_PUBLIC_APP_URL=""` broke the PayMongo→app redirect (needed a real URL + dev-server restart — **confirmed fixed, `.env.local` now has `http://localhost:3000`**); ngrok free-tier interstitial page; PayMongo dashboard webhook pointed at the Vercel deployment, not the local tunnel (worked around via a self-signed webhook for local testing)
- 2026-08-12 — `Booking.customerId` made nullable — two-phase flow: hold created (`customerId: null`, provisional pricing) before customer known; `PATCH /api/bookings/[id]` resolves customer + re-prices member-aware once Name/Phone/Email are collected. `booking-pricing.ts`/`customer-resolution.ts` extracted as shared logic
- 2026-08-12 — DETAILS step removed; Name/Contact/Email + booking creation moved to a post-SUMMARY Payment page; checkout hand-off reverted to immediate redirect (5 steps total)
- 2026-08-12 — PayMongo line items consolidated to one row per booking (was one + one per add-on); `payment_method_types` expanded to `['card','gcash','grab_pay','paymaya']`, confirmed live-enabled for the test account
- 2026-08-12 — **Critical webhook bugfix**: real PayMongo events use `payment.paid`, not `checkout_session.payment.paid` as previously assumed — every prior "webhook confirmed working" entry had only been tested against the wrong payload shape; this was the first genuine real-payload confirmation. Also fixed `Payment.paidAt` to use the payload's actual `paid_at`, not server-processing time
- 2026-08-13 — Home page rebuilt with the real theme (design tokens, Navbar, Footer, all sections, live resource-count wiring via direct Prisma query)
- 2026-08-13 — Home polish pass: reveal-on-scroll, CTA banner decorative blobs, image variety, hover micro-interactions
- 2026-08-14 — Home redesigned against a client reference image: tokens split (`brand-light` vs `background`), Choose Your Sport/Testimonials removed, Hero/StatsBar/HowItWorks/Facilities (renamed from Gallery) rebuilt. Confirmed deviation: Navbar stays sticky above Hero rather than moving below it, per the reference. Hero/Footer copy flagged as placeholder pending client confirmation
- 2026-08-14 — Navbar/Hero refinement pass (5 items vs. reference — icon set, spacing, etc.)
- 2026-08-14 — Navbar/Hero/StatsBar/CtaBanner follow-up pass (4 items vs. latest reference screenshots) — social icons/My Account pill removed from Navbar, replaced with a Book Now link
- 2026-08-14 — Doc-only: newly installed UI/animation skills documented in CLAUDE.md
- 2026-08-15 — Navbar/Hero visual-match pass vs. a new reference screenshot — real brand logo asset swapped in
- 2026-08-15 — Font system replaced sitewide: Lora → Manrope (body) + Parisienne (script wordmark), Fraunces added as `font-serif` (not yet used); Navbar simplified to emblem-only logo, buttons removed
- 2026-08-15 — StatsBar/How It Works copy+color pass
- 2026-08-17 — Navbar link styling (uppercase, tracking) + CtaBanner dot-pattern/copy/button fixes
- 2026-08-17 — `/book`'s gate + heading moved onto brand tokens (cream/Fraunces/Manrope/brand-dark)
- 2026-08-17 — How It Works equal-height card fix (missing `h-full` on the `Reveal` wrapper)
- 2026-08-18 — SPORT step: added non-member starting-price labels
- 2026-08-18 — DATE & TIME UI pass: duration button group, calendar past-dates styling, time-slot borders
- 2026-08-18 — StepIndicator active-label visibility bugfix (CSS color issue, not missing markup)
- 2026-08-18 — StepIndicator + AddOnsStep moved onto brand tokens
- 2026-08-18 — ReviewStep (SUMMARY) redesigned as a ledger-style card
- 2026-08-18 — SPORT/COURT/DATE & TIME moved onto brand tokens + a pre-selection bugfix + shared Back/Continue bar restyled
- 2026-08-18 — AddOnsStep: Coaching pax count decoupled from guest count
- 2026-08-18 — PaymentStep redesigned to match; new shared `BookingSummary` component extracted (used by SUMMARY and Payment page)
- 2026-08-18 — `/api/resources` fetch lifted to page-load instead of gate-continue-click, so it runs concurrently with the announcement gate
- 2026-08-18 — AnnouncementGate gained an inert "Already a member? Sign in" placeholder line; SPORT gained full simulator tier pricing; selected states across SPORT/COURT/DATE & TIME made bolder (`border-2`/10%-tint)
- 2026-08-18 — New generic `Modal` component built; SPORT step's pricing moved off-card into it
- 2026-08-18 — New `Icons.tsx` (hand-authored inline SVGs) — sport icons on SPORT, badge icons on SUMMARY's ledger rows
- 2026-08-18 — Navbar CTA buttons reinstated: "Book a Court" + "Become a Member" (desktop + mobile)
- 2026-08-18 — Membership Application form built: public `/membership` page, 3 gov-ID uploads to a private Storage bucket, creates a pending `MembershipApplication` — no payment/activation/admin-review yet
- 2026-08-18 — Bugfix: `POST /api/membership-applications` error-handling gap — a DB failure outside the try block could throw unhandled instead of the standard `{error}` JSON shape
- 2026-08-19 — First live VERIFY of the membership application flow against real Storage credentials — all 12 checks passed
- 2026-08-19 — Doc-only: nav-tab page status (Home/Book Now done; News/Café & Bar/Membership/About not started) recorded in Next Up
- 2026-08-19 — Admin panel slice 1: AdminUser email/password login via Auth.js (Credentials provider, JWT, no adapter)
- 2026-08-19 — Admin panel slice 2: bookings list/detail/reschedule + session `role` callbacks added to `auth.ts`. Found and fixed two real blockers (no `AdminUser` row existed; `AUTH_SECRET` was empty). Flagged: `middleware.ts`'s matcher wasn't actually gating the reschedule route — later found broader (see 2026-08-19 entry below)
- 2026-08-19 — Bugfix: post-login "hang" on `/admin/login` — `signIn()` had no `redirectTo`, so it silently redirected back to the login page itself
- 2026-08-19 — Admin nav expanded 2→5 tabs with Coming Soon placeholders for Resources/Memberships/Bulletin
- 2026-08-19 — Admin frame restructured: top-nav → foldable left sidebar + topbar + bento content card
- 2026-08-19 — Bookings list restructured: trimmed columns, merged Start/End into one cell, added a View action
- 2026-08-19 — Bookings list: re-added a Reference column; 4 status-filter links replaced with a single Filter modal (status + date range)
- 2026-08-19 — Admin Memberships built as a read-only frame (list + detail, signed-URL gov-ID images) — no approve/reject yet
- 2026-08-19 — Bookings list's date column changed from slot start/end to `createdAt` ("Submitted") — deliberate; slot detail stays on the detail page
- 2026-08-19 — Memberships detail page visual redesign (status shown once, as a colored pill)
- 2026-08-19 — Admin Resources & Pricing built as a read-only frame
- 2026-08-19 — Resources & Pricing split into 3 tabs (Courts/Simulators/Guest Fee) with inert CRUD-preview buttons
- 2026-08-19 — Resources & Pricing cards made individually foldable (collapsed by default)
- 2026-08-19 — **Confirmed broader**: `middleware.ts` doesn't execute at all locally (Next 16/Turbopack/Windows), not just for the one route flagged above — every gated page/route is actually protected by its own `(protected)/layout.tsx`/route-level `auth()` check, not middleware
- 2026-08-19 — Resources & Pricing: resource-level Add/Edit/Delete wired for real; one-time cleanup of stray Booking test data from earlier manual testing
- 2026-08-19 — `prisma/bootstrap-admin.ts` retroactively documented and deleted — the real `AdminUser` it was meant to create already existed from an earlier unlogged run
- 2026-08-20 — Diagnosed `/admin/memberships`'s ~1.7s load as Prisma round-trip count, not query/render cost — fixed via `relationLoadStrategy: 'join'` (`relationJoins` preview feature)
- 2026-08-20 — Same `relationLoadStrategy: 'join'` fix applied to `/admin/resources`
- 2026-08-20 — Admin Pricing/Add-on/Guest-Fee mutation, edit-only: 3 new PATCH routes + `PriceEditModal`
- 2026-08-20 — Browser-verified the Pricing/Add-on/Guest-Fee edit slice live, via an already-authenticated session (not Claude Code login)
- 2026-08-20 — Admin membership review mutation wired: `PATCH /api/admin/memberships/[id]` (approve/reject + required rejection reason)
- 2026-08-20 — Doc-only: PROJECT_CONTEXT.md's stale membership-flow note reconciled with shipped behavior (rejection reason is required, not optional)
- 2026-08-20 — Membership page (`/membership`) visual refinement: per-month price lines added, 6-Month marked as featured
- 2026-08-20 — Home layout pass: Hero eyebrow added; StatsBar changed to a flat full-width dark band; How It Works redone as a 3-circle timeline; new "Two Sides, One Winston" section added; CtaBanner texture changed
- 2026-08-20 — Home layout pass round 2: a real mock UI became available for the first time — corrected drift from the text-only first pass (colors deliberately kept on our own tokens, not the mock's)
- 2026-08-20 — Navbar converted `sticky`→`fixed` (so the transparent-over-Hero state actually shows the photo); TwoSides gained a day-to-night gradient background
- 2026-08-20 — TwoSides gradient extended for contrast; Navbar scroll transition smoothed; all 7 CTA buttons given a gradient/shadow/motion treatment
- 2026-08-20 — Restraint pass: Hero wordmark enlarged one step; Navbar button heights fixed; the gradient/shadow/motion button treatment dialed back to flat single-tone
- 2026-08-20 — Button radius/size/legibility pass: all 7 buttons `rounded-full`→`rounded-lg`; Navbar buttons resized to match; "Become a Member" gained scroll-conditional light/dark text — closes out Home's button treatment
- 2026-08-20 — Café & Bar page built (`/cafe-bar`, 5 components)
- 2026-08-20 — Café & Bar gained a client-side Café/Bar mode switch
- 2026-08-20 — Membership page split into an info page (`/membership`) + a relocated application form (`/membership/apply`)
- 2026-08-20 — About page built (`/about`) — Our Story copy flagged as placeholder pending real content
- 2026-08-20 — News page built (`/news`), completing all 6 marketing pages. One-off decision (confirmed one-off, not standing): frontend content shape was designed before the DB schema question (extend `Bulletin` vs. new model), which stayed open until the Bulletin-unification work below
- 2026-08-21 — Home stat-banner latency fixed by dropping the DB dependency entirely (hardcoded constants) rather than caching it
- 2026-08-21 — Home mobile-responsiveness audit (audit-only) across 320–639px — one blocking contrast issue (TwoSides' mobile-gradient link) + 4 polish findings, written to `docs/audits/2026-08-21-home-mobile-audit.md`
- 2026-08-21 — Bulletin admin read-only frame built
- 2026-08-21 — `Bulletin` model unified to match News/AnnouncementGate's shape (`excerpt`, `category` enum, `imageUrl`, etc.)
- 2026-08-21 — Bulletin admin CRUD fully wired (create/edit/delete + image upload)
- 2026-08-21 — AnnouncementGate wired to live Bulletin data; `/news` date format switched to a shared formatter — **Bulletin unification complete, no hardcoded `NEWS_ITEMS`/`SAMPLE_NOTICES` remain anywhere**
- 2026-08-21 — `/news` wired to the live Bulletin table (was hardcoded `NEWS_ITEMS`)
- 2026-08-21 — Doc-only: PROJECT_CONTEXT.md's stale claim that bulletins appear on the homepage corrected (they surface on `/news` and `/book`'s gate)
- 2026-08-21 — Member login/account scaffold added — hardcoded sample data only, no backend (sessionStorage mock)
- 2026-08-21 — `/account` visual redesign — still sample data
- 2026-08-21 — `/account` Navbar-legibility bugfix (light hero background made the transparent Navbar's light text unreadable) + two-column layout
- 2026-08-21 — Recent Bookings pagination (10/page) + Navbar Sign In/Sign Out toggle (sessionStorage-based) + "Become a Member" removed from Navbar
- 2026-08-21 — `/account` Recent Bookings widened to full column span; Membership Status emblem contrast fixed
- 2026-08-21 — `/account` Recent Bookings horizontal-scrollbar bugfix (CSS overflow-axis coupling) + QR "View Full Screen" modal + Navbar button reorder
- 2026-08-22 — `/account` sidebar gained an inert Book-a-Court element; Navbar swaps it for an account icon when the mock session is active
- 2026-08-22 — Book-a-Court placeholder relocated into MembershipStatusCard's footer; structural fix to shared `Modal.tsx` affecting all its consumers
- 2026-08-22 — `/account` Profile-card height-match fix (removed a stray `md:items-start` overriding grid stretch); Recent Bookings scroll workaround removed
- 2026-08-22 — Doc-only: CLAUDE.md's stale "auth method undecided" wording corrected to reflect the already-made email/password decision
- 2026-08-22 — Post-approval member activation flow built (schema + issuance + email + `/activate` page) — deliberately stops short of real member login (next entry)
- 2026-08-23 — Real Auth.js member login wired: second `member-credentials` provider, `/account`'s gate is now a real server-side session check, Navbar reads a real session. `/account`'s displayed content is still sample data (next entry). **Critical gap found in VERIFY, flagged as blocking**: a `role: 'member'` session passed every admin auth check, since none of them checked `role` — only session existence
- 2026-08-24 — Closed the admin-gate gap above: every `/api/admin/*` route (9 files, 11 occurrences) and `(protected)/layout.tsx` now also require `role === 'admin'`
- 2026-08-24 — `/account` wired to real `Booking`/`Membership` data — no `SAMPLE_*` constants left. Membership selection prefers an active/non-expired row, falls back to the most recent of any status, or an empty state
- 2026-08-26 — Resend sending domain verified (`winstonsipandserve.club`) — closes the sandbox-sender limitation; sending from `no-reply@winstonsipandserve.club`, reply-to `winstonsipandserve@gmail.com`
- 2026-08-26 — VERIFY pass found `RESEND_API_KEY` was empty — no real send was possible yet; flagged for Arjay
- 2026-08-26 — Fixed duplicate Prisma query logging (listener was re-attaching on every hot-reload); documented the local-dev DB connection decision (`:5432` session-mode, not pooled `:6543`) as locked-in in CLAUDE.md
- 2026-08-26 — Real end-to-end activation-email delivery confirmed once `RESEND_API_KEY` was set — real admin approval → real inbox delivery with a working `/activate` link
- 2026-08-26 — Resend MCP connected in both this planning chat and Claude Code CLI
- 2026-08-26 — Branded email layout built (`buildBrandedEmail`, shared by all future email cases) — first consumer is the activation email; rejection/forgot-password not built here yet. New public `email-assets` Storage bucket for the logo
- 2026-08-27 — Visual polish pass on the branded email layout (logo contrast, top strip, eyebrow label, CTA styling, divider, card border, proper hidden-preheader technique)
- 2026-08-27 — Approval email rewritten from a bare notice into a warm, benefit-led welcome (perks box, tier personalization)
- 2026-08-27 — `sendRejectionEmail` added — second `buildBrandedEmail` consumer; calmer context box (not the celebratory approval box) surfacing the admin's reason, plain inline `/book` link (not a CTA button)
- 2026-08-27 — Member password reset built end-to-end — `PasswordResetToken` (1hr expiry), enumeration-safe forgot-password route, reset route, `/forgot-password` + `/reset-password` pages, `sendPasswordResetEmail` as the third `buildBrandedEmail` consumer. **All three tracked membership email cases (approve/reject/forgot-password) now closed out**
- 2026-08-27 — Real membership data reset at Arjay's request, for a clean testing slate (Customer rows preserved, `passwordHash` reset to null) — gov-ID images from the deleted applications are now orphaned in Storage (flagged, not cleaned up)
- 2026-08-27 — `/reset-password` invisible-Navbar bug fixed (missing `bg-brand-dark` hero band) — `/forgot-password` has an identical bug in its lower section, not yet fixed
- 2026-08-27 — `PROGRESS.md` compressed (552KB → ~28KB): full pre-2026-08-28 history moved verbatim into new `PROGRESS_ARCHIVE.md`; this file rewritten to current-state Build Status + one-line Completed Log entries + a trimmed Next Up. `CLAUDE.md`/`PROJECT_CONTEXT.md` untouched. See the "Entry format" note at the top of this file for the convention new entries should follow going forward
- 2026-08-27 — `/forgot-password/page.tsx`'s leftover `bg-background` legacy token fixed → `bg-brand-light`, matching `/reset-password`'s lower section. Closes out the last item from the 2026-08-27 forgot-password-flow build
- 2026-08-27 — `sendBookingConfirmationEmail` added as the 4th `buildBrandedEmail` consumer — booking-reference ledger card (sport/date/time/duration/guest fee/add-ons/total), Café & Bar cross-sell, membership upsell CTA. Reused `formatCentavos` from `src/lib/format.ts` for peso formatting rather than writing a duplicate (date/time formatting had no existing match, written fresh). Not yet wired to the PayMongo webhook — preview-only, sample email fired to arjayical22@gmail.com for template review.
- 2026-08-27 — Resource Add/Delete removed from the admin panel; replaced with a Disable/Enable toggle. New `Resource.disabledReason` column (nullable, cleared server-side whenever `isActive` flips back to `true`, regardless of request body). `POST /api/admin/resources` and `DELETE /api/admin/resources/[id]` deleted; `PATCH /api/admin/resources/[id]` extended to accept `isActive`/`disabledReason`. `ResourceFormModal` deleted (no longer used); new `DisableResourceModal` (reuses the shared `Modal`) prompts for an optional reason on disable. Disabled resources stay visible in the admin list (with their reason shown inline) but are now rejected server-side, not just UI-hidden: `POST /api/bookings` and `GET /api/availability` both 400 `{ error: 'Resource not found' }` for an inactive resource id, matching the not-found response shape so a disabled resource isn't distinguishable from a nonexistent one. New courts/simulators are now DB-injected only — no in-app create path.
- 2026-08-27 — Admin Pricing/Add-on rows gained full create/delete: `POST /api/admin/pricing-rules` and `POST /api/admin/add-on-pricing-rules` (409 on duplicate unique-combination, checked explicitly rather than relying on a raw Prisma P2002); `DELETE` added to both `[id]` routes (`PricingRule` hard-deletes with no referential check; `AddOnPricingRule` 409s with a friendly message if any `BookingAddOn` still references it). `PriceEditModal` gained a create mode (single-field POST form) alongside its existing multi-field edit/PATCH mode. Every previously-inert control in `ResourcesTabs` (disabled "+Add", disabled edit pencil) is now live — each price cell shows either a value with edit+delete, or a working "+Add"; the disabled-pencil placeholder was removed outright rather than kept as an inert state. Guest Fee confirmed staying edit-only, permanently, by design (its shape has nothing to key a second row on) — not a placeholder awaiting a future pass.
- 2026-08-27 — Full membership test-data reset #2, this time including Storage image cleanup — closes out the orphaned-gov-ID-images item from the 2026-08-27 reset #1, and clears everything created since (application, membership, credit ledger, activation token, password reset tokens). `Customer` rows preserved, `passwordHash` re-nulled. New `scripts/reset-membership-data.ts` (gitignored, standalone). Blocked mid-run by the local session-mode pooler maxing out at 15 idle connections unrelated to any live process; cleared with Arjay's explicit one-off approval to run `pg_terminate_backend` on the idle Supavisor sessions via the (normally read-only-scoped) Supabase MCP.
- 2026-08-27 — Audited all `scripts/` for Prisma connection cleanup: the 4 preview-*-email scripts are Resend-only (raw `fetch`, no Prisma import), confirmed untouched. `reset-membership-data.ts` was the only Prisma consumer, and its existing `.catch().finally(() => prisma.$disconnect())` chain had a real gap — the `catch` called `process.exit(1)` synchronously, which killed the process before `.finally()` ever ran, so a failed reset never disconnected. Fixed by swapping to `process.exitCode = 1`, letting the process exit naturally after cleanup runs on both paths. Closes out the idle-connection note from the 2026-08-27 membership-reset entry above.
- 2026-08-27 — `Bulletin` extended with richer optional metadata: 2 new `BulletinCategory` values (General, FacilityMaintenance — 4 originals unchanged), new `BulletinPriority` enum (Normal/High, default Normal), and 8 new nullable columns (affectedFacility, impact, action, eventStartAt/eventEndAt, expiresAt, ctaLabel/ctaUrl). Kept the existing `body` column rather than renaming it — only its admin-form label changed to "Description" — to avoid an unnecessary column rename on a live table. Admin Add/Edit modal and both admin API routes updated to match, with CTA label/URL enforced as a provided-together-or-neither pair (same pattern as the existing social platform/URL fields). Status: schema + admin modal done; `/news` and `AnnouncementGate` still render only the pre-existing fields — wiring the new fields into those surfaces (including `expiresAt` filtering) is not yet done. Required one out-of-scope type-only touch: `NewsGrid.tsx`'s `NewsCategory` union type had to widen to include the 2 new enum values so `/news/page.tsx` would still compile, since `NewsCard` displays `item.category` as plain text with no per-category branching.
- 2026-08-27 — New Bulletin fields wired into `/news` and `AnnouncementGate`. Shared `src/lib/bulletin.ts` (`bulletinNotExpiredWhere`/`bulletinOrderBy`) is now imported by both the `/news` query and `GET /api/bulletin/gate-notices` so expiry filtering (`expiresAt IS NULL OR expiresAt > now()`) and sort order (priority desc, then `publishedAt` desc) can't drift between the two — admin's own Bulletin list query is untouched, so staff still see expired rows there. `/news` (`NewsCard`) now renders the full field set: High-priority badge, "Affects: X" facility line, `body` as a labeled "Description" (replacing the old unlabeled excerpt paragraph), Impact/Action subsections, a formatted event-start/end line, and a CTA button — each omitted individually when its field is null. `AnnouncementGate` stays compact by design: priority badge plus a single "Facility — Action" line, with impact/event-window/CTA deliberately left off. No category filter UI existed on `/news` to extend with the 2 newer categories (Part 3.6 was a no-op, confirmed no such tabs/buttons exist). Verified live via Playwright: 6 fresh test bulletins (one per category, mixing High/Normal priority, past/future/null `expiresAt`, one full-field and one required-only) confirmed expired rows hidden on both public surfaces but still listed in `/admin/bulletin`, High-priority sorting first, full-field vs. required-only rendering both clean, and the pre-existing "Sample announcemint" plus the full `/book` wizard (SPORT step through resource list) unaffected — all 6 test rows deleted afterward via the admin panel.
- 2026-08-27 — Bulletin Add/Edit modal widened (384px → 768px, `Modal`'s shared `max-w-sm` default made an opt-in `maxWidthClassName` prop rather than changed globally, so every other consumer is untouched) and its compact fields regrouped into a responsive 2-column grid (Category/Priority, Event Start/End, Social Platform/URL, CTA Label/URL), collapsing to one column below `md`. Color/token styling of the admin form (cream vs. gray) remains an open, separately-flagged question — not addressed here. Follow-up pass, same modal, still uncommitted: native scrollbar replaced with a slim custom one (new `.scrollbar-thin` utility in `globals.css` — `scrollbar-width: thin`/`scrollbar-color` for Firefox, `::-webkit-scrollbar*` for Chromium/Safari — applied only to this form's own scroll container, so `PriceEditModal`/`DisableResourceModal`/other `Modal` consumers are untouched; confirmed via live regression screenshots against both); form's max-height raised 70vh → 80vh to cut scroll length; and the flat field list broken into 6 labeled sections (Content, Classification, Impact Details, Scheduling, Media & Links, Publish) via a small local `FormSection` wrapper, each divided by a thin `border-gray-100` rule with a `text-xs uppercase tracking-wide text-gray-500` label — reusing the same section-heading convention already used in `BookingsFilterModal`, no new colors/fonts. Field order/pairing from the prior pass untouched. Verified live: Add Bulletin renders all 6 sections with the new scrollbar top-to-bottom; a temporary test bulletin was created, edited, and saved successfully to confirm the new layout doesn't break submission, then deleted. Note: the pre-existing "Sample announcemint" bulletin referenced in earlier entries no longer exists in the local dev DB (table was empty at the start of this pass) — verification substituted a temporary bulletin instead. `tsc --noEmit` clean. Mobile-width (390px) resize verification was not completed — the connected browser session's window-resize tool had no effect on actual viewport width in this environment (window.innerWidth stayed at desktop size regardless of the requested resize), so the "sections stack cleanly on mobile" check is unverified live; no regression is expected since the responsive `grid-cols-1 md:grid-cols-2` classes are unchanged from the prior pass and the new section wrapper is a plain full-width `flex flex-col` block, but this should be spot-checked manually before considering the mobile pass fully verified.
- 2026-08-27 — 6 realistic sample bulletins seeded via new `scripts/seed-sample-bulletins.ts` (gitignored, standalone, mirrors `reset-membership-data.ts`'s Prisma-singleton/`.finally($disconnect)`/`process.exitCode` conventions), one per `BulletinCategory`, for visual review on `/news` and `/book` — persistent, not test data. `Bulletin.imageUrl` turned out to be a required column with no "no image" option, so all 6 use `/images/placeholder.jpg` (the same fallback path already named in `NewsGrid.tsx`'s own doc comment) rather than the originally-specified `null`. A pre-existing "Open Pickleball Tournament 2026" row from earlier manual admin testing collided by title with the Tournament sample and had already-expired/placeholder content; per Arjay's choice, the script overwrites (not skips) a title match, so re-running it always converges on the 6 spec bulletins rather than leaving stale duplicates.
- 2026-08-28 — Closes the `imageUrl`-required gap flagged in the entry above: `Bulletin.imageUrl` made nullable (hand-written migration `20260828010000_make_bulletin_image_url_nullable`, `prisma migrate deploy`), and per-category required-field rules added as data (`BULLETIN_CATEGORY_RULES` in new `src/lib/bulletin-validation.ts`) — image required only for Renovation/Tournament/Community, `eventEndAt`/`expiresAt` required for all except General, CTA pair required only for Tournament/Community; `affectedFacility`/`impact`/`action`/`eventStartAt` are now required unconditionally for every category (previously all-optional). Both admin API routes (`route.ts`/`[id]/route.ts`) had their duplicated `VALID_CATEGORIES`/parsing block extracted into that shared file so POST/PATCH can't drift, following the `booking-hold.ts`/`booking-pricing.ts` precedent. `BulletinFormModal`'s Category select moved to its own first section ahead of Content, with client-side validation mirroring the server and `*` markers (unconditional for the always-required fields, conditional via `BULLETIN_CATEGORY_RULES` for the rest). `BulletinList` and `NewsCard` now render cleanly with a null image (gray placeholder box in the admin list; `NewsCard` skips the image block entirely and relocates the category/priority badges into the card body so that information isn't lost). `PATCH` allows a category switch away from an image-required category to keep an existing image rather than forcing removal, since no removal affordance exists yet.
- 2026-08-28 — Admin Bulletin page layout fix: removed the italic "Add/Edit/Delete are live…" description line under the heading, and restructured `bulletin/page.tsx` to use the same fixed-header/scrollable-body pattern as Bookings/Memberships (`flex h-full flex-col` outer container, heading + Add button fixed outside a `min-h-0 flex-1 overflow-y-auto overflow-x-auto` wrapper) — previously the whole content card scrolled together as one block. Since the "+ Add Bulletin" button lived inside `BulletinList.tsx`'s own render output alongside the list rather than in the page, keeping it fixed required extracting it into a new small client component, `BulletinAddButton.tsx` (button + its own `addModalOpen` state + the existing `BulletinFormModal` in add mode); `BulletinList.tsx` now only renders the list and the edit modal. Verified live: no scrollbar with 1 bulletin, heading/button stay fixed while the list scrolls with 10 (9 temporary `TEMP-SCROLLTEST-*` rows inserted for the check, all deleted afterward via the admin panel's own Delete button), no double-scrollbar on the outer `<main>`, and Bookings/Memberships confirmed byte-identical in wrapper class and scroll behavior (not touched).
- 2026-08-28 — Small padding fix on the Bulletin admin list container: added `p-4` to `bulletin/page.tsx`'s scrollable wrapper so cards no longer sit flush against the container border (Bookings' equivalent wrapper has no comparable inner padding to match — it's a table with its own per-cell padding, not a card list — so `p-4` was chosen to match the spacing scale `BulletinList.tsx`'s own cards already use). Verified live: 16px gap confirmed on all sides via computed styles, and holds at both scroll boundaries with 6 temporary test bulletins (all deleted afterward via the API).
- 2026-08-28 — Closes the 2026-08-27-flagged "admin Bulletin modal cream/brand-token" open question as a non-issue: a grep across `BulletinAddButton.tsx`/`BulletinFormModal.tsx`/`BulletinList.tsx` for brand-token classes (`brand-dark`/`brand-mid`/`brand-light`/`accent-primary`/`accent-light`/`font-serif`/`Fraunces`/`Parisienne`) found zero matches — all three files were already neutral-gray-only. The same grep surfaced a real, previously-unflagged violation instead: `BulletinList.tsx`'s Published status badge used `bg-green-100`/`text-green-800`, breaking the sitewide "no green anywhere" rule — fixed to `bg-gray-900`/`text-white` (solid dark vs. light gray, still visually distinct from the unpublished `bg-gray-100`/`text-gray-500` badge, which is unchanged).
- 2026-08-28 — Admin Dashboard tab built, replacing the bare "Welcome to the admin panel" placeholder: 5 stat cards (Bookings Today, Revenue This Month, Pending Applications, Active Memberships, Resource Utilization), 3 `recharts` charts (stacked bar for the 14-day bookings trend, gradient area for the 6-month revenue trend, donut + written legend for bookings-by-resource-type), and two activity lists (Recent Bookings with neutral status pills, Pending Membership Applications with a "Review →" link to `/admin/memberships`). All new: `DashboardStatCard`/`DashboardStats`/`DashboardCharts`/`DashboardActivity` components, `dashboard-mock-data.ts`, two new `AdminIcons.tsx` glyphs (`RevenueIcon`/`TrendingIcon`, the latter unused for now — no obvious slot in the current 5-card/3-chart layout, kept for a future stat/chart that needs an up-trend glyph). Hardcoded mock data only in this pass, every export carrying a comment naming its future real Prisma source — no live queries added. Every chart/UI color is drawn from the existing neutral gray scale (`#111827`/`#374151`/`#6b7280`/`#9ca3af`/`#d1d5db`/`#e5e7eb`/white); no recharts default palette, no brand tokens, no color-coded up/down deltas. `recharts` added as a new approved dependency (see CLAUDE.md).
- 2026-08-28 — Member-aware booking flow shipped: `isActiveMember(customerId)` extracted from `customer-resolution.ts` as the shared source of truth; `/book` converted to an async server component (new `BookingPageClient.tsx` holds the prior client logic) that reads the session and builds a `memberContext` for a logged-in active member; `POST /api/bookings` is now session-aware — resolves `isMember` itself, prices at the member rate, attaches `customerId` directly at hold creation (no PATCH round-trip), and forces `guestCount` to 0 server-side for members regardless of what the client sends. `BookingForm` derives a `rateTier` from `memberContext` and threads it through duration options, ball-boy/coaching pricing, and the price estimate; `SportStep`'s pricing modal shows real member rates (confirmed live: tennis court ₱650/hr vs ₱750 non-member, golf-sim 30-min tier ₱450 appears only for members); `AddOnsStep` hides the guest-count control for members and shows a short explanatory line instead; `PaymentStep` skips the name/phone/email form for a known member, showing "Booking under {name} ({email})" instead. `MembershipStatusCard`'s "Book a Court" placeholder is now a real `Link` to `/book`. `PATCH /api/bookings/[id]` (the anonymous two-phase pricing/re-pricing path) is untouched and confirmed still working via VERIFY.
- 2026-08-28 — `/book/confirmation` moved onto brand tokens: was still on legacy Next-starter tokens (`bg-zinc`, `dark:` variants, `bg-foreground`/`text-background` on the Check Again button) across all 3 files, had no `Navbar`/`Footer` at all, and left the `cancelled`/`not_found` terminal states as dead ends. `BookingConfirmation.tsx` kept its own ledger (Part 1: `BookingSummary` reuse was assessed and rejected — its `isCourt` prop has no clean source in `BookingDetail`, since the API only returns a display `resource.typeName` like "Tennis Court", not the underlying `category` enum, and its footer copy is estimate-only phrasing that's wrong for an already-paid booking) restyled onto the identical `rounded-2xl border border-brand-dark/10 bg-brand-light shadow-xl shadow-brand-dark/10` card treatment, plus a "Back to Home" CTA. `ConfirmationContent.tsx`'s loading/stalled/cancelled/not-found copy and Check Again button moved to brand tokens; `cancelled` and `not_found` each gained a "Back to Home" text link so they're no longer dead ends. `page.tsx` gained `Navbar`/`Footer` plus the `/reset-password`/`/account` fix pattern for the invisible-Navbar-on-a-light-page bug (a `bg-brand-dark` header band directly below `<Navbar />`, not a padding-only patch) — documented as a reusable Known Issue in CLAUDE.md. `/book` itself (gate + wizard) deliberately left with the same unfixed bug — see Next Up.
- 2026-08-28 — `sendBookingConfirmationEmail` wired into the PayMongo webhook's `payment.paid` handler (`src/app/api/webhooks/paymongo/route.ts`) — customers now receive two emails on a paid booking (PayMongo's own generic receipt, plus this branded one). Fires only on the actual `pending_payment → confirmed` transition, guarded by the pre-existing `booking.payment?.status === 'paid'` early-return so a redelivered webhook event never double-sends. Booking lookup expanded to include `customer`/`resource.resourceType`/`addOns.addOnService`/`addOns.addOnPricingRule`; guest fee recomputed via `prisma.guestFeeRule.findFirst()` (mirrors `booking-pricing.ts`'s query), add-on display names built from a local `ADD_ON_EMAIL_LABELS` map with a raw-slug fallback and a "(N pax)" suffix when `addOnPricingRule.paxCount` is set. Email failures are swallowed internally by `sendBookingConfirmationEmail` itself, so the webhook still returns 200 to PayMongo either way.
- 2026-08-28 — One-time cleanup of all sample/test Booking data via new `scripts/reset-booking-data.ts` (gitignored, standalone, mirrors `reset-membership-data.ts`'s Prisma-singleton/before-after-counts/`.finally($disconnect)`/`process.exitCode` conventions), ahead of the next task. Deletes only `BookingAddOn`/`Payment`/`BookingReschedule`/`Booking` rows (FK-safe children-first order, single transaction); `Customer`/`Membership`/every other model untouched. Also clears the 3 stray `pending_payment` test bookings flagged in the 2026-08-28 member-aware-booking VERIFY entry, plus every other test booking created across the recent SportStep/webhook-email/confirmation-page VERIFY passes. Console output: Booking 6→0, Payment 4→0, BookingAddOn 2→0, BookingReschedule 0→0; Customer (6) and Membership (1) unchanged. `/admin/bookings` confirmed showing an empty list, not an error. Live login to `/account` wasn't exercised (no known password for `arjayrafaelical@gmail.com`'s member session), but a direct DB check confirmed its underlying data is exactly as expected post-cleanup (0 bookings, 1 membership still attached) and `RecentBookingsList.tsx` already has a proper zero-length empty state ("No bookings yet."), so no live-render risk. One other non-member `Customer` row (`winstonsipandserve@gmail.com`, from earlier anonymous-booking tests) confirmed still present, along with all 6 pre-existing `Customer` rows.
- 2026-08-28 — Admin Dashboard wired to real Prisma queries, replacing the hardcoded mock data: `dashboard-mock-data.ts` deleted, new `src/lib/dashboard-data.ts` exports `getDashboardData()` (same `DashboardStats`/`BookingsTrendPoint`/`RevenueTrendPoint`/`ResourceBreakdownEntry`/`RecentBooking`/`RecentApplication` interfaces, unchanged). `page.tsx` is now an async server component fetching once and threading props down; the 3 chart/stat/activity components now take props instead of importing mock consts — no visual change. New PH-week/month window helpers (`currentPhWeekWindow`/`currentPhMonthWindow`/`phMonthStartUtc`/`toPhMonthKey`) added to `business-hours.ts` following its existing fixed-UTC+8-no-DST convention; `formatShortDate` added to `format.ts`. Resource Utilization % = booked hours (confirmed bookings, active resources, current PH week) ÷ (active resource count × business hours × 7 days). Reused the existing `formatTier()` switch (duplicated as a local, unexported helper in both admin memberships pages) rather than extracting a new shared module. Verified live: zero-state renders cleanly (no NaN/crash) against the real empty DB, then a temporary seed (`scripts/seed-dashboard-test-data.ts`, gitignored, `--cleanup` flag) exercised every stat/chart/list with a hand-computed expected result — all matched exactly (Bookings Today 2, Revenue This Month ₱2,000.00, Resource Utilization 1%, resource-type breakdown, Recent Bookings/Applications ordering) — then cleaned up back to the empty state. `/admin/bookings`/`/admin/memberships`/`/admin/resources`/`/admin/bulletin` confirmed unaffected. `tsc --noEmit` clean; zero brand-token matches in all touched files. Flagged, not fixed: neither Recent Bookings nor Pending Membership Applications has an explicit empty-state message — an empty list currently just renders no `<li>` rows.
- 2026-08-28 — Doc-only: the three byte-identical `formatTier` duplicates (both admin memberships pages + `dashboard-data.ts`, flagged but left in place by the prior Dashboard entry) extracted into a single shared `formatMembershipTier` export in `format.ts`, following the existing shared-helper convention (`booking-hold.ts`/`booking-pricing.ts`/`bulletin-validation.ts`). Pure move — all three copies were confirmed byte-identical via diff before extraction, so no behavior reconciliation was needed.
- 2026-08-28 — A 4th `formatMembershipTier` duplicate was found in `membership-pricing.ts` (used by `/account` and the admin membership-approval API route) during the earlier `format.ts` consolidation; confirmed byte-identical to `format.ts`'s version and consolidated in this pass — `membership-pricing.ts` now only exports `MEMBERSHIP_TIER_PLANS`/`computeMembershipEndDate`, all 5 call sites in the codebase now import `formatMembershipTier` from `format.ts`. Live /account tier-rendering verification was skipped — no active Membership row exists locally to exercise it against — relying instead on tsc + the confirmed byte-identical function body; still flagged in Next Up as the existing "seed one when this path needs a real check" item.
- 2026-08-28 — Membership tier-activation payment (backend half) built: new `MembershipPayment` model (RLS deny-all, `prisma migrate deploy`) decouples payment from `Payment` (booking-scoped FK stays untouched — see new CLAUDE.md Architecture Decision). Admin approve (`PATCH /api/admin/memberships/[id]`) no longer creates a `Membership` — it only flips status + sends a new "complete your payment" email (`sendMembershipPaymentEmail`, 5th `buildBrandedEmail` consumer); `Membership`/credit-ledger/`MemberActivationToken` creation moved into a new additive `metadata.membershipPaymentId` branch on the PayMongo webhook, sitting alongside the untouched booking branch (dispatched by which metadata key is present; neither present is now a log/no-op, not a 400 — the old bookingId-required 400 no longer applies since the route now serves two payment kinds). New `POST /api/membership-payments` (create-or-resume checkout, mirrors `/api/checkout`'s resume pattern exactly, one `MembershipPayment` row per application unless still pending) and public `GET /api/membership-applications/[id]` (id/status/tierName/hasMembership only, no PII) routes. `sendActivationEmail` gained an optional receipt block (activation fee / F&B credit / total paid via the existing `ledgerRow` helper) — verified byte-identical HTML when the new params are omitted. `MembershipReviewActions.tsx`'s approve confirm-dialog copy updated to match the new flow. Verified live end-to-end: admin-UI approve (Playwright, `window.confirm` patched since the harness's browser can't answer native dialogs) → real Resend-delivered payment email → real `POST /api/membership-payments` PayMongo checkout (resume-on-repeat confirmed) → self-signed webhook → `Membership`/ledger/token/enhanced-activation-email all correct and matching `MEMBERSHIP_TIER_PLANS` exactly → replayed webhook confirmed idempotent (no duplicates) → reject branch and a full anonymous booking hold→checkout→webhook regression both confirmed unaffected. One gotcha hit and worked around, not a code defect: Resend rejects `@example.com` recipients outright (422), so verify customers needed real Gmail+alias addresses instead — same convention as prior verify passes. All test data (2 applications, 2 customers, 1 membership, ledger row, token, 1 booking+payment, 3 storage placeholder images) cleaned up via a scoped script, not the blanket `reset-membership-data.ts` (which would have also deleted the one real pre-existing `Membership` row) — confirmed post-cleanup counts exactly match pre-test baseline (1 membership, 6 customers, 1 application, 0 bookings). Also hit and resolved: local Prisma migration was blocked by the session-mode pooler's 15-connection cap (stale idle connections from a prior dev-server run) — Supabase MCP's role lacks `pg_signal_backend` so `pg_terminate_backend` 403'd; resolved instead by killing local `node.exe` processes (explicit user approval) — worth remembering as the actual fix now that the MCP-based one from 2026-08-27 is confirmed not to work with the current role. No customer-facing `/membership/pay/[id]` page exists yet — see Next Up.
- 2026-08-28 — `/membership/pay/[id]` and `/membership/pay/confirmation` built (mirroring `/book`'s payment/confirmation pattern), closing out the tier-activation payment flow end-to-end. New shared `membership-display-status.ts` backs a 4-state indicator (Pending/Approved—Awaiting Payment/Active Member/Rejected) on both admin Memberships pages, also fixing a pre-existing `bg-green-100` violation on the detail page found while touching it.
- 2026-08-28 — Doc-only reconciliation pass, discovered while verifying the membership-reminders work: CLAUDE.md's RLS bullet was stale at "17 tables" (real count, grep-verified: 19 — including `membership_payments`, added 2026-08-28, now the named example in place of the older `member_activation_tokens`) and its Transactional email branding bullet listed only 4 of the real 7 `buildBrandedEmail` consumers; this file's own Schema line model count was stale at "20" (real count: 19 — the prior breakdown miscounted the original base as 17 instead of 16, and had never accounted for `MembershipPayment`). No code touched.
- 2026-08-28 — Membership expiry/renewal reminders built end-to-end: 3 new nullable timestamp columns on `Membership` (`reminder14SentAt`/`reminder3SentAt`/`expiredNoticeSentAt`, hand-written migration `20260828030000_add_membership_reminder_tracking`), each backfilled from `end_date` against its own threshold so no pre-existing row fires an email it would have "missed" pre-feature (verified: the one real `Membership` row expires 2027-02-28, well outside all three windows, so all three columns correctly stayed `NULL` post-backfill). Two new `buildBrandedEmail` consumers in `resend.ts` — `sendMembershipExpiryReminderEmail` (14-day friendly / 3-day urgent tone, CTA to `/membership/apply`, credit-forfeiture line shown only when `creditBalanceCentavos > 0`) and `sendMembershipExpiredEmail` (calmer tone, mirrors `sendRejectionEmail`'s context-box pattern) — bringing the `buildBrandedEmail` consumer count to 7. New `GET /api/cron/membership-reminders` (mirrors `expire-bookings`'s `CRON_SECRET` bearer-auth pattern exactly), 3 sequential find-then-send-then-stamp passes (14-day / 3-day / expired), each row's sent-at column updated immediately after its send rather than batched, so a mid-run failure can't cause a retry to double-send; new daily `vercel.json` entry at `0 1 * * *` (staggered from `expire-bookings`'s midnight slot). Verified live end-to-end via a new scoped `scripts/verify-membership-reminders.ts`: 4 temporary Customer+MembershipApplication+Membership rows (10-days-out, 2-days-out, 2-days-ago, 20-days-out control) — real cron invocation confirmed exactly the 3 expected emails delivered (Resend logs) and the correct row-by-row sent-at columns, a second immediate invocation confirmed zero new sends (idempotency), and cleanup restored exact pre-test counts (6 customers / 1 application / 1 membership). One deliberate test-data design choice: the "2-days-out" row was seeded with `reminder14SentAt` pre-stamped (simulating an earlier cron cycle already having sent it), since a genuinely fresh row at that `endDate` would legitimately fire both the 14-day and 3-day reminders in the same run — the prompt's own spec calls this dual-fire correct, not a bug, but pre-stamping isolates the 3-day-only path more clearly for a one-shot verification. `tsc --noEmit` clean; RLS confirmed still enabled on `memberships` post-migration. Not re-run as full regression suites (spot-checked only, no code touched in these paths): `expire-bookings` cron, the other 5 `buildBrandedEmail` consumers, booking/membership-payment flows.
- 2026-08-28 — Closes out the 2026-08-28 "Doc-only reconciliation pass" entry above: CLAUDE.md's RLS table count and `buildBrandedEmail` consumer list are now actually corrected (19 tables / 7 consumers, both grep-verified against `schema.prisma` and `resend.ts`), and this file's own Schema line was corrected in the same pass.
- 2026-08-29 — Self-service membership renewal built end-to-end for already-activated members whose membership has expired. Schema: `Membership.applicationId` and `MembershipPayment.applicationId` are now optional (hand-written migration `20260829010000_add_membership_renewal_support`, `prisma migrate deploy`), and `MembershipPayment` gained a direct required `customerId` (backfilled from the existing `application_id` join), since a renewal has no `MembershipApplication` to reach the customer through; `CreditTransactionReason` gained a new `renewal` value alongside the existing `activation`. The PayMongo webhook's `handleMembershipPaymentWebhook` now branches on `membershipPayment.applicationId === null`: a renewal creates a brand-new `Membership` row with `applicationId: null`, a `reason: 'renewal'` ledger row, and sends the new `sendMembershipRenewalEmail` (8th `buildBrandedEmail` consumer, reuses `sendActivationEmail`'s receipt-block pattern) instead of ever touching `MemberActivationToken`/`sendActivationEmail` — the pre-existing first-time application→approval→payment path is unchanged, just reading `membershipPayment.customer`/`.tier` instead of `application.customer`/`.requestedTier` now that those are directly on `MembershipPayment`. New session-gated `POST /api/account/membership-renewal` (401 if not a member session, 409 if already active, create-or-resume checkout mirroring `POST /api/membership-payments`' pattern) and public `GET /api/membership-payments/[id]` (id/status/tierName/hasMembership only, no PII, mirrors `GET /api/membership-applications/[id]`) back two new customer-facing pages: `/account/renew` (3 tier cards, redirects to `/account` if already active or `/membership/apply` if the customer has no membership history at all) and `/account/renew/confirmation` (polling, mirrors `/membership/pay/confirmation` minus the rejected-state branch). `MembershipStatusCard` now shows a "Renew Membership" CTA above "Book a Court" only when `isExpired` is true; "Book a Court" itself is unchanged and always renders. No admin-side "Send Renewal Link" action built — flagged in Next Up as a separate follow-up.
- 2026-08-29 — Doc-only: reconciled three CLAUDE.md bullets left stale by the 2026-08-29 renewal feature (Membership payment model, Membership credit ledger, Transactional email branding consumer count 7→8) — that prompt only updated PROGRESS.md at the time.
- 2026-08-29 — Fixed two real bugs in the admin Memberships display-status badge, both stemming from `getMembershipDisplayStatus` deriving state from `application.membership` (the application's own dangling 1:1 link) instead of the customer's actual current membership: (1) an "Active Member" badge never actually checked `endDate`, so an expired-but-never-renewed membership still showed active; (2) a renewed customer's newer `applicationId: null` Membership row was invisible to this lookup entirely, since it isn't linked to the original application. `getMembershipDisplayStatus` now takes `latestMembership: { endDate } | null` (the customer's most recent `Membership` row of any status/`applicationId`, same resolution `/account` already used for a single customer) and derives a new `expired` state via a real `endDate >= now` check. New `src/lib/membership-latest.ts` (`getLatestMembershipsByCustomerIds` batched + `getLatestMembershipByCustomerId` wrapper) is shared by the list page (one batched query, not N+1) and the detail page, so the two can never drift. `pending`/`rejected`/`awaiting_payment` states are unchanged.
- 2026-08-29 — Admin-side "Send Renewal Link" action built, closing out the manual fallback flagged when self-service renewal shipped. Schema: `MembershipPayment.initiatedByAdminId` (nullable FK to `AdminUser`, hand-written migration `20260829020000_add_membership_payment_initiated_by_admin`, RLS/deny-all reconfirmed post-migration) tracks which admin sent a link, mirroring the `reviewedBy`/`performedBy` accountability pattern. New `sendRenewalPaymentLinkEmail` (9th `buildBrandedEmail` consumer). New session-gated `POST /api/admin/memberships/[id]/send-renewal-link` (admin-only, reuses `getLatestMembershipByCustomerId` for the same expired-only guard the button's own visibility already enforces client-side, create-or-resume against a pending renewal `MembershipPayment` so repeat sends never duplicate a row) and public `POST /api/membership-payments/[id]/checkout` (mirrors `POST /api/membership-payments`'s checkout-session creation block, 409s if already paid or if `applicationId` isn't null). New customer-facing `/membership/renew/[id]` (mirrors `/membership/pay/[id]`) + `CompleteRenewalPaymentButton`, and admin-facing `SendRenewalLinkButton` (neutral-gray tier-picker modal, reuses the shared `Modal` component the same way `MembershipReviewActions`' `RejectModal` already does) rendered on `/admin/memberships/[id]` only when `displayStatus === 'expired'`. Zero webhook changes needed — the existing `applicationId === null` dispatch branch already treats an admin-initiated renewal payment identically to a self-service one. Confirmation-page reuse (`/account/renew/confirmation`) verified session-free, as required for an admin-sent link a customer clicks with no active session.
- 2026-08-29 — First real Vercel build attempt (on `dev`, carrying the admin renewal-link work) failed at `tsc` with dozens of "property does not exist" errors spanning nearly every model added since the original schema (`passwordHash`, `MemberActivationToken`, `MembershipPayment`, `Bulletin.imageUrl`/`socialPlatform`, etc.). Root cause was a stale cached Prisma Client, not any application bug: Vercel's `node_modules` cache persists across deploys when the lockfile is unchanged, so `@prisma/client`'s postinstall-only generation was silently never re-running. Fixed by changing `package.json`'s `build` script to `"prisma generate && next build"`, making generation unskippable on every build regardless of install-cache state. This is a build-infra issue, unrelated to the renewal feature itself, which was otherwise unaffected (no logic bug in any of the 3 renewal prompts).
- 2026-08-29 — Diagnosed and fixed two stacked Vercel deploy blockers found while redeploying `dev`/`staging` after the Prisma-cache fix: (1) Vercel Authentication ("Standard Protection") was gating every deployment project-wide, including Production, since no custom domain exists yet to exempt it — turned off to unblock testing/review/webhooks; (2) the actual root cause of every 404 regardless of build success — Project Settings' Framework Preset was set to "Other" instead of "Next.js", likely since project creation — meant Vercel never wired up Next.js's routing/serverless functions even on a clean build. Fixed Framework Preset to Next.js and redeployed `dev`/`staging`; both confirmed Ready and rendering the real site live via direct URL check. Production still runs on its own pinned "Production Overrides" tied to the original Aug 7 stub — will only pick up the fix once actually promoted (staging → main).
- 2026-08-29 — `winstonsipandserve.club` added to the Vercel project as a custom domain (apex only, no `www` redirect) and connected to Production — DNS/SSL confirmed live via direct URL check (correctly serves Vercel's own 404, since Production has had no deployment since the original Aug 7 scaffold — not a domain/DNS problem). Production's `NEXT_PUBLIC_APP_URL` updated from the placeholder `winston-booking-site.vercel.app` to `https://winstonsipandserve.club` in the same pass. No redeploy triggered, deliberately — per Branch Promotion Policy, both changes sit ready and only take effect once `staging` → `main` is actually promoted. `[www.winstonsipandserve.club](https://www.winstonsipandserve.club)` and a `staging` subdomain were both considered and deliberately deferred, not set up here.
- 2026-08-29 — Closed the previously-flagged middleware role-gate gap: the `jwt`/`session` callbacks (setting `token.id`/`token.role`/`session.user.id`/`session.user.role`) were relocated from `auth.ts` into the edge-safe `auth.config.ts` verbatim (no logic change), so both the full `auth.ts` config and `middleware.ts`'s lightweight `NextAuth(authConfig).auth` wrapper now share one definition instead of `auth.ts` duplicating it. `middleware.ts`'s `auth()` check now also verifies `req.auth.user?.role === 'admin'`, redirecting to `/admin/login` exactly like the missing-session case, not just checking session existence. `tsc --noEmit` clean. Verified live via Playwright against a temporary seeded admin + member (created and cleaned up via a standalone gitignored-pattern script, deleted after the run): admin login → Dashboard and `/admin/memberships` still load real data; member login → `/account` still renders real `Membership` data; the member session hitting `/admin/bookings` still redirects to `/admin/login` (via `(protected)/layout.tsx`'s existing gate) and a POST to `/api/admin/bulletin` still 401s for that session; `GET /api/auth/session` confirmed `id`/`role` values decode correctly and match the seeded customer. The middleware role-gate line itself is **unverified against a real request** — `middleware.ts` doesn't execute in this local Next 16/Turbopack/Windows dev setup (pre-existing known issue) — flagged below for a live staging check.
- 2026-08-29 — Doc-only re-verification: re-read `auth.config.ts`/`middleware.ts`/`auth.ts` directly against the entry above and confirmed it describes real, live code — `auth.config.ts` genuinely carries the `jwt`/`session` callbacks and `middleware.ts` genuinely checks `role === 'admin'`, not just aspirational/planned state.
- 2026-08-29 — Verified member re-pricing and `/account` tier display live (per the standing Next Up item below). A real active `Membership` already existed (`arjayrafaelical@gmail.com`, six_month) but its password wasn't available to this session, so a temporary Customer+Membership was seeded instead (three_month, via new gitignored `scripts/verify-member-repricing-and-tier.ts`, removed after). Anonymous-booking flow: hold created at ₱750.00 (non-member tennis-court rate); entering the seeded member's email on the Payment page correctly showed "Your final price is ₱650.00 (was ₱750.00 for non-members) — member rate applied.", required the confirmation click, and proceeded to a real PayMongo Checkout Session for the corrected ₱650.00 (not completed). `/account`: logged in as the seeded member, page rendered without error, Membership Status card showed "3-MONTH" with correct activation/credit amounts and expiry date. One real bug found and **not** fixed, per this task's scope — see Next Up.
- 2026-08-29 — Fixed the member re-pricing 400 flagged above: `priceBooking()` (`src/lib/booking-pricing.ts`) no longer rejects an anonymous-hold `guestCount > 0` that later resolves to an active `Membership` at Payment-page attach time — it now drops the guest fee entirely and re-prices at the member court rate, returning a new `guestFeeWaived: boolean` on its result (true only when `guestCount > 0 && isMember`). `PATCH /api/bookings/[id]` surfaces this flag in its response; `BookingForm.tsx`/`PaymentStep.tsx` thread it through so the Payment-page notice reads "Your final price is X (was Y for non-members, including guest fee) — member rate applied, guest fee waived." when true, unchanged from the original "member rate applied." copy when false (pure rate change, no guest fee involved). The genuine-non-member path (guest fee still applies) and the session-aware member direct-booking path (`guestCount` still forced to 0 server-side in `POST /api/bookings`) are both untouched. `tsc --noEmit` clean. Verified live via Playwright + direct API calls against a temporary seeded active member (same `scripts/verify-member-repricing-and-tier.ts` convention, removed after): the exact repro from the bug report (anonymous tennis-court hold, `guestCount: 1`, ₱900.00 non-member-incl.-guest-fee) now PATCHes 200 (was 400) and re-prices to ₱650.00 with the new richer notice copy rendering correctly in the UI; regression-tested guestCount:0→member (original notice copy byte-identical) and guestCount:2→genuine non-member (fee still applies, total unchanged, `guestFeeWaived: false`) via direct API calls. All temp test data (2 customers, 1 membership, 3 bookings) cleaned up — post-cleanup counts (6 customers, 1 membership, 0 bookings) match pre-test baseline exactly.
- 2026-08-29 — `dev` → `staging` promoted (commit `81ad776`, fast-forward merge, 7 commits: middleware `role === 'admin'` gate fix, guestCount+member-re-pricing fix, Prisma-generate build fix, plus 3 doc-only commits) — Vercel staging deployment confirmed Ready. Live VERIFY pass against the deployed staging URL confirmed all of: (1) middleware's `role === 'admin'` gate — a member session hitting an `/admin/...` URL correctly redirects to `/admin/login`, closing out the one check that couldn't be exercised locally; (2) admin login/Dashboard/`/admin/memberships` regression-clean; (3) member login/`/account` regression-clean; (4) the guestCount+member-re-pricing fix reproduced correctly against staging's DB (no 400, guest fee dropped, richer notice copy shown).
- 2026-08-29 — `/book` fixed to match `/book/confirmation`'s shell pattern: added `Navbar`/`Footer` and a `bg-brand-dark` header band ("Book Now" eyebrow, "Book a Court or Simulator" heading, moved here from `BookingPageClient.tsx`) to `page.tsx`, closing the last invisible-Navbar-on-a-light-page instance (see CLAUDE.md Known Issues). `BookingPageClient.tsx`'s outer wrapper moved off the legacy `bg-background` token onto `bg-brand-light`. Verified live: nav text legible unscrolled against the dark band, scroll-conditional solid-state transition unchanged, AnnouncementGate/wizard both render correctly below the band on `bg-brand-light`, Footer renders once at the bottom in both states, a seeded active member (same `scripts/verify-member-repricing-and-tier.ts` convention, removed after) confirmed `memberContext` still reaches `BookingForm` with a working session, mobile width (375px) confirmed clean, and `/book/confirmation`/`/account`/`/reset-password` confirmed visually unaffected. Zero legacy-token matches (`bg-background`/`dark:`/`border-black`/`bg-foreground`/`text-background`) in both touched files.
- 2026-08-29 — Visual restyle of `/admin/login` onto the existing neutral-gray admin system (`bg-gray-50` page, centered `rounded-2xl` white card, gray-900 button) matching `PriceEditModal`/`AdminSidebar`'s conventions — no behavior change, `authenticate` server action and `error=1` redirect pattern untouched. Verified live: DOM/class spot-check confirmed zero `style={{` attributes and zero legacy-token matches; wrong-password submit correctly redirected to `/admin/login?error=1` with the new red error styling rendering; mobile width (375px) confirmed clean. Real-credential success-path login not exercised — no plaintext AdminUser password was available to this session (read-only DB access only); the untouched `authenticate` action plus the working error-path redirect through that same action is the evidence this wasn't broken. The page had no already-authenticated redirect before this change and still doesn't — none was added, matching scope.
- 2026-08-29 — Added a reusable `PasswordInput` component (`src/components/ui/PasswordInput.tsx`) — labeled password field with a right-aligned show/hide toggle (eye/eye-slash icons matching `AdminIcons.tsx`'s inline-SVG convention), className props left overridable so a future brand-token consumer (member `/login`, `/reset-password`, `/activate`) can reuse it without editing this file. Wired into `/admin/login` only, replacing its plain `type="password"` input; `authenticate` server action, email field, and error-message rendering untouched. Verified live: toggle button renders inside the field and correctly flips the input's `type` attribute (DOM check) without clearing the typed value or navigating/submitting the form; button is `type="button"`, reachable via Tab, and toggles via mouse click (synthetic Enter/Space key events didn't register in this Browser-pane automation, but the element is a native `<button>` with no custom key handling, so real Enter/Space activation is guaranteed by standard HTML semantics — not a code gap). Real-credential login (provided directly by Arjay for this test) redirected to `/admin`; a bad-credential submit still redirected to `/admin/login?error=1` with the existing error message — both outcomes regression-clean. Zero legacy-token matches in both touched files. `/login`, `/reset-password`, `/activate` untouched (git status confirms only the two in-scope files changed).
- 2026-08-29 — `AdminTopbar`'s Sign Out is now a two-step confirmation instead of an instant sign-out: extracted the inline server action into `adminSignOut()` (`src/lib/actions/admin-auth-actions.ts`, unchanged `signOut({ redirectTo: '/admin/login' })` call) and added `SignOutButton.tsx` (client component, `useTransition`-driven), matching `SendRenewalLinkButton.tsx`/`MembershipReviewActions.tsx`'s existing button+`Modal` neutral-gray convention exactly (Cancel: `border-gray-200`/`text-gray-600`; confirm: `bg-gray-900`/white; both `disabled:opacity-60` during the pending request). `AdminTopbar.tsx` stays a server component — only the button+modal became client, `email`/initials rendering untouched. Verified live against a real admin session: clicking Sign Out opens the modal without signing out; Cancel, Escape, and backdrop click all close it with the session still active on the same page (existing `Modal` behavior, unchanged); the modal's own Sign Out button actually signs out — confirmed by navigating back to `/admin` afterward and landing on `/admin/login`, not cached content. Regression-checked `/admin`, `/admin/bookings`, `/admin/resources`, `/admin/memberships`, `/admin/bulletin` — topbar/initials/tooltip unaffected on all five. `tsc --noEmit` clean; zero legacy/brand-token matches in the three touched/new files; `git status` confirms only `AdminTopbar.tsx` (modified) plus the two new files changed.
- 2026-08-29 — Investigated the `bookings_customer_id_fkey` mismatch flagged in Next Up. Found `ON UPDATE CASCADE` already matched the live constraint; the real gap was `schema.prisma` never explicitly annotating `onDelete` on `Booking.customer`, defaulting to Prisma's implicit `SetNull` for an optional relation, against a live constraint that is actually `RESTRICT`. Decided RESTRICT is correct — it matches the app's existing "disable, don't delete" precedent (`Resource.isActive`) and no live delete-customer feature exists — and made `schema.prisma` say so explicitly (`onDelete: Restrict, onUpdate: Cascade`). No migration needed; the database was already correct, and the live constraint was re-queried afterward and confirmed byte-identical to before this change.
- 2026-08-29 — `/admin/memberships`'s `STATUS_FILTERS` bar now filters on the derived `MembershipDisplayStatus` (Pending/Awaiting Payment/Active/Expired/Rejected) instead of the raw `ApplicationStatus` enum, closing the gap flagged in the 2026-08-29 expired-status entry — Awaiting Payment/Active/Expired are no longer all lumped under one "Approved" tab. All/Pending/Rejected keep the original DB-paginated query untouched; the 3 derived sub-filters fetch every `approved` application once (`relationLoadStrategy: 'join'`, matching this page's pre-existing exception), derive each row's display status via the existing `getMembershipDisplayStatus`, filter in memory, then paginate the filtered array in JS. Verified live via Playwright against a temporary seeded admin + 37 temporary applications spanning all 6 states (including 27 `active` rows to cross a page boundary within a derived filter), all deleted after the run: each tab shows the exact expected row count and paginates correctly (Active: 25 on page 1, 3 on page 2 — the extra row being one genuinely-active pre-existing membership, not a bug); `/admin/memberships/[id]` opens correctly for a representative row in all 6 states; `/admin`, `/admin/bookings`, `/admin/resources`, `/admin/bulletin` regression-clean. `tsc --noEmit` clean; only `src/app/admin/(protected)/memberships/page.tsx` touched.
- 2026-08-29 — Dashboard's Recent Bookings and Pending Membership Applications now show an explicit empty-state message ("No bookings yet." / "No pending applications.", matching `BulletinList.tsx`'s existing `text-sm text-gray-500` admin-panel convention) instead of rendering blank when their list is empty — closes the gap flagged in the 2026-08-28 Dashboard real-data-wiring entry.
- 2026-08-29 — Performance fix for `/admin/memberships`: Vercel runtime logs had shown `memberships:pageTotal` at 2.422s against a ~200ms actual Prisma commit, traced to the 2026-08-29 display-status fix's `getLatestMembershipsByCustomerIds` call paying a second full connection-priming round trip as a separate query after the main list fetch. Folded into the same query via a nested `customer: { include: { memberships: { orderBy: { startDate: 'desc' }, take: 1 } } }` include (confirmed via Prisma query-event logging to compile to a single query with a `LEFT JOIN LATERAL` under the existing `relationLoadStrategy: 'join'`, not N+1), removing one top-level Prisma call per branch; `latestMembershipsByCustomer` is now derived from the already-fetched nested data instead of a second round trip. `membership-latest.ts` (still the source of truth for the `[id]` detail page), `membership-display-status.ts`, and the detail page itself are untouched (byte-identical, confirmed via `git diff`). Local `memberships:pageTotal` measured 595.66ms post-fix (was not independently re-measured pre-fix locally, since the 2.422s baseline was a Vercel-runtime number, not local — local dev's session-mode pooler doesn't exhibit the same connection-priming cost pattern as staging/production's transaction-mode pooler per the existing CLAUDE.md note); the query-count reduction itself (2 top-level queries instead of 3, confirmed via live query logs) is the change that removes the extra priming round trip wherever it occurs. `tsc --noEmit` clean. Verified live via the Browser pane against a temporary 37-row seed (`scripts/seed-membership-status-filters.ts`, reused as-is) spanning all 6 derived statuses: every filter tab (All/Pending/Awaiting Payment/Active/Expired/Rejected) and pagination boundary (Active: 25 rows page 1, 3 page 2) rendered identical content to before; all 5 non-pending detail-page statuses spot-checked; `/admin`, `/admin/bookings`, `/admin/resources`, `/admin/bulletin` regression-clean. Cleanup confirmed via `scripts/seed-membership-status-filters.ts --cleanup` — post-cleanup counts (6 customers / 1 application / 1 membership) match the pre-seed baseline exactly.
- 2026-08-29 — Diagnosed a Vercel/Supabase region mismatch as the dominant cause of the noticeable `/admin/*` latency on deployed `dev`: `x-vercel-id` response headers showed serverless functions executing in `iad1` (US East/Virginia) — Vercel's default, since `vercel.json` had no `regions` field — while the Supabase project runs in `ap-southeast-2` (Sydney). Every DB round trip was crossing that distance. Supavisor pooler logs corroborated it: each navigation produced a fresh burst of `Backend authenticated` events followed by the connection closing within seconds, confirming per-invocation connection setup with no reuse across requests, on top of the transcontinental RTT. Measured live (logged in, real clicks, no code changed): login `POST /admin/login` ~5,300ms; Bookings/Resources & Pricing/Memberships/Bulletin tab navigations ~1,600–1,900ms each. Read-only investigation — no fix applied in this pass (Vercel MCP access didn't extend to this project's server-side runtime logs, so evidence came from response headers, client-side Performance-API timing, and Supabase's own logs).
- 2026-08-30 — Re-verified after the region fix: Vercel Function Region changed to `syd1` (Sydney) to match Supabase's `ap-southeast-2`, `dev` redeployed. `x-vercel-id` now reads `sin1::syd1::...` on both page and API routes (was `sin1::iad1::...`). Re-measured the same actions logged in with real clicks: login `POST /admin/login` dropped from ~5,300ms to 822ms; Dashboard 318ms; Bookings dropped from ~1,900ms to 330ms; Resources & Pricing from ~1,895ms to 309ms; Memberships from ~1,860ms to 311ms; Bulletin from ~1,618ms to 294ms — a consistent ~82–85% reduction across every action, including login, confirming the region mismatch was the dominant cause rather than one of several contributing factors. The ~300ms now remaining per tab is in line with the separately-tracked connection-priming floor (see Next Up) — a smaller, already-known cost, not evidence the region fix under-delivered. Doc-only/read-only pass — no code, env vars, or further Vercel settings changed.
- 2026-08-30 — Doc-only correction: CLAUDE.md's Local-dev database connection bullet and this file's Next Up connection-priming bullet both cited ~500–600ms as the connection-priming floor, but that figure (measured 2026-08-29) was confounded by the Vercel/Supabase region mismatch fixed earlier today — not a clean priming number. Both bullets now cite ~300ms (the region-fix-corrected figure from the entry directly above) as the current baseline for any future Prisma Accelerate / pooling-strategy / Fluid Compute investigation, with the original ~500–600ms measurement kept in place and reframed as region-confounded rather than deleted.
- 2026-08-30 — Re-investigated the AdminSidebar fold-button bug flagged in Next Up: reproduced live against the deployed `dev` URL (real clicks, screenshots) and confirmed it's a harness measurement artifact, not a real bug. The fold button correctly collapses/expands the sidebar both directions — confirmed visually via screenshot each time. `getComputedStyle`/`getBoundingClientRect` read via browser-automation JS immediately after a click occasionally returned a stale value one state behind (matching the pre-existing "Browser pane doesn't composite frames" known issue), self-correcting on a subsequent read a moment later. No code changed; `AdminSidebar.tsx` is confirmed working as designed.
- 2026-08-30 — Reconciled the `/api/resources` round-trip discrepancy flagged in Next Up: measured directly against the deployed `dev` URL via real `fetch()` calls and cold full-page navigations to `/book` (where this endpoint fires on load). Results: 175–259ms across 5 warm fetches, 222ms and 687ms on two separate cold navigations — `x-vercel-id` confirmed `sin1::syd1` on every call, i.e. already running in the post-region-fix region. Neither stale figure holds up: the real cost lines up with the already-documented ~300ms connection-priming floor (occasionally higher on a genuinely cold invocation), not the old 1.3–4s or 20–100ms numbers. Most likely explanation: the 1.3–4s instrumentation reading predated the 2026-08-30 region fix (same `iad1`-vs-Sydney cause that hit every admin route), and the 20–100ms reading was a Browser-pane measurement artifact, the same failure mode confirmed separately on the AdminSidebar fold-button investigation. No code changed.
- 2026-08-30 — Closed out the connection-priming-floor investigation flagged in Next Up: confirmed via the Vercel dashboard (Project Settings → Functions) that Fluid Compute is already **Enabled** on this project, and Function Region is set to Asia Pacific (`syd1`), matching the earlier region fix — both were default/already-applied, not missing. Fluid Compute has been Vercel's default for all new projects since April 2025, and this project was scaffolded after that date, so the ~300ms floor already reflects Fluid Compute's warm-instance connection reuse, not an unoptimized baseline. Live measurement (see the `/api/resources` reconciliation entry above) already showed the expected warm/cold pattern — 175–260ms on warm hits, up to ~700ms on a cold instance — consistent with this. Two further levers were researched and deliberately deferred, not pursued: switching from Supabase's PgBouncer transaction-mode pooler to a native `pg` Pool + Vercel's `attachDatabasePool` (free, but a real `src/lib/prisma.ts` refactor plus a new dependency for a further ~100–200ms on an internal-only admin panel), and Prisma Accelerate (low code change, but a recurring paid service plus a new dependency for the same marginal gain). Decision: not worth the added complexity/cost right now given this is admin-panel-only latency, not customer-facing checkout latency — revisit only if `/admin/*` traffic or scope grows enough to matter. No code changed.
- 2026-08-30 — Re-enabled Vercel Authentication (Standard Protection) project-wide, closing out the Vercel Authentication decision flagged in Next Up — done now rather than deferred to go-live, since dev/staging had been sitting fully open with no login wall. Confirmed Standard Protection is the only protection tier available on Hobby (Pro-only Advanced Deployment Protection add-on gates the "All Deployments" scope and Password Protection). Created a Protection Bypass for Automation secret (`VERCEL_AUTOMATION_BYPASS_SECRET`) for future webhook/automation use and redeployed `dev` so it's actually live on that deployment. Verified live: unauthenticated requests to the dev URL now redirect to Vercel's login page; Arjay's own logged-in browser session and Claude's browser-based verification are unaffected (auto-exempted as team members); the only currently-Enabled PayMongo webhook points at a local ngrok tunnel and is unaffected by this change; a stale Disabled webhook pointing at a bare `.vercel.app` URL was left untouched. Not yet independently verified: the bypass secret's end-to-end effectiveness via header or query param — flagged for a follow-up check via an automated client that can set custom headers. Production's custom domain remains exempted per Standard Protection's existing rule, unchanged by this pass. Doc-only change here; the actual settings change was made directly in the Vercel dashboard, not via code.
- 2026-08-30 — Verified the Protection Bypass for Automation secret end-to-end via a direct `curl` test (closes the one open caveat from the same-day Standard Protection re-enable entry): a plain request to the dev URL gets a `302` to Vercel's `sso-api` login gate, while the same request with the `x-vercel-protection-bypass` header gets a `307` to the app's own `/admin/login` — confirming Vercel's edge layer accepts the bypass and forwards the request into the app (the subsequent redirect to `/admin/login` is just the app's normal no-session behavior, not a second block). An earlier attempt to verify this via `web_fetch`'s query-param form gave a false negative, most likely due to that tool sanitizing URL parameters that look like credentials — not a real problem with the bypass secret or Vercel's feature. No code changed.
- 2026-08-30 — Public-site palette change (client-requested via Arjay): repainted `--color-brand-dark`, `--color-brand-mid`, `--color-accent-primary` to new hex values and added a new unused `--color-accent-teal` token in `src/app/globals.css`'s `@theme` block; Hero's 3-stop gradient overlay replaced with a flat `bg-brand-dark/55` solid overlay. `CtaBanner`/`Facilities`/`TwoSides` needed no edits — they repainted automatically via the shared tokens, confirmed byte-identical via `git diff`. Admin panel confirmed unaffected (still neutral-gray). Next Up: decide `accent-teal` placement (link hovers / icon accents / secondary button) — defined in the theme but not yet applied anywhere.
- 2026-08-30 — Home/sitewide visual pass: Facilities' 5 tiles now render distinct Picsum stock placeholders (temporary, pending real venue photography — see Next Up) via a new per-tile `imageSeed`, with sharp (`rounded-none`) corners on the image containers; `next.config.ts` gained `picsum.photos` to `images.remotePatterns`. Hero's flat `bg-brand-dark/55` overlay (added in the palette-change entry above) was removed entirely, replaced by a new `.hero-text-shadow` utility (`globals.css`) applied to the eyebrow/headline/subhead for legibility over the bare photo. Every button/CTA across the public site (~35 files) changed `rounded-*` → `rounded-none`, including icon-only circular controls (mobile nav toggle, guest-count +/− steppers, footer social icons) — cards, badges, form inputs, and step-indicator dots were deliberately left untouched throughout. One listed line, `TwoSides.tsx:32`, was skipped and left as `rounded-full`: it's the decorative "&" ornament `<div>` between the two columns, not a button. Admin panel confirmed unaffected. `tsc --noEmit` clean; verified live via the Browser pane (Home, `/book`'s full 6-step wizard, About, Café & Bar incl. Bar mode, Membership + `/apply`, News, `/login`, `/forgot-password`, `/admin` login) at both desktop and mobile widths.
- 2026-08-30 — `CtaBanner`'s `bg-gradient-to-br from-brand-dark to-brand-mid` + decorative `<Sunburst>` line-burst replaced with a real photo background (`public/images/cta-background.jpg`, a pickleball net/court shot provided by Arjay) plus a flat `bg-brand-dark/70` overlay for text/button legibility — consistent with the rest of this session's no-gradient pass. `bg-brand-dark` kept on the `<section>` itself as a solid fallback in case the image fails to load. `Sunburst.tsx` itself untouched; grep confirmed 4 other live consumers (`TwoSides`, `ApplyCta`, `SpeakeasyFeature`, `AboutCta`), so it isn't orphaned. Default `object-cover` center crop used as-is — no `object-position` override needed; the mobile-width (375–440px) render showed the net post and balls framed cleanly with no awkward cutoff. `tsc --noEmit` clean; verified live via the Browser pane at mobile pane width (photo + overlay rendering correctly, headline/subtext/both buttons clearly legible, zero gradient/Sunburst references remaining) — desktop-width (1024–1440px) screenshot capture hit a tooling issue in this session (blank/stale frames after `resize_window`) despite the underlying DOM/computed-style checks (correct `fill`+`object-cover` sizing, correct 70%-alpha overlay, correct paint order) confirming the same non-responsive styling applies at any width; worth a spot-check on an actual desktop browser before promotion. Other Home sections (Facilities, footer) confirmed rendering unaffected around the changed section.
- 2026-08-30 — `accent-teal` applied for the first time: `/cafe-bar`'s `ModeSwitch` Bar-mode active state changed from `bg-accent-primary` to `bg-accent-teal`, giving Café (red, daytime) and Bar (teal, speakeasy/night) visually distinct active pills for the first time — Café's active state (`bg-accent-primary`, unchanged) and every other cafe-bar component untouched. Verified live: default Café-active state unchanged (red pill), clicking Bar shows the teal fill with legible text and the correct heading swap ("Winston After Dark"), clicking back to Café reverts correctly; `aria-pressed` confirmed true/false on the right button after each click via the DOM (not just visually); focus-visible outline still present on Tab; mobile width (375px) renders both states without overflow/clipping.
- 2026-08-30 — Fixed `ModeSwitch`'s outer pill container (`rounded-full` → `rounded-none`) — missed in the sitewide button-corner pass earlier today since it's a wrapping `<div>`, not a `<button>` tag; both inner buttons were already correct. Not a tracked Next Up item.
- 2026-08-31 — Investigated a reported mojibake bug ("CafÃ©" instead of "Café") in `ModeSwitch.tsx`: not reproducible. Hexdump of the file's Café string confirmed proper 2-byte UTF-8 (`0xC3 0xA9`), and a repo-wide grep for the `Ã` mojibake pattern (excluding `node_modules`/`.git`/`.next`) returned zero hits anywhere in the codebase. No code changed. Verified live via the Browser pane: "Café" renders correctly in both modes, Bar's teal active state + "Winston After Dark" heading swap still work, reverting to Café still works, and both pill corners are still `rounded-none`.
- 2026-08-31 — Re-verified the 2026-08-21 Home mobile-responsiveness audit against current `dev` (post-2026-08-30 palette/photo/corner passes): TwoSides' contrast failure still blocking (now ~1.6–1.7:1, down from the original ~1.07:1, but still nowhere near AA), Navbar opacity seam/hamburger-size/tap-target findings all unchanged, StatsBar's 2-line wrap confirmed still present at 320/375/414 (resolved on its own at 639px, wide enough not to wrap) — written to `docs/audits/2026-08-21-home-mobile-audit.md`'s new "Re-verification — 2026-08-31" section. Same pass also checked the two background-swapped sections (CtaBanner, Hero) for new issues from the photo/overlay swap: none found — CtaBanner's overlay-blended contrast is 9.4–15.7:1 across all widths, Hero's text averages 4.96–5.80:1 even before crediting `.hero-text-shadow` (one isolated sub-pixel-scale dip to ~1.28:1 in the tagline noted as a minor caveat, not a blocking finding).
- 2026-08-31 — Fixed all 5 re-verified Home mobile findings (`TwoSides.tsx`, `Navbar.tsx`, `StatsBar.tsx`, `Footer.tsx`): **(1)** TwoSides' "Book a Court →" link now sits on an opaque mobile-only backdrop (`bg-brand-light`, `rounded-none`, `px-3 py-2`, reverting to `md:bg-transparent md:px-0 md:py-0`) — deviated from the prompt's suggested `bg-brand-dark/70` starting point because a dark backdrop caps out at **2.79:1 even at full opacity** (computed: `text-accent-primary` #CD1818 has too low a luminance itself to clear 4.5:1 against any dark backdrop, no matter the opacity), so a light backdrop was used instead — measured **5.12:1 at all 4 widths** (320/375/414/639), position-independent since the backdrop is fully opaque; confirmed reverting to the original transparent/no-padding desktop rendering at 1024/1440. **(2)** Navbar's mobile-menu opacity seam fixed by deriving `headerSolid = scrolled || menuOpen` and using it for both the header's background class and (extending slightly beyond the literal ask, to avoid trading one legibility bug for another) the logo's invert class — an unscrolled menu-open state would otherwise have kept the inverted/white logo on a now-solid light header; verified header and panel now compute to the identical solid `rgb(253,243,231)` at all 4 widths, scrolled-state behavior unchanged (regression-checked). **(3)** Hamburger button `h-10 w-10` → `h-11 w-11`, measured 44×44px at all 4 widths (was 40×40px), icon glyph unchanged at 24×24px so it doesn't look oversized. **(4)** TwoSides' two links and Footer's Quick Links gained `py-2` (Footer's `Link` also made `block` so the padding actually participates in layout) — measured tap-target height now 36px for all of them (was ~20px/19px), no visual collision with adjacent content at any width. **(5)** StatsBar's 4 cells gained `min-h-[140px] sm:min-h-0` (140px = the natural height of the 2-line-wrapped cell, measured directly) — all 4 cells now measure a uniform 140px at 320/375/414/639 (previously row 1 was 139px vs. row 2's 123px), reverting to natural/no-min-height at `sm:` (640px) where the grid also switches to 4 columns; confirmed 639px (still below `sm:`, so the min-height still applies there) reads as generously spaced rather than awkward, not padded-looking. `tsc --noEmit` clean. Verified live via the Browser pane at 320/375/414/639 (all 5 fixes, contrast/sizes/screenshots) and 1024/1440 (computed-style spot-check only — screenshot capture at these widths hit the same known blank-frame tooling issue noted in the 2026-08-30 CtaBanner entry); full-page regression screenshots at 320px confirmed Hero/Facilities/Footer render unaffected around the changed sections.
- 2026-08-31 — `accent-teal` extended to `/membership`'s tier-card perk checkmarks: `TierCards.tsx`'s shared checkmark markup (one `PERKS.map`, rendered identically across all 3 cards) changed `bg-accent-primary/10 text-accent-primary` → `bg-accent-teal/10 text-accent-teal` on the icon backdrop + glyph. 6-Month card's red border/"MOST POPULAR" badge (`accent-primary`, untouched) confirmed unchanged via computed-style check. Grepped all `CheckIcon` consumers — only `MembershipStatusCard.tsx` besides this file, and it has its own independent class string, so no other page shares this markup. `tsc --noEmit` clean; verified live (desktop computed-color check on all 12 checkmarks, 375px screenshot).
- 2026-08-31 — CtaBanner's desktop-width (1024–1440px) rendering visually confirmed clean, closing the spot-check flagged in the 2026-08-30 CtaBanner entry and carried in Next Up since. Verified directly via browser at 1536px (natural desktop width): the pickleball paddle/ball photo renders correctly with `object-cover`, no awkward crop, the `bg-brand-dark/70` overlay gives strong text/button legibility, and both CTA buttons render cleanly. Confirmed outside a Claude Code prompt (planning-chat browser session), not by this repo's own VERIFY tooling.
- 2026-08-31 — 6 sample bulletins (one per `BulletinCategory`) (re-)seeded via `scripts/seed-sample-bulletins.ts` with placeholder images (`/images/placeholder.jpg`), for `/news`/`/book` visual review. The existing script from the 2026-08-27 seed already satisfied every field `BULLETIN_CATEGORY_RULES` currently requires per category (`affectedFacility`/`impact`/`action`/`eventStartAt` unconditionally; `imageUrl` for Renovation/Tournament/Community; `eventEndAt`/`expiresAt` for all but General; `ctaLabel`+`ctaUrl` for Tournament/Community) — no data gaps from fields added since 2026-08-27/28, so no updates to the script's content were needed. Ran it against local dev: first run created all 6 fresh (0 pre-existing rows matched by title); confirmed via direct SQL that all 6 rows have the correct category, a non-null placeholder `image_url`, and every category-required field populated; re-ran the script a second time and confirmed title-match convergence (0 created, 6 updated, row count still 6 — no duplicates). Browser-verified `/news` (all 6 render with correct category/priority labels, full field content, CTAs on Tournament/Community, placeholder image visible) and `/book`'s `AnnouncementGate` (top 3 non-expired notices render correctly with High Priority badges). Could not complete the `/admin/bulletin` Edit-modal validation check from this prompt's VERIFY list — logging into `/admin/login` requires typing a password into the form, which this session's credential-entry restriction blocks even for the documented local test account; Arjay should spot-check the 6 rows' Edit modals himself to confirm no missing-required-field errors appear (the DB-level field check above is a reasonable substitute but isn't the same proof). No cleanup performed — this is persistent seed data, left in place per the original 2026-08-27 precedent.
- 2026-08-31 — Doc-only: corrected CLAUDE.md's stale Navbar-invisible-on-light-page Known Issue bullet — it still listed `/book` and `/forgot-password` as unfixed, but both were already fixed per the 2026-08-29 and 2026-08-27 entries above. Verified both fixes are actually live in `src/app/book/page.tsx` and `src/app/forgot-password/page.tsx` (the `bg-brand-dark` band pattern) before editing the doc. No code changed.
- 2026-08-31 — `public/images/` reorganized into `hero/`, `cta/`, `facilities/` subfolders: `hero-background.jpg`/`cta-background.jpg` moved into their own folders (`Hero.tsx`/`CtaBanner.tsx` `src` paths updated to match); Facilities' 5 tiles switched from Picsum stock placeholders to 5 renamed local copies of the existing `placeholder.jpg` (one per facility, `imageSeed` field replaced with `image`). `picsum.photos` removed from `next.config.ts`'s `images.remotePatterns` as no longer needed. Root `placeholder.jpg` left untouched — still referenced by seeded Bulletin data. `tsc --noEmit` clean; verified live via the Browser pane (all 8 images — hero, cta, 5 facilities tiles — confirmed loading via DOM `complete`/`naturalWidth` checks; `/news` regression-checked, confirming `placeholder.jpg` still resolves at its original root path).
- 2026-08-31 — Member QR code decision implemented (closes the "not yet decided" item — front-desk check-in): new `Customer.checkInToken` column (nullable, unique, hand-written migration `20260831010000_add_customer_check_in_token`, RLS deny-all reconfirmed post-migration), generated via `crypto.randomBytes(24).toString('base64url')`. New shared `src/lib/check-in-token.ts` (`generateCheckInToken`/`getOrCreateCheckInToken`/`regenerateCheckInToken`/`generateQrCodeDataUrl`, the last wrapping the new `qrcode` dependency's `QRCode.toDataURL`) is the single source of truth for both token generation and QR image rendering, so `/account`'s initial render and the new regenerate endpoint can never drift in size/format. `/account/page.tsx` calls `getOrCreateCheckInToken`/`generateQrCodeDataUrl` only in the non-null-membership branch (a customer with no membership never gets a token) and threads `qrCodeDataUrl` into `MembershipStatusCard`, which now renders a real `data:image` `<img>` (both the thumbnail and the "View Full Screen" modal) in place of the old `api.qrserver.com` placeholder, and gained a "Regenerate Code" control (button + confirm `Modal`, `useTransition`-driven, mirroring `SendRenewalLinkButton.tsx`/`SignOutButton.tsx`'s existing pattern) that calls new session-gated `POST /api/account/check-in-token/regenerate`. `qrcode` + `@types/qrcode` added as new approved dependencies (see CLAUDE.md). Verified live end-to-end against a temporary seeded active member (new gitignored `scripts/verify-check-in-token.ts`, mirroring `verify-member-repricing-and-tier.ts`'s convention, removed after): `checkInToken` confirmed `null` pre-load, non-null and DB-matching post-load with a real `data:image` `<img>` src (both thumbnail and modal), unchanged across a reload (idempotent), and changed to a new distinct value + new distinct `data:image` src after clicking Regenerate → Confirm (DB and rendered UI both confirmed); a parallel no-membership customer confirmed `checkInToken` stays `null` and no QR section renders. `tsc --noEmit` clean; RLS confirmed intact on `customers` post-migration; `/book`, `/admin/login` spot-checked unaffected (no shared files touched); post-cleanup counts (8 customers / 1 membership) match the pre-test baseline exactly. Admin-side scan/verify UI (Prompt 2) deliberately not built here — see Next Up.
- 2026-08-31 — Admin-side check-in scan/verify (Prompt 2) built, closing out the item flagged above. `/account/page.tsx`'s inline computation of `activationCentavos`/`creditCentavos`/`remainingCreditCentavos`/`expiryDateLabel`/`isExpired` for a given `Membership` was not previously factored out — extracted into a new exported `buildMembershipDisplayFields(membership)` in `src/lib/membership-latest.ts` (async, since it still needs its own `MembershipCreditTransaction` lookup for `creditCentavos`), with `/account/page.tsx` updated to call it — confirmed byte-identical rendering via live browser check against the same seeded active member used for the endpoint checks below. `getLatestMembershipsByCustomerIds`/`getLatestMembershipByCustomerId` were widened from returning `{ endDate: Date }` to the full `Membership` row (needed as `buildMembershipDisplayFields`'s input) — backward-compatible, since both existing consumers (`send-renewal-link/route.ts`, the admin membership detail page) only ever read `.endDate`, confirmed via `tsc --noEmit` with no other files touched. New `GET /api/admin/check-in/[token]/route.ts` (admin-session-gated, mirrors `memberships/[id]/route.ts`'s 401 check exactly): 404 `{ error: 'Code not recognized' }` for an unrecognized token, 200 `{ found: true, hasMembership: false, name }` for a customer with no membership, 200 with full tier/expiry/credit fields (via `getLatestMembershipByCustomerId` + `buildMembershipDisplayFields`) for one with a membership — no email/PII included on the no-membership or not-found branches. New `src/components/admin/CheckInScanner.tsx` (`'use client'`, new `html5-qrcode` dependency) initializes `Html5Qrcode` and requests the back/environment camera only inside a post-mount `useEffect`; on decode it pauses the scanner, calls the verify endpoint, and renders a color-coded result card (green active, amber expired, gray no-membership, red not-recognized) with a "Scan Next" button that resumes scanning; camera-permission-denied/no-camera renders an inline fallback message instead of crashing; scanner stops on unmount. New `src/app/admin/(protected)/check-in/page.tsx` (server component, mirrors the bulletin page's heading/card shell) renders it. `AdminSidebar.tsx` gained a 6th "Check-In" nav entry (appended at the end) and `AdminIcons.tsx` gained a new hand-authored `CheckInIcon` (scan-frame-with-inner-square, matching the existing 1.5 stroke-width/24×24 viewBox convention). Verified live end-to-end against temporary seed data (new gitignored `scripts/verify-admin-check-in-scan.ts`, mirroring `verify-admin-renewal-link.ts`'s real-HTTP-login convention via `/api/auth/csrf` + `/api/auth/callback/credentials`, removed after): direct authenticated `GET` calls confirmed all 4 cases (active → 200 with correct tier/expiry/credit fields, expired → 200 `isExpired: true`, no-membership → 200 `hasMembership: false` with no email key, garbage token → 404) plus a 401 for an unauthenticated call; `/admin/check-in` loaded live as an authenticated admin, rendering the new sidebar tab (correctly highlighted active) and the graceful camera-denied fallback message (no crash, no uncaught console errors — only the deliberate `console.error` diagnostic log in the catch branch); all other 5 admin tabs (Dashboard, Bookings, Resources & Pricing, Memberships, Bulletin) regression-spot-checked unaffected; `/account` re-verified live for the seeded active member post-refactor, rendering values byte-identical to the API response (3-Month, ₱2,000.00 activation + ₱3,500.00 F&B credit, ₱3,500.00 of ₱3,500.00 remaining, September 30, 2026 expiry) — the parallel expired-member browser login attempt was flaky in this automation environment (session didn't switch after a second credentials submit) and wasn't forced further, since `/account` and the verify endpoint call the exact same `buildMembershipDisplayFields` function, and the expired case was already proven correct via the direct authenticated API call. `tsc --noEmit` clean. **Known limitation, not fully verified**: a real end-to-end camera scan of a physical/on-screen QR code cannot be exercised by browser automation (no real camera feed available) — Arjay should manually verify this final step with a real phone/laptop camera against a real member's `/account` QR code. Post-cleanup counts (8 customers / 1 membership / 2 admin users) match the pre-test baseline exactly.
- 2026-08-31 — Closes the camera-scan limitation flagged above: Arjay manually verified `/admin/check-in` end-to-end with a real phone/laptop camera against a real member's `/account` QR code, live and working. Member check-in QR (backend, `/account` UI, admin scan/verify) is now fully closed out with no outstanding threads.
- 2026-08-31 — Investigated a reported "Regenerate QR" bug: not reproducible. Network tab showed three separate regenerate calls, all `200`, each correctly followed by a fresh `data:image` response — concluded a transient blip rather than a real bug, no code changed. Worth a closer look if it recurs under a specific repeat-click pattern, but not chased further absent a repro.
- 2026-08-31 — F&B membership credit now has a real spend path: a member booking through the session-aware direct-booking route (`POST /api/bookings`) is confirmed instantly and paid via `Membership.creditBalanceCentavos` when that balance fully covers the grand total — full-or-nothing by design, no partial-credit application. Schema additions (hand-written migration `20260831020000_add_membership_credit_redemption_support`, RLS reconfirmed intact post-migration on `payments`/`membership_credit_transactions`/`memberships`/`bookings`): `CreditTransactionReason` gained `booking_redemption`; `MembershipCreditTransaction` gained a nullable `bookingId` FK (null for pre-existing activation/renewal rows); `Payment` gained a `method` enum (`paymongo` default / `membership_credit`) so a credit-covered booking's `Payment` row is distinguishable from a real PayMongo charge. The credit check/decrement happens inside the same `$transaction` as booking creation, guarded by an `updateMany({ where: { creditBalanceCentavos: { gte: grandTotal } } })` rather than a read-then-write — this is what makes two concurrent bookings racing the same balance resolve to exactly one `creditCovered: true`, never a double-spend or a negative balance. `getActiveMembership` was extracted alongside the existing `isActiveMember` in `customer-resolution.ts` (byte-identical behavior for existing callers) so the route has the real `Membership` row, not just a boolean, to check the balance against and decrement. Booking-confirmation-email logic (previously duplicated inline in the PayMongo webhook handler) was extracted into a new shared `src/lib/booking-confirmation.ts` (`sendBookingConfirmationEmailForBooking`, optional `creditRedemption` param) so both the webhook's `payment.paid` path and the new instant-confirm path send the identical email shape; `resend.ts`'s template gained an optional "This booking was covered by your F&amp;B credit. You have X remaining." notice block, rendered only when that param is passed. `BookingForm.tsx` redirects straight to `/book/confirmation` on `creditCovered: true`, skipping the Payment step and `/api/checkout` entirely. Explicitly unchanged, per scope: `/api/checkout`, the anonymous hold-then-`PATCH`-attach path, `PaymentStep.tsx`, `booking-pricing.ts`'s pricing math, and anything membership-application/renewal-related — a member with insufficient credit (or no membership, or an anonymous booker) goes through the exact pre-existing PayMongo flow with no behavior change. Verified end-to-end via a temporary seeded set (admin + 4 members: full-cover, insufficient, lapsed, concurrent-race; new gitignored `scripts/verify-membership-credit-redemption.ts`, removed after): full-cover case produced a `confirmed` booking, a `membership_credit`/`paid` `Payment` with both PayMongo id fields null, a `-grandTotal` `booking_redemption` ledger row, and a `Membership` balance decremented to exactly 0 — confirmed both via direct DB checks and a real Resend-delivered confirmation email showing the credit note; insufficient-credit case confirmed the pre-existing flow completely unchanged, including a self-signed `payment.paid` webhook simulation (matching the real PayMongo payload shape documented elsewhere in this file) correctly confirming the booking with `method` staying `paymongo` and no credit note in that email; anonymous and lapsed-membership bookings regression-clean (`isMember: false`, non-member pricing, unaffected); concurrency case fired two real concurrent `POST /api/bookings` calls against a balance sized for exactly one — exactly one came back `creditCovered: true`, the other a normal `pending_payment` booking, final balance exactly 0 (not negative, not double-spent); admin `/admin/bookings` list + detail page both confirmed rendering the credit-covered booking cleanly with no code changes needed (grep confirmed neither page has any UI assumption tied to `paymongoCheckoutSessionId`/`paymongoPaymentIntentId` existing). Also confirmed live via the Browser pane: submitting the real booking wizard as a fully-covered member redirects straight to `/book/confirmation` with zero `/api/checkout` network calls made. `tsc --noEmit` clean; `git status` confirmed no out-of-scope files touched. Post-cleanup counts (8 customers / 1 membership / 3 bookings / 2 payments / 1 credit transaction / 2 admin users) match the pre-test baseline exactly.
- 2026-08-31 — Doc-only: corrected CLAUDE.md's Booking confirmation and Membership credit ledger bullets, both left stale by the 2026-08-31 credit-redemption work — the webhook-confirmation bullet didn't note the credit-covered exception, and the ledger bullet still said no spending events existed.
- 2026-08-31 — Doc-only: corrected CLAUDE.md's "Customer & auth model" bullet, which still described member login as server-action-driven — updated to reflect the 2026-08-31 client-side `signIn()` conversion.
- 2026-08-31 — Doc-only: removed a stray `prisma/manual-sql/add-membership-credit-redemption-support.sql` created during the 2026-08-31 credit-redemption prompt — off-convention (manual-sql is reserved for the exclusion-constraint/RLS/email-assets-bucket master copies only); the real migration under `prisma/migrations/` is unaffected.
- 2026-08-31 — Three member sign-in/sign-out fixes on the public site. `LoginForm.tsx` converted from a server action (`signIn(...redirectTo)`) to client-side `signIn('member-credentials', { redirect: false })` + `router.push('/account')` — fixes the Navbar not reflecting a fresh login until a tab switch/refocus, since the server-action path never touched `next-auth/react`'s client session context that `useSession()` reads. `/login` no longer has a `?error=1` query param; the "Invalid email or password." message is now local component state. Added a password show/hide toggle to `/login` via the existing `PasswordInput` component (brand-toned overrides, not its default admin gray styling). `Navbar.tsx`'s Sign Out (desktop and mobile) now opens a confirmation `Modal` before calling `signOut()`, mirroring `MembershipStatusCard`'s public/brand-token confirm-modal pattern (`rounded-none` buttons) rather than the admin `SignOutButton`'s gray styling. Admin login/sign-out (separate files) untouched. Verified live via the Browser pane against a temporary seeded member (new gitignored `scripts/verify-login-flow.ts`, removed after): password toggle shows/hides/reverts correctly with value preserved and no accidental submit; correct login updates the Navbar to the signed-in state immediately (no tab switch) and lands on `/account`; wrong credentials show the inline error with fields preserved and a clean URL; Sign Out confirmation modal (desktop and mobile, mobile menu closes cleanly first) both Cancels (stays signed in) and Confirms (signs out, Navbar reverts immediately); Escape and backdrop-click both dismiss the modal without signing out. `/admin/login` regression-spot-checked unaffected. `tsc --noEmit` clean; `git status` confirmed only `LoginForm.tsx`, `login/page.tsx`, and `Navbar.tsx` changed. Post-cleanup customer count matches the pre-test baseline exactly.
- 2026-08-31 — `priority`/`BulletinPriority` removed from `Bulletin` entirely (schema column + enum, validation, both admin API routes, the admin Add/Edit form's Priority section, `/news`, `AnnouncementGate`, `NewsCard`'s two High-Priority badge blocks, and the seed script) — client/business decision that announcements shouldn't carry a priority. Hand-written migration `20260831030000_drop_bulletin_priority` (drops the column then the enum type); RLS reconfirmed intact on `bulletins` post-migration. `bulletinOrderBy` simplified to `publishedAt: 'desc'` only. `tsc --noEmit` clean; re-ran `scripts/seed-sample-bulletins.ts` (6 rows updated, no column error); browser-verified live as admin — the Add/Edit modal has no Priority field, a new bulletin saves successfully, and editing one of the 6 pre-existing rows (which had a stored `priority` value before the drop) saves with no error; `/news` and `/book`'s `AnnouncementGate` both render all bulletins with no High Priority badge; other 4 admin tabs regression-spot-checked unaffected. Found in passing, not fixed here (out of scope): `scripts/` is entirely gitignored (`.gitignore` line 36), so `seed-sample-bulletins.ts` is not actually git-tracked despite its own header comment describing it as persistent/reusable — flagging since a future clean checkout won't have it.
- 2026-08-31 — Fixed a "Cannot pause, scanner is not scanning" unhandled rejection in `CheckInScanner.tsx`, caused by `handleDecode` having no guard against being invoked more than once per scan cycle before `pause()` took effect. Added a synchronous `isHandlingScanRef` guard (set on first decode, before any async work; cleared in `handleScanNext`) so duplicate decode events are dropped instead of calling `pause()` again. Client-only fix, no schema/API changes, no seed/cleanup needed.
- 2026-08-31 — Added a 6-digit fallback check-in code alongside the existing QR token, for admin manual entry when scanning isn't practical. New `Customer.checkInCode` column (nullable, unique, `VARCHAR(6)` zero-padded, hand-written migration `20260831040000_add_customer_check_in_code`, RLS reconfirmed intact post-migration). `check-in-token.ts`'s `getOrCreateCheckInToken`/`regenerateCheckInToken` now always create/rotate the token and code together in one `update()` call (never independently) and return `{ token, code }`; a new collision-retry pattern (`isUniqueCheckInCodeViolation` catching Prisma `P2002` on the `check_in_code` constraint, up to 10 attempts) was introduced here since nothing existing needed it. Admin lookup logic was extracted into a new shared `src/lib/admin-check-in.ts` (`resolveAdminCheckInResult`) so the pre-existing `GET /api/admin/check-in/[token]` and a new `GET /api/admin/check-in/code/[code]` route produce byte-identical response shapes — confirmed via a live side-by-side call in the verify script. `/admin/check-in` gained a Scan/Enter Code tab toggle (`CheckInTabs.tsx`); the scan result card was extracted into `CheckInResultCard.tsx` so both paths render identically, and a new `CheckInCodeEntry.tsx` rejects anything other than exactly 6 digits client-side before any network call. `/account`'s `MembershipStatusCard` now displays "Check-in code: NNNNNN" under the QR thumbnail and in the full-screen modal, updating in place (no reload) when Regenerate is confirmed. Verified live end-to-end (new gitignored `scripts/verify-check-in-code-fallback.ts`, mirroring `verify-admin-check-in-scan.ts`'s convention): active/expired/no-membership/garbage codes all correct, token-route regression byte-identical, two consecutive regenerates confirmed no stale-code carryover, and the admin UI tab toggle/code entry/result cards all confirmed live via the Browser pane (including client-side digit-only filtering and the sub-6-digit block). `tsc --noEmit` clean; `git status` confirmed scope matched exactly (check-in files only); post-cleanup counts (8 customers / 1 membership / 2 admin users) match the pre-test baseline exactly. Deferred, not forgotten: no rate-limiting on code-entry attempts — a 6-digit code has only 1,000,000 possibilities, so this is worth revisiting before this becomes a public-facing (rather than staff-operated, behind admin auth) surface.
- 2026-08-31 — Fixed a documentation gap in CLAUDE.md — the member check-in (QR + fallback code) decision had only ever lived as a stale "Open / Not Yet Decided" bullet (incorrectly implying admin-side scan/verify wasn't built, and not reflecting the 2026-08-31 code-fallback addition at all). Moved the full, current decision into Architecture Decisions; the Open/Not Yet Decided entry is now a short pointer to it, avoiding duplicated/stale detail. Docs-only, no code changed.
- 2026-08-31 — Added `sendStaffBookingNotificationEmail`, `buildBrandedEmail`'s 10th consumer — an internal notification to `winstonsipandserve@gmail.com` (the existing `REPLY_TO_ADDRESS`) firing alongside the customer confirmation on every booking that transitions to `confirmed`, wired into the single shared `sendBookingConfirmationEmailForBooking` call site so both the PayMongo-webhook-paid and credit-redeemed instant-confirm paths cover it automatically with no risk of drift. Reuses the exact same ledger-row/ledger-data computation as the customer email (guest fee, add-ons, total) plus a customer contact block (name/email/phone) and a payment-method line ("Paid via PayMongo" or "Paid via F&B Credit — X applied, Y remaining"), and a "View in Admin" CTA linking to `/admin/bookings/{id}` (still gated by the existing admin auth check — no new unauthenticated surface). Subject line establishes a `[Booking] New booking — {resourceLabel} — {date}, {time}` convention, intended to be reused by future staff-notification email types (membership application/approval/payment/renewal — see Next Up). Purely additive — no schema change, no new env var, `sendBookingConfirmationEmail`'s own body untouched (confirmed via `git diff` showing zero removed lines in `resend.ts`). Verified end-to-end via a temporary seeded pair (new gitignored `scripts/verify-staff-booking-notification.ts`): a webhook-paid anonymous tennis booking (guestCount 1 + Ball Boy add-on) and a credit-redeemed member pickleball booking (no guest fee, no add-ons) both confirmed via direct DB checks, then both staff + both customer emails confirmed actually delivered and pulled in full via the Resend MCP — staff email ledger rows matched the customer email's exactly (guest fee row present/absent, add-on row present/absent, in the same order), payment-method line correct for each case, and both "View in Admin" links resolved to the correct booking id. `booking_no_overlap` exclusion constraint reconfirmed intact via `pg_constraint` (unrelated to this change, per standing convention since the code touches `Booking` rows). `tsc --noEmit` clean; `git status` confirmed only `src/lib/resend.ts`, `src/lib/booking-confirmation.ts`, and `PROGRESS.md` changed. Post-cleanup counts (8 customers / 5 bookings / 4 payments) match the pre-test baseline exactly.
- 2026-09-01 — Added `sendStaffMembershipApplicationEmail`, `buildBrandedEmail`'s 11th consumer, wired directly into `POST /api/membership-applications` right after `prisma.membershipApplication.create` succeeds — continues the `[Booking]`-style convention from the 2026-08-31 staff booking notification with its own `[Membership Application]` subject prefix. Sends to the existing `REPLY_TO_ADDRESS` staff inbox with the applicant's name/email/contact number/address, requested tier (via the already-imported `formatMembershipTier`), submitted date, and a "View in Admin" CTA to `/admin/memberships/{applicationId}` — deliberately excludes the gov-ID image URLs, since that storage bucket is private and "View in Admin" is the intended way to view them. Purely additive: no schema change, no new env var, fires only after a successful create (validation failures, the 409 duplicate-pending guard, and upload failures never reach it), and doesn't touch the existing `uploadedPaths` storage-cleanup path. Verified end-to-end via a temporary seeded set (new gitignored `scripts/verify-staff-membership-application-notification.ts`): all three tiers submitted successfully with the staff email firing each time (confirmed via the Resend MCP — correct subject/tier/contact details/View-in-Admin link, no gov-ID URLs anywhere in the HTML body); a missing-field submission correctly 400'd with no `Customer` row created and no email; a duplicate-pending submission correctly 409'd with no duplicate application row and no email; a repeat applicant (existing `Customer` resolved via `resolveCustomer`, updated name/phone) correctly triggered a fresh staff email reflecting the updated contact details, with no duplicate `Customer` row. Regression-confirmed the existing admin approve/reject flow (`sendMembershipPaymentEmail`/`sendRejectionEmail`) and the 2026-08-31 `sendStaffBookingNotificationEmail` path both still fire unaffected. `tsc --noEmit` clean; `git status` confirmed only `src/lib/resend.ts`, `src/app/api/membership-applications/route.ts`, and `PROGRESS.md` changed. Post-cleanup counts (8 customers / 1 membership application) match the pre-test baseline exactly.
- 2026-09-01 — Added `sendStaffMembershipActivationEmail` (`[Membership Payment]`) and `sendStaffMembershipRenewalEmail` (`[Membership Renewal]`), `buildBrandedEmail`'s 12th and 13th consumers, wired into `handleMembershipPaymentWebhook` in `src/app/api/webhooks/paymongo/route.ts` — closes out the last two staff-notification types alongside booking and application submission. As a behavior-preserving refactor, `tierName` (`formatMembershipTier(membershipPayment.tier)`) and `expiryDateLabel` (the `Intl.DateTimeFormat` computation previously local to the renewal branch) were hoisted to right after `endDate` is computed, so both new functions and the pre-existing `sendMembershipRenewalEmail`/`sendActivationEmail` calls all read the same values — confirmed byte-identical customer-email output since neither existing function's body changed at all (`git diff` on `resend.ts` shows a pure insertion). The new staff activation email fires unconditionally on every non-renewal membership payment, deliberately not gated behind the `!membershipPayment.customer.passwordHash` check that gates `sendActivationEmail`/token creation — this is what lets it also cover the edge case of a payment for a customer who already has a password (e.g. re-approved after a prior membership), where the customer-facing activation email correctly stays silent. The renewal staff email has no CTA/link at all (mirrors `sendRejectionEmail`'s no-CTA pattern) since `Membership.applicationId` is null by design for renewals, so there's no reliable admin record to link to. Verified end-to-end via a temporary seeded set (new gitignored `scripts/verify-staff-membership-payment-notifications.ts`, removed after): first-time activation (no `passwordHash`) fired both the customer activation email and the new staff email with correct ledger/tier/expiry and a working "View Application" link; the no-token edge case (customer already has a `passwordHash`) confirmed the customer email correctly did NOT fire while the staff email still did, proving the two sends are decoupled; renewal fired both the customer renewal email and the new staff email, the latter's HTML confirmed via the Resend MCP to contain no CTA table/link anywhere; a redelivered webhook (same `membershipPaymentId`, already `paid`) confirmed the existing early-return still prevents all four emails from double-firing (no duplicate `Membership` rows created). Regression-confirmed via the same script: a real booking through to `confirmed` still fires `sendStaffBookingNotificationEmail`, and a membership application submission still fires `sendStaffMembershipApplicationEmail`. `tsc --noEmit` clean; `git status` confirmed only `src/lib/resend.ts`, `src/app/api/webhooks/paymongo/route.ts`, and `PROGRESS.md` changed. Post-cleanup counts (8 customers / 1 membership / 0 pending membership payments / 3 membership credit transactions / 6 bookings) match the pre-test baseline exactly.
- 2026-09-01 — Added an insufficient-credit advisory modal at the booking Review step for active members whose F&B credit balance won't fully cover the booking total (including exactly zero), gated behind a new `handleConfirmBookingClick` check in `BookingForm.tsx` before `POST /api/bookings` ever fires. `memberContext` now carries `creditBalanceCentavos` via `getActiveMembership` (replacing the boolean-only `isActiveMember` call in `book/page.tsx`) — same single query as before, just exposing the full row. UI-only/advisory: the server's atomic full-or-nothing check in `POST /api/bookings` remains the sole enforcement point, completely unchanged. Verified end-to-end via a temporary seeded set (gitignored `scripts/verify-insufficient-credit-modal.ts`, removed after): zero-balance member (correct zero-balance wording), partial-balance member (₱450 credit vs ₱650 total — correct amounts shown, "Continue Booking" proceeds through pending_payment → checkout → webhook-paid, credit balance confirmed untouched at both checkpoints via direct DB query), full-cover member (no modal, instant credit-covered redirect regression-confirmed live), and anonymous booking (no modal, unchanged flow regression-confirmed live) — all via the Browser pane. Escape key and backdrop click both dismiss the modal identically to "Go Back," with no request fired and all selections intact; "Start Over" leaves no stale modal state. `tsc --noEmit` clean; `git status` confirmed only `src/app/book/page.tsx`, `src/components/booking/BookingPageClient.tsx`, and `src/components/booking/BookingForm.tsx` changed.
- 2026-09-01 — Added rate limiting to `/admin/check-in`'s Enter Code lookup: a new `CheckInLookupAttempt` model (FK'd to `AdminUser.id`, `ON DELETE CASCADE`, isolated migration + RLS deny-all in `prisma/manual-sql/check-in-lookup-attempt-table.sql`) logs each failed code lookup, and `src/lib/check-in-rate-limit.ts` blocks the 11th failed lookup within any rolling 5-minute window per admin — a soft abuse-detection counter (DB-backed, expire-on-write cleanup, no locking/transaction needed), not a financial commitment. `GET /api/admin/check-in/code/[code]` now checks the limit immediately after the existing admin-session check and returns 429 (with a best-effort `retryAfterSeconds`) before ever querying `Customer` on a blocked attempt; successful lookups are never recorded. `CheckInCodeEntry.tsx` renders the 429 case through the same `CheckInResultCard` styling already used for "Code not recognized," with a distinct message. The QR/token route (`GET /api/admin/check-in/[token]`) is untouched and intentionally excluded — its 24-random-byte token space isn't brute-forceable. Verified end-to-end via a temporary seeded pair of admins (gitignored `scripts/verify-check-in-rate-limit.ts`, removed after): 10 failed lookups as Admin A recorded 10 rows and resolved normally, the 11th returned 429 (with `retryAfterSeconds`) without adding an 11th row and without resolving even a real member's valid code; Admin B's own lookups were unaffected and logged nothing on success; backdating Admin A's rows outside the 5-minute window (simulating expiry) correctly purged them via the opportunistic cleanup and unblocked the next lookup; 15 rapid token-route lookups were never blocked. Also confirmed live in the Browser pane as a real logged-in admin — the Enter Code tab correctly showed "Code not recognized" for a bad code and "Too many attempts. Try again in about 1 minute." after tripping the limit, both via the same red-bordered result card. `booking_no_overlap` exclusion constraint and the new table's RLS deny-all policies (`anon`+`authenticated`) reconfirmed via direct Postgres queries. `tsc --noEmit` clean; `git status` confirmed only `prisma/schema.prisma`, the new migration, `src/app/api/admin/check-in/code/[code]/route.ts`, `src/lib/check-in-rate-limit.ts` (new), `src/components/admin/CheckInCodeEntry.tsx`, `src/components/admin/CheckInResultCard.tsx`, `CLAUDE.md`, and `PROGRESS.md` changed.
- 2026-09-01 — Closed a real gap: the anonymous, not-logged-in booking path (`PATCH /api/bookings/[id]`) could re-price at member rate — and would soon have been able to spend F&B credit — for anyone entering an existing active member's real name/email/phone at Pay Now, with no authentication at all. Fixed by hardcoding `isMember: false` for that path's `priceBooking` call, regardless of the resolved `Customer`'s actual membership status; `Customer` look-up-or-create/attachment for record-keeping is unchanged, so the booking still shows in that member's account history once they log in. `PaymentStep.tsx`'s "final price changed" notice was collapsed from two member-rate/guest-fee-specific branches (the guest-fee-waived branch was now permanently dead) into one tier-agnostic "pricing was updated since you started this booking" message; `guestFeeWaived` removed from the `PriceUpdate` interface and `BookingForm.tsx`'s setter. Also fixed a pre-existing bug found along the way: `BookingSummary`'s estimate caption was a static "non-member rate" string shown identically on both flows, wrong on the member path since it always said non-member even when member pricing was shown — now threads the existing `rateTier` value through `BookingForm` → `ReviewStep`/`PaymentStep` → `BookingSummary` for an accurate caption on both. Session-aware member direct-booking (`POST /api/bookings`) is completely unchanged and unaffected — regression-confirmed member rate and full-or-nothing credit redemption still work exactly as before. No schema changes. Verified live via the Browser pane: anonymous booking with a brand-new email charged non-member rate as before (regression baseline); anonymous booking using an existing active member's real contact details charged non-member rate, left that member's `creditBalanceCentavos` and `MembershipCreditTransaction` rows untouched, still routed to PayMongo checkout instead of auto-confirming, and still correctly attached `Booking.customerId` to that member's real `Customer` row — spot-checked across a court and a golf-sim resource; a simulated mid-hold `PricingRule` edit still surfaced the new tier-agnostic notice with the correct new total; guest fee still charged (never waived) for a genuine non-member with guests; `/admin/bookings` showed the correct non-member amount and correctly attached customer for the case-2 booking; `booking_no_overlap` exclusion constraint reconfirmed via `pg_constraint`. `tsc --noEmit` clean; `git status` confirmed only `src/app/api/bookings/[id]/route.ts`, `src/components/booking/steps/PaymentStep.tsx`, `src/components/booking/steps/ReviewStep.tsx`, `src/components/booking/BookingForm.tsx`, `src/components/booking/BookingSummary.tsx`, `CLAUDE.md`, and `PROGRESS.md` changed — no scope leakage into `src/app/api/bookings/route.ts` or `src/lib/customer-resolution.ts`.
- 2026-09-01 — Three-part admin Check-In polish pass, no verification-logic changes. (1) Fixed a real race in `CheckInScanner.tsx`: the mount effect's cleanup checked `scanner.isScanning` before calling `.stop()`, but under React Strict Mode's dev-mode double-invoke the cleanup can run before the `.start()` promise resolves, so `isScanning` was still `false` and `.stop()` was skipped — leaving a live camera stream and its injected `<video>` node behind for a second `Html5Qrcode` instance to stack on top of. Rewrote it so both possible orderings (cleanup-before-resolve and resolve-before-cleanup) each call `.stop()` then `.clear()` exactly once, whichever happens later. (2) `CheckInTabs.tsx`'s `activeTab` now defaults to `null` instead of `'scan'`, so neither the camera nor the code-entry form mounts until the admin explicitly picks one — a neutral "Choose Scan or Enter Code to check in a member." prompt renders in the gap, with neither pill styled active. (3) Redesigned `CheckInResultCard.tsx` to drop the Active Member state's `green-50`/`green-200`/`green-700` classes — a real violation of CLAUDE.md's "no default Tailwind green for status badges" convention — in favor of a neutral white/gray-bordered card; all four states (active/expired/no-membership/not-found·rate-limited) now share one icon+label header layout (new `CheckCircleIcon`/`AlertTriangleIcon`/`XCircleIcon`/`InfoIcon`, added to `AdminIcons.tsx` following its existing 24×24/stroke-1.5 convention, since none of the four existed there or in the public-site `Icons.tsx`), with the expired/no-membership/error states keeping their already-compliant amber/gray/red coloring — copy unchanged throughout. Verified live via the Browser pane against a real logged-in admin (reusing `scripts/verify-admin-check-in-scan.ts`'s seed convention, plus a one-off backfill of `checkInCode` on its three seeded customers so all four result states — including rate-limited, tripped via 10 rapid lookups — could be driven through the Enter Code tab and screenshotted): confirmed neither tab renders pre-choice, Enter Code works standalone with zero `<video>` elements ever appearing, and all four/five result cards (active/expired/no-membership/not-found/rate-limited) render with the correct copy, correct icon, and — checked via a DOM regex scan of the card's `innerHTML` — zero `green-*` classes anywhere on the Active Member card. Rapid Scan↔Enter-Code switching (6 toggles) produced no console errors, no duplicate/stray `<video>` elements, and no unhandled rejections — each Strict-Mode double-mount's `.start()` failure (camera access is sandboxed/blocked in this browser automation environment, so this could only exercise the failure/teardown path, not a real live feed) was caught and handled exactly once per mount, consistent with the race fix. **Known limitation, same shape as the original 2026-08-31 camera-scan entry**: a real "exactly one live feed across a Strict-Mode remount" check needs an actual camera and can't be exercised by this browser automation — Arjay should spot-check this on a real device (rapid tab switching back to Scan, confirming only one video feed ever appears). `tsc --noEmit` clean; `git status` confirmed only `src/components/admin/CheckInScanner.tsx`, `src/components/admin/CheckInTabs.tsx`, `src/components/admin/CheckInResultCard.tsx`, `src/components/admin/AdminIcons.tsx`, and `PROGRESS.md` changed. Post-cleanup counts (8 customers / 1 membership / 2 admin users) match the pre-test baseline exactly.
- 2026-09-01 — Applied the established Navbar-legibility dark-band fix (previously `/reset-password`, `/book`, `/book/confirmation`, `/forgot-password`) to `/membership/apply`: the back-link + "Become a Member" heading/subheading are now wrapped in their own `bg-brand-dark` band (`pt-40 pb-20 md:pt-48 md:pb-28`, matching `/reset-password`/`/account`'s shape) directly below `<Navbar />`, with the back-link and heading text flipped to light-on-dark (`text-accent-light`/`text-brand-light`). The form card (`MembershipApplicationForm`) and its own container below the band are untouched — same `bg-background` wrapper, just re-padded (`pt-16 pb-16 md:pt-20`) now that the heading moved out of it. Verified live via the Browser pane: Navbar text is legible against the new dark band on first paint at `/membership/apply` (direct URL load), back-link/heading legible within the band, form fields unchanged; confirmed the Navbar's fixed-position solid-on-scroll behavior is unaffected (`position: fixed`, `bg-brand-light` on its wrapper) via a DOM check post-scroll; regression-checked `/book` and `/membership` both render correctly, confirming no shared component was altered. No known instances of the Navbar-legibility bug remain as of 2026-09-01.
- 2026-09-01 — Added a branded membership-certificate PDF (new `@react-pdf/renderer` dependency, approved exception, v4.9.0, no peer-dependency warnings) attached to the first-time membership activation email only: `src/lib/membership-certificate-pdf.tsx` renders name/tier/activation fee/F&B credit/total paid/expiry/real PayMongo payment reference/a static "Approved by Admin" label/the 3 member perks/a placeholder rules-and-regulations paragraph. `sendActivationEmail` now moves the perks list out of the email body and into the PDF only when `hasReceipt && tierName` (i.e. a genuine membership activation, never the plain-activation-link case); PDF generation failure is caught and logged, falling back to sending the email with no attachment rather than blocking activation. `sendStaffMembershipActivationEmail` gained a "PayMongo Reference" ledger row. Found and fixed a real staleness bug while wiring the webhook: the in-memory `membershipPayment` object fetched before the `$transaction` never reflects the `paymongoPaymentIntentId` value the transaction itself writes, so both new call sites use the webhook's own local `paymentIntentId` variable instead — using the stale field would have always rendered "Not available"/blank. Also found and fixed a real font bug during verification: react-pdf's built-in Helvetica has no glyph for `formatCentavos`'s ₱ sign, rendering it as a garbled overlapping character — fixed by registering Noto Sans (via its Google Fonts CDN TTF URLs) as the certificate's font instead of the built-in base-14 font. Verified end-to-end via a temporary self-signed-webhook script (removed after, same convention as `scripts/verify-membership-renewal.ts`) for all 3 tiers: real sent emails inspected via the Resend MCP (`list-emails`/`get-email`/`list-sent-email-attachments`) confirmed perks removed from the body, PDF attached, and the downloaded PDF's every field correct (including the ₱ sign rendering correctly post-fix) for 3-month/6-month/12-month; staff email's new PayMongo Reference row confirmed correct per tier. Regression-confirmed via the same method: a renewal webhook's customer + staff emails are byte-identical to before (no attachment, no reference row), and the pre-existing booking-confirmation webhook branch (exercised via `scripts/verify-staff-booking-notification.ts`) is unaffected. Forced a temporary PDF-render failure and confirmed the activation email still sends with no attachment and the error is `console.error`-logged, then reverted. `tsc --noEmit` clean; `npm ls @react-pdf/renderer` confirms 4.9.0; `git status` confirmed only `package.json`, `package-lock.json`, the new `src/lib/membership-certificate-pdf.tsx`, `src/lib/resend.ts`, `src/app/api/webhooks/paymongo/route.ts`, `CLAUDE.md`, and `PROGRESS.md` changed.
- 2026-09-02 — Admin UI polish + full test-data reset script. Added an optional `variant?: 'brand' | 'neutral'` prop to the shared `Modal` component (default `'brand'`, exact prior classes unchanged) so admin surfaces no longer render in the public-site brown/cream styling — confirmed via a repo-wide grep that none of the 11 existing `<Modal>` callers pass `variant`, so every public/member modal (Navbar sign-out, `/account`'s QR/regenerate modals, `/book`'s insufficient-credit advisory, admin's own pre-existing Disable Resource/Price Edit/Reject Application/Send Renewal Link modals) is unaffected. `BookingsFilterModal` and `BulletinFormModal` now pass `variant="neutral"`. Memberships' 6-pill status-filter row was replaced with a new `MembershipsFilterModal.tsx` (modeled on `BookingsFilterModal.tsx`, neutral variant, same URL-shape Run/Clear behavior as the old pills) plus a non-functional "Export" stub button copied from `/admin/bookings`'s existing one — `page.tsx`'s underlying filter query logic is unchanged. `BulletinFormModal`'s Category and Title fields were merged into one "Details" section laid out as a 2-column grid, matching the grid pattern already used for Event Start/End and CTA Label/URL further down the same form; no validation/category-rule/submit-handler changes. Also added `scripts/reset-all-test-data.ts`, a new disposable script (not run as part of this prompt) that supersedes `reset-booking-data.ts`/`reset-membership-data.ts` combined — it additionally hard-deletes `Customer` rows (confirmed by Arjay: test/dev data only, no backup needed), reusing `reset-membership-data.ts`'s Supabase Storage cleanup logic (copied in, not imported) to empty the `membership-applications` bucket first. Deletion runs in one `$transaction` across all 11 dependent tables (children-before-parents order documented in the script's header) and prints before/after counts for all 11 plus 9 "(should be unchanged)" tables (`ResourceType`/`Resource`/`PricingRule`/`AddOnService`/`AddOnPricingRule`/`GuestFeeRule`/`Bulletin`/`AdminUser`/`CheckInLookupAttempt`). `tsc --noEmit` clean; `git status` confirmed only `src/components/ui/Modal.tsx`, `src/components/admin/BookingsFilterModal.tsx`, `src/components/admin/BulletinFormModal.tsx`, `src/app/admin/(protected)/memberships/page.tsx`, the new `src/components/admin/MembershipsFilterModal.tsx`, the new `scripts/reset-all-test-data.ts`, and `PROGRESS.md` changed.
- 2026-09-02 — `scripts/reset-all-test-data.ts` executed manually by Arjay. Storage bucket `membership-applications` was already empty (0 objects). DB wipe removed 7 `Booking` rows (with 3 `BookingAddOn`, 4 `Payment`, 0 `BookingReschedule`, 4 `MembershipCreditTransaction`), 2 `MembershipApplication`, 2 `Membership`, 1 `MembershipPayment`, 2 `MemberActivationToken`, 0 `PasswordResetToken`, and 8 `Customer` rows — all now at 0. Confirmed unchanged: `ResourceType` (5), `Resource` (9), `PricingRule` (21), `AddOnService` (2), `AddOnPricingRule` (16), `GuestFeeRule` (1), `Bulletin` (0), `AdminUser` (2), `CheckInLookupAttempt` (1).
- 2026-09-02 — Removed `/book`'s and `/news`'s hero sections. `book/page.tsx`'s inline "Book Now" hero `<section>` was deleted (`BookingPageClient` now sits in a `pt-28 md:pt-32` wrapper for navbar clearance); `news/page.tsx`'s `<NewsHero />` was removed the same way and the now-orphaned `src/components/news/NewsHero.tsx` deleted outright (confirmed via repo-wide grep it was `news/page.tsx`'s only consumer). Since both pages' top sections are now plain light background instead of a dark hero, `Navbar.tsx` gained a `FORCE_SOLID_PAGES = ['/book', '/news']` check inside its scroll effect: on those two routes it calls `setScrolled(true)` and returns immediately, attaching no scroll listener at all, so every existing `scrolled`/`headerSolid`-derived branch (header background, logo invert, nav-link/Sign-In colors) already renders correctly with no other changes needed — this is the same dark-band pattern used elsewhere in spirit, but here the fix is "force solid" rather than "add a dark band," since these two pages have no hero content to band. `BookingPageClient.tsx` and `NewsGrid.tsx` were read-only (root elements confirmed to have no conflicting top spacing) and not modified. Verified via the Browser pane at 1440px and mobile widths: `/book` and `/news` both load hero-less with an immediately solid cream navbar (dark logo, dark nav text) and unclipped content; regression-checked `/`, `/about`, `/membership`, `/cafe-bar` all still transition transparent→solid on scroll and back (confirmed via `window.scrollTo`+dispatched `scroll` event and reading the header's class list, since this environment's Browser pane doesn't reliably composite screenshots — see the dev-environment quirk noted in Build Status); confirmed the mobile hamburger dropdown panel still renders as one continuous opaque `bg-brand-light` surface with the header (no seam) by inspecting both elements' computed classes/background color after opening the menu via a dispatched click. `tsc --noEmit` clean; grep confirms zero remaining references to `NewsHero` anywhere in `src/`; `git status` confirmed only `src/app/book/page.tsx`, `src/app/news/page.tsx`, `src/components/layout/Navbar.tsx` (deletion of `src/components/news/NewsHero.tsx`), and `PROGRESS.md` changed. No test data involved, no cleanup needed.
- 2026-09-02 — Correction to the same-day hero-removal entry above: it left a doubled top-padding wrapper on `/book`/`/news` (an oversized blank cream band, since `BookingPageClient`/`NewsGrid` already carry their own sufficient `py-16`/`py-24 md:py-28` root padding), a faint navbar seam over hero pages (`bg-brand-light/[0.01]` unscrolled, not fully transparent), and an invisible dark-on-dark hamburger icon on unscrolled hero pages (static `text-brand-dark`, never following `headerSolid`). Fixed all three: removed the `pt-28 md:pt-32` wrapper `<div>`s in `book/page.tsx`/`news/page.tsx` entirely (their children now render directly after `<Navbar />`); `Navbar.tsx`'s unscrolled header background changed from `bg-brand-light/[0.01]` to `bg-brand-light/0`; the hamburger `<button>`'s className is now conditional on `headerSolid` (`text-brand-dark` solid / `text-accent-light` unscrolled-over-hero), matching the existing logo-invert/Sign-In-button pattern in the same file. `BookingPageClient.tsx`/`NewsGrid.tsx` untouched, per scope. Verified via the Browser pane: `/book` and `/news` both show normal navbar-to-content spacing with no oversized gap or clipping; `/`, `/about`, `/membership`, `/cafe-bar` unscrolled header confirmed `bg-brand-light/0` via class inspection (this environment's `getComputedStyle` doesn't reliably resolve Tailwind v4 theme colors, so verification used class-based checks per the standing convention, not computed background-color), scrolled confirmed `bg-brand-light`, and scroll-back-to-top confirmed it reverts to `/0` with the same existing transition — no new flash/jump risk introduced. Hamburger confirmed `text-accent-light` unscrolled-over-hero (mobile, `/about`), `text-brand-dark` scrolled, `text-brand-dark` with the mobile menu open while unscrolled (`headerSolid` includes `menuOpen`, dropdown panel `bg-brand-light` matches the now-solid header — no seam), and `text-brand-dark` on `/book` (always-solid). Sign In/Book a Court button classNames spot-checked unchanged (still conditioned on `scrolled` alone, untouched by this prompt). `tsc --noEmit` clean; `git status` confirmed only `src/app/book/page.tsx`, `src/app/news/page.tsx`, `src/components/layout/Navbar.tsx`, and `PROGRESS.md` changed. No test data involved, no cleanup needed.
- 2026-09-02 — `/book`'s pre-wizard announcement gate is now vertically centered in the navbar-to-footer space instead of top-anchored: `BookingPageClient.tsx`'s root `flex` wrapper now applies `justify-center` only while `!started`, so the 5-step wizard (rendered once `started`) is completely unaffected and stays top-anchored as before. `AnnouncementGate.tsx`'s outer wrapper padding (`py-2` → `py-10 md:py-16`) was also increased, giving the decorative dot-pattern band noticeably more room around the card so the footer no longer reads as the dominant element on first load. Verified live via the Browser pane at 375/1440px: gate card vertically centered pre-`started` with no oversized blank gap; clicking "Continue to Booking" renders the wizard's Step 1 top-anchored, confirming no regression. `tsc --noEmit` clean; `git status` confirmed only `src/components/booking/BookingPageClient.tsx` and `src/components/booking/AnnouncementGate.tsx` changed.
- 2026-09-02 — `/news` gained a client-side category filter and stronger visual treatment. New `CATEGORY_LABELS` map added to `bulletin-validation.ts` (e.g. `Closure` → "Facility Closure", `FacilityMaintenance` → "Facility Maintenance") — the first human-readable display labels for `BulletinCategory`; previously both the filter and card badges would have shown the raw enum key. `NewsGrid.tsx` converted to a client component with a 7-pill filter row (All + the 6 categories, `useState`-driven, no URL/query-param involvement) sitting above the grid; filtering to a category with zero published bulletins shows a distinct "No announcements in this category yet." message, kept separate from the pre-existing true-empty-state copy. `NewsCard.tsx`'s border strengthened (`border`/`brand-dark/10` → `border-2`/`brand-dark/20`) and both its category badges (image-overlay and no-image variants) now render `CATEGORY_LABELS[item.category]` instead of the raw value. `AnnouncementGate.tsx` deliberately untouched — `/book`'s gate still shows raw category text, confirmed via grep that it doesn't import `CATEGORY_LABELS`. Verified live via the Browser pane at 375/800/1440px (seeded the 6 sample bulletins via `scripts/seed-sample-bulletins.ts`, then removed them via a direct Prisma delete afterward — the admin-panel deletion route requires an authenticated admin session not readily available to this automation, so a direct DB delete was used for the same net effect): each of the 6 category pills filters to only its matching bulletin with the pill visually marked active and others inactive, "All" restores the full list, and every card's badge label matches its corresponding filter pill's label exactly. `tsc --noEmit` clean; `git status` confirmed only `src/components/news/NewsGrid.tsx`, `src/components/news/NewsCard.tsx`, `src/lib/bulletin-validation.ts`, and `PROGRESS.md` changed.
- 2026-09-02 — Doc-only: corrected CLAUDE.md's stale Navbar Known Issue paragraph — it still described /book as fixed via the bg-brand-dark band pattern (accurate as of 2026-08-29, superseded 2026-09-02 when /book's hero was removed). Now documents both coexisting fixes (dark band for pages that keep a hero, FORCE_SOLID_PAGES for /book and /news which have none) and updates the "no known instances remain" date accordingly.

---

## Next Up

- The Supabase MCP's role cannot run `pg_terminate_backend` (`permission denied`, no `pg_signal_backend`) — the 2026-08-27 PROGRESS entry describing that as the fix for the session-mode pooler's 15-connection cap is now known stale; the actual fix is restarting/killing the local `node.exe` process(es) holding the idle connections.
- Guest Fee remains edit-only, permanently, by design (`GuestFeeRule`'s shape — `id`/`amountCentavos`/timestamps only — has nothing to key a second row on); Pricing/Add-on rows now support full create/edit/delete.
- Confirm Production's pinned "Production Overrides" (Framework Preset, etc.) actually get corrected as part of the eventual staging → main promotion — don't assume today's Project Settings fix alone reaches Production. The new custom domain already sits connected to Production and currently 404s for exactly this reason — confirmed 2026-08-29.
- `[www.winstonsipandserve.club](https://www.winstonsipandserve.club)` (no-redirect fallback) and a `staging` subdomain (e.g. `staging.winstonsipandserve.club` → `staging` branch) were both considered on 2026-08-29 and deliberately deferred — no urgency, revisit if either becomes useful (e.g. routine staging review, or nearer the actual `main` promotion).
- `accent-teal` placement decided and applied 2026-08-30/2026-08-31: `/cafe-bar`'s Café/Bar mode toggle, and `/membership`'s tier-card checkmarks. Extending to secondary-button outlines was discussed as a possible follow-up but decided against, not deferred — no further extension is planned unless a new need comes up. CtaBanner's "Explore Membership" button was also considered for `accent-teal` and decided against (stays on the existing button treatment) — not an open question.
- Facilities now use local placeholder copies under `public/images/facilities/` instead of Picsum — swapping in real venue photography later means replacing those 5 files directly (or updating the `image` path per facility), same net task as before, just no longer dependent on an external image host.
- F&B membership credit now has its first real spend path (`booking_redemption`, see the 2026-08-31 Completed Log entry) — previously idle since POS doesn't exist yet. Partial-credit application (today is full-or-nothing only) is decided against — full-or-nothing remains intentional; an advisory insufficient-credit modal was added instead, see the 2026-09-01 Completed Log entry. Extending credit redemption to the anonymous hold-then-`PATCH`-attach path was decided against on 2026-09-01, not deferred: that path is unauthenticated, so member rate/credit access must stay confined to the session-aware `POST /api/bookings` path — see the 2026-09-01 Completed Log entry on the anonymous PATCH pricing fix.
- All four planned staff-notification types are now shipped: bookings (`sendStaffBookingNotificationEmail`, 2026-08-31), membership application submission (`sendStaffMembershipApplicationEmail`, 2026-09-01), and membership-payment activation/renewal (`sendStaffMembershipActivationEmail`/`sendStaffMembershipRenewalEmail`, `[Membership Payment]`/`[Membership Renewal]`, second 2026-09-01 entry). No further staff-notification type is currently planned — revisit this line if a new one comes up.
- `scripts/reset-booking-data.ts` and `scripts/reset-membership-data.ts` are now superseded by `scripts/reset-all-test-data.ts` (2026-09-02) for any future full test-data reset — the two older scripts still work standalone (Booking-only / Membership-only) but the new script is the one to reach for when a clean slate including `Customer` rows is needed. Run 2026-09-02 with the result logged in that day's second Completed Log entry; remains available for any future full reset.
- `/book` and `/news` no longer have their own hero sections (removed 2026-09-02) — if either page ever gets real content back at the top (e.g. a reinstated heading), remember to also revisit whether it still needs `FORCE_SOLID_PAGES` forcing the navbar solid, since that was specifically a workaround for having no dark hero to blend into.
- `CATEGORY_LABELS` (added 2026-09-02, `bulletin-validation.ts`) is now the single source of truth for human-readable `BulletinCategory` display text, used by `/news`'s filter pills and `NewsCard`'s badges — if a 7th category is ever added, it needs an entry here (TypeScript's `Record<BulletinCategoryValue, string>` will already fail to compile without one). `/news`'s category filter is client-side/`useState`-only, no URL query param — revisit if deep-linking to a specific category is ever wanted.

---

## Open Decisions

See CLAUDE.md → "Open / Not Yet Decided" for the current list — not duplicated here to avoid two sources of truth.
